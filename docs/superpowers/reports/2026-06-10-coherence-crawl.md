# Coherence Crawl — split-brain derivation bugs

2026-06-10. Static pass: 81 agents over 10 shared facts (map → judge → adversarial verify).
Companion: same-day browser fact-matrix crawl (live screenshots, /tmp/coherence).
Bug class: two surfaces derive the SAME fact differently and can disagree for the same row.
NOTE: verify agents for night-title and verification-gates hit session limits — those facts' findings are unverified and EXCLUDED here; re-run if needed.

## Confirmed findings (27)

### [HIGH] Matches list renders the clear_photo_url mirror verbatim while /matches/[lockId] signs the gallery — real users see an initial in the list and the face on the detail
- fact: counterpart-photo
- surface: /Users/lucas/projects/After5/apps/web/app/matches/page.tsx:35-36,46 (locks embed projects profiles.clear_photo_url; pickCounterpart passes it through)
- surface: /Users/lucas/projects/After5/apps/web/app/matches/MatchesList.tsx:50-57,70 (PolaroidAvatar hands the raw string to next/image, no signing, no rooted-path check, no onError)
- surface: /Users/lucas/projects/After5/apps/web/app/matches/[lockId]/page.tsx:97-124 (signs profile_photos gallery rows via signClearUrls, with a shape-tolerant mirror fallback)
- surface: /Users/lucas/projects/After5/supabase/functions/generate-blur/index.ts:72-88 (writer: mirror value is a RELATIVE private-storage path '<uid>/<photoId>.jpg' for every real user)
- why: Same fact, two derivations: the list trusts the mirror column verbatim; the detail signs gallery paths. For every real production user the mirror holds a relative storage path, which next/image cannot load — in prod the optimizer 400s the request (/_next/image?url=uid%2Fid.jpg is neither rooted nor absolute) and the layered-underneath initial shows; in dev/e2e the defaultLoader throws 'Failed to parse src' and the page errors. The da08d7d fix made the DETAIL accept every shape the list accepts, but never made the list accept the writer's canonical shape, so the split-brain survives in the inverse direction: list = initial avatar, detail one tap away = signed face. E2E seeds mask it because seed.ts:144 deliberately writes rooted public-asset paths ('cheap surfaces read the mirror directly'), the one shape the verbatim renderer handles.
- repro: As a real user whose photo went through generate-blur (mirror = '<uid>/<photoId>.jpg'), lock a date, then open /matches: the card shows the pink initial avatar (prod) or the route errors (dev). Tap into /matches/[lockId]: the hero shows the signed clear photo.
- verifier note: keep high (every real production user with a photo sees the wrong avatar on the primary dates tab, and the route hard-errors in dev); medium would be defensible since it is cosmetic degradation with the correct photo one tap away, not data loss or a security issue

### [HIGH] Inbox ThreadRow renders the post-lock clear_photo_url mirror verbatim — initial avatar in the thread list, signed face on the linked match detail; the claimed onError fallback does not exist
- fact: counterpart-photo
- surface: /Users/lucas/projects/After5/apps/web/app/inbox/page.tsx:83-93,124 (chat_threads -> offers embed projects clear_photo_url; counterpartPhotoUrl = r.lock_id ? counterpart?.clear_photo_url ?? null : null)
- surface: /Users/lucas/projects/After5/apps/web/app/messages/ThreadList.tsx:17-39,62 (Avatar passes the raw string to next/image; the comment promises 'swaps to the same fallback if the photo URL errors' but no onError handler is wired)
- surface: /Users/lucas/projects/After5/apps/web/app/matches/[lockId]/page.tsx:97-124 (the disagreeing signed-gallery derivation, reachable from the same thread via the E18 lock nav edge)
- why: Identical verbatim-mirror family as the matches list but on the unified inbox: a post-lock thread row can never load a real user's mirror value (relative storage path -> optimizer 400 in prod, render throw in dev), so the inbox shows the initial while the match detail it links to shows the face. The reveal gate (clear only when lock_id) is correct, but the chosen source/format makes the post-lock branch dead for real users. The in-code comment claiming error fallback is false, so a future expired-URL case also has no handler. In dev the throw takes down the whole /inbox route, not just one avatar.
- repro: Real user with a generate-blur mirror locks a night (thread gets lock_id), then opens /inbox: the thread row shows the initial; following the lock link to /matches/[lockId] shows the signed photo.
- verifier note: keep high — affects every real (non-seed) user with a locked night on a primary tab (inbox), and crashes the whole /inbox route in dev; though note MatchesList shares the identical verbatim-mirror defect, so the durable fix is a shared mirror normalizer (rooted -> render, else sign), not an inbox-only patch

### [HIGH] Interested list (host's revealed view) renders the mirror verbatim with a stock-landscape fallback — every real candidate degrades to the SAME '/places/place-walk.jpg' photo
- fact: counterpart-photo
- surface: /Users/lucas/projects/After5/apps/web/app/dates/[slug]/interested/page.tsx:63-88 (queue_entries embed projects candidate clear_photo_url verbatim into photo_url)
- surface: /Users/lucas/projects/After5/apps/web/app/dates/[slug]/interested/InterestedList.tsx:281,350 (Polaroid src={c.photo_url ?? '/places/place-walk.jpg'}), :119-132 (realtime enrichment re-selects the same raw column)
- surface: /Users/lucas/projects/After5/apps/web/components/Polaroid.tsx:59,82-96 (onError swaps any failing src to FALLBACK_SRC '/places/place-walk.jpg')
- why: Double divergence on the surface where the host decides who to offer the night to. (a) Verbatim-mirror family: a real candidate's relative-path mirror 400s at the image optimizer, Polaroid's onError fires, and the row shows the stock walk photo — so DIFFERENT candidates render IDENTICAL stock landscapes, and the host ranks/offers against faces that are not the candidates (vs the offer/feed surfaces for the same people, which correctly sign blurred gallery-derived assets). (b) Fallback-shape divergence: every other person-surface falls back to the brand initial avatar; this one substitutes a venue photo for a person, both via the ?? default and via onError, so even a legitimately photo-less candidate renders as a street scene. The seed comment in seed.ts:136-143 records exactly this reading as a bug ('a street scene in the polaroid read as a bug during founder walkthroughs') yet the real-user path still produces it.
- repro: Host with 2+ real (generate-blur) interested candidates opens /dates/[slug]/interested: all candidate polaroids settle on the identical '/places/place-walk.jpg' image after their optimizer requests 400.
- verifier note: keep high — the host ranks, shortlists, and sends the night's one offer against images that are not the candidates, and all real candidates collapse to one identical stock landscape on the core decision surface; post-lock the same person shows a face on match detail and a street scene here.

### [HIGH] profiles.clear_photo_url mirror rendered verbatim on inbox rows, matches list, and host interested list — real users' storage-relative paths are unrenderable (next/image parse throw) while matches detail signs them
- fact: blur-tier
- surface: /Users/lucas/projects/After5/apps/web/app/inbox/page.tsx:124 -> /Users/lucas/projects/After5/apps/web/app/messages/ThreadList.tsx:31-39 (post-lock thread-row avatar)
- surface: /Users/lucas/projects/After5/apps/web/app/matches/page.tsx:35-36 -> /Users/lucas/projects/After5/apps/web/app/matches/MatchesList.tsx:51-58,70 (matches list polaroid)
- surface: /Users/lucas/projects/After5/apps/web/app/dates/[slug]/interested/page.tsx:88 -> /Users/lucas/projects/After5/apps/web/app/dates/[slug]/interested/InterestedList.tsx:281,350 (host queue polaroids)
- surface: /Users/lucas/projects/After5/apps/web/app/matches/[lockId]/page.tsx:100-117 (the one surface that normalizes: signs gallery rows, branches public '/' vs storage path)
- why: Split-brain on input shape. profiles.clear_photo_url has two shapes in prod: seed profiles carry public asset paths ('/seeds/x.jpg') while every real onboarded user gets a storage-relative path ('<uid>/clear.jpg' or '<uid>/<file>.jpg', written by supabase/functions/generate-blur/index.ts:48,79,86 — no leading slash, no scheme). The da08d7d fix taught ONLY matches/[lockId] to accept both shapes; the three surfaces above still hand the raw column to next/image with no sign-or-public branch. next/image throws synchronously ('Failed to parse src') on a relative src without a leading slash — it is not a load error, so ThreadList's `src &&` guard and Polaroid's onError fallback (components/Polaroid.tsx:88-95) never engage. Net effect for a real (non-seed) matched pair: /matches/[lockId] shows the counterpart's clear face, while the /matches list and the post-lock /inbox row for the same person render-error (error boundary takes the page) — the exact list-vs-detail archetype, unfixed on the list side and newly present on two more surfaces. The host's /dates/[slug]/interested page has the same shape hazard pre-lock for any real candidate.
- repro: As a user whose photo was uploaded through onboarding (profiles.clear_photo_url = '<uid>/clear.jpg', e.g. QA acct 5f387641), lock a night with someone, then open /inbox (messages zone) or /matches. Compare with /matches/[lockId], which signs and renders the same photo fine. Pre-lock: open /dates/[slug]/interested as a host with a real candidate queued.
- verifier note: medium — the claimed page-crash mechanism is dev-only; in production builds the surfaces degrade gracefully to the initial avatar (no error boundary, no crash, no data exposure). What remains is a real, currently-reproducible coherence bug (list/inbox show initial, detail shows face) that will hit every real onboarded user with a photo, but it is cosmetic-coherence, not availability.

### [HIGH] Hand-built and custom-venue plans crash both night-detail RPCs (22P02 uuid cast) while the offer page renders them fine — same night: full plan on /offers, blind summary on feed, 'plan's being put together' on /matches
- fact: night-plan-stops
- surface: /Users/lucas/projects/After5/supabase/migrations/20260610090000_fix01_get_night_detail_stop_order.sql (line 55: left join places pj on pj.id = (arr.s->>'place_id')::uuid — unguarded cast)
- surface: /Users/lucas/projects/After5/supabase/migrations/20260610100000_fix02_get_lock_night_detail.sql (line 61: identical unguarded cast)
- surface: /Users/lucas/projects/After5/apps/web/lib/itinerary/edit.ts (line 54: addBlankStop writes place_id: '')
- surface: /Users/lucas/projects/After5/supabase/migrations/20260603120100_m85_create_blank_itinerary.sql (line 29: scaffold stop has "place_id":"")
- surface: /Users/lucas/projects/After5/apps/web/lib/places/normalize.ts (googlePlaceToStop: place_id = `custom:<googleId>`)
- surface: /Users/lucas/projects/After5/apps/web/app/feed/NightDetailSheet.tsx (lines 191-198: RPC error → catch → blind summary only)
- surface: /Users/lucas/projects/After5/apps/web/app/matches/[lockId]/page.tsx (lines 154-174: RPC error → night stays null → 'plan's being put together', and title/time/cover are lost too since the whole row errors)
- surface: /Users/lucas/projects/After5/apps/web/app/offers/[offerId]/page.tsx (lines 103-108: direct RLS embed + normalizeNightDetailStops — unaffected, renders the full plan)
- surface: /Users/lucas/projects/After5/apps/web/app/inbox/StandbyList.tsx (getNightDetail .catch(() => null) → 'night no longer available' for a night that is available)
- why: The app's own shipped write paths produce non-UUID place_id values in itineraries.stops: the 'start from scratch' canvas (create_blank_itinerary) and the editor's add-stop helper write place_id: '' (validateStopsForSave only requires a non-empty place_name, so '' survives save), and the M3.5 custom-venue search writes place_id: 'custom:<googleId>'. Both fix01 (get_night_detail) and fix02 (get_lock_night_detail) cast (s->>'place_id')::uuid with no guard, so one such stop anywhere in the array makes the ENTIRE RPC call raise 22P02 — not degrade. EMPIRICALLY CONFIRMED on prod (ufufmcpnysvwtutpbian): running fix01's exact stop subexpression against itinerary d4ca5600-64b3-4fc4-8c11-4a7e7a69af14 (title 'asdasdas') errors with `22P02: invalid input syntax for type uuid: ""`; prod already holds 5 stops with place_id='' across 5 itineraries (all currently unposted drafts, so no live night is broken yet — but it is one post_night away, and post_night copies stops verbatim). The split-brain: the candidate journey renders the SAME night from three pipelines — feed sheet (RPC → error → blind summary card, no stops/hook/costs, console.warn), offer screen (direct itineraries embed + normalizeNightDetailStops → full timeline with names, times, coords), lock detail (RPC → error → 'plan's being put together', and because the whole row errors the lock page also loses the night title, time window, and cover — the exact regression fix02 was shipped to fix comes back for these plans). The inbox StandbyList additionally mislabels such a night 'night no longer available'.
- repro: As host A: /plans → start from scratch (create_blank_itinerary) → type a stop name, leave it un-linked to a venue (place_id stays '') → save → post it as a night. As candidate B: the night's feed card opens its detail sheet → get_night_detail returns SQL error 22P02 → sheet shows only the blind summary (console: '[night-detail] get_night_detail failed'). Have B express interest, A sends an offer: /offers/[offerId] shows the FULL stop timeline. Lock it: /matches/[lockId] shows 'plan's being put together' with no title/time. SQL-only repro: run fix01's jsonb_agg subquery against itinerary d4ca5600-64b3-4fc4-8c11-4a7e7a69af14 on prod → 22P02. Same failure for any plan containing a custom-venue stop ('custom:…').
- verifier note: keep high — it is a deterministic crash of both night-detail RPCs on the primary "start from scratch" creation path, already seeded 5 times in prod data; the only mitigating fact (no affected itinerary has been posted as a night yet) makes it latent today, not less severe once triggered, and it silently re-breaks the lock-detail surface fix02 just shipped to fix

### [HIGH] Lock detail falls back to the hour-truncated RPC time and renders it with fake minute precision while the matches list shows 'date tbd' for the same lock
- fact: time-display
- surface: /Users/lucas/projects/After5/apps/web/app/matches/[lockId]/page.tsx:199 (startsAt={lock.instance?.starts_at ?? nightWindowStart})
- surface: /Users/lucas/projects/After5/apps/web/app/matches/[lockId]/LockDetail.tsx:65,119-121,199-204 (WHEN_OPTS minute:'2-digit'; upcoming derived from the winning value)
- surface: /Users/lucas/projects/After5/apps/web/app/matches/page.tsx:47 (startsAt: l.instance?.starts_at ?? null — no RPC fallback)
- surface: /Users/lucas/projects/After5/apps/web/app/matches/MatchesList.tsx:28-32,86-91 (whenLabel, fallback='date tbd')
- surface: /Users/lucas/projects/After5/supabase/migrations/20260610100000_fix02_get_lock_night_detail.sql:40 (date_trunc('hour', di.starts_at))
- surface: /Users/lucas/projects/After5/apps/web/app/matches/lock-view.ts:72-85 (bucketLocksByStart treats unknown start as upcoming)
- why: Exact structural analog of the fixed clear_photo_url bug: two surfaces, same lock row, different fallback chains. When the locks->date_instances embed nulls (RLS gap/missing instance), the list degrades honestly to 'date tbd' while the detail silently substitutes get_lock_night_detail's time_window_start — which is date_trunc('hour', starts_at) — and formats it with minute:'2-digit'. A 7:30pm night reads as a confident 'friday, jun 12, 7:00 pm', up to 59 minutes early, on the one screen people use to show up on time. Secondary disagreement: LockDetail computes upcoming from whichever value won (line 121), so once the truncated hour passes the detail header flips to 'you went out' / 'hope it was cute.' while bucketLocksByStart (no fallback, !startsKnown => upcoming) keeps the same lock under 'upcoming' on the list.
- repro: Make the instance embed null while the party RPC still returns the night (e.g. a lock whose date_instances row is hidden from the lock-stage RLS policy, or any embed miss). Load /matches (row shows 'date tbd', bucketed upcoming) then /matches/[lockId] (header shows the hour-truncated time with ':00' minutes; after the truncated hour passes, status copy reads past while the list still says upcoming).
- verifier note: low

### [HIGH] Offer-received email renders the expiry deadline in the server's timezone/locale; the app renders it viewer-local — the emailed deadline is hours wrong for every non-UTC user
- fact: time-display
- surface: /Users/lucas/projects/After5/apps/web/lib/email/send-offer-received.ts:37-42 (expiresLabel: at.toLocaleString(undefined, {weekday,hour,minute}) executed server-side, no timeZone option)
- surface: /Users/lucas/projects/After5/apps/web/app/offers/[offerId]/OfferDetail.tsx (in-app viewer-local expiry/countdown for the same offers.expires_at)
- why: expiresLabel runs in the Node/Vercel server context, so toLocaleString(undefined, ...) resolves to the SERVER's timezone (UTC) and locale. The same offers.expires_at row renders viewer-local in the app. For a Pacific user an offer that actually expires Friday 11:00 AM PT is emailed as 'Fri, 6:00 PM' — the recipient believes they have 7 extra hours and can miss the offer entirely. Unlike the RLS-conditional findings, this fires on every offer email to every user outside UTC. Fix shape: format with an explicit timeZone (host city TZ) + label, or drop the absolute time for a relative one.
- repro: Trigger sendOfferReceivedEmail for any offer while the viewer's device TZ differs from the server's; compare the email's 'expires' line against the offer page's expiry rendering for the same offer id.
- verifier note: keep high — it fires on every offer-received email, affects effectively 100% of users (single-city PT product on a UTC server), states a deadline ~7h later than reality with no TZ marker, and the failure mode (recipient misses the offer window) breaks the core matching loop

### [HIGH] Feed price filter treats NULL total_cost_pp as 'fails every cap' while every display surface treats NULL as 'no price' — unpriced nights silently vanish when any max-price filter is set
- fact: price
- surface: /Users/lucas/projects/After5/supabase/migrations/20260609120100_dlb02_browse_feed_dealbreakers.sql:140
- surface: /Users/lucas/projects/After5/apps/web/app/feed/NightDetailSheet.tsx:176-177,256-258
- surface: /Users/lucas/projects/After5/apps/web/app/feed/SwipeDeck.tsx:624-629
- surface: /Users/lucas/projects/After5/supabase/migrations/20260419193959_initial_schema.sql:128
- surface: /Users/lucas/projects/After5/scripts/qa-feed-seed.sql:88 (itinerary insert omits total_cost_pp)
- surface: /Users/lucas/projects/After5/apps/web/components/DatesFilter.tsx:78
- why: Split-brain between filter semantics and display semantics for the same column. The predicate `(f.max_price is null or it.total_cost_pp <= f.max_price)` evaluates to NULL when total_cost_pp is NULL, so the WHERE clause drops the row. The display side (NightDetailSheet) treats the same NULL as 'omit the wallet chip' and shows the night normally. total_cost_pp is a nullable decimal(8,2), and real writers produce NULL totals (qa-feed-seed.sql inserts itineraries with no total; post_night forks copy it verbatim). The dlb02 migration's own comment two lines above says 'NULL facts never trip' for dealbreakers — the price gate right below violates that convention. Compounding it, the empty-feed widening nudge (SwipeDeck.tsx:624-629) tells the user 'your max price is $N' and offers to raise it to $120/$200 — which can never re-surface a NULL-priced night. The legacy /dates directory uses the OPPOSITE convention for the identical fact: DatesFilter.tsx:78 coerces `total_cost_pp ?? 0`, so a NULL-total plan passes 'Under $25' as if free.
- repro: Seed a night whose itinerary has total_cost_pp = NULL (e.g. qa-feed-seed.sql rows, or any hand-seeded fork). As a viewer with no filters: night appears in the feed, detail sheet shows no price chip. Open filters, set max price $200 ('$200 and under'). The night disappears from browse_feed_for_viewer results even though it displayed as priceless; the widening nudge then suggests raising a cap that is irrelevant.
- verifier note: medium — mechanism is real but the dominant write paths (generate-plan persist.ts computes the total; update_itinerary_stops coalesces to 0) make NULL totals rare organically; 0 of 10 currently-live feed nights are affected. Blast radius today is seed nights and unedited blank drafts, not the organic corpus.

### [HIGH] Host 'pull this offer back' is a silent server no-op: match_withdraw is candidate-keyed, so the host UI reports success while offers.status stays 'active'
- fact: offer-lock-status
- surface: /Users/lucas/projects/After5/apps/web/app/dates/[slug]/interested/InterestedList.tsx (confirmWithdraw line ~221-233; withdraw link in frozen rank-1 row ~287-296)
- surface: /Users/lucas/projects/After5/apps/web/lib/after5/match.ts (withdraw(), line 137-139 -> edge 'match-withdraw')
- surface: /Users/lucas/projects/After5/supabase/migrations/20260527126800_p5_pass_expire_withdraw.sql (match_withdraw, lines 87-118)
- surface: /Users/lucas/projects/After5/supabase/migrations/20260527127800_p5_match_cohort_allowlist.sql (latest match_withdraw, lines 400-435 — same candidate-keyed body)
- why: match_withdraw resolves an offer only when `offers.candidate_id = p_actor` (line 418-419) and otherwise updates `queue_entries ... where candidate_id = p_actor` (423-425). When the HOST calls it from InterestedList's withdraw confirm (E12 'pull this offer back'), p_actor is the creator: the offer lookup misses, the queue update matches zero rows, no notification fires, and the function returns void — which the edge envelope reports as success. The UI toasts 'offer pulled.' and confirmWithdraw never even mutates local rows, so the same fact ('is my offer out?') reads 'withdrawn' to the host in the moment, 'offer sent' on the host's next reload, and 'live offer, accept enabled' to the candidate the entire time. The candidate can accept an offer the host believes they cancelled. The InterestedList comment calls the wrapper 'candidate-agnostic'; the RPC is candidate-bound — classic split derivation of who the actor is.
- repro: As host: make an offer from /dates/{id}/interested (rank-1 freezes, 'offer sent' pill). Tap 'withdraw' -> 'pull it'. See 'offer pulled.' toast. Reload: the pill and frozen rank-1 are back, offers.status is still 'active'. As the candidate: /offers/{offerId} still shows the live countdown; tapping accept creates a lock.
- verifier note: keep high

### [HIGH] Cancelled locks: locks RLS says you can see the match, reveal predicates say you can't see the person — /matches/[lockId] renders the generic 'couldn't load that' error instead of the designed cancelled state, list shows 'someone' with no photo, and the chat thread stays open/messageable
- fact: offer-lock-status
- surface: /Users/lucas/projects/After5/supabase/migrations/20260527127700_p5_reveal_hardening.sql (match_reveal_allowed_pair: lock branch requires l.status in ('active','completed'); offer branch requires accepted or live-active — a cancelled lock's offer was flipped to 'passed')
- surface: /Users/lucas/projects/After5/supabase/migrations/20260527126900_p5_b_complete.sql (match_cancel_lock non-safety branch lines 235-251: lock->'cancelled', offer->'passed', queue->'offer_passed'; does NOT call close_chat_thread or clear chat_threads.lock_id)
- surface: /Users/lucas/projects/After5/supabase/migrations/20260527127400_p5_host_pre_offer_disclosure.sql (host_queue policy covers only interested/shortlisted/standby/offer_active — not offer_passed)
- surface: /Users/lucas/projects/After5/apps/web/app/matches/[lockId]/page.tsx (counterpart null -> 'couldn't load that. something glitched.', lines 76-90; the designed cancelled chip/caption in LockDetail lines 116/181 is unreachable)
- surface: /Users/lucas/projects/After5/apps/web/app/matches/MatchesList.tsx (counterpart null -> 'someone', no photo, lines 65/70)
- surface: /Users/lucas/projects/After5/apps/web/app/inbox/page.tsx (photo/name gate keys on lock_id EXISTENCE line 124; isMessageable on thread state/revoked_at line 129 — non-safety cancel leaves the thread open)
- why: After match_cancel_lock (non-safety), NO permissive profiles SELECT policy opens the counterpart row in either direction: the lock is 'cancelled' (reveal predicate wants active/completed), the offer is 'passed', and the host-queue policy's status set excludes 'offer_passed'. But every lock surface was built assuming a readable counterpart: LockDetail has a designed cancelled state ('cancelled' chip, "didn't happen." caption) that can never render because the page short-circuits to the 'couldn't load that / something glitched' error when pickCounterpart returns null; the matches list degrades differently ('someone', blank polaroid) — two surfaces, two different wrong stories for the same row. Meanwhile the inbox thread row stays in the list and isMessageable stays true (cancel never revokes the thread), so users can keep messaging a 'someone' whose identity the DB just revoked. Same revocation applies to no_show locks (status not in active/completed), though flag_no_show currently has no UI producer. NOTE: this is adjacent to the deferred WR-04 'cancelled-lock reveal' todo, but the concrete error-page + 'someone' + still-messageable divergence chain appears unreported.
- repro: Two accounts, lock a date, then cancel ('mutual') from LockDetail. Canceller and counterpart both: /matches list now shows the card as 'someone' / 'cancelled' chip with no photo; tapping it shows 'couldn't load that. something glitched.' (not the designed cancelled state); /inbox still lists the thread, nameless but messageable.
- verifier note: keep high — cancel is a first-class button in LockDetail, the failure is an error page on a primary flow plus an open chat channel to a de-identified user; though if the team treats it as a WR-04 duplicate, the net-new portion (error page + messageable thread) alone still merits medium-high

### [HIGH] Inbox badge never moves for incoming chat messages while the toast on the same page announces them (and the badge's own seed counts them)
- fact: unread-counts
- surface: /Users/lucas/projects/After5/apps/web/components/InboxTabBadge.tsx:22-24
- surface: /Users/lucas/projects/After5/apps/web/app/api/inbox/unread/route.ts:25-43
- surface: /Users/lucas/projects/After5/apps/web/components/NotificationToast.tsx:13-17
- surface: /Users/lucas/projects/After5/apps/web/app/messages/ThreadList.tsx:49-50,94-98
- why: Split-brain between the badge's seed and its live path. The seed (GET /api/inbox/unread) INCLUDES unread chat messages in `total` (threadsUnread folded at route.ts:36). But the realtime path skips them: InboxTabBadge.tsx:23 `if (row.type === 'new_message') return; // messages bump via their own path, not double-counted` — verified by exhaustive grep that NO such path exists: the only `setCount` mutations are the seed fetch, the +1 for non-message notifications, and the `notif:read` listener; `notif:read` is dispatched only by ActivityList (mark-read), and Conversation's chat_mark_read (chat.ts:70, Conversation.tsx:106-113) emits no event. Meanwhile NotificationToast subscribes to the SAME realtime insert with NO type filter and toasts 'new message'. So on /home, /matches, /my-nights, /account (badge + toast both mounted with userId): a counterpart's message fires a visible toast while the badge sits frozen at its mount-time value; dismiss the toast and there is no persistent unread indicator until the next navigation remounts the shell and re-seeds. The thread-row unread pills (/inbox) and the badge agree only at page-mount instants, never live.
- repro: Two accounts with an open thread. Viewer A idles on /home (badge shows e.g. nothing/0). User B sends a chat message. A sees a 'new message' sonner toast but the inbox tab badge does not appear/increment. A navigates to any other tab → badge now shows 1 (re-seeded from /api/inbox/unread).
- verifier note: medium

### [MEDIUM] Detail hero/RevealModal pick photos[0] by sort_order while every mirror-fed surface tracks is_primary — reordering the gallery makes surfaces show different photos of the same person
- fact: counterpart-photo
- surface: /Users/lucas/projects/After5/apps/web/lib/after5/photos.ts:34-42 (listMyPhotos orders by sort_order), :70-83 (reorderPhotos updates sort_order ONLY — never is_primary, never re-runs generate-blur), :87-105 (setPrimary flips is_primary then re-mirrors via generate-blur; the DB flip commits even if the blur invoke fails)
- surface: /Users/lucas/projects/After5/apps/web/app/account/profile/ProfileEditor.tsx:128-151 (reorder and make-primary are independent user actions)
- surface: /Users/lucas/projects/After5/apps/web/app/matches/[lockId]/page.tsx:100-101 + LockDetail.tsx:184 (hero + RevealModal = photos[0] = lowest sort_order)
- surface: /Users/lucas/projects/After5/apps/web/app/offers/[offerId]/page.tsx:44,89-93 and /Users/lucas/projects/After5/supabase/migrations/20260606120000_e15_browse_feed_host_hint.sql (feed/offer blurred hint = profiles mirror = is_primary photo as of the last successful generate-blur)
- why: Two keys for 'the' photo of one person: sort_order position 0 (gallery surfaces) vs is_primary mirrored into profiles (feed blur, offer blur, matches/inbox avatars). A user who drags photo B to the front without tapping 'make primary' is teased pre-lock as blurred photo A on the feed and offer page, but revealed post-lock as photo B in the hero and ceremony modal — the blur the candidate accepted is of a different image than the reveal. A second drift window: setPrimary commits the is_primary flip before invoking generate-blur, so a failed invoke leaves the mirror (and thus all pre-lock blurs) pointing at the OLD primary indefinitely while gallery-driven surfaces already show the new one.
- repro: Upload two photos, drag #2 first in the profile editor without using make-primary, have someone lock with you: feed/offer showed photo #1's blur; /matches/[lockId] hero and RevealModal show photo #2.
- verifier note: keep medium — both photos are real photos of the same person, so no security/privacy breach, but the list-vs-detail mismatch is user-visible one tap apart and the tease-blur/reveal mismatch undercuts the consent premise of the blind-reveal product; the PhotoManager copy actively steers users into the divergent state.

### [MEDIUM] OfferDetail never reads its own status prop — a revisited accepted offer shows the rung-2 blurred face plus live accept/pass buttons while inbox and matches show the same person clear
- fact: blur-tier
- surface: /Users/lucas/projects/After5/apps/web/app/offers/[offerId]/OfferDetail.tsx:46 (destructures props WITHOUT status; status declared at :28), :98-113 (blur-[3px] avatar), :147-178 (always-active accept/pass/not-interested)
- surface: /Users/lucas/projects/After5/apps/web/app/offers/[offerId]/page.tsx:43,123 (fetches and passes status; no redirect for accepted/passed/expired)
- surface: /Users/lucas/projects/After5/apps/web/app/inbox/page.tsx:124 + /Users/lucas/projects/After5/apps/web/app/matches/[lockId]/page.tsx (the post-lock surfaces that disagree)
- surface: /Users/lucas/projects/After5/apps/web/lib/after5/notif-map.ts:34 (persistent /offers/<id> deep link in inbox activity)
- why: Split-brain on reveal state for the same offer row: the offer page hardcodes rung-2 by route identity and ignores offers.status entirely, while the inbox row derives clear from chat_threads.lock_id and /matches/[lockId] from the locks row. After accepting, the 'X picked you' notification row keeps deep-linking /offers/<id>; the page re-renders the host BLURRED with actionable accept/pass/not-interested buttons (gated only by busy/countdown), even though the user can see the same person CLEAR one tab over. Worse copy inversion: once expires_at passes on an already-accepted offer, ExpiryCountdown flips `expired` and the page says 'this one slipped away' about a night that actually locked. Direction is conservative (under-reveal) but it is a genuine two-surfaces-disagree on one state field, plus dead/misleading actions (re-accept/pass will only error server-side).
- repro: Accept an offer, then tap the original 'picked you' row in /inbox activity (or revisit /offers/<offerId>). Observe blurred avatar + live accept/pass vs the clear photo on /inbox messages and /matches/<lockId>. Wait past expires_at and reload to see 'this one slipped away' on a locked match.
- verifier note: keep medium — conservative direction (under-reveal, no privacy leak; stale accept/pass only error server-side), but a genuine two-surface disagreement on one status field with inverted terminal copy

### [MEDIUM] Stop coordinates have two sources of truth: the RPCs clobber inline lat/lng/place_slug with the places-join result (NULL on miss) while the offer page keeps inline values — map links and RouteMap inclusion differ per surface for the same stop
- fact: night-plan-stops
- surface: /Users/lucas/projects/After5/supabase/migrations/20260610090000_fix01_get_night_detail_stop_order.sql (lines 50-55: (s - 'reservation_url') || jsonb_build_object('lat', pj.lat, 'lng', pj.lng, 'place_slug', pj.slug) — jsonb || OVERWRITES inline keys with SQL NULLs on join miss)
- surface: /Users/lucas/projects/After5/supabase/migrations/20260610100000_fix02_get_lock_night_detail.sql (lines 56-61: same clobber)
- surface: /Users/lucas/projects/After5/apps/web/app/offers/[offerId]/page.tsx (lines 103-108: direct embed → inline coords only, no catalog enrichment)
- surface: /Users/lucas/projects/After5/apps/web/components/PlanTimeline.tsx (lines 64-67: coords → maps deep-link, else name-search link)
- surface: /Users/lucas/projects/After5/apps/web/components/itinerary/RouteMap.tsx (line 82: filters to stops with numeric lat+lng; feed sheet's 'short hop apart' placeholder uses the unfiltered count)
- why: The same stop's coordinates derive from inline jsonb on the offer page but from the places catalog on the feed/lock RPC surfaces, and the RPC merge actively destroys inline values: `|| jsonb_build_object('lat', pj.lat, ...)` replaces inline lat/lng/place_slug with JSON nulls whenever the LEFT JOIN misses (place_id absent, place row deleted, or catalog row with null coords). Verified on prod: 31 stops carry inline coords with NO catalog match (these lose their coords on feed/lock — per-stop 'map' link degrades from a coordinate deep-link to a name text-search, and they drop out of the feed sheet's RouteMap, whose pin count can then disagree with the visible timeline), while 5 stops have catalog coords but NO inline coords (these show a coordinate map link on feed/lock but only a name-search link on the offer page). No currently-live night contains either shape (0 affected date_instances right now), so this is corpus-latent rather than live, but both shapes exist in prod data and nothing in the write path (update_itinerary_stops persists as-given) prevents posting one. place_slug has the same dual source, which is why post-lock /places links only work for catalog-merged stops.
- repro: Pick one of the 31 prod stops with inline coords and a non-matching place_id, post its itinerary as a night: feed detail sheet renders the stop's 'map' link as ?query=<name> and omits it from RouteMap; the offer page for the same night renders ?query=<lat>,<lng>. SQL: stops where jsonb_typeof(s->'lat')='number' AND the places join (on guarded uuid cast) returns null lat → 31 rows; the inverse → 5 rows.
- verifier note: medium -> low (corpus-latent: 0 affected date_instances/locks on prod right now; mechanism real and reachable via normal posting/editing flows, but current user-visible impact is zero and worst-case symptom is a degraded map link plus a missing RouteMap pin)

### [MEDIUM] ICS calendar export pins every stop to the download day in the device timezone while the page on the same screen labels the times 'Pacific Time'
- fact: time-display
- surface: /Users/lucas/projects/After5/apps/web/lib/calendar.ts:12-28,34,81 (toUtcStamp uses local.setHours = device TZ despite the PT comment; buildIcs(it, date = new Date()); downloadIcs calls buildIcs with no date)
- surface: /Users/lucas/projects/After5/apps/web/components/itinerary/ItineraryActions.tsx:79 (onClick={() => downloadIcs(itinerary)}), mounted by ItineraryView.tsx:231 on /dates/[slug]
- why: Third derivation of stop.start_time with its own semantics, and the one that lands in the user's actual calendar. downloadIcs never passes a date, so every VEVENT is stamped on the day the user tapped 'add to calendar', regardless of when the night happens. toUtcStamp's comment claims PT->UTC but the implementation interprets HH:MM in the DEVICE timezone — so a non-Pacific user reads '7:00 pm · Pacific Time' on screen and gets a calendar event at 7:00 pm their-local time, today. Two disagreements (date and timezone) between a page and the artifact it exports.
- repro: From a non-Pacific device, open /dates/[slug] for a plan, tap the calendar action, and inspect the .ics: DTSTART is the download date at the HH:MM interpreted in device TZ, vs the on-screen 'Pacific Time' label.
- verifier note: keep medium — wrong-instant/wrong-day events landing in users' real calendars justify medium, but it sits at the low end: the TZ disagreement requires an off-Pacific device and the date disagreement is with reality, not with anything the page displays

### [MEDIUM] Rating eligibility is derived three different ways: list/detail exclude cancelled, the rate page gates time-only, and the match_ratings RLS insert checks participation only — ratings can land on cancelled or not-yet-happened locks and feed reliability_score
- fact: offer-lock-status
- surface: /Users/lucas/projects/After5/apps/web/app/matches/page.tsx (ratable excludes status==='cancelled', line 52)
- surface: /Users/lucas/projects/After5/apps/web/app/matches/[lockId]/LockDetail.tsx (rate link gated ratingOpen && status!=='cancelled', line 213)
- surface: /Users/lucas/projects/After5/apps/web/app/matches/[lockId]/rate/page.tsx (self-described HARD gate checks ONLY isRatingOpen + existing rating, lines 43-63; lock.status selected line 28, unused)
- surface: /Users/lucas/projects/After5/supabase/migrations/20260525120800_p0_match_ratings.sql (insert policy = rater is a lock party, lines 23-32 — no lock status, no time window)
- surface: /Users/lucas/projects/After5/apps/web/lib/after5/match.ts (submitRating is a DIRECT browser table insert, lines 169+ — no RPC re-check)
- surface: /Users/lucas/projects/After5/supabase/migrations/20260606130000_e17_recompute_reliability.sql (recompute_reliability aggregates ALL match_ratings rows for the ratee, lines 64-87 — no lock-status filter)
- why: Three derivations of 'can this date be rated' disagree. The DB authority (insert RLS) accepts a rating from any lock participant at ANY time for ANY lock status — including an upcoming lock (rate it before the date via a replayed insert; the page's time gate is bypassable since submitRating is a direct PostgREST insert from the browser). The reachable-UI hole: between upper(time_range)+2h (rating opens) and the cancel/sweep boundary the lock is still 'active' and cancellable; a rate form opened pre-cancel submits successfully post-cancel. recompute_reliability then counts that rating (cancel cancels the rating_window job but any later close_rating_window for another lock, or the already-enqueued sweep job, recomputes over all rows). The matches list comment ('a stale chip can't over-rate, the rate page hard-gates') is half-true: the rate page gates the WINDOW, not the STATUS — though for cancelled locks the reveal revocation (previous finding) now accidentally blocks the page via counterpart-null redirect, the RLS-level hole and the stale-open-form path remain.
- repro: Open /matches/{lockId}/rate the moment the window opens (date end +2h, lock still 'active'). In a second session, cancel the lock ('mutual'). Submit the form in the first session: the insert passes RLS (participation only) and the rating is recorded against a cancelled date; it is included the next time recompute_reliability runs for that ratee. Alternatively replay the match_ratings insert with devtools for an upcoming lock — RLS accepts it.
- verifier note: keep medium — the cancelled-lock path needs a two-party action inside a bounded (though ≥1h, every-date) window, and the upcoming-lock path needs a hand-crafted PostgREST insert; but it corrupts the trust-critical, user-visible reliability_score irrevocably, so do not downgrade

### [MEDIUM] Losing candidates' queue rows zombie forever after the host locks with someone else: queue_entries says 'interested' while date_instances says 'matched'/'completed' — inbox 'your queue' permanently shows identity-stripped 'this night's gone' rows and never empties
- fact: offer-lock-status
- surface: /Users/lucas/projects/After5/supabase/migrations/20260527126400_p5_accept_lock.sql (match_accept_offer flips ONLY the winner's queue row, lines 107-108)
- surface: /Users/lucas/projects/After5/supabase/migrations/20260604121000_e5_loop_completion.sql (sweep_loop_terminus never touches queue_entries)
- surface: /Users/lucas/projects/After5/apps/web/app/inbox/StandbyList.tsx (selects candidate's status='interested' rows, lines 45-50; hydrates via get_night_detail)
- surface: /Users/lucas/projects/After5/supabase/migrations/20260601210000_m5_get_night_detail.sql (gates on status='seeking' AND starts_at>now(), lines 75-76 -> nulls for matched/completed nights)
- surface: /Users/lucas/projects/After5/apps/web/components/StandbyCard.tsx (null detail -> 'a night you slid in on' / 'this night's gone' fallback, lines 87-88/166)
- surface: /Users/lucas/projects/After5/apps/web/app/inbox/page.tsx (standbyCount over the same interested rows keeps bothEmpty false forever, lines 138-145)
- why: No writer ever resolves the non-winning candidates' 'interested' rows when an instance locks: match_accept_offer updates only `candidate_id=cand`; match_auto_roll runs only on negative offer resolution; the terminus sweep updates locks and date_instances but not queue_entries; only the candidate's own manual 'pull my interest' deletes a row. So the candidate-side surface (queue_entries: 'interested', "you're in line") permanently disagrees with the instance-side authority ('matched' -> 'completed'). Because get_night_detail nulls for non-seeking nights, the inbox row degrades to the identity-free 'a night you slid in on / this night's gone' card — the user can't even tell WHICH night it was — and it sits there forever, one per lost queue, with 'pull my interest' as the only exit. The head-count on /inbox uses the same filter, so the inbox can never reach its empty state again. The degrade copy also tells the wrong story ('gone') when the truth is 'someone else got it', and the same null-degrade fires for a still-seeking night the moment starts_at passes (racing the card's own countdown).
- repro: Two candidates slide in on the same night; host offers and locks with candidate A. Candidate B's /inbox 'your queue' row flips to 'a night you slid in on / this night's gone' and remains there indefinitely (days later, after the date completed, it's still listed; 'quiet in here' never shows).
- verifier note: keep medium — affects the candidate's primary inbox surface, accumulates one permanent dead row per lost queue with no notification and a never-recovering empty state, but is non-corrupting and user-clearable via 'pull my interest'

### [MEDIUM] Offer emails render the counterpart's name in stored casing while every in-app surface lowercases it
- fact: counterpart-name-age
- surface: /Users/lucas/projects/After5/apps/web/lib/email/send-offer-received.ts:103 (fromName = host?.first_name?.trim() || 'someone' — no toLowerCase)
- surface: /Users/lucas/projects/After5/apps/web/lib/email/offer-received.ts:49,55,74,80 (subject `${fromText} sent you a night out`, h1, preheader, text body)
- surface: /Users/lucas/projects/After5/apps/web/lib/email/send-offer-expiring.ts:111 + offer-expiring.ts:41,47,68,74 (subject `your offer from ${fromText} is about to lapse`)
- surface: /Users/lucas/projects/After5/apps/web/app/api/offers/email/route.ts:79,95,111 (forwards caller-supplied fromName unmodified)
- surface: Contrast: apps/web/app/offers/[offerId]/OfferDetail.tsx:90 (JS toLowerCase), apps/web/app/matches/MatchesList.tsx:65, apps/web/app/feed/NightCard.tsx:195, plus CSS `lowercase` everywhere else
- why: Same fact (counterpart first_name), two formats a user sees minutes apart: the email subject shows whatever casing was typed at onboarding (BasicsStep only trims — apps/web/app/onboarding/steps/BasicsStep.tsx:82) while the app shows the name uniformly lowercased. 'Maya sent you a night out' in the inbox, then 'maya picked you' on the offer page it links to. Worse with unusual stored casing ('mAYA', 'MAYA') — the app silently repairs it, the email does not. The email body's surrounding copy is otherwise all-lowercase brand voice ('hey — Maya wants to take you out. ... later, lucas'), so the verbatim-cased name is the only capitalized token, which reads as a template bug rather than a choice. This is the exact split-brain shape: one renderer normalizes, the sibling renderer of the same column does not.
- repro: Set a profile's first_name to 'MAYA' (or just observe any normally-capitalized name). Have that profile send an offer. Recipient's email subject: 'MAYA sent you a night out'. Tap through to /offers/[id]: header and hero read 'maya picked you'.
- verifier note: low

### [MEDIUM] Tapping (or mark-all on) an inbox reconfirm/safety-checkin row silently dismisses the lock-detail soft card driven by the same notifications.read_at
- fact: unread-counts
- surface: /Users/lucas/projects/After5/apps/web/app/inbox/ActivityList.tsx:29-46,166-178
- surface: /Users/lucas/projects/After5/apps/web/app/matches/[lockId]/page.tsx:188-210
- surface: /Users/lucas/projects/After5/apps/web/app/api/notifications/route.ts:61-70
- why: Two surfaces give the same read_at bit two different meanings. The inbox activity zone treats read_at as 'row was seen' and sets it via POST /api/notifications. The lock detail page treats the SAME rows' read_at as 'reconfirm/check-in still due' — matches/[lockId]/page.tsx:201 `if (n.read_at) continue;` gates reconfirmDue/checkinDue/reconfirmNoReply. ActivityList.onActivate marks read BEFORE navigating (line 169-170 optimistic + `void markRead(ids)` then `router.push`), so tapping the 'confirm you're still on' row writes read_at and the destination lock page server-renders with the reconfirm card already gone — the notification deep-links to a card its own tap deleted. Worse, 'mark all read' (onMarkAll, and the POST with all:true which has no type filter) clears pending date_reconfirm and safety_checkin rows wholesale, dismissing day-of safety prompts the user never acted on.
- repro: Trigger a date_reconfirm notification for an active lock. On /inbox tap the 'confirm you're still on' row → land on /matches/[lockId] with no reconfirm card (race-dependent but the POST usually wins; deterministic via 'mark all read' first, then visit the lock page: card absent despite no reconfirmation).
- verifier note: Keep medium, but note it sits at the low end: the cards are deliberately notify-only (D-03/D-04 — acking never mutates lock state), and since ackReconfirm persists nothing, confirmation is never recorded even in the intended flow. Impact is a self-defeating deep link plus silently dismissed day-of safety prompts, not enforcement bypass or data loss. The safety_checkin wholesale dismissal via mark-all-read is what justifies medium over low.

### [MEDIUM] Unread badge exists or vanishes depending on which tab you stand on: /feed and /inbox render BottomTabShell without userId
- fact: unread-counts
- surface: /Users/lucas/projects/After5/apps/web/app/feed/SwipeDeck.tsx:455
- surface: /Users/lucas/projects/After5/apps/web/app/inbox/page.tsx:185
- surface: /Users/lucas/projects/After5/apps/web/components/BottomTabShell.tsx:97
- surface: /Users/lucas/projects/After5/apps/web/app/home/page.tsx:110
- why: BottomTabShell.tsx:97 mounts InboxTabBadge only when `tab.badge && userId`. /home, /matches, /matches/[lockId], /my-nights, /account, and /dates/[slug]/interested all pass user.id, but SwipeDeck.tsx:455 (`<BottomTabShell />`, the primary discovery surface) and inbox/page.tsx:185 omit it — same fact, rendered or dropped per route. /feed also mounts no NotificationToast (grep confirms; /home, /inbox, /matches, /matches/[lockId], /my-nights, /account do), so while swiping the user has zero live unread signal of any kind: no badge, no toast. The /inbox omission may read as intentional (you're looking at the inbox) but it also unmounts the only listener for ActivityList's notif:read events (see latent finding below), and the badge stays absent even while the user reads the activity zone with unread thread pills visible below it.
- repro: With 3 unread items, visit /home → badge '3' on the inbox tab. Navigate to /feed → badge gone entirely. Receive a new message while on /feed → no toast, no badge. Navigate back to /home → badge '4'.
- verifier note: medium (unchanged) — no data loss, but the badge drops out on the highest-traffic surface and new activity arrives silently while swiping

### [MEDIUM] new_message deep link routes to /matches while every other surface homes chat at /inbox/[threadId]
- fact: unread-counts
- surface: /Users/lucas/projects/After5/apps/web/lib/after5/notif-map.ts:52
- surface: /Users/lucas/projects/After5/apps/web/components/NotificationToast.tsx:15-16
- surface: /Users/lucas/projects/After5/apps/web/app/messages/ThreadList.tsx:47,54
- surface: /Users/lucas/projects/After5/apps/web/app/inbox/page.tsx:176
- why: Same fact ('new message in thread X') resolves to two destinations. notif-map.ts:52 `new_message: { ... hrefFor: () => '/matches' }` ignores the payload's thread_id (which chat_send_message writes: jsonb_build_object('thread_id', p_thread, ...) in 20260601100200_p7_chat_send_rpc.sql:71-72), while ThreadRow links threads at basePath '/inbox' (inbox/page.tsx:176 passes basePath="/inbox"). Stale since the #84 inbox move: the toast's 'view' action for an incoming message lands the user on the matches/dates list where there is no chat entry point for that thread, instead of /inbox/[threadId] (or at least /inbox).
- repro: Receive a chat message while on /home → tap 'view' on the 'new message' toast → you land on /matches, not the conversation.
- verifier note: keep medium — it dead-ends the primary CTA for every incoming chat message on a core-loop surface (pre-lock threads have no reachable path from the landing page), but the message remains discoverable via the inbox tab, so it does not warrant high.

### [LOW] Three different failure renders for the same broken/expired photo URL: initial avatar (hero, list avatars) vs stock landscape photo (RevealModal/ProfileCard via Polaroid) 
- fact: counterpart-photo
- surface: /Users/lucas/projects/After5/apps/web/app/matches/[lockId]/LockDetail.tsx:64-87 (HeroPolaroid: no onError; broken img lets the underlying initial show)
- surface: /Users/lucas/projects/After5/apps/web/components/ProfileCard.tsx:62-73 + /Users/lucas/projects/After5/apps/web/components/Polaroid.tsx:59,87-96 (RevealModal path: onError swaps the failing src to '/places/place-walk.jpg')
- surface: /Users/lucas/projects/After5/apps/web/app/matches/MatchesList.tsx:50-57 and /Users/lucas/projects/After5/apps/web/app/messages/ThreadList.tsx:31-39 (no onError; initial shows)
- why: Hero and RevealModal share one server-side photos[] derivation (coherent source), but their failure formatting diverges: signClearUrls mints 1h tokens served from a 30min cache, so a tab left open past token expiry (or a bfcache restore) can have photos[0] 4xx at render time. The hero then quietly degrades to the initial avatar while opening the reveal modal on the same screen swaps the same dead URL to a stock street-scene photo presented inside the person's profile card — two contradictory renders of one fact, one screen apart, and the stock-photo variant misrepresents a person as a venue.
- repro: Open /matches/[lockId], leave the tab idle >1h so the signed URL expires, then tap 'see their profile': hero behind shows the initial, the modal's ProfileCard polaroid shows '/places/place-walk.jpg'.
- verifier note: keep low — narrow trigger (single-photo profile + >1h idle tab), but the wrong render misrepresents a person as a venue on the reveal surface; simplest fix is removing or replacing Polaroid's FALLBACK_SRC for tone='dating'

### [LOW] Host interested list falls back to a stock VENUE photo for a person's face, unlike every other surface's initial chip
- fact: blur-tier
- surface: /Users/lucas/projects/After5/apps/web/app/dates/[slug]/interested/InterestedList.tsx:281,350 (Polaroid src={c.photo_url ?? '/places/place-walk.jpg'})
- surface: /Users/lucas/projects/After5/apps/web/app/dates/[slug]/interested/InterestedList.tsx:95-132 (realtime-inserted rows arrive with photo_url null until the fire-and-forget profile fetch patches them)
- surface: contrast: /Users/lucas/projects/After5/apps/web/app/messages/ThreadList.tsx:18-30, /Users/lucas/projects/After5/apps/web/app/matches/MatchesList.tsx:38-49, /Users/lucas/projects/After5/apps/web/app/offers/[offerId]/OfferDetail.tsx:109-113 (all use an initial-letter chip)
- why: Fallback-shape divergence on the same fact (a person with no photo). Every other avatar surface degrades to a brand initial chip; this one substitutes '/places/place-walk.jpg' — a walking-tour venue image — as the candidate's face. A host briefly (or persistently, for photo-less candidates) sees a stock landscape presented as a real person they might offer a night to, which can misattribute identity at a decision point. Realtime inserts guarantee a window where every new heart renders the venue photo before the profile fetch lands.
- repro: As a host with the interested page open, have a candidate without a clear photo (or any candidate, during the realtime-insert window) heart the night: the polaroid shows the place-walk stock image as their photo.
- verifier note: low (unchanged) — identity-misattribution at the shortlist/offer decision point, but transient in the common case (every onboarded user has a photo), no incorrect action is enforced, and the fix is trivial (drop the ?? coalesce and let Polaroid's gradient fallback render, or add an initial-chip variant)

### [LOW] Standby queue countdown runs on the hour-truncated time and goes silent up to 59 minutes before the night starts, while the feed (raw starts_at gate) still lists it
- fact: time-display
- surface: /Users/lucas/projects/After5/apps/web/lib/countdown.ts:19 (returns '' once then < now)
- surface: /Users/lucas/projects/After5/apps/web/app/inbox/StandbyList.tsx:69 (starts_at = get_night_detail time_window_start, hour-truncated per 20260610090000_fix01:34)
- surface: /Users/lucas/projects/After5/apps/web/components/StandbyCard.tsx:156-163 (urgency chip)
- surface: /Users/lucas/projects/After5/supabase/migrations/20260609120100_dlb02_browse_feed_dealbreakers.sql:124 (and di.starts_at > now() — raw)
- why: The 'started/expired' cutoff compares now against the truncated hour, but liveness everywhere else (browse_feed listing, offer eligibility) is gated on raw starts_at. For a 7:30pm night, between 7:00 and 7:30 the standby row drops its 'tonight' urgency line (reads as expired/started) while the identical night is still browsable in the feed and can still convert. Mild because the divergence is an omission, not a wrong value — but it is the urgency signal disagreeing with the surface that creates the urgency.
- repro: Queue interest on a night starting at HH:30; view /inbox between HH:00 and HH:30 — no countdown chip — while /feed still shows the night.
- verifier note: none — low is correct: the divergence is a silent omission of the countdown chip for at most 59 minutes per night, the card otherwise stays functional, and no wrong value is displayed

### [LOW] Total chip vs sum of per-stop chips in the same detail sheet: independent Math.round per stop can disagree with the rounded column total when costs are fractional
- fact: price
- surface: /Users/lucas/projects/After5/apps/web/app/feed/NightDetailSheet.tsx:176-177 (Math.round of the column total)
- surface: /Users/lucas/projects/After5/apps/web/components/PlanTimeline.tsx:142 (Math.round per stop)
- surface: /Users/lucas/projects/After5/apps/web/app/plans/[id]/edit/EditableStopCard.tsx:81-82 (Number(input) accepts decimals; no running total shown to the host)
- surface: /Users/lucas/projects/After5/supabase/migrations/20260602140000_m3_update_itinerary_stops.sql:47 (::numeric — no integer constraint; column is decimal(8,2))
- why: One screen, two stores, two rounding passes. The sheet's wallet chip rounds the column total once; PlanTimeline rounds each stop independently. Nothing constrains estimated_cost_pp to integers: the canvas editor's Number(e.target.value) accepts 12.5, the column is decimal(8,2), and update_itinerary_stops stores/sums numerics exactly. Two stops at $12.50 render as '$13 pp' + '$13 pp' (reads as $26) under a total chip of '$25 pp'. Low because both writers preserve the underlying sum exactly and host-entered costs are usually whole dollars — but the editor shows the host no derived total, so fractional entries go unnoticed until the chips disagree.
- repro: In /plans/[id]/edit set two stops to 12.5, save, post the night, open the feed detail sheet: total chip '$25 pp' above stop chips '$13 pp' and '$13 pp'.
- verifier note: low

### [LOW] Host 'N interested' chip on /my-nights counts every queue row including passed_by_host, which the interested list silently excludes — the chip count exceeds the people the host can see, and never drops when the host passes on candidates
- fact: offer-lock-status
- surface: /Users/lucas/projects/After5/apps/web/app/my-nights/page.tsx (plain row count per instance, lines 48-57; comment claims status 'only moves forward')
- surface: /Users/lucas/projects/After5/apps/web/app/my-nights/NightsSegments.tsx (chip '${interested} interested' for seeking nights, lines 84-86)
- surface: /Users/lucas/projects/After5/apps/web/app/dates/[slug]/interested/InterestedList.tsx (passed_by_host excluded from BOTH sections — SHORTLIST_STATUSES line 145 + interested filter line 152; 'standby' also excluded)
- why: Same fact (how many people are in this night's queue) derived with two different filters. my-nights counts all queue_entries rows; the interested list hides 'passed_by_host' (E12 silent decline, by design) and 'standby'. After a host passes on K candidates, the chip still reads the old total while the tap-through list shows N-K people with no trace of the difference — the host sees '5 interested', opens the list, finds 2, and has no way to reconcile. Also, profiles of offer_passed/offer_expired candidates shown in the shortlist section are no longer readable under any policy (host_queue covers only pre/active stages), so their outcome-pill rows degrade to 'someone' — a second, smaller mismatch on the same page.
- repro: Host with 5 interested candidates passes on 3 via the UserMinus silent-decline flow. /my-nights chip still says '5 interested'; /dates/{id}/interested shows 2.
- verifier note: low (no change) — cosmetic count inflation that misleads the host about live interest; no data loss, no privacy leak, self-corrects only if candidates withdraw. The one-line fix is adding a status filter (or .not.in('passed_by_host','standby')) to the my-nights tally query.

### [LOW] A past-started seeking night reads 'open' to the host for up to 3+ hours while every candidate surface already treats it as gone
- fact: offer-lock-status
- surface: /Users/lucas/projects/After5/apps/web/app/my-nights/NightsSegments.tsx (status-only buckets/chips: seeking -> 'open', lines 37-44/78-93)
- surface: /Users/lucas/projects/After5/supabase/migrations/20260604121000_e5_loop_completion.sql (seeking -> expired only at lower(time_range)+3h, lines 93-96, on a 15-min cron)
- surface: /Users/lucas/projects/After5/supabase/migrations/20260601210000_m5_get_night_detail.sql (candidates lose the night at starts_at, line 76; feed gates likewise)
- surface: /Users/lucas/projects/After5/apps/web/components/StandbyCard.tsx (queued candidates' rows flip to 'this night's gone' at the same starts_at boundary)
- why: The host surface is status-only and the candidate surfaces are clock-only, with a 3h+cron gap between the two boundaries. From starts_at until the sweep flips the instance to 'expired', the host sees an 'open' (even editable — NightCardActions keys on status==='seeking') night that no candidate can see in the feed, open from a standby row, or be offered; queued candidates' inbox rows simultaneously read 'this night's gone'. The host gets no signal that the night is already invisible. Deliberate-looking on each side individually, but nobody picked the same boundary.
- repro: Post a night for 7pm, let 7pm pass with no lock. 7:01pm-10:00pm: /my-nights chip says 'open' (with edit actions); the night is absent from every candidate's feed and any queued candidate's standby row shows 'this night's gone'. At ~10pm the sweep flips it to 'expired'.
- verifier note: low (unchanged) — no data corruption, bounded 3h15m window, and the night is partially still actionable (host can still convert shortlisted candidates via make_offer), but the host gets zero signal that discovery is dead and queued candidates see the literal opposite state.

## Refuted by adversarial verify (19)

- **Reveal ladder inverts mid-funnel: offer page shows rung-2 blur while the chat surfaces for the SAME offer show no photo at all — three surfaces derive the tier from three different fields**
  - The mechanical facts all verify: pre-lock threads exist at offer creation (open_chat_thread in 20260525124500_p2_chat_core.sql is called by match_make_offer), the offer page renders a signed rung-2 blurred face (apps/web/app/offers/[offerId]/page.tsx:86-101), the inbox row gates the photo on lock_id and shows an initial chip pre-lock (apps/web/app/inbox/page.tsx:124), and the thread header is name-only (apps/web/app/messages/[threadId]/page.tsx:24,109-114). But this is not a split-brain derivation bug: unlike the da08d7d archetype (both surfaces intended the same output, one fallback silently failed), every surface here renders exactly what its explicit in-code contract specifies — the inbox comment says "Pre-lock threads render the initial avatar" as a conscious blind-contract decision, the thread page comment says "never fetch the clear photo pre-lock" and that surface shows no photo at ANY stage (internally uniform, no tier derivation at all), and the offer page implements REQ-E15 rung-2 by design. Nothing fails or mis-derives; the chat surfaces deliberately under-reveal, which is the blind contract's safe direction. The user sees different reveal presentations per surface, not two surfaces contradicting each other about the same data row. The proposed fix (sign blurred_photo_url for pre-lock inbox rows, unify tier derivation) is a defensible UX-polish feature request about ladder monotonicity, not a coherence defect — the finding's own "why" concedes each surface is individually safe.
- **Queue rows drop the host hint the feed promised: same night shows name+age+blurred face on /feed but zero host identity in inbox 'your queue' (get_night_detail blind projection)**
  - Fails the simultaneity gate and the omission is documented design, not a derivation gap. (1) The surfaces can never disagree side-by-side: hearting calls record_swipe (supabase/migrations/20260527120100_s5_record_swipe.sql) which inserts a swipes row, and browse_feed_for_viewer (20260606120000_e15_browse_feed_host_hint.sql line 94: `not exists (... from swipes ...)`) excludes swiped nights — the night leaves /feed the instant it appears in the inbox queue. The finding's repro is inherently sequential/memory-based. (2) The queue's blindness is an explicit contract, not a missed extension: E24's StandbyCard.tsx (built after E15, lines 97-119) constructs a FeedNight and deliberately nulls host_blurred_photo_url/host_first_name/host_age with the comment "this surface never holds identity data"; StandbyList.tsx:15-24 documents the T-07-16 blind contract; and the E15 migration header scopes D-01 to "consciously relaxes the pure-blind FEED by these 3 hints and NOTHING else". (3) No value-level disagreement exists — the queue omits the slot rather than rendering a conflicting value, and the finding itself concedes "Data-source gap, not a state-derivation bug". The post-heart surfaces (StandbyCard row + the NightDetailSheet it opens, both fed by get_night_detail) are mutually consistent. At most this is a UX enhancement request (carry the rung-1 hint into the standby row), not a split-brain coherence bug.
- **setPrimary commits is_primary flips before the generate-blur mirror write — on invoke failure the pre-lock blurred face (profiles mirror) and post-lock clear gallery can be different photos of the same person**
  - The write-ordering hazard exists (setPrimary commits both is_primary flips at photos.ts:91-102 before the fallible generate-blur invoke at :103, the edge fn at generate-blur/index.ts:72-81 is the only mirror writer, no trigger or repair path), but the claimed user-visible split-brain does not. The cited disagreeing surface is wrong: matches/[lockId]/page.tsx:100-101 builds the gallery from listMyPhotos, which orders by sort_order (photos.ts:39) and never consults is_primary, so the failed flip changes nothing on the lock detail — the 'hero leads with photo B' claim is false unless B coincidentally has sort_order 0. Even on fully successful setPrimary, the pre-lock blurred face (mirror = primary) and the lock-detail hero (sort-0 photo) routinely differ, so these two tiers have no face-coherence contract that the error path breaks. The post-lock gallery is a superset that still contains old primary A (the blur's source), so the surfaces never contradict. The residual desync (profiles mirror = A vs profile_photos.is_primary = B) is rendered on no comparable surface pair: all pre-lock surfaces (browse_feed RPC, offers/[offerId]/page.tsx:44, feed/teaser.ts:140) read the same mirror and stay mutually consistent, and the only is_primary renderer is the owner's own /account/profile editor, which no other user sees and which on error reverts its local star and toasts failure (ProfileEditor.tsx:146-149). What survives is a different, narrower defect worth a separate note: an error-path mirror/is_primary desync with no repair plus a misleading editor (failure toast despite committed flips; star reappears on reload) — a durability/UX wart, not a two-surface coherence disagreement any single user can observe.
- **reservation_url scrub is re-implemented independently in three layers; the pre-lock offer page depends ONLY on the TS whitelist while 106 prod stops carry reservation_url and a sibling component renders it on purpose**
  - All cited code facts verify (normalizeNightDetailStops whitelist at packages/api-client/src/feed.ts:292-313 omits reservation_url; offers/[offerId]/page.tsx:124-140 relies solely on it; feedback/[token]/page.tsx:76-82 has a third independent map; StopCard.tsx:164 renders reservation_url), but the finding fails the REAL bar by its own admission: "Not a user-visible divergence today", repro "Latent — no current repro". No user can see two surfaces disagree for the same row: all three scrub layers currently agree, and the only observable asymmetry (host's own plan editor shows the booking URL while blind dating surfaces redact it) is the privacy feature working as designed for different viewer roles, not a split-brain defect. StopCard is only consumed by legacy planner surfaces (plans/[id]/edit, CreateFlow, ItineraryView, places), never on a dating surface. This is a defense-in-depth/maintainability note (centralize the scrub, add a never-emits test), not a coherence bug per the archetype, which required the same viewer seeing the same fact rendered differently.
- **Three incompatible stop-name fallbacks and a raw-vs-formatted time split between the host picker and every candidate surface**
  - Code claims verified accurate (four divergent stop normalizers: feed.ts:298 'a spot' fallback; PostNightForm.tsx:43-55 'stop N' + raw time; feedback/[token]/page.tsx:76-82 no fallback; calendar.ts:12-14,44 strict HH:mm split), but no user can see two surfaces disagree on a real data row. The name divergence requires a stop lacking place_name, which is unproducible through the product: validateStopsForSave (apps/web/lib/itinerary/edit.ts:77) rejects saving any stop without a non-empty place_name, generate-plan/seeds/canvas always write place_name, and the finding's own prod check confirms 0 thin-named stops and all 1926 start_times are strict HH:mm. The repro requires manually inserting smoke-script SQL. The feedback page (alive via daily cron) serves legacy planner itineraries that always carry place_name, so its blank-name degrade is also unreachable. The buildIcs ISO hazard is equally latent and sits on the legacy planner surface. The single observable difference today — host picker '19:00' vs candidate '7pm' for the same stop — is the same fact in two formats, both correct; a meaning-preserving formatting inconsistency, not a disagreement (unlike the archetype's face-vs-initial factual split). What survives is a code-hygiene note (consolidate the four normalizers into normalizeNightDetailStops) and a cosmetic time-format nit, neither a real coherence bug per the user-visible-disagreement bar.
- **/api/email-plan emails whatever stops the client POSTs — the PDF 'plan' is never reconciled with the stored itineraries.stops row**
  - The claimed disagreement is unobservable: the only /api/email-plan caller is the anon-only AnonTeaser (CreateFlow.tsx:472-494) — the authed result surface has no email button — while /plans/[id]/edit redirects anon users to /login and 404s for non-owners (app/plans/[id]/edit/page.tsx:14-22). So the user class with the email CTA can never reach the edit canvas, and the user class who can edit never sees the email CTA; the finding's repro is structurally impossible. Further, anon gated itineraries deliberately omit the DB id (lib/create/blur-gate.ts:16-19) and the caller posts no itinerary_id, so there is no itineraries.stops row the emailing user can surface in-app to compare against the PDF. Finally, an emailed PDF is a point-in-time snapshot artifact — not reflecting later edits is expected email semantics, not two live surfaces deriving one fact differently; the 'POST a crafted body' angle is unauthenticated-endpoint abuse (spam), a different bug class. Incidentally, the anon caller actually posts the GATED (stripped) itinerary, contradicting the finding's premise that the payload matches the stored row at send time.
- **Reciprocal chooser fabricates new Date() as a night's start time when starts_at is missing — every other surface degrades to 'date tbd'**
  - The fabrication fallback exists in code (page.tsx:88) but is dead/unreachable, so the two surfaces can never actually disagree. (1) date_instances.starts_at is timestamptz NOT NULL since creation (20260525120300_p0_date_instances.sql:34) and no migration ever dropped it — the `string | null` in the page's local OfferRow cast overstates nullability. (2) The null-embed path is also unreachable: the finding's RLS premise is exactly the gap migration 20260527127500_p5_offer_recipient_date_read.sql closed — date_instances_select_offer_recipient grants the offer CANDIDATE read of the instance for offers in ('active','accepted'), and the page only loads status='active' offers where the viewer (a verified pair party) is creator or candidate of every row; creator reads come from the existing creator policies. (3) Dangling offers can't exist: offers.date_instance_id is NOT NULL with ON DELETE CASCADE. Therefore the chooser always renders the real starts_at, identical to what every other surface formats; no user can observe the fabricated new Date(). Residual value is only a hardening note: the `?? new Date().toISOString()` (and the `?? {starts_at: null}` embed default) would silently mask a future RLS/schema regression instead of failing loud — worth replacing with a 'date tbd' degrade, but it is not a live split-brain bug.
- **Same itineraries.stops jsonb rendered by two formatter families: legacy to12h is verbatim + pinned 'Pacific Time' and emits raw garbage for ISO-shaped start_time that PlanTimeline TZ-converts**
  - Refuted by prod data audit + writer analysis. (1) Prod query: 1,926/1,926 itineraries.stops start_time values are bare HH:MM; 0 ISO-shaped, 0 other — the claimed hard-divergence (raw ISO rendered verbatim by to12h on /dates+PDF vs TZ-converted by PlanTimeline) and the mixed-shape-within-one-list case operate on data that does not exist. (2) No writer produces ISO: generate-plan validates strictly-increasing parseHHMM (improve.ts:291-296); editor/seeds/PostNightForm all write HH:MM; the get_night_detail RPC and normalizeNightDetailStops pass the string through verbatim, so PlanTimeline's ISO branch is defensive dead code, not a live second source. (3) For real HH:MM data the two formatters show identical wall-clock digits to every viewer: StopTime anchors '1970-01-01T<HH:MM>' zone-less, parsed AND rendered viewer-local, so no conversion occurs — '7:00 pm' vs '7pm' is a style fork only, and the 'Pacific Time' label on /dates is factually correct for the Kelowna corpus while the dating surface merely omits a label. (4) Comparability is further limited: /dates/[slug] renders only is_public=true catalog rows. No user can see the two surfaces disagree on the same row; this is a latent-robustness note (inconsistent tolerance for a never-occurring input shape), not a split-brain bug.
- **Teaser feed re-implements the hour-blinding app-side and its unparseable-date branch ships the raw untruncated starts_at to the client**
  - No user-visible disagreement is reachable. (1) The two implementations serve mutually exclusive audiences — apps/web/app/feed/page.tsx:33 routes verified viewers to the RPC and pre-verification viewers to teaserFeed — so the same user never sees both derivations of a row, and even across the verification transition setUTCMinutes(0,0,0) on PostgREST's UTC ISO output equals date_trunc('hour') under Supabase's UTC sessions. (2) The raw-passthrough fallback at teaser.ts:120 only fires when new Date() can't parse starts_at, but starts_at always arrives as PostgREST ISO 8601, which JS parses — the branch is dead. (3) Even if it fired, both renderers of time_window_start (NightCard.tsx:148-153, NightDetailSheet.tsx:296-301) format with { hour: 'numeric' } only — minutes are never displayed — so a minute-precise value renders the identical hour string, and a truly unparseable value hits LocalTime's NaN fallback (blank/"soon"), not a wrong time. What remains is a duplicated-derivation maintenance hazard (worth collapsing to one blinding implementation), not a coherence bug a user can see; the finding's own text concedes the branch is near-dead and the leak is payload-only.
- **Zero-cost night: host's plan picker says '$0 pp' while the candidate's detail sheet hides the total entirely and shows per-stop 'free' chips — three different zero/null normalizations for one row**
  - All cited code mechanics verified accurate (PostNightForm.tsx:65 `!= null` prints '$0 pp'; NightDetailSheet.tsx:229-230/310 `> 0` hides the wallet chip at zero — note the finding's line cite 176-177 is wrong; PlanTimeline.tsx:141-142 renders 0 as 'free'; TeaserGallery.tsx:26 prints '$0'; update_itinerary_stops coalesces to 0 so the state is reachable). But no user can see two surfaces DISAGREE on the fact: every render communicates the same truth (the night is free) in different formats or omits it — nothing shows a wrong price, unlike the face-vs-initial archetype. The candidate's detail sheet self-heals on the same screen (per-stop 'free' chips sit directly under the suppressed total chip, and suppressing a $0 wallet chip is a defensible explicit choice). The host picker and candidate sheet are never simultaneously comparable by one user (feed/get_night_detail exclude own nights), and the candidate's list card (NightCard.tsx) shows no price at all, so no list-vs-detail contradiction exists. What remains is a cosmetic zero-formatting inconsistency ('$0 pp' vs 'free' vs '$0') worth a shared formatter — copy polish, not a split-brain coherence bug.
- **Total price disappears mid-funnel: feed sheet sells a '$X pp' total from the column, but the offer and lock pages render only per-stop chips from the stops JSONB — and the lock page discards the total its own RPC already returns**
  - Refuted against current code. (1) The offer page DOES select total_cost_pp (apps/web/app/offers/[offerId]/page.tsx:137) and builds a full NightDetailNight including it (line 155). (2) The lock page DOES keep row.total_cost_pp (apps/web/app/matches/[lockId]/page.tsx:170); its comment says "nothing dropped anymore". (3) Both OfferDetail (lines 184-187) and LockDetail (lines 378-381) feed that row to the SAME NightDetailSheet as `preloaded`; the sheet uses `detail = hasPreloaded ? preloaded : fetched` (line 202), so the identical "$X pp" total chip (line 229) renders pre-swipe, on the offer, and on the lock. All three sources read the same itineraries.total_cost_pp column (get_night_detail fix01 line 44, get_lock_night_detail fix02 line 50, direct select). A normalizer/unifier (one column + one sheet component) makes the surfaces agree by construction. The finding describes the pre-fix-02 shape; the 2026-06-10 fix-02 + founder-rule sheet threading closed exactly this gap. The leftover "stops JSONB sum vs column total" divergence is a writer-side data-quality concern that would show within a single sheet, not a cross-surface split-brain.
- **Weekly digest emails a NULL total as '$0' (implying free) for rows the dating app shows as price-omitted — and the public qa seed rows (NULL total, empty stops) are eligible, mailing out as '$0 · 0 hr'**
  - The claimed user-visible "$0" digest for NULL-total rows cannot occur with any data a real recipient can receive, on two independent grounds. (1) The seed rows the finding relies on are local-only by hard guard: scripts/qa-feed-seed.sql aborts unless run with `-v target=local` and is documented "local dev only (postgres superuser)", while the weekly digest is sent exclusively by the prod Vercel cron (/api/cron/weekly-broadcast in vercel.json, CRON_SECRET-gated, Resend). The qa rows never exist in the database the digest queries. (2) Independent of the seed, the digest's eligible set (is_public=true AND slug NOT NULL AND title NOT NULL, weekly-digest.ts:25-27) is exhausted by audit of every itineraries writer: generate-plan persist.ts:114-116 always writes a numeric total_cost_pp (Itinerary.total_cost_pp is typed `number`, types.ts:201, and is used in arithmetic at persist.ts:39-48) and is the ONLY writer that produces is_public=true plus a slug (slug UPDATE at persist.ts:152-163); the dating post_night fork RPC (20260602140100_m3_post_night_fork.sql:51-55) forces is_public=false AND slug=NULL, so every night the dating feed shows is a private fork excluded from the digest by two filters; create_blank_itinerary inserts is_public=false with NULL title and no slug; update_itinerary_stops recomputes total_cost_pp as coalesce(sum(...),0) — numeric, never NULL. So no row satisfying the digest filter can carry a NULL total. The split-brain framing also fails its own "same data row" test: the feed's NightDetailSheet renders the host's forked private copy, never the public catalog row the digest links — the two surfaces never render the same row, and where values flow (fork copies total_cost_pp verbatim) they agree numerically. What remains is a latent style asymmetry (`?? 0` in weekly-digest.ts:39 and dates/[slug]/page.tsx:122 vs the feed's omit-when-null/zero convention at NightDetailSheet.tsx:229-230) that could only bite if a future writer started producing public, slugged, NULL-total rows — defensive-coding feedback, not a reachable coherence bug today.
- **Source-vs-fork price snapshot: host edits to a plan after posting update the picker/canvas price but not the live night, with no snapshot indication on either side**
  - The mechanics are accurately described, but this is not a split-brain derivation: the two surfaces render two DIFFERENT rows (source plan vs post_night fork) whose divergence is a deliberate, explicitly commented design ("the canonical generated plan is never touched by later edits", 20260605120200_e11 migration). Every surface that renders the NIGHT (feed sheet, offer, lock, host's /my-nights) consistently reads the fork; /my-nights shows no price at all (selects only title/cover/inputs), so the host is never shown an incorrect price for the live night on any surface. The picker card at /nights/new is plan metadata on a plan-selection screen and makes no claim about any posted night. The snapshot semantic is also the correct behavior — silently repricing a live night under candidates who already expressed interest at the old price would be the real bug. What remains is a mental-model/affordance gap (no "posted nights keep their original details" hint), which the finding itself concedes is "deliberate snapshot design"; that is a UX enhancement request, not a coherence bug where a user sees two surfaces disagree about the same data row.
- **/offers/[offerId] derives liveness from expires_at only; offers.status is fetched, passed as a prop, and never read — resolved offers render a live countdown with enabled accept/pass, and pass/not-interested on them no-op while reporting success**
  - Already fixed in the current tree. The finding claims OfferDetail.tsx fetches offers.status but never reads it; in the file as it exists now (/Users/lucas/projects/After5/apps/web/app/offers/[offerId]/OfferDetail.tsx), status IS destructured (line 65) and is the master gate: the ExpiryCountdown plus accept/pass/not-interested buttons mount only inside a status==='active' branch (lines 212-257); status==='accepted' renders a "you're locked in" state linking to /matches/{lockId} (page.tsx lines 108-117 resolves the lock id); 'passed'/'expired' render terminal copy. The file header (lines 10-16, "COHERENCE (live crawl 2026-06-10)") documents this exact remediation, and the finding's cited line numbers (28/46/49/138/150/161) do not match the current file — the finding was generated against a stale pre-fix version. With the UI gated on status, no user can see a resolved offer with a live countdown or trigger the silent no-op pass/withdraw from this surface; the DB-side silent-0 return in match_resolve_offer_negative remains but is unreachable defense-in-depth, not a user-visible split-brain.
- **Matches list renders name-only while feed, offer, lock detail and reveal all render 'name, age' — the age is even fetched, then dropped**
  - All cited code facts verify (matches page selects age, MatchesList.tsx:83 renders name only; feed/offer/lock-detail render 'name, age'; rate page over-fetches), but the finding fails the bug-class criterion it was filed under. The two surfaces never disagree: the matches list shows a strict subset ('maya') of what the detail shows ('maya, 31'), both derived identically from the same profiles.first_name column — no wrong, stale, or conflicting value is ever displayed. The finding itself concedes 'omission, not disagreement', which is dispositive: a split-brain bug needs two derivations that can diverge, and an unrendered field has no second derivation. The compact matches-list card (avatar + name + night title + time + rate chip) reads as a deliberate density choice for an already-matched logistics roster, not a broken fallback like the archetype's failed photo signing. The rate-page dead fetch is shared-select-shape over-fetching (same select string feeding pickCounterpart/LockRowWithParties), a micro-efficiency nit, not coherence. Legitimate low-priority polish suggestion (add ', age' to the card or trim the select), but not this bug class.
- **Accessible-name casing split: aria-labels and alt text carry stored casing where the visible text is CSS-lowercased, and the normalization layer differs page to page**
  - Code facts verified (verbatim first_name in aria-labels/alt at InterestedList.tsx:281/290/302/342/350/361, RevealModal.tsx:47/50, ThreadList.tsx:55 vs JS-lowercased MatchesList.tsx:65; no write-time casing normalization per packages/validators/src/profile.ts:104), but the bug fails the perceptibility bar. No visible surface ever disagrees — all visible name renderings are lowercase via JS or CSS. The split lives only in accessible-name strings, where both surfaces convey the same correct name; screen readers pronounce 'Maya'/'maya'/'MAYA' identically under default settings, and the repro requires an all-caps stored name plus non-default SR verbosity. Unlike the archetype (face vs initial = different facts), this is a normalization-style inconsistency, not two surfaces disagreeing about a data row.
- **Teaser feed and browse_feed RPC are two independent derivations of host_first_name/host_age feeding the same renderers (latent split, currently consistent)**
  - Not a user-visible coherence bug, by the finding's own admission ("Not currently user-observable (projections match)"). Verified in code: both paths project profiles.first_name/age verbatim with no divergent fallback/format (teaser.ts:141-142 vs dlb02 migration line 101), both feed the identical FeedNight type and the same NightCard/NightDetailSheet renderers, so no second derivation of display logic exists. Additionally, the two surfaces are mutually exclusive per user — feed/page.tsx:23-33 routes verified viewers to the RPC and pre-verification viewers to the teaser — so a single user can never compare the two renderings of the same row, ruling out the archetype's same-user list/detail contradiction even hypothetically. The duplication is a deliberate, heavily documented mirror contract (teaser.ts:8-21, 67-71), i.e. a maintenance hazard requiring a future code change to manifest, not an existing split-brain symptom.
- **Dormant divergent count: GET /api/notifications unreadCount includes never-cleared new_message rows that every live surface excludes**
  - The mechanics check out (GET /api/notifications counts unread new_message rows that chat_mark_read never clears, while /api/inbox/unread and /api/inbox/activity filter them out), but the divergent count is never rendered anywhere: the only client uses of /api/notifications are POST mark-read calls in apps/web/app/inbox/ActivityList.tsx; no component consumes the GET or its unreadCount. One side of the claimed split-brain is dead code reachable only via curl, so no user can ever see two surfaces disagree for the same row. By the stated criterion (real only if user-visible disagreement; one path being dead code refutes it), this is a latent hygiene issue on a stale endpoint, not a real coherence bug.
- **Latent notif:read contract mismatch: {all:true} zeroes the chat component of the badge, and decrements trust the client's ids.length over the server's updated count**
  - Code claims all verify ({all:true} zeroes the combined activity+chat badge count while markAllRead only clears notifications rows; decrements trust client ids.length over the server's returned updated count, and flattenItem invents per-member read_at). But the disagreement is unobservable by construction: the ONLY notif:read dispatcher (ActivityList) mounts solely on /inbox (inbox/page.tsx:167), and /inbox renders <BottomTabShell /> without userId (line 185), so InboxTabBadge never mounts there (BottomTabShell.tsx:97 gates on userId). Every page that mounts the badge has no dispatcher; window events don't cross browser tabs; BottomTabShell is per-page so no stale listener survives navigation; the badge re-seeds from server truth (/api/inbox/unread) on every mount. Dispatcher and listener never co-exist, so no user can ever see the badge disagree with the thread pills — the event path is dead code today, and the finding's own repro admits 'Not reproducible today.' Per the verification criterion (real only if two surfaces can visibly disagree for the same data), this is a latent contract mismatch / tripwire, not a live coherence bug. Worth a hardening note on any future change that passes user.id into the /inbox (or /feed) shell.