---
phase: 07-enhancements-and-polish-p3
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260606140100_e23_browse_feed_city_and_tune.sql
  - supabase/tests/e23_feed_contract.sql
autonomous: true
requirements: [REQ-E22, REQ-E23]
must_haves:
  truths:
    - "browse_feed_for_viewer returns exactly the 14 e10 cols + 3 e15 host-hint cols + city_name; anon stays non-executable; the (starts_at,id) keyset is byte-identical (D-03)"
    - "feed order reflects relevance: vibe overlap weighted by COUNT of matched tags (not boolean) plus a light mutual-compat nudge, keeping the targeting×4 term and keyset stable (D-03)"
    - "city_name = cities.name on the card; distance uses venue/first-stop coords when present else city-centroid (E23 finer distance)"
  artifacts:
    - path: "supabase/migrations/20260606140100_e23_browse_feed_city_and_tune.sql"
      provides: "browse_feed_for_viewer DROP+CREATE built on the e15 body: +city_name, tuned soft-score, finer distance"
      contains: "drop function if exists browse_feed_for_viewer"
    - path: "supabase/tests/e23_feed_contract.sql"
      provides: "REGRESSION: column set + anon-non-exec + keyset stability + city_name correctness"
  key_links:
    - from: "supabase/migrations/20260606140100_e23_browse_feed_city_and_tune.sql"
      to: "cities"
      via: "cc.name as city_name (existing cc join, e15:72)"
      pattern: "as city_name"
    - from: "supabase/migrations/20260606140100_e23_browse_feed_city_and_tune.sql"
      to: "authenticated role"
      via: "re-emitted grant tail after DROP"
      pattern: "grant\\s+execute on function browse_feed_for_viewer"
---

<objective>
One feed-RPC re-CREATE covers E22 (relevance tune) and E23 (city label + finer distance). Built on the CURRENT e15 body, DROP+CREATE because the return shape changes, re-emitting the verbatim privilege tail and preserving every e10 + host-hint column and the keyset tail.

Purpose: round out relevance + the human city label without rebuilding the feed (D-03). The regression test is the most important new test in the phase — it locks the privacy/host-hint/keyset contract.
Output: browse_feed_for_viewer migration + e23 contract SQL test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-enhancements-and-polish-p3/07-RESEARCH.md
@.planning/phases/07-enhancements-and-polish-p3/07-PATTERNS.md

<interfaces>
Build on the LIVE body: supabase/migrations/20260606120000_e15_browse_feed_host_hint.sql.
  - signature/RETURNS TABLE: e15:21,28-36 (14 e10 cols + host_blurred_photo_url, host_first_name, host_age)
  - distance (city-centroid today): e15:57 `st_distance(cc.centroid, me.pt) as distance_m`
  - existing joins: `cities cc` at e15:72, `left join places pl on pl.id = di.venue_id` at e15:73
  - hard gates: e15:88-103 (gender/age/price/distance) — PRESERVE all
  - ORDER BY score block + keyset tail: e15:107-118
  - revoke/grant tail: e15:121-123
Latest applied migration: 20260606130200_e19_lock_rpc_producers.sql — new file MUST timestamp after it.
NightCard.tsx:54-56 already reads + lowercases city_name (no UI change needed here).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: browse_feed_for_viewer DROP+CREATE — city_name + finer distance + tuned soft-score</name>
  <files>supabase/migrations/20260606140100_e23_browse_feed_city_and_tune.sql</files>
  <read_first>
    supabase/migrations/20260606120000_e15_browse_feed_host_hint.sql (FULL — this is the body to build on, not e10).
    07-PATTERNS.md "20260606140100_e23_browse_feed_city_and_tune.sql" assignment (signature, distance, score, tail).
    07-RESEARCH.md "Code Examples / E23 feed-RPC tune + city_name" + Pitfall 1 + Pitfall 3.
  </read_first>
  <behavior>
    - RETURNS TABLE = the exact 14 e10 cols + host_blurred_photo_url + host_first_name + host_age + new `city_name text`.
    - city_name = cities.name (via the existing cc join); no new join added for the label.
    - distance_m = st_distance over venue coords when pl.lat/pl.lng present, else cc.centroid (finer distance, city-centroid fallback).
    - ORDER BY score: targeting×4 term unchanged; vibe overlap = COUNT of matched tags (cardinality of the intersect array) not boolean; who_pays + time_bucket terms unchanged; + a light mutual-compat nudge (both sides' gender prefs align). Keyset tail `di.starts_at asc, di.id asc` byte-identical.
    - anon cannot EXECUTE; authenticated can.
  </behavior>
  <action>
    Create supabase/migrations/20260606140100_e23_browse_feed_city_and_tune.sql (timestamp after 20260606130200, unique prefix). DROP+CREATE (not CREATE OR REPLACE — adding city_name changes the RETURNS TABLE so Postgres resets privileges; Pitfall 1 / e15 "Pitfall 2 / T-05-03"): `drop function if exists browse_feed_for_viewer(uuid, geography, timestamptz, uuid, int);` then re-create from the e15 body. Append `city_name text` after host_age in RETURNS TABLE; add `cc.name as city_name` to the SELECT (cc cities join already exists at e15:72). Replace the e15:57 distance with the venue-coord-else-centroid expression (pl is the existing `left join places pl on pl.id = di.venue_id`). Tune ONLY the score expression inside the existing `order by (<score>) desc, di.starts_at asc, di.id asc`: vibe boolean → `coalesce(cardinality(array(select unnest(it.vibe_tags) intersect select unnest(f.vibes))), 0)`, plus `+ (case when me.gender = any(cr.gender_preferences) and cr.gender = any(me.gender_preferences) then 1 else 0 end)`. Keep the keyset tail byte-identical (Pitfall 3 / D-03). Preserve all 14 e10 cols, the 3 host-hint cols, the date_trunc('hour') time-blind, and every hard gate (e15:88-103). Re-emit the revoke public / revoke anon / grant authenticated tail verbatim. Keep `language sql security definer set search_path = public, extensions`. Implements REQ-E22 (D-03) + REQ-E23.
  </action>
  <verify>
    <automated>psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -f supabase/tests/e23_feed_contract.sql</automated>
  </verify>
  <acceptance_criteria>
    - source: migration is DROP+CREATE; RETURNS TABLE appends `city_name text`; SELECT has `cc.name as city_name`; distance uses `coalesce(...st_point(pl.lng, pl.lat)::geography..., cc.centroid)`; score uses `cardinality(array(... intersect ...))` + the mutual-compat case; keyset tail unchanged; revoke/grant tail re-emitted.
    - behavior: all e23_feed_contract.sql assertions pass.
    - test-command: e23_feed_contract.sql exits clean against local.
  </acceptance_criteria>
  <done>Feed RPC re-CREATEs cleanly on a fresh local reset; contract test green; anon denied; keyset stable.</done>
</task>

<task type="auto">
  <name>Task 2: e23_feed_contract.sql — the regression lock</name>
  <files>supabase/tests/e23_feed_contract.sql</files>
  <read_first>
    07-RESEARCH.md §Validation Architecture (the e23 contract test row) + "The feed-RPC re-CREATE regression check ... is the single most important new test".
    An existing supabase/tests/*.sql (e.g. e10_browse_feed_filters.sql) for the harness/assertion style + a known seed night + cities row to assert city_name.
  </read_first>
  <action>
    Create supabase/tests/e23_feed_contract.sql asserting: (1) the function's returned column set is EXACTLY 14 e10 cols + host_blurred_photo_url + host_first_name + host_age + city_name (query pg_proc / information_schema or call the RPC and assert the row keys); (2) anon has NO EXECUTE privilege and authenticated does (check has_function_privilege); (3) keyset stability — calling with `(p_after_starts, p_after_id)` from page 1's last row yields page 2 with no duplicate or skipped date_instance_id across the boundary; (4) city_name equals cities.name for a known seed night. This test is consumed by Task 1's verify and by the Plan 09 phase gate. Note: this test can be authored first (RED) — it will fail until Task 1's migration is applied.
  </action>
  <verify>
    <automated>psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -f supabase/tests/e23_feed_contract.sql</automated>
  </verify>
  <acceptance_criteria>
    - source: the file asserts column set, anon-non-exec, keyset no-dup/no-skip, and city_name correctness.
    - behavior: passes once Task 1's migration is applied to the local stack.
    - test-command: psql run exits 0 with all assertions passing.
  </acceptance_criteria>
  <done>e23_feed_contract.sql locks the column/privilege/keyset/city_name contract and passes against the new RPC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → browse_feed_for_viewer (DEFINER) | blind feed read; the RPC must keep the host-hint contract (limited hint only) and never re-expose to anon |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Information Disclosure | feed RPC re-CREATE drops host-hint cols / re-grants anon | mitigate | Build on the e15 body; preserve all 14 e10 + 3 host-hint cols; re-emit verbatim revoke anon/public + grant authenticated; e23_feed_contract.sql asserts column set + anon denied |
| T-07-05 | Tampering | DEFINER search_path | mitigate | Keep `set search_path = public, extensions` |
| T-07-06 | Denial of Service | keyset pagination instability from the E22 tune | mitigate | Change only the score expression; keep `(starts_at,id)` keyset tail byte-identical (D-03); e23 test asserts no dup/skip across the page boundary |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (07-RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- e23_feed_contract.sql passes: exact column set, anon non-exec, keyset stable, city_name correct.
- Migration timestamp 20260606140100 sorts after 20260606130200.
- No host-hint column or hard gate dropped vs the e15 body.
</verification>

<success_criteria>
browse_feed_for_viewer returns city_name + finer distance + a stronger (count-weighted + mutual-compat) relevance order, with the privacy/host-hint/keyset contract provably intact.
</success_criteria>

<output>
Create `.planning/phases/07-enhancements-and-polish-p3/07-02-SUMMARY.md` when done.
</output>
