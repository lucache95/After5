---
phase: 07-enhancements-and-polish-p3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api-client/src/feed.ts
  - supabase/migrations/20260606140000_e20_get_night_detail_coords.sql
  - supabase/tests/e20_night_detail_coords.sql
autonomous: true
requirements: [REQ-E20, REQ-E23, REQ-E24]
must_haves:
  truths:
    - "get_night_detail returns per-stop lat/lng/place_slug for catalog stops; a non-catalog place_id degrades to null with no row error (D-01 graceful degrade)"
    - "NightDetailStop carries place_slug; FeedNight carries city_name; a withdrawInterest wrapper exists — the single feed.ts contract every downstream Phase-7 frontend plan consumes"
    - "get_night_detail stays CREATE OR REPLACE (stops jsonb shape unchanged, contents widen), search_path pinned, anon non-executable"
  artifacts:
    - path: "supabase/migrations/20260606140000_e20_get_night_detail_coords.sql"
      provides: "get_night_detail re-CREATE adding lat/lng/place_slug per stop"
      contains: "create or replace function get_night_detail"
    - path: "packages/api-client/src/feed.ts"
      provides: "place_slug on NightDetailStop + normalizer, city_name on FeedNight, withdrawInterest wrapper"
      contains: "withdrawInterest"
    - path: "supabase/tests/e20_night_detail_coords.sql"
      provides: "per-stop coords + non-catalog degrade assertion"
  key_links:
    - from: "supabase/migrations/20260606140000_e20_get_night_detail_coords.sql"
      to: "places"
      via: "left join places pj on pj.id = (s->>'place_id')::uuid"
      pattern: "left join places pj"
    - from: "packages/api-client/src/feed.ts"
      to: "queue_entries"
      via: "withdraw_interest RPC wrapper"
      pattern: "withdraw_interest"
---

<objective>
Establish the Phase-7 data contract: re-CREATE get_night_detail so every stop carries lat/lng/place_slug (E20 map + E21 venue links), and make ALL feed.ts type/wrapper additions for the phase in one place so the three Wave-1 DB plans and the Wave-2/3 frontend plans never collide on feed.ts.

Purpose: feed.ts is the shared hotspot (E20 needs place_slug on NightDetailStop, E23 needs city_name on FeedNight, E24 needs the withdrawInterest wrapper). Owning all of it here keeps Wave 1 parallel.
Output: get_night_detail migration + e20 SQL test + the consolidated feed.ts contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-enhancements-and-polish-p3/07-RESEARCH.md
@.planning/phases/07-enhancements-and-polish-p3/07-PATTERNS.md

<interfaces>
Current NightDetailStop (packages/api-client/src/feed.ts:219-232) ALREADY declares lat/lng but the RPC never populates them; it lacks place_slug.
normalizeNightDetailStops (feed.ts:260-279) maps rich stop keys (place_name, estimated_cost_pp). The rich itineraries.stops element key for the catalog id is CONFIRMED `place_id` and may also carry `place_slug` directly (07-PATTERNS Q1 resolved).
FeedNight (feed.ts:4-22) has the 14 e10 cols + 3 e15 host-hint cols; lacks city_name.
Existing wrapper shape to mirror: cancelNight/updateNight (feed.ts:78-113) + actorId(client) (feed.ts:68-74).
get_night_detail current body: supabase/migrations/20260601210000_m5_get_night_detail.sql — stops scrub at :64-69, revoke/grant tail at :88-90, `language sql security definer set search_path = public, extensions` at :42.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: feed.ts Phase-7 contract (place_slug, city_name, withdrawInterest)</name>
  <files>packages/api-client/src/feed.ts</files>
  <read_first>
    packages/api-client/src/feed.ts:4-22 (FeedNight), :219-279 (NightDetailStop + normalizeNightDetailStops), :68-113 (actorId + cancelNight/updateNight wrapper shape).
    07-PATTERNS.md "packages/api-client/src/feed.ts (MODIFY)" assignment.
  </read_first>
  <action>
    Add `place_slug: string | null;` to the NightDetailStop interface and `place_slug: str(o.place_slug),` to normalizeNightDetailStops (the rich itineraries.stops element carries place_slug; the E20 RPC merges it too, so both the LockDetail direct-itinerary loader and the feed RPC flow through this one normalizer — per E24/D-01 graceful degrade, missing slug is null). Add `city_name: string | null;` to FeedNight (the RPC will return it; NightCard.tsx:54-56 already reads + lowercases the slot, no component change). Export a new `withdrawInterest(client, input: { instance_id: string }): Promise<void>` wrapper mirroring cancelNight/updateNight: read uid via actorId(client), call client.rpc('withdraw_interest', { p_instance, p_actor }), `if (error) throw error`. Do NOT change any existing field. This wrapper backs REQ-E24; place_slug backs REQ-E20/E21; city_name backs REQ-E23.
  </action>
  <verify>
    <automated>pnpm --filter @after5/api-client build && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: NightDetailStop has `place_slug: string | null`; FeedNight has `city_name: string | null`; `withdrawInterest` is exported.
    - behavior: normalizeNightDetailStops reads o.place_slug into place_slug.
    - test-command: `pnpm --filter @after5/api-client build && pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>feed.ts carries place_slug + city_name + withdrawInterest; package builds; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: get_night_detail re-CREATE (per-stop lat/lng/place_slug) + e20 SQL test</name>
  <files>supabase/migrations/20260606140000_e20_get_night_detail_coords.sql, supabase/tests/e20_night_detail_coords.sql</files>
  <read_first>
    supabase/migrations/20260601210000_m5_get_night_detail.sql (full — stops scrub :64-69, definer/search_path :42, revoke/grant tail :88-90).
    07-PATTERNS.md "20260606140000_e20_get_night_detail_coords.sql" assignment (the widening block).
    supabase/migrations/20260419193959_initial_schema.sql:33,37-38 (places.slug, places.lat/lng decimal(9,6)).
    An existing supabase/tests/*.sql for the assertion harness style (e.g. e13_plan_read.sql).
  </read_first>
  <behavior>
    - For a night whose stops reference catalog places, each returned stop jsonb element has non-null lat, lng, place_slug.
    - For a stop whose place_id is not in places, that element has lat/lng/place_slug = null and the call still succeeds (no row error).
    - reservation_url stays scrubbed from each stop element.
    - anon cannot EXECUTE get_night_detail; authenticated can.
  </behavior>
  <action>
    Create supabase/migrations/20260606140000_e20_get_night_detail_coords.sql (timestamp strictly after the latest applied 20260606130200, unique prefix — Phase-6 ordering lesson). Use CREATE OR REPLACE (the RETURNS TABLE shape is unchanged: `stops jsonb` stays the same column; only element contents widen). Copy the m5 body verbatim EXCEPT the stops scrub block: enrich each element by left-joining places on the verified `place_id` key and merging lat/lng/place_slug, keeping the `- 'reservation_url'` scrub — `(s - 'reservation_url') || jsonb_build_object('lat', pj.lat, 'lng', pj.lng, 'place_slug', pj.slug)` over `jsonb_array_elements(it.stops) as s left join places pj on pj.id = (s->>'place_id')::uuid`. Keep `language sql security definer set search_path = public, extensions`. Re-emit the revoke public / revoke anon / grant authenticated tail verbatim. Then write supabase/tests/e20_night_detail_coords.sql asserting the behaviors above against a catalog stop and a non-catalog stop (use a fixture place_id absent from places). Implements REQ-E20 per Claude's-Discretion (coords at the DEFINER RPC boundary, never a client-side places query).
  </action>
  <verify>
    <automated>psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -f supabase/tests/e20_night_detail_coords.sql</automated>
  </verify>
  <acceptance_criteria>
    - source: migration uses `create or replace function get_night_detail`, joins `places pj` on `(s->>'place_id')::uuid`, merges lat/lng/place_slug, keeps the reservation_url scrub and the revoke/grant tail.
    - behavior: catalog stop → non-null coords/slug; non-catalog stop → null coords/slug, no error.
    - test-command: e20_night_detail_coords.sql runs and all assertions pass against the local stack.
  </acceptance_criteria>
  <done>Migration applies on a fresh local reset; e20 SQL test passes; anon denied, authenticated granted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → get_night_detail (DEFINER) | a pre-swipe viewer requests blind-safe night detail; the RPC scrubs host identity and reservation_url |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | get_night_detail re-CREATE | mitigate | Add coords INSIDE the DEFINER RPC (never a client-side places query that could correlate a stop to a host); keep the reservation_url scrub; keep host identity out of the payload (m5 contract preserved) |
| T-07-02 | Elevation of Privilege | get_night_detail privileges | mitigate | Re-emit verbatim `revoke public/anon; grant authenticated` after CREATE OR REPLACE; e20 test asserts anon denied |
| T-07-03 | Tampering | DEFINER search_path | mitigate | Keep `set search_path = public, extensions` on the re-CREATE |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (07-RESEARCH §Package Legitimacy Audit: zero external packages) |
</threat_model>

<verification>
- `pnpm --filter @after5/api-client build && pnpm typecheck` green.
- e20_night_detail_coords.sql passes on the local stack.
- Migration timestamp 20260606140000 sorts strictly after 20260606130200.
</verification>

<success_criteria>
get_night_detail returns per-stop lat/lng/place_slug (catalog) and degrades to null (non-catalog) with the host-blind contract + privileges intact; feed.ts carries the full Phase-7 contract (place_slug, city_name, withdrawInterest).
</success_criteria>

<output>
Create `.planning/phases/07-enhancements-and-polish-p3/07-01-SUMMARY.md` when done.
</output>
