---
phase: 07-enhancements-and-polish-p3
plan: 07
type: execute
wave: 2
depends_on: ["07-01", "07-03"]
files_modified:
  - apps/web/components/StandbyCard.tsx
  - apps/web/app/inbox/StandbyList.tsx
  - apps/web/app/inbox/page.tsx
  - apps/web/components/__tests__/StandbyCard.test.tsx
autonomous: true
requirements: [REQ-E24]
must_haves:
  truths:
    - "a candidate sees their queue position from their own queue_entries row: rank=1 -> 'you're next in line', rank>1 -> 'you're #{rank} in line', with the soft sub-line 'if the spot opens up, you're up.' (E24, no promotion promise)"
    - "a candidate can withdraw a plain interested interest via a neutral (not red, not accent) 'pull my interest' control behind a vaul confirm; success toasts 'pulled. you're off this one.' then refreshes (E24)"
    - "the standby surface reads the candidate's OWN interested queue rows via RLS (candidate_id = auth.uid()); the withdraw calls the withdraw_interest RPC wrapper"
  artifacts:
    - path: "apps/web/components/StandbyCard.tsx"
      provides: "Tier-1 shell standby card: position line + sub-line + neutral withdraw + vaul confirm + sonner"
      min_lines: 40
    - path: "apps/web/app/inbox/StandbyList.tsx"
      provides: "candidate-facing read of own interested queue rows mounting StandbyCard(s)"
    - path: "apps/web/app/inbox/page.tsx"
      provides: "mounts the candidate standby section"
      contains: "Standby"
  key_links:
    - from: "apps/web/app/inbox/StandbyList.tsx"
      to: "queue_entries"
      via: "RLS select status,rank where candidate_id = auth.uid() and status='interested'"
      pattern: "queue_entries"
    - from: "apps/web/components/StandbyCard.tsx"
      to: "withdraw_interest"
      via: "withdrawInterest wrapper"
      pattern: "withdrawInterest"
---

<objective>
Ship the E24 candidate-side standby/waitlist UI: a Tier-1 shell StandbyCard (position line + soft sub-line + neutral withdraw behind a vaul confirm) mounted on the candidate's /inbox surface, reading the candidate's own interested queue rows and withdrawing via the Plan-03 RPC.

Purpose: a candidate's plain interested queue row has no dedicated surface today (Wave-0 trace resolved in 07-PATTERNS Q1: /inbox is the candidate's "what's happening to me" surface). This adds the read view + the withdraw control. Promotion logic is deferred — the sub-line makes no auto-promotion promise.
Output: StandbyCard component + StandbyList read view + /inbox mount + StandbyCard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-enhancements-and-polish-p3/07-RESEARCH.md
@.planning/phases/07-enhancements-and-polish-p3/07-PATTERNS.md
@.planning/phases/07-enhancements-and-polish-p3/07-UI-SPEC.md
@.planning/phases/07-enhancements-and-polish-p3/07-01-SUMMARY.md
@.planning/phases/07-enhancements-and-polish-p3/07-03-SUMMARY.md

<interfaces>
withdrawInterest(client, { instance_id }) — wrapper added in Plan 01 (calls withdraw_interest RPC from Plan 03).
queue_entries candidate-read RLS: queue_candidate_read_own (candidate_id = auth.uid()); select status, rank — supabase/migrations/20260525120500_p0_queue_entries.sql:33-36. rank null until shortlisted.
Neutral vaul confirm + sonner analog: apps/web/app/matches/[lockId]/LockDetail.tsx:260-279 (the `cancel this date` neutral button + vaul sheet — overlay bg-black/40, rounded-t-3xl bg-shell-base, grab handle). messageForCode for error copy.
/inbox: apps/web/app/inbox/page.tsx — candidate's offer/notification surface; loads user via supabase.auth.getUser() (:54); RLS-bound server client. ActivityList at :147.
UI-SPEC §E24 copy: position lines, sub-line `if the spot opens up, you're up.`, control `pull my interest` (neutral), confirm title `pull your interest?` / body `you'll drop off this night's list. you can always slide back in later.` / button `yep, pull it`; success toast `pulled. you're off this one.`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StandbyCard.tsx — position + neutral withdraw + vaul confirm + sonner</name>
  <files>apps/web/components/StandbyCard.tsx</files>
  <read_first>
    apps/web/app/matches/[lockId]/LockDetail.tsx:260-279 (neutral button + vaul cancel pattern).
    07-UI-SPEC.md §"E24 — candidate standby/waitlist UI" + §Copywriting (all E24 copy) + §Color (neutral, not accent/red).
    07-PATTERNS.md §"Neutral vaul confirm + sonner (E24 withdraw)".
  </read_first>
  <behavior>
    - Given rank=1 → renders `you're next in line`; rank>1 → `you're #{rank} in line`; sub-line `if the spot opens up, you're up.`.
    - The `pull my interest` control is a neutral secondary button (rounded-full border-2 border-shell-ink/20 text-shell-ink/70 — NOT accent fill, NOT red).
    - Tapping it opens a vaul confirm (title `pull your interest?`, body copy, button `yep, pull it`); confirming calls withdrawInterest, on success toasts `pulled. you're off this one.` then refreshes; on error toasts messageForCode.
    - The card is a Tier-1 shell surface (warm-cream / shell tokens, NOT a vibe palette).
  </behavior>
  <action>
    Create apps/web/components/StandbyCard.tsx (`'use client'`). Props: the candidate's queue row (instance_id, rank, status) + the night label. Render the position line + soft sub-line (UI-SPEC copy verbatim, lowercase, no em-dashes). The `pull my interest` neutral button mirrors LockDetail's cancel button (border-2 border-shell-ink/20, text-shell-ink/70, >=44px tap target, focus-visible:ring-4 ring-shell-accent/40). Confirm via a vaul Drawer mirroring LockDetail:269-279 (overlay bg-black/40, Drawer.Content rounded-t-3xl bg-shell-base p-6, grab handle). On confirm call `withdrawInterest(client, { instance_id })`; success → sonner `pulled. you're off this one.` + `router.refresh()`; error → sonner error via messageForCode. Tier-1 shell tokens only (no vibePalette). Implements REQ-E24 (the UI half). Do NOT build promotion logic; the sub-line makes no auto-promotion promise.
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/components/__tests__/StandbyCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: rank-conditional position copy; sub-line verbatim; neutral (not accent/red) withdraw control; vaul confirm with the UI-SPEC title/body/button; calls withdrawInterest; sonner success/error.
    - behavior: rank=1 and rank>1 render the right line; confirm path invokes the withdraw wrapper.
    - test-command: StandbyCard.test.tsx passes.
  </acceptance_criteria>
  <done>StandbyCard shows the queue position, withdraws via the RPC behind a neutral vaul confirm, toasts on success.</done>
</task>

<task type="auto">
  <name>Task 2: StandbyList read view + /inbox mount</name>
  <files>apps/web/app/inbox/StandbyList.tsx, apps/web/app/inbox/page.tsx</files>
  <read_first>
    apps/web/app/inbox/page.tsx (FULL — getUser :54, the RLS server client, section layout, ActivityList mount :147).
    07-PATTERNS.md §"No Analog Found" (the Wave-0 placement resolution — /inbox candidate area).
    supabase/migrations/20260525120500_p0_queue_entries.sql:33-36 (candidate-read RLS).
  </read_first>
  <action>
    Create apps/web/app/inbox/StandbyList.tsx — a server component (or a client island fed by an SSR read) that selects the candidate's own pending interest rows: `queue_entries.select('date_instance_id, status, rank')` filtered `candidate_id = user.id` AND `status = 'interested'` (RLS also enforces candidate_id = auth.uid()), joined/looked-up to a night label for display. Render one StandbyCard per row under a section eyebrow `your queue` (UI-SPEC eyebrow pattern). Mount StandbyList in apps/web/app/inbox/page.tsx in the candidate area (near ActivityList). When the candidate has no pending interest rows, render nothing (the section only shows when a queue entry exists — UI-SPEC §States "n/a (only shown when a queue entry exists)"). All copy lowercase, no em-dashes. Implements REQ-E24 (read path + placement).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm vitest run apps/web/app/inbox/__tests__ 2>/dev/null || pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: StandbyList selects queue_entries filtered candidate_id=user.id AND status='interested'; mounts StandbyCard per row; page.tsx mounts StandbyList; hidden when empty.
    - behavior: only the candidate's own interested rows surface; no host or cross-user rows.
    - test-command: `pnpm typecheck` green (and the inbox test if present).
  </acceptance_criteria>
  <done>The candidate sees their pending-interest standby cards on /inbox; the section is hidden when empty.</done>
</task>

<task type="auto">
  <name>Task 3: StandbyCard.test.tsx</name>
  <files>apps/web/components/__tests__/StandbyCard.test.tsx</files>
  <read_first>
    Existing apps/web/components/__tests__/*.test.tsx for RTL + vaul/sonner mocking style.
    07-VALIDATION.md (StandbyCard Wave-0 row).
  </read_first>
  <action>
    Create apps/web/components/__tests__/StandbyCard.test.tsx asserting: rank=1 → "you're next in line"; rank=3 → "you're #3 in line"; the `pull my interest` control is present; opening the confirm shows the UI-SPEC confirm copy. Author RED-first where practical.
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/components/__tests__/StandbyCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: covers rank=1, rank>1, withdraw-control presence, confirm copy.
    - test-command: passes.
  </acceptance_criteria>
  <done>Standby position copy + withdraw control locked by the test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /inbox SSR read → queue_entries | candidate reads OWN rows only (RLS candidate_id = auth.uid()) |
| StandbyCard → withdraw_interest | candidate mutates own interest; RPC re-checks auth.uid() (Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Information Disclosure | StandbyList shows another candidate's queue row | mitigate | RLS queue_candidate_read_own (candidate_id = auth.uid()) + explicit `candidate_id = user.id` filter |
| T-07-15 | Elevation of Privilege | withdraw another user's interest from the UI | mitigate | withdrawInterest passes p_actor = actorId(client); the RPC's auth.uid() gate (Plan 03 T-07-07) is the boundary |
| T-07-16 | Information Disclosure | standby card leaks host identity pre-match | mitigate | StandbyCard shows only the night label + the candidate's own rank/status; no host identity rendered |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (vaul/sonner present; 07-RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- StandbyCard.test.tsx green; `pnpm typecheck` green.
- StandbyList reads only candidate_id = auth.uid() interested rows.
- Withdraw control is neutral (no accent fill, no red).
</verification>

<success_criteria>
A candidate sees their pending-interest queue position on /inbox and can pull that interest via a secure, neutral, confirm-gated control.
</success_criteria>

<output>
Create `.planning/phases/07-enhancements-and-polish-p3/07-07-SUMMARY.md` when done.
</output>
