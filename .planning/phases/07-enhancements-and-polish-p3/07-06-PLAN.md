---
phase: 07-enhancements-and-polish-p3
plan: 06
type: execute
wave: 3
depends_on: ["07-04"]
files_modified:
  - apps/web/app/matches/[lockId]/LockDetail.tsx
  - apps/web/app/matches/[lockId]/page.tsx
  - apps/web/app/places/[slug]/page.tsx
  - apps/web/app/matches/__tests__/lock-detail-slug.test.tsx
autonomous: true
requirements: [REQ-E21]
must_haves:
  truths:
    - "post-match on LockDetail (identity revealed) each stop with a catalog slug links its NAME to /places/[slug]; LockDetail is the ONLY caller that sets linkSlugs=true (D-01)"
    - "a stop with no catalog slug renders its name as plain text, never a broken link or 404 (D-01 graceful degrade)"
    - "the legacy 'build a date here' -> /create CTAs are removed from /places/[slug]; the page becomes a read-only post-match identity surface; no /places global nav (D-01)"
  artifacts:
    - path: "apps/web/app/matches/[lockId]/LockDetail.tsx"
      provides: "PlanTimeline rendered with linkSlugs (post-lock reveal)"
      contains: "linkSlugs"
    - path: "apps/web/app/places/[slug]/page.tsx"
      provides: "venue page with the /create CTAs retired"
  key_links:
    - from: "apps/web/app/matches/[lockId]/LockDetail.tsx"
      to: "apps/web/components/PlanTimeline.tsx"
      via: "linkSlugs prop set true (the only true caller)"
      pattern: "linkSlugs"
    - from: "apps/web/app/matches/[lockId]/page.tsx"
      to: "normalizeNightDetailStops"
      via: "place_slug carried through the loader"
      pattern: "normalizeNightDetailStops"
---

<objective>
Turn on venue links post-match (E21): LockDetail passes linkSlugs to PlanTimeline (the only caller that does), its loader carries place_slug through the normalizer, and the legacy /create CTAs on /places/[slug] are retired so the venue page is a read-only post-match identity surface.

Purpose: venue identity belongs in the loop ONLY after the match (D-01). The blind feed/offer surfaces must never link slugs — that's enforced by linkSlugs defaulting off (Plan 04) and only LockDetail enabling it here.
Output: LockDetail + loader changes + places CTA retirement + the slug-link test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-enhancements-and-polish-p3/07-PATTERNS.md
@.planning/phases/07-enhancements-and-polish-p3/07-UI-SPEC.md
@.planning/phases/07-enhancements-and-polish-p3/07-04-SUMMARY.md
@.planning/phases/07-enhancements-and-polish-p3/07-01-SUMMARY.md

<interfaces>
PlanTimeline (from Plan 04): accepts `linkSlugs?: boolean` (default false); links the stop name to /places/[slug] only when linkSlugs && place_slug.
normalizeNightDetailStops (from Plan 01): reads o.place_slug into NightDetailStop.place_slug.
LockDetail.tsx:244 currently `<PlanTimeline stops={stops} accent={accent} vibeTags={vibeTags} />` — add linkSlugs (post-lock, revealed; the ONLY true caller).
matches/[lockId]/page.tsx:119-129 is LockDetail's stop loader: `.from('itineraries').select('stops, vibe_tags')` then `normalizeNightDetailStops(it?.stops)`. The whole stops jsonb is already selected, so once the normalizer reads place_slug this loader carries it automatically — verify rich rows carry place_slug; null → plain text (D-01).
places/[slug]/page.tsx: retire /create CTAs at :263-269 (header nav `build a date here` + ArrowRight) and :459-465 (body `build a date with this idea` + Sparkles). Do NOT replace with a dating CTA. Check for now-unused ArrowRight/Sparkles imports.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LockDetail passes linkSlugs (post-lock reveal) + verify loader carries place_slug</name>
  <files>apps/web/app/matches/[lockId]/LockDetail.tsx, apps/web/app/matches/[lockId]/page.tsx</files>
  <read_first>
    apps/web/app/matches/[lockId]/LockDetail.tsx:244 (PlanTimeline usage).
    apps/web/app/matches/[lockId]/page.tsx:119-129 (the stop loader + normalizeNightDetailStops).
    07-PATTERNS.md §"LockDetail.tsx + page.tsx (MODIFY)" + §"Open Questions RESOLVED Q2".
    07-UI-SPEC.md §"E21 — venues into the loop (post-match only)".
  </read_first>
  <behavior>
    - LockDetail renders PlanTimeline with linkSlugs={true} — the only caller that does.
    - A stop whose normalized place_slug is present links its name to /places/[slug]; a stop without a slug renders plain text.
    - The loader at page.tsx:119-129 carries place_slug (it selects the whole stops jsonb and runs the normalizer that now reads place_slug).
  </behavior>
  <action>
    In LockDetail.tsx:244 add `linkSlugs` (=true) to the PlanTimeline call (post-lock, identity revealed). Confirm matches/[lockId]/page.tsx:119-129 selects the full `stops` jsonb (it does) so the Plan-01 normalizer carries place_slug with no extra select change; if a richer select is needed to surface place_slug, add it minimally. For rows whose rich stops lack place_slug, the slug is null → plain text (D-01 graceful degrade). Do NOT set linkSlugs anywhere else. Implements REQ-E21 (the post-match venue link).
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/app/matches/__tests__/lock-detail-slug.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: LockDetail's PlanTimeline call sets linkSlugs=true; the loader carries place_slug through normalizeNightDetailStops.
    - behavior: slug present → /places link; slug absent → plain text.
    - test-command: lock-detail-slug.test.tsx passes.
  </acceptance_criteria>
  <done>LockDetail links stop names to /places/[slug] post-match, degrading to plain text without a slug; it is the only linkSlugs caller.</done>
</task>

<task type="auto">
  <name>Task 2: retire the /create CTAs on /places/[slug]</name>
  <files>apps/web/app/places/[slug]/page.tsx</files>
  <read_first>
    apps/web/app/places/[slug]/page.tsx:263-269 + :459-465 (the two /create CTAs) + the import block.
    07-UI-SPEC.md §"Retired copy (E21 / D-01)" + 07-PATTERNS.md §"places/[slug]/page.tsx (MODIFY)".
  </read_first>
  <action>
    Delete the two `/create` CTAs in apps/web/app/places/[slug]/page.tsx: the header nav `<Link href="/create">build a date here<ArrowRight/></Link>` (:263-269) and the body `<Link href="/create"><Sparkles/>build a date with this idea</Link>` (:459-465). Do NOT replace with a dating-vertical CTA — the page becomes a read-only post-match identity surface (D-01). Remove now-unused imports (ArrowRight / Sparkles / any /create-only helper). Confirm there is no `/places` entry in the bottom-tab shell (verify; do not add one). Implements REQ-E21 (retire the legacy funnel).
  </action>
  <verify>
    <automated>grep -c 'href="/create"' apps/web/app/places/\[slug\]/page.tsx | grep -qx 0 && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: no `href="/create"` remains in places/[slug]/page.tsx; no unused ArrowRight/Sparkles import; no replacement dating CTA added.
    - behavior: the venue page renders read-only (no build-a-date funnel).
    - test-command: grep count is 0 AND `pnpm typecheck` green.
  </acceptance_criteria>
  <done>The legacy /create funnel is gone from venue pages; the page is a read-only identity surface; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: lock-detail-slug.test.tsx</name>
  <files>apps/web/app/matches/__tests__/lock-detail-slug.test.tsx</files>
  <read_first>
    Existing apps/web/app/matches/__tests__/ (or component test) style for rendering LockDetail/PlanTimeline with fixture stops.
    07-VALIDATION.md (Plan 06 verify row).
  </read_first>
  <action>
    Create apps/web/app/matches/__tests__/lock-detail-slug.test.tsx asserting that with linkSlugs on, a stop with place_slug renders a /places/[slug] link and a stop without place_slug renders plain text. (The blind-off case is already covered by PlanTimeline.test.tsx in Plan 04.) Author RED-first where practical.
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/app/matches/__tests__/lock-detail-slug.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: covers slug-present link + slug-absent plain text under linkSlugs.
    - test-command: passes.
  </acceptance_criteria>
  <done>Post-match slug linking + graceful degrade locked by the test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LockDetail (post-lock, revealed) | venue identity is allowed here; the slug link is enabled ONLY on this revealed surface |
| /places/[slug] page | a read-only post-match identity surface; no funnel into the legacy planner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-18 | Information Disclosure | slug link enabled on a non-revealed surface | mitigate | linkSlugs set ONLY in LockDetail (post-lock); default-off elsewhere (Plan 04); PlanTimeline + NightDetailSheet tests assert blind surfaces stay link-free |
| T-07-19 | Information Disclosure | broken /places link / 404 leaking a non-catalog place | mitigate | slug-absent stops render plain text (graceful degrade, D-01); test covers it |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (07-RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- lock-detail-slug.test.tsx green; `pnpm typecheck` green.
- `grep -c 'href="/create"' apps/web/app/places/[slug]/page.tsx` == 0.
- LockDetail is the only linkSlugs=true caller (grep across callers).
</verification>

<success_criteria>
Post-match venue links work on LockDetail (graceful degrade without a slug), the legacy /create funnel is retired from venue pages, and the blind contract holds everywhere else.
</success_criteria>

<output>
Create `.planning/phases/07-enhancements-and-polish-p3/07-06-SUMMARY.md` when done.
</output>
