---
phase: 07-enhancements-and-polish-p3
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/my-nights/page.tsx
  - apps/web/app/my-nights/__tests__/archive-bucket.test.tsx
autonomous: true
requirements: [REQ-E25]
must_haves:
  truths:
    - "/my-nights has an upcoming/archive two-segment toggle: upcoming = seeking/matched/active, archive = completed/expired/cancelled (D-02 scoped E25)"
    - "archive reuses the existing NightCard row + lifecycleLabel corner chip — no new card; an empty archive shows the funny empty state"
    - "toggle segments are >=44px tap targets, active segment uses the app's existing selected-state convention (accent or shell.pink)"
  artifacts:
    - path: "apps/web/app/my-nights/page.tsx"
      provides: "upcoming/archive segment + status bucketing over the already-fetched rows"
      contains: "archive"
    - path: "apps/web/app/my-nights/__tests__/archive-bucket.test.tsx"
      provides: "bucket filter + empty-state assertion"
  key_links:
    - from: "apps/web/app/my-nights/page.tsx"
      to: "date_instances.status"
      via: "bucket filter (upcoming vs archive)"
      pattern: "completed|expired|cancelled"
---

<objective>
Ship the E25 (D-02 scoped) archive view: a two-segment upcoming/archive toggle on /my-nights bucketing the host's nights by status. No new card — reuse the existing row treatment.

Purpose: the only E25 UI work besides the detail skeleton (D-02 — draft/typing/receipts/claim are deferred). Independent of all DB and other frontend work.
Output: my-nights segment toggle + archive bucket test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-enhancements-and-polish-p3/07-PATTERNS.md
@.planning/phases/07-enhancements-and-polish-p3/07-UI-SPEC.md

<interfaces>
apps/web/app/my-nights/page.tsx — SSR-fetches the host's own nights (creator_id = user.id, :164-169); has lifecycleLabel (:48-56) + existing NightCard row + corner chip.
status values: 'seeking','matched','active','completed','expired','cancelled'.
upcoming = ['seeking','matched','active']; archive = ['completed','expired','cancelled'].
UI-SPEC §E25 archive: tab labels `upcoming` (default) / `archive`, lowercase; empty archive heading `nothing in the rear-view yet`, body `your past nights and matches land here once they wrap.`; >=44px tap targets; active segment uses accent or shell.pink.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: upcoming/archive segment toggle on /my-nights</name>
  <files>apps/web/app/my-nights/page.tsx</files>
  <read_first>
    apps/web/app/my-nights/page.tsx (FULL — :48-56 lifecycleLabel, :164-169 fetch, the existing row/card render).
    apps/web/app/my-nights/__tests__/ (existing tests for the render harness).
    07-UI-SPEC.md §"E25 — archive view/tab" + §Copywriting (tab labels + empty state).
  </read_first>
  <behavior>
    - Default segment = upcoming; rows shown are status in seeking/matched/active.
    - Switching to archive shows rows with status in completed/expired/cancelled.
    - Empty archive renders heading `nothing in the rear-view yet` + body `your past nights and matches land here once they wrap.`.
    - Each segment control is a >=44px tap target; the active segment uses the existing selected-state convention.
  </behavior>
  <action>
    Add a two-segment client toggle (a small `'use client'` segment control, or a client wrapper around the existing list) filtering the already-fetched nights by bucket — do NOT add a second DB query. Reuse the existing NightCard row + lifecycleLabel corner chip (no new card design). Labels lowercase `upcoming` / `archive`, default `upcoming`. Empty archive → the UI-SPEC funny empty state. Segments >=44px tap targets; active segment uses accent or shell.pink per the app's existing selected-state convention; focus-visible ring `focus-visible:ring-4 ring-shell-accent/40`. All copy lowercase, dry, no em-dashes. Implements REQ-E25 (D-02 archive half).
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/app/my-nights/__tests__/archive-bucket.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: page renders an upcoming/archive toggle; bucketing filters in-memory rows by status; empty-archive copy matches UI-SPEC verbatim.
    - behavior: archive shows completed/expired/cancelled; upcoming shows seeking/matched/active.
    - test-command: archive-bucket.test.tsx passes.
  </acceptance_criteria>
  <done>The toggle buckets correctly, reuses the existing card, shows the funny empty state, and meets the >=44px / focus-ring contract.</done>
</task>

<task type="auto">
  <name>Task 2: archive-bucket.test.tsx</name>
  <files>apps/web/app/my-nights/__tests__/archive-bucket.test.tsx</files>
  <read_first>
    apps/web/app/my-nights/__tests__/ (existing test style + fixtures).
    07-VALIDATION.md (the archive-bucket Wave-0 row).
  </read_first>
  <action>
    Create apps/web/app/my-nights/__tests__/archive-bucket.test.tsx asserting the bucket filter (render a fixture set spanning all 6 statuses; assert upcoming shows only seeking/matched/active and archive shows only completed/expired/cancelled) and the empty-archive copy. Author first (RED) if practical.
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/app/my-nights/__tests__/archive-bucket.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: test covers both buckets + empty archive.
    - test-command: passes against the implemented toggle.
  </acceptance_criteria>
  <done>Bucket filter + empty-state behavior locked by the test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR read → /my-nights | host reads their OWN nights (creator_id = user.id); no new data path introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Information Disclosure | archive bucket exposes another user's nights | accept | No new query — bucketing filters the existing creator_id-scoped fetch in memory; RLS unchanged |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (07-RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- archive-bucket.test.tsx green.
- No new DB query added; existing creator_id-scoped fetch reused.
</verification>

<success_criteria>
A host can toggle between upcoming and archived nights on /my-nights, with the archive reusing the existing card + chip and a funny empty state.
</success_criteria>

<output>
Create `.planning/phases/07-enhancements-and-polish-p3/07-08-SUMMARY.md` when done.
</output>
