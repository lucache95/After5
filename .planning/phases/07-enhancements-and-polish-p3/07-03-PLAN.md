---
phase: 07-enhancements-and-polish-p3
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260606140200_e24_withdraw_interest.sql
  - supabase/tests/e24_withdraw_interest.sql
autonomous: true
requirements: [REQ-E24]
must_haves:
  truths:
    - "withdraw_interest deletes ONLY the caller's own plain interested queue_entries row; a non-owner call raises auth_mismatch (P5001); shortlisted/offer/locked rows are never touched"
    - "withdraw_interest is the only write path (queue_entries is default-deny on write, C7); it pins search_path, revokes public/anon, grants authenticated"
    - "a candidate can read their own queue_entries rank/status via existing RLS and cannot read another candidate's row"
  artifacts:
    - path: "supabase/migrations/20260606140200_e24_withdraw_interest.sql"
      provides: "withdraw_interest DEFINER RPC — lighter sibling of match_withdraw"
      contains: "create or replace function withdraw_interest"
    - path: "supabase/tests/e24_withdraw_interest.sql"
      provides: "deny-non-owner + auth + status-scope + RLS deny assertions"
  key_links:
    - from: "supabase/migrations/20260606140200_e24_withdraw_interest.sql"
      to: "queue_entries"
      via: "delete where candidate_id = p_actor and status = 'interested'"
      pattern: "delete from queue_entries"
---

<objective>
Add the candidate's plain-interest withdraw path: a new DEFINER RPC withdraw_interest that deletes only the caller's own `interested` queue row, mirroring match_withdraw's auth.uid() gate but dropping the v2-gate, advisory lock, and offer resolution.

Purpose: E24's "withdraw a pending interest" is distinct from the offer-stage withdraw. queue_entries is default-deny on write, so a DEFINER RPC is the only correct mutation path.
Output: withdraw_interest migration + e24 SQL test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-enhancements-and-polish-p3/07-RESEARCH.md
@.planning/phases/07-enhancements-and-polish-p3/07-PATTERNS.md

<interfaces>
Analog: match_withdraw in supabase/migrations/20260527126800_p5_pass_expire_withdraw.sql:87-118 — copy the auth gate (`if p_actor is distinct from auth.uid() then raise exception 'auth_mismatch' using errcode='P5001'; end if;`), drop the rest.
queue_entries: supabase/migrations/20260525120500_p0_queue_entries.sql — status enum includes interested/shortlisted/standby; NO insert/update/delete RLS policy (default-deny on write, C7, :23-26); candidate-read RLS `queue_candidate_read_own` at :33-36 (candidate_id = auth.uid()).
PARAM ORDER (locked correction): required param must precede a defaulted one — signature is `withdraw_interest(p_instance uuid, p_actor uuid default auth.uid())`.
Latest applied migration: 20260606130200 — new file timestamps after it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: withdraw_interest DEFINER RPC</name>
  <files>supabase/migrations/20260606140200_e24_withdraw_interest.sql</files>
  <read_first>
    supabase/migrations/20260527126800_p5_pass_expire_withdraw.sql:87-118 (match_withdraw auth pattern).
    supabase/migrations/20260525120500_p0_queue_entries.sql:23-36 (default-deny write C7 + candidate-read RLS).
    07-PATTERNS.md "20260606140200_e24_withdraw_interest.sql" assignment (full RPC + why DEFINER is the only path).
  </read_first>
  <behavior>
    - withdraw_interest(p_instance, p_actor=auth.uid()) deletes the row where date_instance_id=p_instance AND candidate_id=p_actor AND status='interested'.
    - When p_actor is distinct from auth.uid() → raise auth_mismatch (P5001), no delete.
    - A shortlisted / offer_* / locked / standby row for the same candidate+instance is NOT deleted.
    - anon cannot EXECUTE; authenticated can.
  </behavior>
  <action>
    Create supabase/migrations/20260606140200_e24_withdraw_interest.sql (timestamp after 20260606130200, unique prefix). Signature `withdraw_interest(p_instance uuid, p_actor uuid default auth.uid())` (required param FIRST — locked param-order correction). Body: the auth.uid() gate raising 'auth_mismatch' errcode 'P5001', then `delete from queue_entries where date_instance_id = p_instance and candidate_id = p_actor and status = 'interested';`. `language plpgsql security definer set search_path = public`. NO USING(true), no v2 gate, no advisory lock, no offer resolution, no notification (pre-offer plain-interest withdraw). Re-swipe-after-withdraw is allowed (Claude's discretion): MVP simplest is delete the interest only — do NOT also delete the swipe row. Re-emit `revoke execute on function withdraw_interest(uuid, uuid) from public, anon; grant execute on function withdraw_interest(uuid, uuid) to authenticated;`. Implements REQ-E24 (backend half).
  </action>
  <verify>
    <automated>psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -f supabase/tests/e24_withdraw_interest.sql</automated>
  </verify>
  <acceptance_criteria>
    - source: signature has p_instance before the defaulted p_actor; DELETE predicate is `candidate_id = p_actor and status = 'interested'`; auth gate raises P5001; search_path pinned; revoke public/anon + grant authenticated present.
    - behavior: all e24_withdraw_interest.sql assertions pass.
    - test-command: e24_withdraw_interest.sql exits clean against local.
  </acceptance_criteria>
  <done>withdraw_interest deletes only the caller's own interested row, blocks non-owners with P5001, leaves other statuses intact, anon non-executable.</done>
</task>

<task type="auto">
  <name>Task 2: e24_withdraw_interest.sql — deny-non-owner + status-scope + RLS</name>
  <files>supabase/tests/e24_withdraw_interest.sql</files>
  <read_first>
    07-RESEARCH.md §Validation Architecture (e24 test row) + §Security Domain (withdraw + RLS threats).
    An existing supabase/tests/*.sql with role-switching (e.g. b_pass_expire_withdraw.sql, a_revealed_rls_negative.sql) for the set-local-role / auth.uid() simulation style.
  </read_first>
  <action>
    Create supabase/tests/e24_withdraw_interest.sql asserting: (1) calling withdraw_interest as the owner deletes only their own `interested` row for that instance; (2) a non-owner actor → raises P5001 (auth_mismatch), no delete; (3) a same-candidate `shortlisted` (or offer/locked) row for the same instance survives the call; (4) candidate-read RLS — a candidate can SELECT their own queue_entries(status, rank) and a different candidate cannot read it (queue_candidate_read_own deny-non-owner). Use the project's existing role/auth-simulation harness. Authored first (RED) — fails until Task 1's migration applies.
  </action>
  <verify>
    <automated>psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -f supabase/tests/e24_withdraw_interest.sql</automated>
  </verify>
  <acceptance_criteria>
    - source: asserts owner-delete-only, non-owner P5001, status-scope preservation, RLS deny-non-owner read.
    - behavior: passes once Task 1's migration is applied.
    - test-command: psql run exits 0.
  </acceptance_criteria>
  <done>e24 SQL test green; the auth + status-scope + RLS guarantees are locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → withdraw_interest (DEFINER) | a candidate mutates their own queue row; queue_entries is default-deny on write so the RPC's auth check IS the boundary |
| client → queue_entries (RLS read) | a candidate reads their own rank/status; RLS denies cross-user reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Elevation of Privilege | withdraw another user's interest | mitigate | `p_actor is distinct from auth.uid()` → P5001 + `candidate_id = p_actor` predicate in the DELETE |
| T-07-08 | Tampering | wrong-status delete (removing an offer/locked row) | mitigate | `status = 'interested'` predicate; e24 test asserts shortlisted/offer/locked rows untouched |
| T-07-09 | Tampering | DEFINER search_path injection | mitigate | `set search_path = public` on the new RPC; no USING(true) |
| T-07-10 | Information Disclosure | candidate reads another candidate's queue row | mitigate | existing RLS `queue_candidate_read_own` (candidate_id = auth.uid()); e24 test asserts deny-non-owner |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (07-RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- e24_withdraw_interest.sql passes: owner-only delete, P5001 non-owner, status-scope, RLS deny.
- Migration timestamp 20260606140200 sorts after 20260606130200.
</verification>

<success_criteria>
A candidate can withdraw their own pending interest via a secure-by-default DEFINER RPC that cannot touch another user's row or a non-interested row.
</success_criteria>

<output>
Create `.planning/phases/07-enhancements-and-polish-p3/07-03-SUMMARY.md` when done.
</output>
