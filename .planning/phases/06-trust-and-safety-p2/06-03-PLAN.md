---
phase: 06-trust-and-safety-p2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260605120100_e19_safety_dispatch_rpcs.sql
  - supabase/functions/process-jobs/handlers.ts
  - apps/web/app/matches/[lockId]/LockDetail.tsx
  - apps/web/app/matches/[lockId]/page.tsx
  - supabase/tests/e19_safety_handlers.sql
autonomous: true
requirements: [REQ-E19]

must_haves:
  truths:
    - "A day_of_reconfirm job dispatches a date_reconfirm notification ('still on?') to both parties (per D-04)"
    - "A safety_checkin job dispatches a safety_checkin notification ('all good?') to both parties (per D-03)"
    - "Both dispatch RPCs NEVER raise on a cancelled/resolved/missing lock — they drain cleanly, no poison-loop (handler callRpc throws on RPC error)"
    - "A no-ack reconfirm does NOT auto-cancel the lock — locks.status is unchanged (per D-04, soft warning only)"
    - "LockDetail renders the soft 'still on?' reconfirm + 'all good?' check-in cards (no red, no auto-escalation, per D-03/D-04), gated by loader flags like the existing ratingOpen block"
  artifacts:
    - path: "supabase/migrations/20260605120100_e19_safety_dispatch_rpcs.sql"
      provides: "dispatch_date_reconfirm(uuid) + dispatch_safety_checkin(uuid) stale-tolerant DEFINER RPCs"
      contains: "dispatch_date_reconfirm"
    - path: "supabase/functions/process-jobs/handlers.ts"
      provides: "day_of_reconfirm + safety_checkin entries in the HANDLERS table"
      contains: "dispatch_date_reconfirm"
    - path: "apps/web/app/matches/[lockId]/LockDetail.tsx"
      provides: "reconfirm + check-in soft cards gated by loader flags"
    - path: "supabase/tests/e19_safety_handlers.sql"
      provides: "SQL assertions: both handlers dispatch; idempotent/never-raise; no-ack does NOT auto-cancel"
  key_links:
    - from: "supabase/functions/process-jobs/handlers.ts"
      to: "dispatch_date_reconfirm / dispatch_safety_checkin"
      via: "callRpc in the HANDLERS table"
      pattern: "dispatch_(date_reconfirm|safety_checkin)"
    - from: "supabase/migrations/20260605120100_e19_safety_dispatch_rpcs.sql"
      to: "dispatch_notification"
      via: "perform dispatch_notification for both parties"
      pattern: "dispatch_notification"
---

<objective>
Deliver the E19 consumer half: two stale-tolerant DEFINER dispatch RPCs (date_reconfirm + safety_checkin), the two job handlers that call them, and the soft 'still on?' / 'all good?' surfaces on LockDetail. The producer half (enqueues in the two lock RPCs) is plan 06-04, which depends on these RPCs and job handlers existing.

Purpose: the back half of the loop reaches out — a morning-of reconfirm and a post-date safety ping — without ever blocking, auto-cancelling, or alarming (D-03/D-04 soft posture). Safety notifications already bypass consent/quiet/rate-limit in dispatch_notification.
Output: one gated migration (two dispatch RPCs), two handler entries, the LockDetail soft cards + loader flags, and the SQL safety-critical assertions (dispatch, poison-loop idempotency, no-auto-cancel).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trust-and-safety-p2/06-RESEARCH.md
@.planning/phases/06-trust-and-safety-p2/06-PATTERNS.md
@.planning/phases/06-trust-and-safety-p2/06-UI-SPEC.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

From supabase/functions/process-jobs/handlers.ts:
- export async function callRpc(db, fn, args): Promise<void> — THROWS on RPC error (this is why dispatch RPCs must never raise on a resolved lock)
- const id = (j, k) => (j.payload[k] as string | undefined) ?? null;
- export const HANDLERS: Record<string, Handler> = { ... rating_window: async (db, job) => { await callRpc(db, "close_rating_window", { p_lock: id(job, "lock_id") }); }, ... }
  Mirror the rating_window entry for the two new handlers.

From close_rating_window (20260527127200_p5_job_rpcs_backfill.sql:79-94) — the never-raise template:
- `if p_lock is null then return; end if;` (null guard)
- `select ... into ... from locks where id=p_lock;` then `if not found then return; end if;` (stale: drain)
- `if <already-resolved/wrong-status> then return; end if;` (idempotent no-op)
- trailing `revoke all on function ... from public, anon, authenticated;`

From flag_no_show (20260604121000_e5_loop_completion.sql:137-140) — the lock-load shape:
- `select creator_id, matched_user_id, date_instance_id, status into cre, matched, inst, lst from locks where id=p_lock;`

From dispatch_notification (20260525123600_p2_dispatch_notification.sql):
- signature: dispatch_notification(p_user uuid, p_type notification_type, p_payload jsonb)
- safety types (date_reconfirm/safety_checkin/safety_alert) bypass consent/quiet/rate-limit; fail-loud to admin_alert
- notification_type enum already includes date_reconfirm, safety_checkin, safety_alert (20260525123400)

From apps/web/app/matches/[lockId]/LockDetail.tsx:
- export interface LockDetailProps { lockId; status; counterpart; threadId; startsAt; ratingOpen: boolean; justLocked; photos; photoError; prompts; stops; vibeTags }
- The ratingOpen gate (lines 139-146): `{ratingOpen && status !== 'cancelled' && ( <Link .../> )}` — copy this gate shape for the new cards.
- The existing cancel flow (lines 148-168): vaul Drawer + CancelWithReasonPicker + onCancel — the "gotta bail" path REUSES this; do NOT add a new destructive surface.
- sonner `toast` already imported (line 6).

From apps/web/app/matches/[lockId]/page.tsx (loader):
- selects the lock row (id, status, locked_at, rating_closed_at, date_instance_id, instance:date_instances(...)) and computes ratingOpen via isRatingOpen(lock.instance). Add the reconfirm/check-in flags here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dispatch_date_reconfirm + dispatch_safety_checkin DEFINER RPCs (migration)</name>
  <files>supabase/migrations/20260605120100_e19_safety_dispatch_rpcs.sql</files>
  <read_first>
    - supabase/migrations/20260527127200_p5_job_rpcs_backfill.sql:79-94,100 (never-raise template + revoke-all)
    - supabase/migrations/20260604121000_e5_loop_completion.sql:137-140 (lock-load shape)
    - supabase/migrations/20260525123600_p2_dispatch_notification.sql (dispatch_notification signature + safety bypass)
    - 06-RESEARCH.md §Pattern 4 (full skeleton) + §Migrations List row (b)
    - 06-PATTERNS.md "2026060x_e19_safety_dispatch_rpcs.sql" entry
  </read_first>
  <action>
    Create supabase/migrations/20260605120100_e19_safety_dispatch_rpcs.sql with TWO functions, both language plpgsql, security definer, set search_path=public. (1) dispatch_date_reconfirm(p_lock uuid) returns void: null-guard returns early; `select creator_id, matched_user_id, date_instance_id, status into cre, matched, inst, lst from locks where id=p_lock`; `if cre is null then return; end if;` (stale: drain cleanly, NOT raise); `if lst <> 'active' then return; end if;` (cancelled/no_show/completed: no reconfirm, drain — poison-loop avoidance); then `perform dispatch_notification(cre, 'date_reconfirm', jsonb_build_object('lock_id', p_lock, 'instance', inst));` and the same for `matched`. (2) dispatch_safety_checkin(p_lock uuid) mirrors (1) exactly but dispatches type `'safety_checkin'`. Trailing grants: `revoke all on function dispatch_date_reconfirm(uuid) from public, anon, authenticated;` and the same for dispatch_safety_checkin (handler/service-role only). Do NOT add any enforcement, status mutation, or auto-cancel — these only dispatch notifications (D-03/D-04). Do NOT apply to prod — local apply gated to 06-05.
  </action>
  <verify>
    <automated>grep -q "dispatch_date_reconfirm" supabase/migrations/20260605120100_e19_safety_dispatch_rpcs.sql && grep -q "dispatch_safety_checkin" supabase/migrations/20260605120100_e19_safety_dispatch_rpcs.sql && grep -c "set search_path=public" supabase/migrations/20260605120100_e19_safety_dispatch_rpcs.sql | grep -qx 2 && grep -c "lst <> 'active'" supabase/migrations/20260605120100_e19_safety_dispatch_rpcs.sql | grep -qx 2 && ! grep -qi "update locks set status" supabase/migrations/20260605120100_e19_safety_dispatch_rpcs.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - source: both RPCs present, dispatch to both parties, never raise on stale/non-active lock (drain cleanly)
    - security: both pin search_path=public + revoke-all from public/anon/authenticated; no USING(true)
    - behavior: NO status mutation / auto-cancel in either RPC (soft posture D-03/D-04) — grep asserts no `update locks set status`
    - test-command: grep gate prints OK (local apply + advisor gated to 06-05)
  </acceptance_criteria>
  <done>Two stale-tolerant dispatch RPCs that notify both parties and drain cleanly on any non-active lock.</done>
</task>

<task type="auto">
  <name>Task 2: Add day_of_reconfirm + safety_checkin handlers to the HANDLERS table</name>
  <files>supabase/functions/process-jobs/handlers.ts</files>
  <read_first>
    - supabase/functions/process-jobs/handlers.ts:26-67 (callRpc, id helper, HANDLERS table, the rating_window entry at :64)
    - supabase/migrations/20260605120100_e19_safety_dispatch_rpcs.sql (Task 1 output — the RPC names)
    - 06-RESEARCH.md §Code Examples (handlers.ts additions) + §Pattern 4
    - 06-PATTERNS.md "process-jobs/handlers.ts" entry
  </read_first>
  <action>
    In supabase/functions/process-jobs/handlers.ts add two entries to the HANDLERS table, mirroring the rating_window entry exactly: `day_of_reconfirm: async (db, job) => { await callRpc(db, "dispatch_date_reconfirm", { p_lock: id(job, "lock_id") }); },` and `safety_checkin: async (db, job) => { await callRpc(db, "dispatch_safety_checkin", { p_lock: id(job, "lock_id") }); },`. No new imports. The job_type enum already includes day_of_reconfirm + safety_checkin (20260525123000). The process-jobs edge function redeploys on this change (deploy gated to 06-05).
  </action>
  <verify>
    <automated>grep -q "day_of_reconfirm: async" supabase/functions/process-jobs/handlers.ts && grep -q "safety_checkin: async" supabase/functions/process-jobs/handlers.ts && grep -q "dispatch_date_reconfirm" supabase/functions/process-jobs/handlers.ts && grep -q "dispatch_safety_checkin" supabase/functions/process-jobs/handlers.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - source: both handlers present, each calling the matching dispatch RPC with p_lock from the job payload
    - behavior: handlers mirror the rating_window pattern (callRpc → DEFINER RPC); never-raise guaranteed by the Task-1 RPCs
    - test-command: grep gate prints OK
  </acceptance_criteria>
  <done>The cron consumer routes the two safety job types to their dispatch RPCs.</done>
</task>

<task type="auto">
  <name>Task 3: Soft reconfirm + check-in cards on LockDetail + loader flags</name>
  <files>apps/web/app/matches/[lockId]/LockDetail.tsx, apps/web/app/matches/[lockId]/page.tsx</files>
  <read_first>
    - apps/web/app/matches/[lockId]/LockDetail.tsx:19-43 (LockDetailProps), :139-146 (ratingOpen gate shape), :148-168 (cancel flow / vaul Drawer to reuse for "gotta bail"), :6 (sonner import)
    - apps/web/app/matches/[lockId]/page.tsx (loader — where ratingOpen is computed; add reconfirm/check-in flags)
    - 06-UI-SPEC.md §E19 (card chrome `rounded-3xl bg-shell-ink/[0.05] p-4`, titles `still on?` / `all good?`, buttons + copy, blush soft-warning, NO red, "something's wrong" → vaul confirm dispatching safety_alert)
    - 06-PATTERNS.md "LockDetail.tsx" entry (loader-flag gate + cancel-flow reuse + toasts)
  </read_first>
  <action>
    Extend LockDetailProps with `reconfirmDue: boolean`, `reconfirmNoReply: boolean`, `checkinDue: boolean` (mirror the existing `ratingOpen: boolean`). Render three conditional blocks gated like the ratingOpen block: (1) reconfirmDue → a `rounded-3xl bg-shell-ink/[0.05] p-4` card above the night section, title `still on?` (font-heading text-xl lowercase), two buttons `yep, still on` (semibold ink on a soft fill — acks the reconfirm) and `gotta bail` (quiet outline that opens the EXISTING cancel flow / vaul Drawer — do NOT add a new destructive surface, NO auto-cancel). (2) reconfirmNoReply → a single quiet line on a blush/amber low-opacity wash: `no reply on the day-of check yet.` — NO CTA, NO red, NO escalation (D-04 soft warning). (3) checkinDue → same card chrome, title `all good?`, primary `all good` ack + a quiet secondary text-link `something's wrong` that opens a vaul confirm sheet `flag this date?` with a single accent `yes, flag it` action (the only accent affordance here) — this is the sole surface that should result in a safety_alert dispatch. Ack/flag fire sonner toasts per the UI-SPEC copy contract (`good. have fun.` / `flagged. someone's on it.` / failure `didn't go through. try that again.`). Use cn() for conditional classes; reuse the existing vaul + sonner imports; introduce NO red/destructive token. In matches/[lockId]/page.tsx compute reconfirmDue/reconfirmNoReply/checkinDue from lock + notification/job state (a date_reconfirm/safety_checkin notification exists for this lock and is unacked / past-window) and pass them to LockDetail; keep the derivation light, mirroring isRatingOpen.
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit && grep -q "still on?" apps/web/app/matches/\[lockId\]/LockDetail.tsx && grep -q "all good?" apps/web/app/matches/\[lockId\]/LockDetail.tsx && grep -q "reconfirmDue" apps/web/app/matches/\[lockId\]/LockDetail.tsx && ! grep -qiE "bg-red|text-red|destructive" apps/web/app/matches/\[lockId\]/LockDetail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: LockDetailProps has reconfirmDue/reconfirmNoReply/checkinDue; three gated blocks render; loader supplies the flags
    - behavior: "gotta bail" reuses the existing cancel flow (no new destructive surface, no auto-cancel); "something's wrong" opens a vaul confirm; acks/flags toast
    - color: no red/destructive token introduced (grep asserts no bg-red/text-red/destructive); blush soft-warning; neutral card fill (UI-SPEC §Color)
    - test-command: `tsc --noEmit` clean + grep gate (titles present, no red)
  </acceptance_criteria>
  <done>The date lifecycle surface shows warm, soft reconfirm + check-in states; visual-verify @420px deferred to 06-05.</done>
</task>

<task type="auto">
  <name>Task 4 (Wave 0 SQL scaffold): e19 safety-handlers SQL assertions (dispatch + poison-loop + no-auto-cancel)</name>
  <files>supabase/tests/e19_safety_handlers.sql</files>
  <read_first>
    - supabase/migrations/20260605120100_e19_safety_dispatch_rpcs.sql (Task 1 output)
    - supabase/migrations/20260525123600_p2_dispatch_notification.sql (notifications table the assertions read)
    - 06-RESEARCH.md §Validation Architecture → REQ-E19 rows; §Pitfall 3
    - 06-VALIDATION.md (the 4 safety-critical assertions)
  </read_first>
  <action>
    Create supabase/tests/e19_safety_handlers.sql as a SQL assertion script (local-apply posture). Seed a completed-loop fixture (a lock with two party profiles + a date_instance). Assert: (a) call dispatch_date_reconfirm(lock) on an ACTIVE lock → assert a date_reconfirm notification row exists for BOTH parties. (b) call dispatch_safety_checkin(lock) on an active lock → assert a safety_checkin notification for both parties. (c) POISON-LOOP/idempotency: cancel the lock (status != 'active'), call dispatch_date_reconfirm again → assert it returns void with NO error and NO new notification rows (drains cleanly). (d) NO-AUTO-CANCEL (D-04): on an active lock, call dispatch_date_reconfirm (simulate a no-ack reconfirm), then assert locks.status is UNCHANGED (still 'active') — the reconfirm never mutates lock status. RAISE EXCEPTION on any failed assertion. Executed in 06-05 after local migration apply; this task authors it.
  </action>
  <verify>
    <automated>test -f supabase/tests/e19_safety_handlers.sql && grep -q "dispatch_date_reconfirm" supabase/tests/e19_safety_handlers.sql && grep -q "dispatch_safety_checkin" supabase/tests/e19_safety_handlers.sql && grep -qi "status" supabase/tests/e19_safety_handlers.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - source: script covers all 4 safety-critical assertions (both dispatch, poison-loop idempotency, no-auto-cancel)
    - behavior: assertions RAISE on mismatch; the cancelled-lock call asserts clean drain + 0 new rows; the active-lock call asserts unchanged status
    - test-command: file exists with both RPC names + a status assertion (grep gate prints OK)
  </acceptance_criteria>
  <done>The safety-critical SQL contract (dispatch + never-raise + no-auto-cancel) is authored, ready for 06-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job consumer (service-role) → dispatch RPCs | handlers run as service-role; the RPCs validate the lock they act on |
| client → safety_alert dispatch | the "something's wrong" path triggers a safety_alert (routes to mod/admin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | DoS / Repudiation | safety job poison-loop | mitigate | dispatch RPCs never raise on cancelled/resolved/missing lock (drain cleanly) → no backoff/dead-letter storm; SQL assertion (d) proves it |
| T-06-09 | Elevation | dispatch RPC search_path hijack | mitigate | both RPCs `set search_path=public`; advisor run in 06-05 |
| T-06-10 | Tampering | dispatch RPC mutating lock state (unintended enforcement) | mitigate | RPCs ONLY dispatch_notification; grep gate asserts no `update locks set status`; honors D-03/D-04 soft posture |
| T-06-11 | Repudiation | safety dispatch silently dropped | accept | dispatch_notification fails loud to admin_alert on safety types (existing fail-loud chain) — no new path |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs this plan (RESEARCH §Package Legitimacy Audit — zero installs) |
</threat_model>

<verification>
- `pnpm --filter web exec tsc --noEmit` clean
- both dispatch RPCs present, search_path-pinned, revoke-all, no status mutation (grep gates)
- both handlers route to the dispatch RPCs
- LockDetail soft cards render with no red; loader supplies the flags
- supabase/tests/e19_safety_handlers.sql authored with all 4 safety-critical assertions (run in 06-05)
</verification>

<success_criteria>
The two safety dispatch RPCs notify both parties and never poison-loop on a resolved lock; the handlers route the two job types; LockDetail shows soft 'still on?' / 'all good?' states with no auto-cancel, no enforcement, no red. REQ-E19 consumer half complete (producers in 06-04; local apply + advisor + visual-verify in 06-05).
</success_criteria>

<output>
Create `.planning/phases/06-trust-and-safety-p2/06-03-SUMMARY.md` when done
</output>
