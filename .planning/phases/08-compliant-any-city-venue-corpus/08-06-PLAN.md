---
phase: 08-compliant-any-city-venue-corpus
plan: 06
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03", "08-04", "08-05"]
files_modified:
  - .planning/phases/08-compliant-any-city-venue-corpus/08-06-SUMMARY.md
autonomous: false
user_setup:
  - service: foursquare
    why: "Live venue ingestion (cold-start + seed_city) + the gated prod-apply smoke require a Foursquare NEW Places API key, server-side only. BLOCKER — not available at planning time."
    env_vars:
      - name: FOURSQUARE_API_KEY
        source: "Foursquare developer console (new Places API project) → set as a Supabase edge secret on ufufmcpnysvwtutpbian, NEVER NEXT_PUBLIC_, never committed"
    dashboard_config:
      - task: "Confirm the new Places API (places-api.foursquare.com), Bearer auth + X-Places-Api-Version; verify the version string (2025-06-17 [ASSUMED]) and the 4 top-level category IDs against the live console before the live smoke"
        location: "docs.foursquare.com/fsq-developers-places + categories table"
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "The full edge-fn Deno suite + pnpm test pass green locally with NO live Foursquare key (everything fixture-mocked)"
    - "Both Phase-8 migrations apply cleanly via supabase db reset and the Supabase security advisor is clean (no new RLS regression on places — CLAUDE.md)"
    - "Per gated-prod-apply: the two migrations are applied to prod ufufmcpnysvwtutpbian ONLY behind an explicit human checkpoint, with a places before/after row + source-distribution check"
    - "Per Area 1+3: once the user provides FOURSQUARE_API_KEY, a live ingestion smoke (one seed_city / cold-start in a non-Kelowna city) returns real Foursquare-sourced places with non-null coords + parsed hours"
  artifacts:
    - path: ".planning/phases/08-compliant-any-city-venue-corpus/08-06-SUMMARY.md"
      provides: "phase-gate evidence: suite-green log, advisor result, prod before/after counts, live smoke result"
      contains: "advisor"
  key_links:
    - from: "local-green suite"
      to: "gated prod-apply"
      via: "human checkpoint (key + migration review)"
      pattern: "checkpoint"
---

<objective>
The phase gate (mirrors v1.0's NN-09 pattern): run the full suite + both migrations + the security advisor LOCALLY green, then — behind explicit human checkpoints, blocked on the user providing FOURSQUARE_API_KEY — apply the two migrations to prod and run a live Foursquare ingestion smoke. Nothing here touches prod or the live API without the user.

Purpose: Prove the phase is correct end-to-end on the compliant corpus, then ship it under the project's gated-prod-apply discipline.
Output: 08-06-SUMMARY.md with the green-suite log, advisor result, prod before/after counts, and live smoke evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-compliant-any-city-venue-corpus/08-CONTEXT.md
@.planning/phases/08-compliant-any-city-venue-corpus/08-RESEARCH.md
@CLAUDE.md

# The migrations + handler being applied/smoked:
@supabase/migrations/20260606150000_data01_places_fsq_source.sql
@supabase/functions/generate-plan/providers/onthefly.ts
@supabase/functions/process-jobs/seed-city.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local full-suite + migrations + advisor green</name>
  <files>.planning/phases/08-compliant-any-city-venue-corpus/08-06-SUMMARY.md</files>
  <action>Run, capturing output into the SUMMARY: (a) `supabase db reset` (both Phase-8 migrations + the relabel apply clean); (b) the full edge-fn Deno suite — `deno test supabase/functions/generate-plan/ supabase/functions/process-jobs/` (foursquare, places-filter, scoring, pipeline, onthefly, handlers); (c) `pnpm test` (vitest, app/packages); (d) the Phase-8 SQL assertion test (supabase/tests/data01_places_fsq_source.sql); (e) the Supabase security advisor against the LOCAL DB after DDL and confirm no new finding on `places` (CLAUDE.md — advisor after every DDL). All must be green with NO FOURSQUARE_API_KEY set (everything fixture-mocked). Record results.</action>
  <verify>
    <automated>supabase db reset >/tmp/p8gate.log 2>&1 && deno test supabase/functions/generate-plan/ supabase/functions/process-jobs/ >>/tmp/p8gate.log 2>&1 && pnpm test >>/tmp/p8gate.log 2>&1 && grep -qiE "error|FAIL" /tmp/p8gate.log && echo REVIEW || echo "local gate green"</automated>
  </verify>
  <done>Migrations apply, full Deno + vitest suite green, SQL test green, advisor clean — all without a live key. Evidence in SUMMARY.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <what-built>The compliant Foursquare corpus pipeline is local-green. Going further needs the live Foursquare NEW Places API key, which only the user can provision.</what-built>
  <how-to-verify>
    1. Create/confirm a Foursquare developer project on the NEW Places API (places-api.foursquare.com).
    2. Set it as a Supabase edge secret on prod: `supabase secrets set FOURSQUARE_API_KEY=<key> --project-ref ufufmcpnysvwtutpbian` (server-side only — NEVER NEXT_PUBLIC_, never committed).
    3. Confirm the API version string (08-RESEARCH assumes `2025-06-17`) and the 4 top-level category IDs against the live console; report any drift so the category constant + version header can be corrected before the smoke.
  </how-to-verify>
  <resume-signal>Reply "key set" (and any version/category corrections), or "skip live" to defer the live smoke + prod-apply to a follow-up.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Both Phase-8 migrations (20260606150000 places source/fsq_place_id/cities.seeded_at + relabel; 20260606150100 seed_city job type) are ready for the GATED prod-apply to ufufmcpnysvwtutpbian. Per CLAUDE.md migrations are NEVER auto-applied to prod.</what-built>
  <how-to-verify>
    1. Review both migration files against prod state (M1 + M35 already applied per memory; places HAS source='discovered' Google-warmed rows on prod, local does not).
    2. Capture prod BEFORE: `select source, count(*) from places group by source;` (expect 'discovered' rows present).
    3. Apply the two migrations to prod (review-then-apply via the gated path — NOT auto-push).
    4. Capture prod AFTER: the same source-distribution query — confirm former 'discovered' rows now read 'google_legacy', curated count unchanged, NO rows deleted.
    5. Re-run the Supabase security advisor on PROD after the DDL; confirm no new finding on `places`.
  </how-to-verify>
  <resume-signal>Reply "applied" with the before/after source counts + advisor result, or describe any discrepancy.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Live Foursquare ingestion smoke (key-gated) + finalize SUMMARY</name>
  <files>.planning/phases/08-compliant-any-city-venue-corpus/08-06-SUMMARY.md</files>
  <action>With FOURSQUARE_API_KEY set (from the checkpoint), run a single live ingestion smoke in a NON-Kelowna city: trigger one seed_city (enqueue + let process-jobs run, or invoke the handler directly with a real city_id) OR a cold-start generation. Verify the upserted places: rows have source='foursquare', non-null lat/lng, parsed opens/closes (Pitfall 1 regression — hours must NOT be ~100% null), rating on the 0-10 scale, and an assembled photo_url. Confirm the new-API auth worked (no 401/400 — Pitfall 6) and the category IDs returned date-relevant venues (Pitfall A1). Compute the city's unverified_rate and record it. Write the final 08-06-SUMMARY.md: local-green log, advisor results (local+prod), prod before/after source counts, and the live smoke evidence. If the user replied "skip live", record the live smoke + prod-apply as a pending todo instead and note the phase is local-green-only.</action>
  <verify>
    <human-check>Live smoke ingested real Foursquare venues with non-null coords + parsed hours in a non-Kelowna city; auth + categories worked; unverified_rate recorded. (Requires the user's key; skipped → recorded as pending todo.)</human-check>
  </verify>
  <done>SUMMARY records the full evidence chain (local green, advisor clean, prod before/after, live smoke or explicit deferral). Phase 8 is ship-ready or cleanly blocked on the key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → prod DDL | gated migration apply to the live public-read places table |
| edge fn → live Foursquare API | first real use of the provisioned edge secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Tampering (data loss) | prod relabel migration | mitigate | Human-gated apply with before/after source-count diff; relabel-only, no delete; published-night references preserved |
| T-08-15 | Elevation/Disclosure | prod places RLS | mitigate | Security advisor re-run on prod post-DDL; columns/index only, no policy change |
| T-08-16 | Information Disclosure | FOURSQUARE_API_KEY | mitigate | Set as a Supabase edge secret via the user checkpoint; never committed, never NEXT_PUBLIC_, never logged |
| T-08-SC | Tampering | dependency installs | accept | No new packages this phase; nothing to install (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- Local: `supabase db reset` + full Deno suite + `pnpm test` + SQL test + local advisor all green with no key.
- Prod (gated): before/after `select source, count(*) from places group by source;` shows discovered→google_legacy, curated unchanged, no deletes; prod advisor clean.
- Live smoke (key-gated): a non-Kelowna seed yields source='foursquare' rows with non-null coords + parsed hours; unverified_rate recorded.
</verification>

<success_criteria>
Phase 8 is local-green across the full suite + both migrations + advisor with no live key; the two migrations are applied to prod only behind a human checkpoint with verified before/after counts; and — once the user provides the key — a live Foursquare ingestion smoke proves real, trustworthy venues land in a non-Kelowna city. All evidence captured in 08-06-SUMMARY.md.
</success_criteria>

<output>
Create `.planning/phases/08-compliant-any-city-venue-corpus/08-06-SUMMARY.md` when done
</output>
