---
phase: 08-compliant-any-city-venue-corpus
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260606150000_data01_places_fsq_source.sql
  - supabase/tests/data01_places_fsq_source.sql
autonomous: true
requirements: [DATA-01, DATA-02]
must_haves:
  truths:
    - "Per Area 2: places.source check accepts 'foursquare' and 'google_legacy' (added to the existing curated/discovered/warmed set)"
    - "Per Area 1: places has fsq_place_id text + a FULL unique index places_fsq_place_id_key (NOT partial — a partial index is not a valid ON CONFLICT arbiter, the M35 trap)"
    - "Per Area 3: cities has seeded_at timestamptz so cold-start can cheaply check 'has this city ever been seeded?'"
    - "Per Area 2: the one-time relabel pass marks existing source='discovered' (Google-warmed) rows as 'google_legacy'; curated Kelowna rows untouched; NO bulk-delete (published nights reference rows)"
    - "Per CLAUDE.md: places stays public-read; migration adds columns/index only, no USING(true), advisor re-run at the phase gate"
  artifacts:
    - path: "supabase/migrations/20260606150000_data01_places_fsq_source.sql"
      provides: "source check extension + fsq_place_id + full unique index + cities.seeded_at + relabel"
      contains: "places_fsq_place_id_key"
    - path: "supabase/tests/data01_places_fsq_source.sql"
      provides: "SQL assertions: columns exist, index is non-partial, relabel ran, curated untouched"
      contains: "google_legacy"
  key_links:
    - from: "places.fsq_place_id"
      to: "ON CONFLICT upsert (08-04/08-05)"
      via: "full unique index"
      pattern: "create unique index.*places_fsq_place_id_key on places \\(fsq_place_id\\)"
    - from: "migration relabel"
      to: "candidate-pool exclusion"
      via: "source='google_legacy' on former 'discovered' rows"
      pattern: "set source = 'google_legacy'"
---

<objective>
The single DATA-01/DATA-02 migration: extend `places.source` to accept `foursquare`/`google_legacy`, add `fsq_place_id` + a FULL unique index (the ON CONFLICT arbiter), add `cities.seeded_at`, and relabel existing Google-warmed (`discovered`) rows to `google_legacy` so they drop out of the LLM pool. Plus a SQL assertion test. Idempotent; local-green only here — prod apply is gated at the phase gate (08-06).

Purpose: The schema + provenance that lets Foursquare rows upsert idempotently and Google-warmed rows be excluded without deletion.
Output: one migration file + one SQL test. NO prod apply in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-compliant-any-city-venue-corpus/08-CONTEXT.md
@.planning/phases/08-compliant-any-city-venue-corpus/08-RESEARCH.md

# The migration to mirror (source column origin + the M35 partial-index lesson):
@supabase/migrations/20260601211000_m1_places_city_source.sql
@supabase/migrations/20260602160000_m35_places_google_id_full_unique.sql
# SQL test convention to follow:
@supabase/tests/s5_feed_columns.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the DATA-01/DATA-02 migration (idempotent)</name>
  <files>supabase/migrations/20260606150000_data01_places_fsq_source.sql</files>
  <action>Create the migration (timestamp 20260606150000 — after the latest existing 20260606140200 e24 block). Idempotent throughout. Per 08-RESEARCH.md "The Migration":
1. Extend the source check: the M1 constraint `check (source in ('curated','discovered','warmed'))` is unnamed — use a `do $$ ... exception when others/duplicate_object` guarded block to drop the existing source check constraint (query pg_constraint for the check on places mentioning 'discovered') and recreate it as `check (source in ('curated','discovered','warmed','foursquare','google_legacy'))`. Keep 'discovered'/'warmed' in the allowed set so the relabel and any legacy rows stay valid.
2. `alter table places add column if not exists fsq_place_id text;`
3. `create unique index if not exists places_fsq_place_id_key on places (fsq_place_id);` — FULL index, NOT partial (Pitfall 5 / M35). NULLs are distinct so curated rows with null fsq_place_id stay valid.
4. `alter table cities add column if not exists seeded_at timestamptz;`
5. One-time relabel: `update places set source = 'google_legacy' where source = 'discovered';` — NO-OP on local/CI (no discovered rows), relabels Google-warmed rows on prod. NO delete.
6. No RLS change (places public-read). Add a header comment noting advisor must re-run at apply (CLAUDE.md).
Follow M1/M35 comment style documenting prod-vs-local state.</action>
  <verify>
    <automated>supabase db reset >/tmp/p8m.log 2>&1 && grep -qi "error" /tmp/p8m.log && echo FAIL || echo "migration applies clean"</automated>
  </verify>
  <done>`supabase db reset` applies the migration with no error; places has fsq_place_id + full unique index; cities has seeded_at; source check accepts the two new values.</done>
</task>

<task type="auto">
  <name>Task 2: SQL assertion test for the migration</name>
  <files>supabase/tests/data01_places_fsq_source.sql</files>
  <action>Write a psql assertion test (mirror s5_feed_columns.sql's `do $$ ... assert ... $$` style) verifying against the freshly-reset local DB: (a) places.fsq_place_id column exists; (b) places_fsq_place_id_key is a unique index AND is NOT partial (query pg_index.indpred IS NULL for that index — this is the M35 regression guard); (c) cities.seeded_at exists; (d) the source check constraint admits 'foursquare' and 'google_legacy' (insert-and-rollback a row with source='foursquare', or assert the constraint definition contains both literals via pg_get_constraintdef); (e) no curated row was relabeled (count of source='curated' unchanged — seed a curated row + a discovered row, run is moot locally so assert the relabel is idempotent: a discovered row becomes google_legacy, a curated row stays curated). Keep it deterministic and self-contained (insert test rows in a transaction, assert, rollback).</action>
  <verify>
    <automated>psql "$(supabase status -o env 2>/dev/null | grep DB_URL | cut -d= -f2- | tr -d '\"')" -v ON_ERROR_STOP=1 -f supabase/tests/data01_places_fsq_source.sql 2>&1 | grep -qiE "error|assert" && echo CHECK || echo "sql test passed"</automated>
  </verify>
  <done>SQL test asserts columns exist, the unique index is non-partial, the source check admits both new values, and the relabel logic preserves curated rows; passes against the local DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration DDL → prod places (public-read) | a schema change on a live public-read table that the dating UI + planner both read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Elevation/Disclosure | places RLS | mitigate | Migration adds columns + index only; no policy change, no USING(true); advisor re-run at the phase gate (08-06) before prod apply |
| T-08-06 | Tampering (data loss) | relabel UPDATE | mitigate | Relabel only, NO delete — published nights' frozen jsonb references preserved; before/after count verified at gated prod-apply |
| T-08-07 | Denial of Service | ON CONFLICT arbiter | mitigate | FULL unique index (not partial) so upserts succeed — avoids the M35 "0 rows upserted → no_candidates" failure |
</threat_model>

<verification>
- `supabase db reset` clean.
- `grep -n "create unique index if not exists places_fsq_place_id_key on places (fsq_place_id);" supabase/migrations/20260606150000_data01_places_fsq_source.sql` — full, no WHERE.
- `grep -n "set source = 'google_legacy' where source = 'discovered'" supabase/migrations/20260606150000_data01_places_fsq_source.sql` present.
</verification>

<success_criteria>
Migration applies idempotently local-green; full unique index on fsq_place_id; cities.seeded_at present; source check extended; Google-warmed rows relabel to google_legacy with curated untouched and no deletes. Prod apply deferred to 08-06.
</success_criteria>

<output>
Create `.planning/phases/08-compliant-any-city-venue-corpus/08-03-SUMMARY.md` when done
</output>
