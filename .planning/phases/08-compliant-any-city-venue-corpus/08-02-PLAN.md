---
phase: 08-compliant-any-city-venue-corpus
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - supabase/functions/generate-plan/places-filter.ts
  - supabase/functions/generate-plan/places-filter.test.ts
  - supabase/functions/generate-plan/scoring.ts
  - supabase/functions/generate-plan/scoring.test.ts
  - supabase/functions/generate-plan/providers/pipeline.ts
  - supabase/functions/generate-plan/providers/pipeline.test.ts
autonomous: true
requirements: [DATA-03]
must_haves:
  truths:
    - "Per Area 4: a venue with null lat/lng is EXCLUDED from the candidate pool (withinRadius returns false on null coords, was silently true)"
    - "Per Area 4: a venue with null hours is NOT asserted open — for a timed slot it is excluded AND marked unverified (isOpenAt returns false on null hours when slotStart is set, was silently true)"
    - "Per Area 4: isOpenAt's relaxed mode (empty slotStart) is unchanged — a null-hours venue can still fill in relaxed retries but carries the unverified marker"
    - "Per Area 4: unverified_rate = (candidates with null coords OR null hours) / total candidates is computed in runPipeline and written to sharedLog as a first-class Phase-9 eval signal"
  artifacts:
    - path: "supabase/functions/generate-plan/places-filter.ts"
      provides: "fail-loud withinRadius"
      contains: "return false"
    - path: "supabase/functions/generate-plan/scoring.ts"
      provides: "fail-loud isOpenAt + unverified marker"
      contains: "unverified"
    - path: "supabase/functions/generate-plan/providers/pipeline.ts"
      provides: "unverified_rate computation into sharedLog"
      contains: "unverified_rate"
  key_links:
    - from: "places-filter.ts:withinRadius"
      to: "candidate pool"
      via: "null-coord exclusion"
      pattern: "typeof lat !== 'number'.*return false"
    - from: "pipeline.ts:runPipeline"
      to: "sharedLog.unverified_rate"
      via: "share of null-coord/null-hours candidates"
      pattern: "unverified_rate"
---

<objective>
Fix the two silent-pass guards (DATA-03): `withinRadius` returns false on null coords, `isOpenAt` returns false on null hours for a timed slot and threads an `unverified` marker. Then compute `unverified_rate` per generation over the candidate pool into `sharedLog`. These are the exact bugs (`places-filter.ts:90`, `scoring.ts:53`) this phase exists to kill — a cold city can no longer read valid when it isn't.

Purpose: A generation in a city with missing coords/hours fails loud, not silently degrades into a date that looks valid but isn't.
Output: inverted guards + unverified threading + `unverified_rate` signal, all unit-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-compliant-any-city-venue-corpus/08-CONTEXT.md
@.planning/phases/08-compliant-any-city-venue-corpus/08-RESEARCH.md

# The two guards being inverted (current silent-pass bodies):
@supabase/functions/generate-plan/places-filter.ts
@supabase/functions/generate-plan/scoring.ts
# The existing guard test to extend (don't break its haversine cases):
@supabase/functions/generate-plan/places-filter.test.ts

<interfaces>
<!-- withinRadius current: line 90 `if (typeof lat !== 'number' || typeof lng !== 'number') return true;` → return false -->
<!-- isOpenAt current: scoring.ts:52-53 `if (!p.opens || !p.closes) return true;` → for set slotStart, return false + mark unverified -->
<!-- runPipeline: providers/pipeline.ts:44 `const candidates = await filterPlaces(...)` then sharedLog.candidate_pool_size is set ~line 64 — add unverified_rate alongside. PipelineError + sharedLog already in this file. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invert withinRadius (fail-loud on null coords) — flip existing test then implement</name>
  <files>supabase/functions/generate-plan/places-filter.test.ts, supabase/functions/generate-plan/places-filter.ts</files>
  <behavior>
    - withinRadius(null, null, lat, lng, 30) → false (was true) — null coords EXCLUDED
    - withinRadius(49.89,-119.50, kelowna, 30) → true (in range, unchanged)
    - withinRadius(51.05,-114.07, kelowna, 30) → false (Calgary far, unchanged)
  </behavior>
  <action>Update the existing places-filter.test.ts assertion `withinRadius(null,null,...)` from `true` to `false` and add a comment that null coords now EXCLUDE (DATA-03 Area 4). Run it RED. Then change places-filter.ts:90 `return true` → `return false` and update the leading comment ("Places with unknown coords are EXCLUDED — a venue we can't proximity-validate must not silently pass"). Implements DATA-03.</action>
  <verify>
    <automated>deno test supabase/functions/generate-plan/places-filter.test.ts</automated>
  </verify>
  <done>withinRadius returns false on null coords; haversine cases still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail-loud isOpenAt + unverified marker</name>
  <files>supabase/functions/generate-plan/scoring.test.ts, supabase/functions/generate-plan/scoring.ts</files>
  <behavior>
    - isOpenAt({opens:null,closes:null}, "18:00") → false (timed slot, null hours EXCLUDED — was true)
    - isOpenAt({opens:null,closes:null}, "") → true (relaxed mode unchanged)
    - isOpenAt({opens:"17:00",closes:"22:00"}, "18:00") → true; ...,"12:00") → false (same-day window unchanged)
    - wraparound {opens:"17:00",closes:"01:00"} at "23:00" → true (unchanged)
    - a place admitted via the relaxed (skipHoursFilter) path with null hours carries unverified:true on its stop
  </behavior>
  <action>Create scoring.test.ts (Deno.test + assertEquals). Because isOpenAt is currently a private fn, EITHER export it (named export, explicit return type) to test directly OR test via the existing placeMatchesSlot/buildItineraryFromTemplate surface — prefer exporting isOpenAt for a tight unit test. Write the behaviors above RED, then in scoring.ts change line 52-53: keep `if (!slotStart) return true;` (relaxed mode) but when slotStart is set and `!p.opens || !p.closes` → `return false`. Add an `unverified?: boolean` to the candidate/stop shape (ItineraryStop in types.ts) and set it true when a place is admitted with missing hours via the relaxed path (in buildItineraryFromTemplate's stop assembly). Per Area 4. Never silent — comment the WHY.</action>
  <verify>
    <automated>deno test supabase/functions/generate-plan/scoring.test.ts</automated>
  </verify>
  <done>isOpenAt excludes null-hours venues from timed slots, preserves relaxed mode; unverified marker threads to the stop shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compute unverified_rate over the candidate pool into sharedLog</name>
  <files>supabase/functions/generate-plan/providers/pipeline.ts, supabase/functions/generate-plan/providers/pipeline.test.ts</files>
  <behavior>
    - given a candidate pool of 4 where 1 has null coords and 1 (different) has null hours → unverified_rate = 0.5
    - a pool with all-valid candidates → unverified_rate = 0
    - sharedLog.unverified_rate is set before itinerary assembly so it is recorded even when assembly later fails
  </behavior>
  <action>Add a pure exported helper `computeUnverifiedRate(candidates: Place[]): number` to pipeline.ts (or a small util it imports) = (count where lat==null||lng==null||!opens||!closes) / total, 0 for empty. In runPipeline, right after `filterPlaces` returns and `sharedLog.candidate_pool_size` is set (~line 64), set `sharedLog.unverified_rate = computeUnverifiedRate(candidates)`. Create pipeline.test.ts unit-testing computeUnverifiedRate against small Place fixtures (build minimal Place objects). Per Area 4 — this is the first-class Phase-9 eval signal. Persist-per-city is deferred (Phase 9); sharedLog is sufficient for MVP.</action>
  <verify>
    <automated>deno test supabase/functions/generate-plan/pipeline.test.ts</automated>
  </verify>
  <done>computeUnverifiedRate is exported + tested; runPipeline writes unverified_rate to sharedLog over the candidate pool.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus rows → candidate pool | untrusted/incomplete venue data (null coords/hours) crosses into the date the user acts on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Spoofing (data integrity) | withinRadius / isOpenAt | mitigate | Invert silent-pass: null coords/hours EXCLUDE (the core DATA-03 fix); a venue cannot masquerade as proximity- or hours-valid |
| T-08-04 | Information Disclosure | unverified_rate in sharedLog | accept | sharedLog is server-side audit only, not returned to client; low-value internal signal |
</threat_model>

<verification>
- `deno test supabase/functions/generate-plan/places-filter.test.ts supabase/functions/generate-plan/scoring.test.ts supabase/functions/generate-plan/pipeline.test.ts` all green.
- `grep -nE "typeof lat !== 'number'" supabase/functions/generate-plan/places-filter.ts` followed by `return false` (no longer `return true`).
- `grep -n "unverified_rate" supabase/functions/generate-plan/providers/pipeline.ts` present.
</verification>

<success_criteria>
Both guards fail loud on missing data; relaxed mode preserved; unverified marker threads to stops; unverified_rate computed over the candidate pool into sharedLog. A cold/thin city can no longer produce a date that reads valid but isn't.
</success_criteria>

<output>
Create `.planning/phases/08-compliant-any-city-venue-corpus/08-02-SUMMARY.md` when done
</output>
