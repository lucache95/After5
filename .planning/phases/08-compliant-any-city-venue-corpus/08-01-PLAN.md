---
phase: 08-compliant-any-city-venue-corpus
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - supabase/functions/generate-plan/foursquare.ts
  - supabase/functions/generate-plan/foursquare.test.ts
  - supabase/functions/generate-plan/__fixtures__/foursquare.ts
autonomous: true
requirements: [DATA-01]
must_haves:
  truths:
    - "Per Area 1: a new foursquare.ts mirrors google-places.ts's mapper/fetch surface — fsqResultToPlaceRow, searchPlaces, geocodeCity, passesQualityFloor, mapFsqCategories, priceToTier, pickHours"
    - "Per Area 1: pickHours parses Foursquare hours.regular ({day,open:'HHMM',close:'HHMM'}) — NOT Google free-text — into opens/closes; a mis-map does not silently null every venue"
    - "Per Area 1: a Foursquare result maps to existing places columns + fsq_place_id + source='foursquare'; rating is read on the 0-10 scale with NO ×2; price 1→$/2→$$/3,4→$$$"
    - "Per code_context: searchPlaces sends Authorization: Bearer + X-Places-Api-Version (new API), never legacy V3 auth"
    - "The whole mapper + fetch surface is unit-tested against plain-object fixtures with NO live key (CI-runnable)"
  artifacts:
    - path: "supabase/functions/generate-plan/foursquare.ts"
      provides: "Foursquare drop-in corpus source mirroring google-places.ts"
      contains: "fsqResultToPlaceRow"
      min_lines: 120
    - path: "supabase/functions/generate-plan/foursquare.test.ts"
      provides: "Deno unit tests for mappers + pickHours + searchPlaces (mock fetchImpl)"
      contains: "pickHours"
    - path: "supabase/functions/generate-plan/__fixtures__/foursquare.ts"
      provides: "FsqResult fixtures: rich venue, null-hours, null-coords, below-floor"
      contains: "export const"
  key_links:
    - from: "foursquare.ts:fsqResultToPlaceRow"
      to: "places columns + fsq_place_id/source"
      via: "row object shape (mirrors googleResultToPlaceRow keys)"
      pattern: "fsq_place_id|source:\\s*'foursquare'"
    - from: "foursquare.ts:searchPlaces"
      to: "places-api.foursquare.com/places/search"
      via: "fetch with Bearer + version headers"
      pattern: "X-Places-Api-Version|Bearer"
---

<objective>
Build `foursquare.ts` as a drop-in mirror of `google-places.ts`: the pure field mappers (fsqResultToPlaceRow, pickHours, mapFsqCategories, priceToTier, passesQualityFloor), a `searchPlaces` fetch against the NEW Foursquare Places API, and a `geocodeCity`. This is the corpus source DATA-01 swaps Google for. TDD-first because `pickHours` is the phase's biggest silent-failure risk (RESEARCH Pitfall 1).

Purpose: A single, license-compliant venue source whose output is byte-shaped like the existing Google path so downstream plans swap one import line.
Output: `foursquare.ts`, `foursquare.test.ts`, and a shared `__fixtures__/foursquare.ts`. No live key needed — everything tests against fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-compliant-any-city-venue-corpus/08-CONTEXT.md
@.planning/phases/08-compliant-any-city-venue-corpus/08-RESEARCH.md

# The interface to mirror (copy neighborhoodFromLatLng/driveClusterFromNeighborhood/slugify verbatim):
@supabase/functions/generate-plan/google-places.ts
# The existing fixture-based test pattern to follow (plain objects, no network):
@supabase/functions/generate-plan/places-filter.test.ts

<interfaces>
<!-- The places-row keys fsqResultToPlaceRow must produce (mirror googleResultToPlaceRow exactly,
     swapping google_place_id→fsq_place_id and source 'discovered'→'foursquare'). See google-places.ts. -->
<!-- RESEARCH gives the canonical FsqResult interface + searchPlaces + pickHours code (08-RESEARCH.md
     "Code Examples"). The day index is 1=Mon..7=Sun; open/close are "HHMM" strings. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FSQ fixtures + RED tests for pickHours, mapFsqCategories, priceToTier, passesQualityFloor</name>
  <files>supabase/functions/generate-plan/__fixtures__/foursquare.ts, supabase/functions/generate-plan/foursquare.test.ts</files>
  <behavior>
    - pickHours: hours.regular=[{day:3,open:"1100",close:"2200"}] → {opens:"11:00", closes:"22:00"}; picks day===3 (Wed) else first entry
    - pickHours: empty/missing regular → {opens:null, closes:null}; malformed "abc" → null (NOT a crash, NOT "00:00")
    - mapFsqCategories: categories[].name "Coffee Shop"→cafe, "Cocktail Bar"→cocktail_bar, "Winery"→winery, "Art Gallery"→gallery, "Hiking Trail"→hike; unknown → 'activity' fallback
    - priceToTier: 1→$, 2→$$, 3→$$$, 4→$$$, undefined→$$
    - passesQualityFloor: rating>=7.0 passes, rating 6.9 fails, date_closed present fails (FSQ rating is 0-10, NOT 0-5)
  </behavior>
  <action>Create __fixtures__/foursquare.ts exporting hand-built FsqResult objects per RESEARCH "Mock-Foursquare-client approach": `richVenue` (full hours/coords/rating 8.4/price 2/photo), `nullHoursVenue` (hours undefined), `nullCoordsVenue` (latitude/longitude undefined), `belowFloorVenue` (rating 5.0). Use the FsqResult shape from 08-RESEARCH.md Code Examples. Then write foursquare.test.ts following places-filter.test.ts's Deno.test + assertEquals style (import from `https://deno.land/std@0.208.0/assert/mod.ts`). Tests import the not-yet-existing exports from ./foursquare.ts — they MUST fail RED. Cover the behaviors above. Do NOT ×2 the rating (Pitfall 3); quality floor is rating>=7.0.</action>
  <verify>
    <automated>deno test supabase/functions/generate-plan/foursquare.test.ts 2>&1 | grep -qiE "error|cannot find|failed" && echo "RED confirmed (expected pre-impl)"</automated>
  </verify>
  <done>Fixtures file + failing test file committed; tests reference foursquare.ts exports that don't exist yet (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement foursquare.ts mappers + searchPlaces (injectable fetchImpl)</name>
  <files>supabase/functions/generate-plan/foursquare.ts</files>
  <behavior>
    - searchPlaces({apiKey,lat,lng,radiusKm,categoryIds,limit}, fetchImpl) sends GET places-api.foursquare.com/places/search with headers Authorization:"Bearer <key>" + "X-Places-Api-Version":"2025-06-17" and query params ll, radius (meters = km*1000), fsq_category_ids, fields, sort, limit; returns json.results ?? []
    - searchPlaces with a stub fetchImpl returning {results:[richVenue]} → returns [richVenue]; the assembled request URL+headers are assertable
    - fsqResultToPlaceRow(richVenue, {id,slug:'kelowna'}, key) → row with fsq_place_id set, source:'foursquare', lat/lng from top-level latitude/longitude, opens/closes from pickHours, quality_score=Math.min(10,Math.round(rating)) (no ×2)
    - non-Kelowna city → neighborhood = city.slug (NOT a Kelowna bucket), drive_cluster 'multiple' (preserve googleResultToPlaceRow's behavior)
  </behavior>
  <action>Implement foursquare.ts using the canonical code in 08-RESEARCH.md "Code Examples": the FsqResult interface, searchPlaces (accept `fetchImpl: typeof fetch = fetch`), pickHours, the SEARCH_FIELDS list, plus fsqResultToPlaceRow (mirror googleResultToPlaceRow key-for-key; swap google_place_id→fsq_place_id, source 'discovered'→'foursquare', drop the ×2 on rating per Pitfall 3, build photo_url via buildFsqPhotoUrl = prefix+'original'+suffix per Pitfall 2). Add mapFsqCategories (the substring rule list in 08-RESEARCH.md "Category taxonomy"), priceToTier (1→$/2→$$/3,4→$$$), passesQualityFloor (rating>=7.0 && !date_closed), and geocodeCity (FSQ search top result → centroid; reuse radius default 25). Import or copy slugify/neighborhoodFromLatLng/driveClusterFromNeighborhood from google-places.ts verbatim. Named exports only, explicit return types (CLAUDE.md). Implements DATA-01.

APPROVAL_STATUS (cross-plan, per plan-check W3): fsqResultToPlaceRow MUST pin `approval_status: 'auto'` (mirror google-places.ts:247). The candidate read-path `filterPlaces` defaults to `['live']` and only cold-start passes `['live','auto']` — so seed_city background rows (08-05) would be INVISIBLE to a later generation that runs with the default. To prevent a seeded city silently reading cold: 08-04's any-city/onthefly generation read-path must pass `approvalStatuses` INCLUDING `'auto'` (not just on the cold-start branch), so Foursquare-sourced 'auto' rows are admitted on every generation for a seeded city. Verify this end-to-end in 08-04.</action>
  <verify>
    <automated>deno test supabase/functions/generate-plan/foursquare.test.ts</automated>
  </verify>
  <done>All foursquare.test.ts tests pass GREEN. searchPlaces sends Bearer + version headers; fsqResultToPlaceRow emits fsq_place_id + source='foursquare'; rating un-doubled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: searchPlaces request-shape test (mock fetchImpl) + null-data fixture coverage</name>
  <files>supabase/functions/generate-plan/foursquare.test.ts</files>
  <behavior>
    - searchPlaces with a capturing stub fetchImpl asserts: URL host places-api.foursquare.com, path /places/search, headers include Authorization "Bearer test-key" + X-Places-Api-Version, query has ll/radius/fsq_category_ids/fields
    - fsqResultToPlaceRow(nullCoordsVenue) → lat:null,lng:null (no crash); fsqResultToPlaceRow(nullHoursVenue) → opens:null,closes:null (Pitfall 1 regression guard)
  </behavior>
  <action>Extend foursquare.test.ts: add a Deno.test that passes a stub `fetchImpl` capturing the Request URL+headers and asserts the new-API auth shape (catches Pitfall 6 auth drift). Add tests mapping nullCoordsVenue / nullHoursVenue through fsqResultToPlaceRow and assert null (not 0, not crash) — these are the fixtures the guard plan (08-02) and cold-start plan (08-04) rely on. No live key (gate any live test behind `if (!Deno.env.get('FOURSQUARE_API_KEY')) return;`).</action>
  <verify>
    <automated>deno test supabase/functions/generate-plan/foursquare.test.ts</automated>
  </verify>
  <done>Full foursquare.test.ts suite green; auth-header shape + null-coord/null-hours mapping locked by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edge fn → Foursquare API | FOURSQUARE_API_KEY (edge secret) crosses here in the Authorization header, server-side only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | foursquare.ts searchPlaces | mitigate | Key only read from `opts.apiKey` (caller passes the edge secret); never logged, never in returned rows, never NEXT_PUBLIC_ |
| T-08-02 | Tampering | mapFsqCategories / fsq_category_ids | mitigate | Category IDs are a fixed server-side constant list (08-RESEARCH category table), never user input |
| T-08-SC | Tampering | npm/deno installs | accept | No new packages this phase (plain fetch + std assert already pinned); slopcheck N/A per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `deno test supabase/functions/generate-plan/foursquare.test.ts` green with no live key set.
- `grep -nE "Bearer|X-Places-Api-Version" supabase/functions/generate-plan/foursquare.ts` confirms new-API auth.
- `grep -n "rating \* 2\|rating\*2" supabase/functions/generate-plan/foursquare.ts` returns nothing (no Google ×2 carried over).
</verification>

<success_criteria>
foursquare.ts exports the full google-places.ts-analog surface; every mapper + searchPlaces is fixture-tested CI-green with no live key; pickHours correctly parses FSQ HHMM per-day hours; rating stays on the 0-10 scale.
</success_criteria>

<output>
Create `.planning/phases/08-compliant-any-city-venue-corpus/08-01-SUMMARY.md` when done
</output>
