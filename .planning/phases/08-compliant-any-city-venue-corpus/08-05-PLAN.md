---
phase: 08-compliant-any-city-venue-corpus
plan: 05
type: execute
wave: 2
depends_on: ["08-01", "08-03"]
files_modified:
  - supabase/migrations/20260606150100_data02_seed_city_job_type.sql
  - supabase/functions/process-jobs/handlers.ts
  - supabase/functions/process-jobs/seed-city.ts
  - supabase/functions/process-jobs/handlers_test.ts
  - apps/web/lib/after5/enqueue-seed-city.ts
autonomous: true
requirements: [DATA-02]
must_haves:
  truths:
    - "Per Area 3: a 'seed_city' job type exists and HANDLERS['seed_city'] fetches Foursquare for the city, fsqResultToPlaceRow→quality-floor→upsert onConflict fsq_place_id, then stamps cities.seeded_at"
    - "Per Area 3: enqueue is dedup'd by city_id (p_dedup_key := city_id) so a city already queued/running won't double-seed (poison-loop safe, mirrors v1.0)"
    - "Per Area 3: the seed is enqueued server-side after the user's profile location is saved (enqueue_job is revoked from authenticated → service-role route only)"
    - "Per Area 3: seed scope is bounded — top-N per date-relevant category, capped per city (not a whole-city crawl)"
  artifacts:
    - path: "supabase/functions/process-jobs/seed-city.ts"
      provides: "seed_city handler body"
      contains: "seeded_at"
    - path: "supabase/functions/process-jobs/handlers.ts"
      provides: "HANDLERS['seed_city'] registration"
      contains: "seed_city"
    - path: "apps/web/lib/after5/enqueue-seed-city.ts"
      provides: "server-side enqueue helper invoked after primary_city_id is saved"
      contains: "enqueue_job"
  key_links:
    - from: "profile location save (server)"
      to: "enqueue_job('seed_city', {city_id}, dedup_key=city_id)"
      via: "service-role rpc after primary_city_id write"
      pattern: "enqueue_job"
    - from: "HANDLERS['seed_city']"
      to: "places upsert onConflict fsq_place_id + cities.seeded_at"
      via: "foursquare fetch + upsert + stamp"
      pattern: "seeded_at"
---

<objective>
Add the async city pre-seed (DATA-02): a `seed_city` job type, a `HANDLERS['seed_city']` that runs the same Foursquare fetch→map→upsert as cold-start and stamps `cities.seeded_at`, and a server-side enqueue (dedup'd by city_id) wired in after the user saves their profile location. Mirrors the v1.0 job-handler + enqueue_job pattern exactly.

Purpose: A user's city warms in the background the moment they set their location, so generation is fast and full by the time they plan.
Output: job-type migration + seed-city handler + enqueue helper + handler test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-compliant-any-city-venue-corpus/08-CONTEXT.md
@.planning/phases/08-compliant-any-city-venue-corpus/08-RESEARCH.md

# The job-handler dispatch pattern + mock-db test convention to mirror:
@supabase/functions/process-jobs/handlers.ts
@supabase/functions/process-jobs/handlers_test.ts
# The corpus source the handler reuses (from 08-01):
@supabase/functions/generate-plan/foursquare.ts
# The job_type enum + enqueue_job RPC origin:
@supabase/migrations/20260525123000_p2_jobs.sql
@supabase/migrations/20260525123100_p2_jobs_rpcs.sql

<interfaces>
<!-- enqueue_job(p_type, p_run_after, p_payload, p_dedup_key) — already live (P2). Revoked from authenticated. -->
<!-- city row for the handler: cities(id, slug, name, region, default_radius_km, centroid). Use the
     ST_Y/ST_X centroid extraction shown in 08-RESEARCH "seed_city handler" example for lat/lng. -->
<!-- foursquare.searchPlaces(opts, fetchImpl) + fsqResultToPlaceRow + passesQualityFloor from 08-01. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'seed_city' to the job_type enum (migration)</name>
  <files>supabase/migrations/20260606150100_data02_seed_city_job_type.sql</files>
  <action>Create migration 20260606150100 (after 08-03's 150000). Idempotent: add 'seed_city' to the job_type enum if it's an enum (`alter type job_type add value if not exists 'seed_city'`), guarded in a `do $$ ... exception when duplicate_object`; OR if job.type is a text+check column, extend the check (mirror how 20260605120100 e12 added a queue status value — read that file's pattern). Inspect 20260525123000_p2_jobs.sql to confirm whether type is an enum or text-check and follow that exactly. No RLS/grant change.</action>
  <verify>
    <automated>supabase db reset >/tmp/p8j.log 2>&1 && grep -qi "error" /tmp/p8j.log && echo FAIL || echo "job-type migration clean"</automated>
  </verify>
  <done>'seed_city' is an accepted job type after db reset; migration idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: seed_city handler + registration</name>
  <files>supabase/functions/process-jobs/seed-city.ts, supabase/functions/process-jobs/handlers.ts, supabase/functions/process-jobs/handlers_test.ts</files>
  <action>Create seed-city.ts exporting a `Handler` (the type from handlers.ts): read+validate `payload.city_id` (uuid; throw on missing — fail-loud so index.ts retries/backoff), load the city (id/slug/name/region/default_radius_km + centroid lat/lng via ST_Y/ST_X per the RESEARCH example), run foursquare.searchPlaces per the fixed FSQ category-id list (bounded limit per category — cap per city, Area 3), map via fsqResultToPlaceRow + passesQualityFloor, dedupe by fsq_place_id, upsert onConflict 'fsq_place_id', then `update cities set seeded_at = now() where id = city_id`. Reuse the FSQ category constant from onthefly.ts (export it there or duplicate the const) so cold-start and seed agree. Register `HANDLERS['seed_city'] = seedCity` in handlers.ts. Inject `fetchImpl`/searchPlaces so the handler is testable without a key. Extend handlers_test.ts: add 'seed_city' to ALL_TYPES; add a test with a fake db (from().select() returns a city; .upsert() captures rows; .update() captures seeded_at) + a stub searchPlaces returning fixtures → assert it upserts onConflict fsq_place_id AND stamps seeded_at AND throws on missing city_id. Mirror the existing fakeDb mock style.</action>
  <verify>
    <automated>deno test supabase/functions/process-jobs/handlers_test.ts</automated>
  </verify>
  <done>HANDLERS['seed_city'] upserts Foursquare rows idempotently on fsq_place_id and stamps cities.seeded_at; throws on missing city_id; handler test green.</done>
</task>

<task type="auto">
  <name>Task 3: Server-side enqueue after profile location is saved</name>
  <files>apps/web/lib/after5/enqueue-seed-city.ts</files>
  <action>FIRST grep the app for where profile location persists: `grep -rn "primary_city_id" apps/web/app apps/web/lib --include=*.ts --include=*.tsx | grep -v .next` and inspect the onboarding BasicsStep server action / account submit (RESEARCH Open Question 3 — the write site wasn't pinned). Create enqueue-seed-city.ts exporting `enqueueSeedCity(cityId: string): Promise<void>` that uses the ADMIN/service-role client (createAdminClient) to call `adminDb.rpc('enqueue_job', { p_type:'seed_city', p_run_after:new Date().toISOString(), p_payload:{ city_id: cityId }, p_dedup_key: cityId })`. Validate cityId is a uuid before enqueue (Zod or a uuid regex). Then wire a call to it at the server-side point where primary_city_id is written (fire-and-forget with a logged .catch, like the email-notification pattern in CLAUDE.md error-handling). Do NOT enqueue from the browser (enqueue_job is revoked from authenticated). Implements DATA-02 trigger.</action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit -p tsconfig.json 2>&1 | grep -i "enqueue-seed-city" && echo CHECK || echo "typecheck clean for enqueue helper"</automated>
  </verify>
  <done>enqueueSeedCity is invoked server-side after primary_city_id is saved, dedup'd by city_id, via the service-role client; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server route (location save) | user-supplied city_id crosses into a privileged enqueue |
| seed handler → Foursquare API | edge secret crosses server-side; bounded background work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Elevation of Privilege | enqueue_job | mitigate | enqueue only from a service-role server route; enqueue_job stays revoked from authenticated; city_id validated as uuid before the rpc |
| T-08-12 | Denial of Service | seed_city job | mitigate | p_dedup_key=city_id (one active seed/city, poison-loop safe); bounded category set + per-category limit cap; existing fail_job backoff dead-letters at attempts≥5 |
| T-08-13 | Tampering (SQLi) | city_id / category ids | mitigate | Parameterized supabase-js queries; fsq_category_ids from a fixed server-side constant, never user input |
</threat_model>

<verification>
- `deno test supabase/functions/process-jobs/handlers_test.ts` green; 'seed_city' in ALL_TYPES.
- `grep -n "p_dedup_key" apps/web/lib/after5/enqueue-seed-city.ts` present (city_id dedup).
- `grep -n "seeded_at" supabase/functions/process-jobs/seed-city.ts` present.
- `supabase db reset` clean (job-type migration applies).
</verification>

<success_criteria>
Setting a profile location enqueues a dedup'd seed_city job server-side; the handler bounded-fetches Foursquare, upserts idempotently on fsq_place_id, and stamps cities.seeded_at. Prod apply of the job-type migration deferred to 08-06.
</success_criteria>

<output>
Create `.planning/phases/08-compliant-any-city-venue-corpus/08-05-SUMMARY.md` when done
</output>
