---
phase: 08-compliant-any-city-venue-corpus
plan: 04
type: tdd
wave: 2
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - supabase/functions/generate-plan/providers/onthefly.ts
  - supabase/functions/generate-plan/providers/onthefly.test.ts
  - supabase/functions/generate-plan/providers/types.ts
files_modified_note: "filterPlaces source-exclusion lands in places-filter.ts owned by 08-02; this plan only reads it. Add the google_legacy pool exclusion as a 08-02 follow within its file — see action."
autonomous: true
requirements: [DATA-01, DATA-02]
must_haves:
  truths:
    - "Per Area 1: OnTheFlyProvider fetches from foursquare.searchPlaces (NOT google-places.searchText); the Google import is gone from the generation path"
    - "Per Area 1: the cold-start upsert uses onConflict 'fsq_place_id' and the env guard reads env.foursquareKey (not env.googleKey)"
    - "Per Area 3: a city that is still thin after the inline warm throws PipelineError('city_warming', 503) — a distinct 'warming up — check back in a moment' signal, NOT the generic no_candidates 422 and NEVER a garbage date"
    - "Per Area 2: google_legacy rows are excluded from the candidate pool (filterPlaces excludes source='google_legacy')"
  artifacts:
    - path: "supabase/functions/generate-plan/providers/onthefly.ts"
      provides: "Foursquare-sourced cold-start with city_warming fallback"
      contains: "foursquare"
    - path: "supabase/functions/generate-plan/providers/onthefly.test.ts"
      provides: "buildWarmRows + city_warming fallback tests (mock fetch/db)"
      contains: "city_warming"
  key_links:
    - from: "onthefly.ts"
      to: "foursquare.searchPlaces"
      via: "import + per-category fetch"
      pattern: "from '\\.\\./foursquare\\.ts'"
    - from: "onthefly.ts cold-start"
      to: "PipelineError('city_warming')"
      via: "post-warm thin-pool check"
      pattern: "city_warming"
---

<objective>
Re-point the cold-start path at Foursquare (DATA-01) and add the "warming up" fallback (DATA-02). `OnTheFlyProvider` swaps its Google import for `foursquare.ts`, upserts on `fsq_place_id`, reads `env.foursquareKey`, and — when a city is still thin after the inline warm — throws a distinct `city_warming` (503) instead of a generic no_candidates or a broken date. Also make `filterPlaces` exclude `google_legacy` rows from the pool.

Purpose: Generation in an unseeded/thin city now uses the compliant corpus and degrades gracefully, never silently.
Output: re-sourced onthefly.ts + tests; google_legacy pool exclusion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-compliant-any-city-venue-corpus/08-CONTEXT.md
@.planning/phases/08-compliant-any-city-venue-corpus/08-RESEARCH.md

# The provider being re-sourced (current Google wiring):
@supabase/functions/generate-plan/providers/onthefly.ts
# The new corpus source (from 08-01) + the env type to extend:
@supabase/functions/generate-plan/foursquare.ts
@supabase/functions/generate-plan/providers/types.ts
# PipelineError lives here; filterPlaces (08-02 territory) gains the google_legacy exclusion:
@supabase/functions/generate-plan/providers/pipeline.ts
@supabase/functions/generate-plan/places-filter.ts

<interfaces>
<!-- GenerationEnv (providers/types.ts) needs `foursquareKey?: string;` added alongside googleKey. -->
<!-- foursquare.searchPlaces(opts, fetchImpl) signature differs from searchText(query,opts):
     it takes categoryIds (the fixed FSQ top-level ids from 08-RESEARCH) not a free-text query. -->
<!-- PipelineError(code, message, status) — construct city_warming with status 503. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add foursquareKey to env + exclude google_legacy from the pool</name>
  <files>supabase/functions/generate-plan/providers/types.ts, supabase/functions/generate-plan/places-filter.ts, supabase/functions/generate-plan/places-filter.test.ts</files>
  <behavior>
    - GenerationEnv has optional foursquareKey
    - filterPlaces query excludes source='google_legacy' (only curated/foursquare/auto rows reach the pool)
  </behavior>
  <action>Add `foursquareKey?: string;` to GenerationEnv in providers/types.ts. In places-filter.ts, add a `.neq('source', 'google_legacy')` (or `.in('source', [...])`) clause to the filterPlaces select so relabeled Google rows never enter the candidate pool (Area 2). Add a places-filter.test.ts case asserting a google_legacy fixture row is excluded by the predicate logic (test the filter clause via a small in-memory filter helper if the query isn't unit-mockable, or assert the select includes the neq). Implements DATA-01/Area 2 exclusion.</action>
  <verify>
    <automated>deno test supabase/functions/generate-plan/places-filter.test.ts</automated>
  </verify>
  <done>env carries foursquareKey; filterPlaces excludes google_legacy; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-source OnTheFlyProvider to Foursquare</name>
  <files>supabase/functions/generate-plan/providers/onthefly.ts, supabase/functions/generate-plan/providers/onthefly.test.ts</files>
  <behavior>
    - buildWarmRows(fsqResults, city, key) → quality-floored, deduped-by-fsq_place_id rows via fsqResultToPlaceRow
    - generate() reads env.foursquareKey; throws generation_unavailable (503) when unset (mirrors current google guard)
    - cold city (count < COLD_THRESHOLD): runs foursquare.searchPlaces per FSQ category id, upserts onConflict 'fsq_place_id', then runPipeline(['live','auto'])
  </behavior>
  <action>Rewrite onthefly.ts: replace the `searchText/googleResultToPlaceRow/GoogleResult` imports with `searchPlaces/fsqResultToPlaceRow/passesQualityFloor/FsqResult` from '../foursquare.ts'. Replace the CATEGORIES free-text list with the fixed FSQ top-level category-ID constant list (08-RESEARCH "Category taxonomy" — keep the IDs as a named const; they are [ASSUMED] and re-verified at the 08-06 live smoke). buildWarmRows now uses passesQualityFloor + fsqResultToPlaceRow keyed/deduped on fsq_place_id. The cold-check count query stays. Change the upsert to `onConflict: 'fsq_place_id'`. Change `env.googleKey` → `env.foursquareKey`. Create onthefly.test.ts (mock searchPlaces via fixtures from 08-01's __fixtures__; fake db with from().select()/upsert()) asserting buildWarmRows quality-floors + dedupes. Implements DATA-01.</action>
  <verify>
    <automated>deno test supabase/functions/generate-plan/providers/onthefly.test.ts</automated>
  </verify>
  <done>onthefly.ts imports only foursquare.ts; upserts onConflict fsq_place_id; reads env.foursquareKey; buildWarmRows tested. No google-places import remains in the generation path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: city_warming fallback for a still-thin city</name>
  <files>supabase/functions/generate-plan/providers/onthefly.ts, supabase/functions/generate-plan/providers/onthefly.test.ts</files>
  <behavior>
    - after the inline warm, if the resulting candidate pool is still < 3 usable → PipelineError('city_warming', "We're still gathering great spots in {city} — check back in a moment.", 503)
    - a healthy city (enough usable rows) does NOT throw city_warming and returns itineraries
  </behavior>
  <action>In onthefly.ts generate(), wrap the runPipeline call: if it throws no_candidates (or if a pre-pipeline usable-count check is < 3) for a city that was just warmed, rethrow as `new PipelineError('city_warming', <warming message with city.name>, 503)` so the client can show the distinct "warming up" state (Area 3, Pitfall 4). Never return a garbage date. Extend onthefly.test.ts: a fake db returning a thin pool → expect city_warming; a healthy pool → no throw. Implements DATA-02.</action>
  <verify>
    <automated>deno test supabase/functions/generate-plan/providers/onthefly.test.ts</automated>
  </verify>
  <done>A still-thin warmed city yields city_warming (503), distinct from no_candidates (422); a healthy city generates normally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generation request → inline Foursquare fetch | synchronous external call in the user's request path using the edge secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Denial of Service | inline cold-start fetch | mitigate | COLD_THRESHOLD gate avoids re-fetching warm cities; bounded category set + per-category limit cap; city_warming short-circuits instead of hanging |
| T-08-09 | Information Disclosure | env.foursquareKey | mitigate | Read from edge env only; never in the city_warming message or returned rows |
| T-08-10 | Spoofing (data integrity) | google_legacy in pool | mitigate | filterPlaces excludes google_legacy so no Google content is LLM-fed (the compliance point) |
</threat_model>

<verification>
- `deno test supabase/functions/generate-plan/providers/onthefly.test.ts` green.
- `grep -n "google-places\|googleResultToPlaceRow\|env.googleKey" supabase/functions/generate-plan/providers/onthefly.ts` returns nothing.
- `grep -n "city_warming\|onConflict: 'fsq_place_id'\|env.foursquareKey" supabase/functions/generate-plan/providers/onthefly.ts` all present.
</verification>

<success_criteria>
The cold-start path uses Foursquare exclusively, upserts on fsq_place_id, reads the FSQ key, excludes google_legacy from the pool, and surfaces a distinct city_warming state for thin cities — never a garbage date.
</success_criteria>

<output>
Create `.planning/phases/08-compliant-any-city-venue-corpus/08-04-SUMMARY.md` when done
</output>
