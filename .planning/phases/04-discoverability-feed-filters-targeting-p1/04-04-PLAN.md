---
phase: 04-discoverability-feed-filters-targeting-p1
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - apps/web/app/feed/NightCard.tsx
  - apps/web/app/nights/new/PostNightForm.tsx
  - apps/web/app/feed/NightCard.test.tsx
  - apps/web/app/nights/new/PostNightForm.test.tsx
autonomous: false
requirements: [REQ-E10]

must_haves:
  truths:
    - "The 'looks for someone like you' fit pill renders ONLY on cards where FeedNight.fit === true (D-03)"
    - "The fit pill is never a score/percentage and never collides with the curated badge"
    - "A quiet reach line under the radius input shows '~N people match this in <city>', updating live (debounced) as targeting changes (D-01)"
    - "The reach line frames a low/zero count positively and NEVER disables the publish CTA"
    - "reachPreview is called with the open case normalized (everyone -> empty/omit) so open nights do not undercount"
  artifacts:
    - path: "apps/web/app/feed/NightCard.tsx"
      provides: "conditional fit pill driven by FeedNight.fit"
      contains: "looks for someone like you"
    - path: "apps/web/app/nights/new/PostNightForm.tsx"
      provides: "debounced reach-preview line under the targeting fieldset"
      contains: "reachPreview"
  key_links:
    - from: "NightCard"
      to: "FeedNight.fit"
      via: "{night.fit && <span>looks for someone like you</span>}"
      pattern: "night.fit"
    - from: "PostNightForm reach line"
      to: "reach_preview RPC"
      via: "debounced reachPreview(client,{target_genders,target_age_range,city,radius_km})"
      pattern: "reachPreview"
---

<objective>
Build the two consumer-side hint surfaces: the "looks for someone like you" fit pill on NightCard (D-03) and the live host reach-preview line on PostNightForm (D-01). Both are quiet, flattering, non-blocking, and extend existing components against 04-UI-SPEC.md.

Purpose: The fit pill turns the server-side soft match into a flattering per-card signal without ranking the feed in the user's face; the reach line gives a host encouraging feedback on their targeting without ever gating posting.
Output: NightCard fit pill + PostNightForm reach line + component tests. Ends in a human visual-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-discoverability-feed-filters-targeting-p1/04-UI-SPEC.md
@.planning/phases/04-discoverability-feed-filters-targeting-p1/04-PATTERNS.md
@docs/superpowers/DESIGN-SYSTEM.md

<interfaces>
<!-- Live UI contracts (verified). Full class strings + analogs in 04-PATTERNS.md + 04-UI-SPEC.md §3/§5. -->
NightCard.tsx (174 lines): is_seed "★ curated" badge (lines 91-98) is the exact precedent for a small conditional pill — absolute left-3 top-3 rounded-full px-3 py-1, style background pal.accent, stickerRotation. Content stack sits on the bottom scrim (~line 101). night: FeedNight (now carries fit from Plan 04-02). pal = vibePalette.

PostNightForm.tsx (723 lines): targeting fieldset lines 375-494; helper-text style (lines 439-441) "open to everyone unless you narrow it." = font-body text-xs lowercase text-shell-ink/55. Radius input id="radius-km" ends ~line 494 — the reach line lands AFTER it. State setters: genders/ageMin/ageMax/radiusKm. EVERYONE LANDMINE: genders init ['everyone'] (line 69), toggleGender falls back to ['everyone'] (lines 98-101) — when calling reachPreview, send empty/omit for the open case. EM-DASH LANDMINE: line 315 already violates stop-slop — do NOT add a new em-dash in the reach line. The form needs the host's city (primary_city_id) to pass to reachPreview.

api-client (Plan 04-02): reachPreview(client,{target_genders?,target_age_range?,city,radius_km?}) -> Promise<number>; FeedNight.fit.

Copy (04-UI-SPEC §Copywriting): pill "looks for someone like you"; reach normal "~{N} people match this in {city}"; low "~{N} match this in {city}. a focused crowd, widen anytime."; loading "counting who's around…"; zero "no one fits this yet in {city}. loosen the targeting and they'll show up." Pill treatment: rounded-full px-3 py-1 font-body text-[13px] font-semibold lowercase shadow-md bg-white/85 text-shell-accent (NOT sage). Reach line: font-body text-[13px] lowercase text-shell-ink/65, aria-live="polite", NOT accent/warning.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fit pill on NightCard (D-03)</name>
  <read_first>
    - apps/web/app/feed/NightCard.tsx (is_seed badge 91-98; content scrim ~101; pal/vibePalette)
    - 04-UI-SPEC.md §Component Contracts #3 + §Color (no sage; accent on white/85)
    - 04-PATTERNS.md "apps/web/app/feed/NightCard.tsx (MODIFY — fit pill)"
  </read_first>
  <behavior>
    - When night.fit === true, render a small "looks for someone like you" pill on the card; when false, render nothing.
    - The pill never shows a score/percentage and does not collide with the ★ curated badge.
  </behavior>
  <action>
    Add a conditional pill rendered only when night.fit is true. Place it in the content stack on the bottom scrim (or top-left without colliding with the is_seed "★ curated" badge). Classes per 04-UI-SPEC §3: rounded-full px-3 py-1 font-body text-[13px] font-semibold lowercase shadow-md, bg-white/85 text-shell-accent (NOT sage/green — keep pink reserved). Copy exactly "looks for someone like you". Static, not actionable. Optional subtle leading sparkle is allowed but keep it quiet. NightCard.test.tsx: pill present when fit=true, absent when fit=false, never renders a number, does not overlap the curated badge (both can coexist).
  </action>
  <verify>
    <automated>cd /Users/lucas/Projects/After5 && pnpm --filter web test -- NightCard 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>NightCard renders "looks for someone like you" only when night.fit===true; never a score; coexists with the curated badge without collision; NightCard.test.tsx green.</acceptance_criteria>
  <done>Fit pill shipped, data-driven, tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Live reach-preview line on PostNightForm (D-01)</name>
  <read_first>
    - apps/web/app/nights/new/PostNightForm.tsx (targeting fieldset 375-494; helper-text 439-441; radius input ~494; genders/everyone landmine 69,98-101; em-dash landmine 315)
    - 04-UI-SPEC.md §Component Contracts #5 + §Copywriting (the four reach states)
    - 04-PATTERNS.md "apps/web/app/nights/new/PostNightForm.tsx (MODIFY — reach line)"
  </read_first>
  <behavior>
    - A debounced reachPreview call fires on genders/ageMin/ageMax/radiusKm change and renders the count line under the radius input.
    - Open targeting (everyone) is normalized to empty/omit before calling reachPreview so open nights do not undercount.
    - The four copy states (normal/low/zero/loading) render correctly; the line is aria-live="polite" and NEVER disables the publish CTA.
  </behavior>
  <action>
    Add a single quiet line after the radius input (~line 494), font-body text-[13px] lowercase text-shell-ink/65, aria-live="polite" (mirror the existing helper-text + the SwipeDeck remaining live region). Wire a debounced (e.g. ~400ms) reachPreview(client, { target_genders, target_age_range, city, radius_km }) on changes to genders/ageMin/ageMax/radiusKm. NORMALIZE the open case: when genders is ['everyone'] (or empty), send [] / omit target_genders so reach_preview counts everyone (the {everyone} landmine). Build target_age_range as an int4range literal from ageMin/ageMax; pass the host's primary city id as city and radiusKm as radius_km. Render the four copy states from 04-UI-SPEC: loading "counting who's around…", normal "~{N} people match this in {city}", low (small N) "~{N} match this in {city}. a focused crowd, widen anytime.", zero "no one fits this yet in {city}. loosen the targeting and they'll show up." MUST NEVER disable/gate the publish CTA. Use a middot or period — NO em-dash (do not replicate line 315). PostNightForm.test.tsx (mock reachPreview): renders loading then the count; low/zero variants; open case calls reachPreview WITHOUT a literal everyone gender; the publish CTA stays enabled regardless of count.
  </action>
  <verify>
    <automated>pnpm --filter web test -- PostNightForm 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>A debounced reach line renders the four states under the radius input; open targeting is normalized before the call; the publish CTA is never disabled by the count; no new em-dash; PostNightForm.test.tsx green.</acceptance_criteria>
  <done>Reach line shipped, debounced, everyone-normalized, non-blocking, tested.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The fit pill on feed cards and the live reach-preview line on the post-night form, rendered at 420px against the Barbiecore contract.</what-built>
  <how-to-verify>
    1. Forced-local render (visual-verify-ui-changes rule): local stack + Next with local Supabase env, sign in as QA.
    2. /feed at 420px: confirm a card with a genuine targeting match shows the small "looks for someone like you" pill (accent on white/85, not green), it does not collide with "★ curated", and non-matching cards have no pill. Confirm it is never a score.
    3. /nights/new: open the targeting fieldset; confirm a quiet "~N people match this in <city>" line appears under the radius input and updates live (debounced) as you change gender/age/radius. Set very narrow targeting -> confirm the low/zero copy is encouraging, never a warning color, and the publish CTA stays enabled. Set "everyone" -> confirm the count reflects everyone (not ~0).
    4. Critique against the 6-pillar rubric (lowercase/dry/no em-dash, accent reserved, ≥44px taps, contrast over vibe photos).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NightCard -> FeedNight payload | the fit signal must not leak host identity/targeting internals |
| PostNightForm -> reach_preview RPC | DEFINER count; must not surface per-user identity, only an aggregate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | the fit pill lets a searcher reverse-engineer who a date targets | mitigate | the pill renders a fixed phrase from a boolean (FeedNight.fit), never the targeting values or a who/score; the blind RPC (Plan 04-01) carries no identity. The card shows only "matches you", not "targets women 25-30 in X". |
| T-04-03 | Information Disclosure | the reach line exposes who is in the searcher pool | accept | reachPreview returns only an aggregate count (Plan 04-01); the UI renders "~N", never identities. Same accepted DEFINER pattern as match_*. |
| T-04-07 | Information Disclosure | the reach count enables enumeration by repeatedly narrowing targeting | accept | the count is coarse/aggregate and gated to authenticated hosts; narrowing reveals only a smaller count, not which individuals. Acceptable per spec (host needs the signal); no per-individual data crosses. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | zero new packages (RESEARCH audit N/A). |
</threat_model>

<verification>
- pnpm --filter web test green (NightCard + PostNightForm).
- Human visual-verify checkpoint approved (6-pillar rubric @420px): pill data-driven + accent (not green) + non-colliding; reach line live/debounced/encouraging/non-blocking + everyone-normalized.
</verification>

<success_criteria>
- Fit pill renders only on fit===true cards, never a score, no collision with curated.
- Reach line is debounced, four-state, everyone-normalized, never disables publish, no em-dash.
- Component tests green; visual-verify approved.
</success_criteria>

<output>
Create `.planning/phases/04-discoverability-feed-filters-targeting-p1/04-04-SUMMARY.md` when done.
</output>
