---
phase: 04-discoverability-feed-filters-targeting-p1
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - apps/web/app/feed/FilterSheet.tsx
  - apps/web/app/feed/SwipeDeck.tsx
  - apps/web/app/feed/page.tsx
  - apps/web/app/feed/FilterSheet.test.tsx
  - apps/web/app/feed/SwipeDeck.test.tsx
  - apps/web/e2e/e10-feed-filters.spec.ts
autonomous: false
requirements: [REQ-E10]

must_haves:
  truths:
    - "A brand-new searcher has nothing filtered (inclusive default); a light 3-chip row (distance/price/vibe) sits below the feed header"
    - "Tapping any chip OR the gear opens the full vaul FilterSheet"
    - "FilterSheet applies -> persists profiles.feed_filters -> closes -> feed re-queries; save failure shows a sonner toast"
    - "When hard filters empty the feed, a filtered-empty state names the most-restrictive hard filter + offers a one-tap loosen + 'post your own night'"
    - "When no hard filter is set and the deck is exhausted, the existing genuinely-empty EmptyDeck copy is unchanged"
  artifacts:
    - path: "apps/web/app/feed/FilterSheet.tsx"
      provides: "real two-group (dealbreakers/nice to have) sheet persisting feed_filters"
      contains: "saveFeedFilters"
    - path: "apps/web/app/feed/SwipeDeck.tsx"
      provides: "3 quick chips + filtered-vs-genuine EmptyDeck branch"
      contains: "FilterSheet"
  key_links:
    - from: "FilterSheet apply"
      to: "saveFeedFilters + feed re-query"
      via: "saveFeedFilters(client,userId,filters) then onApplied (refetch/router.refresh)"
      pattern: "onApplied"
    - from: "quick chip / gear"
      to: "FilterSheet"
      via: "setFilterOpen(true)"
      pattern: "setFilterOpen"
---

<objective>
Build the searcher-facing filter surfaces: turn the FilterSheet stub into the real two-group sheet that persists profiles.feed_filters and re-queries; add the light 3-chip quick row in SwipeDeck (D-04); branch EmptyDeck into filtered-recovery (D-02) vs genuinely-empty. All extend existing stubs against the Barbiecore contract in 04-UI-SPEC.md.

Purpose: This is where the searcher actually filters. Hard filters HIDE (server-side from Plan 04-01); the sheet writes the jsonb; the empty state recovers the user instead of trapping them.
Output: real FilterSheet + quick chips + recovery empty state + component tests + the forced-local e2e flow. Ends in a human visual-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-discoverability-feed-filters-targeting-p1/04-UI-SPEC.md
@.planning/phases/04-discoverability-feed-filters-targeting-p1/04-PATTERNS.md
@docs/superpowers/DESIGN-SYSTEM.md

<interfaces>
<!-- Live UI contracts (verified). Full class strings + analogs in 04-PATTERNS.md + 04-UI-SPEC.md §Component Contracts. -->
FilterSheet.tsx (stub, 64 lines): KEEP the shell verbatim — Drawer.Root/Portal/Overlay(bg-shell-ink/40)/Content(rounded-t-3xl bg-shell-base max-h-[80dvh] max-w-[420px] shadow-fun), the grabber, Drawer.Title (font-heading text-3xl lowercase "filters"), the h-14 rounded-full bg-shell-accent CTA. Replace the placeholder Description + disabled QUICK_CHIPS with two labeled groups.

SwipeDeck.tsx (385 lines): filterOpen state (line 47) + gear trigger (lines 107-118, aria-label="filters") + <FilterSheet open={filterOpen} onOpenChange={setFilterOpen}/> (line 164) already wired. EmptyDeck (lines 348-385) takes {tier} and renders the genuinely-empty copy + "post your own night" Link. remaining live region (line 136).

page.tsx (20 lines, force-dynamic): browseFeed(supabase,{limit:20}) SSR seed -> feedColdStartTier -> <SwipeDeck initial={nights} tier={tier}/>. To drive the filtered-vs-genuine branch, also read the viewer's feed_filters here (getMyProfile-style) and pass as a prop.

api-client (Plan 04-02): saveFeedFilters(client,userId,filters), FeedFilters type, FeedNight.fit, browseFeed (unchanged signature, reads feed_filters server-side).

Chip pattern (PostNightForm.tsx:383-437): role="group"+role="checkbox" aria-checked; min-h-[44px] rounded-full px-4 font-body text-[13px] font-semibold lowercase; selected bg-shell-accent text-white shadow-fun; inactive bg-white/80 text-shell-ink ring-1 ring-shell-ink/10; focus-visible:ring-4 focus-visible:ring-shell-accent/40 motion-reduce:transition-none. sonner toast on save fail: "that didn't save. try again?".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Real FilterSheet (two groups, persist, re-query)</name>
  <read_first>
    - apps/web/app/feed/FilterSheet.tsx (the stub shell to KEEP)
    - apps/web/app/nights/new/PostNightForm.tsx:383-437 (canonical Barbiecore chip + a11y mechanics)
    - 04-UI-SPEC.md §Component Contracts #2 + §Copywriting (group labels, field labels, CTA copy)
    - 04-PATTERNS.md "apps/web/app/feed/FilterSheet.tsx (MODIFY stub -> real)"
  </read_first>
  <behavior>
    - dealbreakers group (hard): who's hosting (host_genders multi), max price (max_price), how far (max_distance_km).
    - nice to have group (soft): vibe (vibes multi), who pays (who_pays multi), when (time_buckets: this weekend/weeknights/daytime), their age (host_age_range two number inputs).
    - apply -> saveFeedFilters(client,userId,builtFilters) -> ON SUCCESS invoke the onApplied callback (the Task1->Task2 contract that tells the parent to close + re-query) -> close; on error -> sonner "that didn't save. try again?" and DO NOT call onApplied. reset filters clears to inclusive defaults (empty object).
  </behavior>
  <action>
    Extend FilterSheet (keep the shell). Replace the placeholder body with two labeled groups per 04-UI-SPEC §2 using the PostNightForm chip classes (role="group"/checkbox, aria-checked, min-h-[44px]). Build a FeedFilters object from selected state, omitting empty groups (inclusive default). Footer: full-width accent "apply filters" CTA + a quiet "reset filters" text button (not accent). On apply call saveFeedFilters with the signed-in userId; on success invoke the onApplied callback prop (the parent wires it to a refetch/router.refresh) and close the sheet; on failure show the sonner toast and skip onApplied. Accept current filters + userId + an onApplied callback as props from SwipeDeck. Never label anything "exclude". motion-reduce on all new controls. FilterSheet.test.tsx: applying persists (saveFeedFilters called with the built shape) AND asserts the onApplied callback fires exactly once on a successful apply; reset clears; a save error (saveFeedFilters rejects) fires the sonner toast AND does NOT call onApplied; chips toggle aria-checked.
  </action>
  <verify>
    <automated>cd /Users/lucas/Projects/After5 && pnpm --filter web test -- FilterSheet 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>FilterSheet renders two groups (dealbreakers/nice to have) with the six field controls; apply calls saveFeedFilters with the built FeedFilters; FilterSheet.test.tsx explicitly asserts the onApplied callback is invoked on a successful apply (not merely that saveFeedFilters was called) and that onApplied is NOT invoked when saveFeedFilters rejects; reset clears; save failure shows the sonner toast; FilterSheet.test.tsx green. Shell unchanged. No "exclude" copy.</acceptance_criteria>
  <done>FilterSheet persists feed_filters and fires onApplied to re-query; the Task1->Task2 callback contract is asserted at the component-test layer; tested.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Real FilterSheet (two groups), the 3-chip quick row + gear, and the filtered-vs-genuine empty state in the feed — rendered at 420px against the Barbiecore contract.</what-built>
  <how-to-verify>
    1. Forced-local render (per the visual-verify-ui-changes rule): start the local stack + Next with the local Supabase env, sign in as the QA account, visit /feed at 420px width.
    2. Confirm: the 3 quick chips (distance/price/vibe) sit below the day-scope h1; tapping a chip OR the gear opens the vaul sheet; the sheet shows "dealbreakers" + "nice to have" groups with the six controls; "apply filters" persists + closes + the feed re-queries; an active chip flips to accent and shows its value.
    3. Set an impossible hard filter (e.g. max distance very low) to empty the feed; confirm the filtered-empty state names the filter + offers "widen to 50km?" + "post your own night" (NOT the funny genuinely-empty copy). Clear filters and confirm the genuinely-empty EmptyDeck copy returns when the deck is exhausted.
    4. Critique against the 6-pillar rubric (copy lowercase/dry/no em-dash, accent reserved per §Color, ≥44px taps, contrast).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Quick chips + filtered-vs-genuine EmptyDeck + page feed_filters seed + e2e</name>
  <read_first>
    - apps/web/app/feed/SwipeDeck.tsx (filterOpen + gear + FilterSheet mount + EmptyDeck 348-385)
    - apps/web/app/feed/page.tsx (SSR seed + feedColdStartTier)
    - apps/web/playwright.config.ts:5,59-64 + apps/web/e2e/_helpers/seed.ts (forced-local harness + fixtures)
    - 04-UI-SPEC.md §Component Contracts #1 + #4 + §Copywriting; 04-PATTERNS.md SwipeDeck + page sections
  </read_first>
  <behavior>
    - 3 quick chips (distance/price/vibe) below the header; tapping any opens FilterSheet (shortcuts, not inline editors); active chip shows its value.
    - EmptyDeck branches: filtered-empty (a hard filter is active) -> "nothing fits those filters." + names the most-restrictive hard filter + one-tap accent loosen (updates feed_filters + re-queries, never auto-relax) + "post your own night"; genuinely-empty (no hard filter) -> existing copy unchanged.
    - page.tsx seeds the viewer's feed_filters so SwipeDeck knows whether a hard filter is active.
  </behavior>
  <action>
    In SwipeDeck: add the 3-chip row beside/below the existing gear (reuse the chip classes; tapping any chip calls setFilterOpen(true)); show the active value when the corresponding feed_filters key is set. Pass current filters + userId + an onApplied refetch callback into FilterSheet (this callback is the contract Task 1 asserts). Branch EmptyDeck to take a hasHardFilter signal + the active filters: render the filtered-recovery copy (heading "nothing fits those filters.", name the most-restrictive hard filter, an accent "widen to {N}km?" that writes feed_filters via saveFeedFilters + re-queries, and "post your own night" -> /nights/new) when a hard filter is active; otherwise keep the shipped genuinely-empty EmptyDeck verbatim. In page.tsx: read the viewer's feed_filters (getMyProfile-style) and pass to SwipeDeck; keep force-dynamic + browseFeed seed. Add SwipeDeck.test.tsx (chip opens sheet; EmptyDeck shows recovery when a hard filter is set, genuinely-empty otherwise). Add apps/web/e2e/e10-feed-filters.spec.ts (forced-local): seed a targeted night + a searcher, post -> matching searcher sees fit pill + boost -> apply a hard filter that hides it -> filtered-empty recovery -> loosen recovers. No em-dashes in copy.
  </action>
  <verify>
    <automated>pnpm --filter web test -- SwipeDeck 2>&1 | tail -15 && CI=1 pnpm --filter web exec playwright test e10-feed-filters 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>3 quick chips open the sheet and reflect active values; SwipeDeck passes an onApplied refetch callback into FilterSheet; EmptyDeck shows filtered-recovery when a hard filter is active and the genuinely-empty copy otherwise; page.tsx seeds feed_filters; SwipeDeck.test.tsx + e10-feed-filters.spec.ts green. force-dynamic kept.</acceptance_criteria>
  <done>Searcher chips + recovery empty state + e2e flow shipped and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FilterSheet/EmptyDeck -> profiles.feed_filters write | self-owned write via saveFeedFilters; RLS is the boundary |
| SwipeDeck -> browse_feed_for_viewer | feed payload must stay blind (no host identity from the fit/boost) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering / EoP | the loosen action / apply writes feed_filters with a userId | mitigate | saveFeedFilters passes the signed-in userId; profiles_owner_all WITH CHECK(id=auth.uid()) gates it at the DB (Plan 04-01 RLS test). No client-trusted id. |
| T-04-02 | Information Disclosure | the fit pill / boost surfaced in the card could imply host identity | mitigate | the card consumes only FeedNight.fit (a computed boolean, no identity) from the blind RPC; UI renders the phrase only, never a who/score (D-03; Plan 04-01 blind test). |
| T-04-06 | Information Disclosure | feed re-query error leaks internals via toast | mitigate | the sonner copy is a fixed friendly string ("that didn't save. try again?"); no raw error text surfaced to the user. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | zero new packages (RESEARCH audit N/A). |
</threat_model>

<verification>
- pnpm --filter web test green (FilterSheet + SwipeDeck), FilterSheet.test.tsx asserting onApplied fires on success.
- CI=1 playwright e10-feed-filters green (forced-local; never against the prod-pointed env).
- Human visual-verify checkpoint approved (6-pillar rubric @420px).
</verification>

<success_criteria>
- Inclusive default (nothing filtered) + 3 quick chips + gear all open the real FilterSheet.
- Apply persists feed_filters + fires onApplied -> re-query; save failure toasts and skips onApplied.
- Filtered-empty recovery vs genuinely-empty EmptyDeck both correct.
- Component + forced-local e2e green; visual-verify approved.
</success_criteria>

<output>
Create `.planning/phases/04-discoverability-feed-filters-targeting-p1/04-03-SUMMARY.md` when done.
</output>
</content>
