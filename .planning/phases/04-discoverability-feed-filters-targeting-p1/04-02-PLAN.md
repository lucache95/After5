---
phase: 04-discoverability-feed-filters-targeting-p1
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/api-client/src/feed.ts
  - packages/api-client/src/profile.ts
  - packages/api-client/src/feed.test.ts
  - packages/api-client/src/profile.test.ts
autonomous: true
requirements: [REQ-E10]

must_haves:
  truths:
    - "FeedNight carries a fit boolean from the RPC payload"
    - "reachPreview(client, {target_genders?, target_age_range?, city, radius_km?}) returns a number from the reach_preview RPC"
    - "saveFeedFilters(client, userId, filters) self-writes profiles.feed_filters via PostgREST (no RPC)"
    - "A typed FeedFilters shape exists and is shared by saveFeedFilters and the FilterSheet"
  artifacts:
    - path: "packages/api-client/src/feed.ts"
      provides: "FeedNight.fit field + reachPreview() wrapper"
      contains: "reachPreview"
    - path: "packages/api-client/src/profile.ts"
      provides: "FeedFilters type + saveFeedFilters() PostgREST writer"
      contains: "saveFeedFilters"
  key_links:
    - from: "saveFeedFilters"
      to: "profiles.feed_filters"
      via: "client.from('profiles').update({feed_filters}).eq('id', userId)"
      pattern: "feed_filters"
    - from: "reachPreview"
      to: "reach_preview RPC"
      via: "client.rpc('reach_preview', {p_...})"
      pattern: "reach_preview"
---

<objective>
Wire the api-client layer to the DB contracts from Plan 04-01: add the fit field to FeedNight, add the reachPreview() RPC wrapper, define the shared FeedFilters type, and add the saveFeedFilters() self-write. These are the typed seams the UI plans (04-03, 04-04) consume.

Purpose: The FilterSheet persists filters via saveFeedFilters; the fit pill reads FeedNight.fit; the reach line calls reachPreview. Centralizing these here keeps web + native consistent and lets the UI compile against named exports.
Output: extended feed.ts + profile.ts with co-located vitest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-discoverability-feed-filters-targeting-p1/04-RESEARCH.md
@.planning/phases/04-discoverability-feed-filters-targeting-p1/04-PATTERNS.md

<interfaces>
<!-- Live api-client contracts (verified). Full templates in 04-PATTERNS.md + 04-RESEARCH.md Code Examples. -->
packages/api-client/src/feed.ts: FeedNight interface (13 fields incl. ambient_sound_path/ambient_sound_name) at top. RPC-wrapper convention (postNight ~line 29): const {data,error}=await client.rpc('fn',{p_...} as never); if(error) throw error; return data as T. browseFeed (~line 165) needs NO signature change. Json type imported from @after5/types; After5Client from ./index.

packages/api-client/src/profile.ts: savePreferences (~line 45) is the self-write PostgREST pattern: const {error}=await client.from('profiles').update(patch).eq('id', userId); if(error) throw error. upsertProfile (~line 20) casts the patch as never.

DB contract (Plan 04-01): profiles.feed_filters jsonb (keys: host_genders text[], max_price number, max_distance_km number, vibes text[], who_pays text[], time_buckets text[], host_age_range [int,int]). reach_preview(p_target_genders text[], p_target_age_range int4range, p_city uuid, p_radius_km numeric) returns integer; target_age_range is a Postgres int4range literal string e.g. '[25,40)'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FeedNight.fit + reachPreview() in feed.ts</name>
  <read_first>
    - packages/api-client/src/feed.ts (FeedNight interface; postNight RPC-wrapper convention; browseFeed)
    - 04-PATTERNS.md "packages/api-client/src/feed.ts (MODIFY)"; 04-RESEARCH.md "FeedNight + reachPreview api-client extension"
  </read_first>
  <behavior>
    - FeedNight gains `fit: boolean`.
    - reachPreview returns the integer count from reach_preview; on error it throws; a null/absent count coerces to 0.
    - target_age_range passes through as the int4range literal string; omitted/undefined params send undefined (open default).
  </behavior>
  <action>
    Add `fit: boolean;` to the FeedNight interface (E10 strong-match flag, D-03). Add an exported async reachPreview(client: After5Client, input: { target_genders?: string[]; target_age_range?: string | null; city: string; radius_km?: number | null }): Promise<number> that calls client.rpc('reach_preview', { p_target_genders: input.target_genders ?? undefined, p_target_age_range: input.target_age_range ?? undefined, p_city: input.city, p_radius_km: input.radius_km ?? undefined } as never), throws on error, returns (data as number) ?? 0. Do NOT change browseFeed's signature. Explicit return types; named export. Add feed.test.ts covering: reachPreview returns the mocked count, throws on rpc error, coerces null to 0; FeedNight type includes fit (a compile-time fixture object with fit).
  </action>
  <verify>
    <automated>cd /Users/lucas/Projects/After5 && pnpm --filter @after5/api-client test -- feed 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>feed.ts exports reachPreview; FeedNight includes fit; feed.test.ts green (returns count, throws on error, null->0). browseFeed signature unchanged.</acceptance_criteria>
  <done>FeedNight.fit + reachPreview() exist, typed, tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FeedFilters type + saveFeedFilters() in profile.ts</name>
  <read_first>
    - packages/api-client/src/profile.ts (savePreferences self-write pattern; upsertProfile cast-as-never)
    - 04-PATTERNS.md "packages/api-client/src/profile.ts (MODIFY)"; 04-RESEARCH.md "Persisting filters from the client"
  </read_first>
  <behavior>
    - saveFeedFilters writes profiles.feed_filters for the given userId via PostgREST; throws on error.
    - FeedFilters is an exported interface with optional keys host_genders, max_price, max_distance_km, vibes, who_pays, time_buckets, host_age_range — matching the jsonb keys the RPC unpacks.
  </behavior>
  <action>
    Add an exported interface FeedFilters with optional fields: host_genders?: string[]; max_price?: number; max_distance_km?: number; vibes?: string[]; who_pays?: string[]; time_buckets?: string[]; host_age_range?: [number, number]. Add exported async saveFeedFilters(client: After5Client, userId: string, filters: FeedFilters): Promise<void> mirroring savePreferences: const {error}=await client.from('profiles').update({ feed_filters: filters as unknown as Json }).eq('id', userId); if(error) throw error. NO RPC (self-owned column, gated by profiles_owner_all). Explicit return type; named export. Add profile.test.ts covering: saveFeedFilters calls update with feed_filters scoped to .eq('id', userId), throws on error.
  </action>
  <verify>
    <automated>pnpm --filter @after5/api-client test -- profile 2>&1 | tail -15 && pnpm --filter @after5/api-client typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>profile.ts exports FeedFilters + saveFeedFilters; profile.test.ts green; package typechecks against the regenerated database.ts (feed_filters present). The update is scoped .eq('id', userId).</acceptance_criteria>
  <done>FeedFilters + saveFeedFilters() exist, typed, tested; package typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> profiles.feed_filters write | self-owned jsonb write via PostgREST; RLS is the boundary, not the client |
| client -> reach_preview RPC | authenticated-only DEFINER count |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering / EoP | saveFeedFilters could be invoked with another user's id | mitigate | the write is gated by profiles_owner_all WITH CHECK(id=auth.uid()) at the DB; the wrapper passes the signed-in userId; a forged userId fails RLS (0 rows / error). No client-trust. Backed by Plan 04-01 e10_feed_filters_rls.sql. |
| T-04-05 | Tampering / DoS | malformed FeedFilters jsonb reaches the RPC | mitigate | FeedFilters type constrains the shape at compile time; the RPC unpacks only whitelisted keys and treats absent/unknown keys as no-filter (Plan 04-01 f CTE + feed_filters_is_object CHECK). |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | zero new packages (RESEARCH audit N/A); no install task. |
</threat_model>

<verification>
- pnpm --filter @after5/api-client test green (feed + profile).
- pnpm --filter @after5/api-client typecheck green against the regenerated database.ts.
</verification>

<success_criteria>
- FeedNight.fit, reachPreview(), FeedFilters, saveFeedFilters() all exist as named exports with explicit return types.
- Co-located vitest covers reachPreview (count/throw/null->0) and saveFeedFilters (scoped self-write/throw).
- The package typechecks against the live regenerated types from Plan 04-01.
</success_criteria>

<output>
Create `.planning/phases/04-discoverability-feed-filters-targeting-p1/04-02-SUMMARY.md` when done.
</output>
