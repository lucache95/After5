---
phase: 03-marketplace-completeness-p1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260605120000_e11_targeting_cols.sql
  - supabase/migrations/20260605120100_e12_queue_status_passed_by_host.sql
  - supabase/migrations/20260605120200_e11_post_night_targeting.sql
  - supabase/tests/e11_targeting.sql
  - packages/api-client/src/feed.ts
  - packages/types/src/database.ts
autonomous: true
requirements: [REQ-E11, REQ-E12]
must_haves:
  truths:
    - "date_instances has target_genders, target_age_range, search_radius_km columns with safe defaults"
    - "queue_status enum includes passed_by_host"
    - "post_night persists targeting columns onto date_instances (not the itinerary fork)"
    - "update_itinerary_stops sets pay_setting and vibe_tags on the forked itinerary"
    - "every new/re-emitted function signature revokes EXECUTE from anon and public, grants only authenticated"
    - "full migration chain applies clean from scratch locally; prod apply NOT performed (gated)"
  artifacts:
    - path: "supabase/migrations/20260605120000_e11_targeting_cols.sql"
      provides: "Additive targeting columns on date_instances"
      contains: "target_genders"
    - path: "supabase/migrations/20260605120100_e12_queue_status_passed_by_host.sql"
      provides: "passed_by_host enum value, in its own migration before any user"
      contains: "add value if not exists 'passed_by_host'"
    - path: "supabase/migrations/20260605120200_e11_post_night_targeting.sql"
      provides: "Extended post_night + update_itinerary_stops signatures"
      contains: "p_target_genders"
    - path: "supabase/tests/e11_targeting.sql"
      provides: "psql-assertion test for targeting persistence + pay/vibe setters + anon revoke"
      contains: "passed_by_host"
  key_links:
    - from: "supabase/migrations/20260605120200_e11_post_night_targeting.sql"
      to: "date_instances target_* columns"
      via: "insert into date_instances value list"
      pattern: "insert into date_instances"
    - from: "packages/api-client/src/feed.ts"
      to: "post_night RPC"
      via: "postNight wrapper passes new params"
      pattern: "p_target"
---

<objective>
Lay the Phase-3 database foundation: the two additive migrations every later plan depends on (per-date targeting columns on `date_instances`; the `passed_by_host` `queue_status` value) plus the extension of the two existing creator RPCs (`post_night` gains targeting params, `update_itinerary_stops` gains pay/vibe setters). Apply locally and prove the migration chain is clean. PROD APPLY IS OUT OF SCOPE / GATED.

Purpose: Door-2 creator controls (E11) and host reject (E12) cannot persist their data until these columns/enum/params exist. This plan is the seam.
Output: 3 LOCAL-only migrations, 1 SQL test, extended `postNight` wrapper, regenerated DB types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-marketplace-completeness-p1/03-CONTEXT.md
@.planning/phases/03-marketplace-completeness-p1/03-RESEARCH.md
@.planning/phases/03-marketplace-completeness-p1/03-PATTERNS.md

# Exemplars to copy (read before editing)
@supabase/migrations/20260602140100_m3_post_night_fork.sql
@supabase/migrations/20260602140000_m3_update_itinerary_stops.sql
@supabase/migrations/20260525120500_p0_queue_entries.sql
@supabase/migrations/20260527124550_s2_notification_type_5b_extend.sql
@supabase/migrations/20260602120700_m4_post_night_drop_4arg.sql
@supabase/tests/m4_post_night_ambient.sql
@supabase/tests/_fixtures.sql

<interfaces>
<!-- queue_status enum today (20260525120500_p0_queue_entries.sql line 3): -->
<!-- ('interested','shortlisted','offer_active','offer_passed','offer_expired','standby','locked') -->
<!-- update_itinerary_stops today sets stops/title/why_note/cover_image_url/totals; does NOT set pay_setting/vibe_tags. -->
<!-- Current grant trio (lines 53-55): revoke execute ... from public; from anon; grant ... to authenticated. -->
<!-- post_night forks the itinerary (copies itineraries columns) then inserts a date_instances row — targeting must be added to the date_instances insert, NOT the fork select (Pitfall 4). -->
<!-- postNight wrapper: packages/api-client/src/feed.ts lines ~19-30. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive targeting columns + passed_by_host enum (two separate migrations)</name>
  <files>supabase/migrations/20260605120000_e11_targeting_cols.sql, supabase/migrations/20260605120100_e12_queue_status_passed_by_host.sql</files>
  <read_first>
    - supabase/migrations/20260525120500_p0_queue_entries.sql (idempotent additive DDL posture — `add column if not exists`, `do $$ ... exception when duplicate_object then null; end $$`)
    - supabase/migrations/20260527124550_s2_notification_type_5b_extend.sql (PATTERNS analog: `alter type ... add value if not exists`)
    - 03-PATTERNS.md §"Additive enum value" + §e11_targeting_cols
  </read_first>
  <action>
    Create `supabase/migrations/20260605120000_e11_targeting_cols.sql`: add three columns to `date_instances` with safe defaults so existing prod rows stay valid (per D-03b / Runtime State Inventory) — `target_genders text[] not null default '{}'` (empty = open to everyone), `target_age_range int4range` (nullable = unbounded), `search_radius_km int` (nullable = city default). Use `add column if not exists` for idempotency. No RLS change (row policies already cover new columns). No backfill needed (defaults cover existing rows).

    Create a SEPARATE migration `supabase/migrations/20260605120100_e12_queue_status_passed_by_host.sql` containing only `alter type queue_status add value if not exists 'passed_by_host';`. It MUST be its own migration file sequenced BEFORE 03-02's reject_candidate RPC (Pitfall 1 / PG ADD VALUE same-tx rule — a new enum value is not usable in the transaction that adds it). Do NOT add the enum value in the same file as any function that references it.
  </action>
  <verify>
    <automated>grep -q "target_genders" supabase/migrations/20260605120000_e11_targeting_cols.sql && grep -q "add value if not exists 'passed_by_host'" supabase/migrations/20260605120100_e12_queue_status_passed_by_host.sql && echo OK</automated>
  </verify>
  <done>Both migration files exist; targeting cols are additive with safe defaults; passed_by_host is in its own file with no function reference.</done>
</task>

<task type="auto">
  <name>Task 2: Extend post_night (targeting) + update_itinerary_stops (pay/vibe) + postNight wrapper</name>
  <files>supabase/migrations/20260605120200_e11_post_night_targeting.sql, packages/api-client/src/feed.ts</files>
  <read_first>
    - supabase/migrations/20260602140100_m3_post_night_fork.sql (post_night fork + date_instances insert; the fork select copies itineraries columns at lines ~46-58, the date_instances insert at ~60-64)
    - supabase/migrations/20260602140000_m3_update_itinerary_stops.sql (setter block lines 42-49; grant trio lines 53-55)
    - supabase/migrations/20260602120700_m4_post_night_drop_4arg.sql (Pitfall 3 precedent: drop stale overload so exactly ONE live signature)
    - packages/api-client/src/feed.ts (postNight wrapper ~19-30, update_itinerary_stops wrapper)
    - 03-PATTERNS.md §e11_post_night_targeting + §"Anon-revoke on new/overloaded RPCs"
  </read_first>
  <action>
    Create `supabase/migrations/20260605120200_e11_post_night_targeting.sql`:

    (a) `update_itinerary_stops` — add `p_pay_setting text default null` + `p_vibe_tags text[] default null` to the signature (after p_cover_image_url) and to the coalesce SET list: `pay_setting = coalesce(p_pay_setting, pay_setting)`, `vibe_tags = coalesce(p_vibe_tags, vibe_tags)` (per D-10). Re-emit the grant trio for the NEW arg-type list — `revoke execute ... from public; from anon; grant ... to authenticated` (Pitfall 2). If the prior 5-arg signature becomes a stale overload, drop it via `drop function if exists update_itinerary_stops(<old arg list>)` so exactly one live signature remains.

    (b) `post_night` — add `p_target_genders text[] default '{}'`, `p_target_age_range int4range default null`, `p_search_radius_km int default null` to the signature. Leave the FORK select UNTOUCHED (Pitfall 4 — targeting lives on date_instances, not itineraries). Add the three columns to the `insert into date_instances (...)` value list, sourced from the new params. Drop any stale post_night overload first (`drop function if exists post_night(<stale arg list>)` per 20260602120700 precedent) so exactly ONE live signature remains, avoiding PGRST203/"is not unique". Re-emit the grant trio for the new signature.

    Then update `packages/api-client/src/feed.ts`: extend the `postNight` wrapper to pass `p_target_genders`/`p_target_age_range`/`p_search_radius_km` and extend the `updateItineraryStops` wrapper to pass `p_pay_setting`/`p_vibe_tags`. No fenced SQL in this file — TS only. Keep additive params optional (defaulted) so existing callers compile unchanged.
  </action>
  <verify>
    <automated>grep -q "p_target_genders" supabase/migrations/20260605120200_e11_post_night_targeting.sql && grep -q "pay_setting = coalesce" supabase/migrations/20260605120200_e11_post_night_targeting.sql && grep -Eq "drop function if exists post_night" supabase/migrations/20260605120200_e11_post_night_targeting.sql && grep -q "p_target" packages/api-client/src/feed.ts && echo OK</automated>
  </verify>
  <done>Both RPCs extended with one live signature each; targeting written to the date_instances insert; pay/vibe set on the itinerary; anon revoked on every re-emitted signature; wrapper passes new params.</done>
</task>

<task type="auto">
  <name>Task 3: SQL test + LOCAL apply gate (db:reset → types → test → typecheck)</name>
  <files>supabase/tests/e11_targeting.sql, packages/types/src/database.ts</files>
  <read_first>
    - supabase/tests/m4_post_night_ambient.sql + supabase/tests/_fixtures.sql (mk_user/mk_itinerary/mk_instance, set_config('request.jwt.claims', ...), per-block ROLLBACK harness)
    - 03-VALIDATION.md (apply order, sampling rate)
    - 03-PATTERNS.md §"SQL tests"
  </read_first>
  <action>
    Create `supabase/tests/e11_targeting.sql` using the shared fixtures (`\i _fixtures.sql`, `mk_user`/`mk_itinerary`/`mk_instance`, jwt-claims set_config, per-block ROLLBACK). Assert: (1) calling the extended `post_night` with target_genders/target_age_range/search_radius_km persists those values onto the resulting `date_instances` row; (2) `update_itinerary_stops` with p_pay_setting/p_vibe_tags writes them onto the itinerary; (3) the new/re-emitted signatures are NOT executable by anon (call as anon → expect permission error). Copy the errcode-assertion `DO $$ ... EXCEPTION WHEN ... THEN ok:=true ... END $$` block style from a_make_offer / m4_post_night_ambient.

    [BLOCKING] Run the LOCAL apply gate and confirm green before marking done: `pnpm db:reset` (full chain applies from scratch) → `pnpm db:types` (regenerates packages/types/src/database.ts so target_* cols + passed_by_host appear) → `pnpm db:test` (all SQL tests incl. the new one pass) → `pnpm -w typecheck`. DO NOT run `pnpm db:push`, do NOT apply to prod, do NOT deploy edge functions to prod — prod apply is GATED and batched by the orchestrator. Run a local DDL security grep-audit on the new migrations: confirm every new/re-emitted function has `security definer`/owner-side `set search_path` where applicable, explicit `revoke ... from anon`, and no `using (true)` was introduced.
  </action>
  <verify>
    <automated>pnpm db:reset && pnpm db:types && pnpm db:test && pnpm -w typecheck</automated>
  </verify>
  <done>New SQL test passes; full local chain (reset→types→test→typecheck) is green; database.ts contains target_genders + passed_by_host; no prod apply performed; grep-audit confirms anon revoked and no new USING(true).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → post_night/update_itinerary_stops RPC | Untrusted creator input crosses into DEFINER functions |
| anon → new/overloaded public functions | Supabase auto-grants EXECUTE to anon on new public funcs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | re-emitted post_night/update_itinerary_stops signatures | mitigate | `revoke execute ... from anon` + `from public`; grant only authenticated (Task 2); SQL test asserts anon cannot call (Task 3) |
| T-03-02 | Tampering | post_night targeting write | mitigate | Targeting written to date_instances insert only; fork select untouched (Pitfall 4); function keeps existing owner re-check |
| T-03-03 | Tampering | post_night overload ambiguity | mitigate | Drop stale overloads so exactly one live signature (Pitfall 3) — prevents wrong-fn dispatch |
| T-03-04 | Tampering | enum migration in same tx as user | mitigate | passed_by_host added in its own migration before any consumer (Pitfall 1) |
| T-03-SC | Tampering | npm/supabase package installs | mitigate | None — this phase installs ZERO new packages (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `pnpm db:reset && pnpm db:types && pnpm db:test && pnpm -w typecheck` all green.
- `packages/types/src/database.ts` contains `target_genders`, `target_age_range`, `search_radius_km`, and `passed_by_host`.
- grep confirms `revoke execute ... from anon` on every new/re-emitted signature; no new `using (true)`.
- NO prod apply / db:push / edge deploy performed (gated).
</verification>

<success_criteria>
- Targeting columns exist on date_instances with safe defaults.
- queue_status includes passed_by_host (own migration, before any user).
- post_night persists targeting; update_itinerary_stops sets pay/vibe; one live signature each.
- Full local migration chain clean; prod GATED.
</success_criteria>

<output>
Create `.planning/phases/03-marketplace-completeness-p1/03-01-SUMMARY.md` when done.
</output>
