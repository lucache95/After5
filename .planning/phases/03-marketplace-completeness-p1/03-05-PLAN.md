---
phase: 03-marketplace-completeness-p1
plan: 05
type: execute
wave: 2
depends_on: ["03-04"]
files_modified:
  - apps/web/app/offers/[offerId]/OfferDetail.tsx
  - apps/web/app/offers/[offerId]/page.tsx
  - apps/web/app/matches/[lockId]/LockDetail.tsx
  - apps/web/app/matches/[lockId]/page.tsx
  - apps/web/app/matches/lock-view.ts
  - supabase/tests/e13_plan_read.sql
  - apps/web/app/offers/[offerId]/__tests__/OfferDetail.test.tsx
autonomous: true
requirements: [REQ-E13]
must_haves:
  truths:
    - "/offers/[offerId] renders the matched night's full stops/venues/timing in the 'the night' section (no longer labelled-but-empty)"
    - "/matches/[lockId] renders a 'the night' section with the full itinerary"
    - "stops load via the existing RLS read path (date_instances offer-recipient/lock-party → itineraries by id) with NO new RPC"
    - "the dead host.bio branch is removed from OfferDetail and its loader (F#5)"
    - "the photo-led reveal ordering is unchanged (D-07) — only the plan is added"
    - "a candidate/lock-party can read the forked stops; a stranger cannot read the instance"
  artifacts:
    - path: "apps/web/app/offers/[offerId]/OfferDetail.tsx"
      provides: "PlanTimeline rendered in 'the night'; host.bio removed"
    - path: "apps/web/app/matches/[lockId]/LockDetail.tsx"
      provides: "'the night' section with PlanTimeline"
    - path: "supabase/tests/e13_plan_read.sql"
      provides: "RLS read test: candidate reads stops, stranger denied"
      contains: "itinerary_id"
  key_links:
    - from: "apps/web/app/offers/[offerId]/page.tsx"
      to: "itineraries.stops"
      via: "date_instances.itinerary_id → itineraries read (RLS USING(true))"
      pattern: "itinerary_id"
    - from: "apps/web/app/offers/[offerId]/OfferDetail.tsx"
      to: "apps/web/components/PlanTimeline.tsx"
      via: "import PlanTimeline"
      pattern: "PlanTimeline"
    - from: "apps/web/app/matches/lock-view.ts"
      to: "itineraries.stops"
      via: "instance select extended with itinerary_id"
      pattern: "itinerary_id"
---

<objective>
Deliver the Phase-3 payoff — "every match has a real plan attached" — by rendering the matched night's full itinerary via the shared `PlanTimeline` (from 03-04) on BOTH `/offers/[offerId]` (OfferDetail) and `/matches/[lockId]` (LockDetail). Fix the offer screen's labelled-but-empty "the night" section. Load stops via the existing RLS read path (no new RPC, per D-12). Delete the dead `host.bio` branch (F#5). Keep the photo-led reveal ordering unchanged (D-07).

Purpose: At the moment it should pay off, the candidate/match sees the actual plan, not just a person and a date.
Output: Extended OfferDetail + LockDetail + their loaders, an E13 RLS read SQL test, an RTL stop-render test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-marketplace-completeness-p1/03-CONTEXT.md
@.planning/phases/03-marketplace-completeness-p1/03-RESEARCH.md
@.planning/phases/03-marketplace-completeness-p1/03-PATTERNS.md
@.planning/phases/03-marketplace-completeness-p1/03-UI-SPEC.md

# Surfaces to extend (read before editing)
@apps/web/app/offers/[offerId]/OfferDetail.tsx
@apps/web/app/offers/[offerId]/page.tsx
@apps/web/app/matches/[lockId]/LockDetail.tsx
@apps/web/app/matches/[lockId]/page.tsx
@apps/web/app/matches/lock-view.ts
@supabase/tests/e_offer_recipient_date_read.sql
@supabase/tests/_fixtures.sql
@packages/api-client/src/feed.ts

<interfaces>
<!-- PlanTimeline (from 03-04): apps/web/components/PlanTimeline.tsx — props stops/accent/vibeTags. -->
<!-- OfferDetail "the night" section ~lines 97-102 (date/time only today); eyebrow "the night" text-shell-accent line 98; ExpiryCountdown present. host.bio render ~line 93; bio prop ~line 26. -->
<!-- offers/[offerId]/page.tsx: loader already embeds instance:date_instances (~42-43); bio: null hardcode ~line 98. -->
<!-- RLS read path (D-12, VERIFIED): date_instances_select_offer_recipient (20260527127500) lets candidate read the instance; itineraries_readable_by_id USING(true) (20260419202912) lets any caller read the forked itinerary by id. Read date_instances → itinerary_id → itineraries.stops → normalizeNightDetailStops. Do NOT use get_night_detail (blind/pre-swipe-only). -->
<!-- LockDetail renders no plan today; insert "the night" between the message {name} block (~77-88) and cancel actions (~99). lock-view.ts instance select needs itinerary_id added. -->
<!-- Degrade copy: offer pre-accept → "the full plan unlocks here."; lock missing stops → "plan's being put together." -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OfferDetail — render plan via RLS read + drop host.bio + RTL test</name>
  <files>apps/web/app/offers/[offerId]/OfferDetail.tsx, apps/web/app/offers/[offerId]/page.tsx, apps/web/app/offers/[offerId]/__tests__/OfferDetail.test.tsx</files>
  <behavior>
    - The offer "the night" section renders all stops (name + time + cost + map) via PlanTimeline.
    - When stops are empty/fail to load, the section shows "the full plan unlocks here." — never a blank labelled section.
    - host.bio is removed from OfferDetail props and render (F#5).
  </behavior>
  <read_first>
    - apps/web/app/offers/[offerId]/OfferDetail.tsx ("the night" ~97-102, host.bio ~93/26, run wrapper ~46-70)
    - apps/web/app/offers/[offerId]/page.tsx (instance embed ~42-43, bio:null ~98)
    - 03-PATTERNS.md §OfferDetail + §"SSR RLS read of the matched plan"; 03-UI-SPEC §E13
  </read_first>
  <action>
    In `offers/[offerId]/page.tsx`: extend the existing `instance:date_instances!...` embed to also pull `itinerary_id`; then do the second RLS read — if `instance.itinerary_id` present, `supabase.from('itineraries').select('stops, vibe_tags').eq('id', instance.itinerary_id).maybeSingle()` (readable via `itineraries_readable_by_id USING(true)`), then `normalizeNightDetailStops(it?.stops)`. Pass `stops` into OfferDetail. Delete the `bio: null` hardcode (F#5). Do NOT use `get_night_detail` (blind/pre-swipe-only).

    In `OfferDetail.tsx`: replace the single date/time `<p>` under the `the night` eyebrow with `<PlanTimeline stops={stops} ... />` (accent from `vibePalette(vibeTags).accent`). Keep the eyebrow, the date line, and `ExpiryCountdown`. Respect the reveal tier (D-07) — do not move the host-reveal placement; only add the plan. Empty/failed stops → degrade copy `the full plan unlocks here.`. Extend `OfferDetailProps` with `stops`; remove the `bio` prop + its render branch.

    Add/extend `apps/web/app/offers/[offerId]/__tests__/OfferDetail.test.tsx` (RTL): assert stops render in "the night"; assert the empty-stops degrade copy renders; assert no host.bio text path remains.
  </action>
  <verify>
    <automated>grep -q "PlanTimeline" apps/web/app/offers/[offerId]/OfferDetail.tsx && grep -q "itinerary_id" apps/web/app/offers/[offerId]/page.tsx && ! grep -q "bio" apps/web/app/offers/[offerId]/OfferDetail.tsx && pnpm --filter web test -- OfferDetail && pnpm -w typecheck</automated>
    <human-check>forced-local Playwright 420px: offer "the night" renders the plan timeline; reveal ordering unchanged; per 03-UI-SPEC</human-check>
  </verify>
  <done>OfferDetail renders stops with degrade fallback; host.bio gone; loader uses the RLS read path; RTL + typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LockDetail — add "the night" section via the same RLS read</name>
  <files>apps/web/app/matches/[lockId]/LockDetail.tsx, apps/web/app/matches/[lockId]/page.tsx, apps/web/app/matches/lock-view.ts</files>
  <behavior>
    - LockDetail renders a "the night" section with the full itinerary via PlanTimeline.
    - Missing stops on a lock shows "plan's being put together." — never a blank section.
  </behavior>
  <read_first>
    - apps/web/app/matches/[lockId]/LockDetail.tsx (message block ~77-88, cancel actions ~99, props ~16-30)
    - apps/web/app/matches/[lockId]/page.tsx + apps/web/app/matches/lock-view.ts (instance select)
    - 03-PATTERNS.md §LockDetail
  </read_first>
  <action>
    In `matches/lock-view.ts`: extend the instance select with `itinerary_id`. In `matches/[lockId]/page.tsx`: do the same two-step RLS read as the offer page (date_instances → itinerary_id → itineraries.stops → normalize) and pass `stops` to LockDetail.

    In `LockDetail.tsx`: add a `the night` section (eyebrow `text-shell-accent` + `<PlanTimeline>`) between the `message {name}` block and the cancel/rate actions. Extend `LockDetailProps` with `stops`. Post-lock the full itinerary is fair game. Missing-stops degrade copy `plan's being put together.`.
  </action>
  <verify>
    <automated>grep -q "PlanTimeline" apps/web/app/matches/[lockId]/LockDetail.tsx && grep -q "itinerary_id" apps/web/app/matches/lock-view.ts && pnpm -w typecheck</automated>
    <human-check>forced-local Playwright 420px: lock "the night" renders below message block, above cancel; per 03-UI-SPEC</human-check>
  </verify>
  <done>LockDetail renders the plan with degrade fallback; lock-view loads itinerary_id; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 3: E13 RLS read SQL test + LOCAL apply gate</name>
  <files>supabase/tests/e13_plan_read.sql</files>
  <read_first>
    - supabase/tests/e_offer_recipient_date_read.sql (positive/negative RLS read precedent)
    - supabase/tests/_fixtures.sql
    - 03-PATTERNS.md §"SSR RLS read of the matched plan"; 03-VALIDATION.md (Pitfall 5)
  </read_first>
  <action>
    Create `supabase/tests/e13_plan_read.sql` (copy from e_offer_recipient_date_read.sql). Assert: (1) an offer-recipient candidate can SELECT the `date_instances` row post-offer and read `itinerary_id`; (2) reading `itineraries` by that id returns the forked stops; (3) a STRANGER (neither creator nor offer-recipient) CANNOT read the instance row (zero rows / RLS deny). Reuse shared fixtures; per-block ROLLBACK.

    [BLOCKING] LOCAL apply gate: `pnpm db:reset` → `pnpm db:types` → `pnpm db:test` → `pnpm -w typecheck`, all green. NO db:push / prod apply / edge deploy (GATED). This plan adds no DDL, but run the full chain to confirm the RLS test passes against the 03-01/03-02 migrations.
  </action>
  <verify>
    <automated>pnpm db:reset && pnpm db:types && pnpm db:test && pnpm -w typecheck</automated>
  </verify>
  <done>e13_plan_read.sql passes (candidate reads stops; stranger denied); full local chain green; no prod apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate/lock-party → date_instances + itineraries read | A non-party could attempt to read a plan they weren't offered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Information Disclosure | candidate reads a plan they weren't offered | mitigate | date_instances_select_offer_recipient gates the instance read; e13_plan_read.sql asserts stranger denied (Task 3) |
| T-03-15 | Information Disclosure | itineraries USING(true) over-broad read | accept | Legacy posture; privacy boundary is the unguessable UUID + the gated instance read; flagged for future hardening (out of scope this phase per RESEARCH) |
| T-03-16 | Information Disclosure | wrong-row read via blind RPC (get_night_detail) | mitigate | Explicitly use the date_instances→itinerary_id→itineraries path, NOT get_night_detail (Pitfall 5) |
| T-03-SC | Tampering | package installs | mitigate | Zero new packages (RESEARCH audit N/A) |
</threat_model>

<verification>
- `pnpm db:test` includes e13_plan_read.sql passing (positive + stranger-denied).
- `pnpm --filter web test -- OfferDetail` + `pnpm -w typecheck` green.
- grep confirms PlanTimeline used on both screens, host.bio gone, itinerary_id in both loaders.
- Forced-local visual-verify of both screens against 03-UI-SPEC; no prod apply.
</verification>

<success_criteria>
- Both /offers/[offerId] and /matches/[lockId] render the full plan via PlanTimeline.
- Offer "the night" no longer empty; host.bio removed; reveal ordering unchanged.
- RLS read path proven (candidate yes, stranger no); local chain clean.
</success_criteria>

<output>
Create `.planning/phases/03-marketplace-completeness-p1/03-05-SUMMARY.md` when done.
</output>
