---
phase: 03-marketplace-completeness-p1
plan: 07
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - apps/web/app/dates/[slug]/interested/InterestedList.tsx
  - apps/web/app/dates/[slug]/interested/page.tsx
  - apps/web/app/dates/[slug]/interested/__tests__/InterestedList.test.tsx
autonomous: true
requirements: [REQ-E12]
must_haves:
  truths:
    - "each new-interest row has a quiet decline control that opens a vaul confirm sheet and silently removes the candidate (no candidate notification)"
    - "an outstanding offer (offer_active) has a withdraw control routed through the existing match-withdraw"
    - "offer outcomes are surfaced as lowercase pills: accepted / they passed / expired / offer out"
    - "a passed_by_host candidate is filtered out of both list sections (silent removal)"
    - "every new action uses optimistic-mutate-with-rollback + a sonner toast"
  artifacts:
    - path: "apps/web/app/dates/[slug]/interested/InterestedList.tsx"
      provides: "decline + withdraw + outcome pills"
      min_lines: 30
    - path: "apps/web/app/dates/[slug]/interested/__tests__/InterestedList.test.tsx"
      provides: "RTL: decline removes silently, withdraw confirms, outcome pills render"
  key_links:
    - from: "apps/web/app/dates/[slug]/interested/InterestedList.tsx"
      to: "rejectCandidate wrapper"
      via: "decline confirm → rejectCandidate(instance, candidate)"
      pattern: "rejectCandidate"
    - from: "apps/web/app/dates/[slug]/interested/InterestedList.tsx"
      to: "withdraw wrapper"
      via: "withdraw confirm → withdraw(instance)"
      pattern: "withdraw"
---

<objective>
Surface E12 host triage on the live interested list: a quiet, silent decline control (per D-04, via the `rejectCandidate` wrapper from 03-02), a withdraw control for an outstanding offer (via the existing `match-withdraw`), and offer-outcome pills (accepted / they passed / expired / offer out). Filter `passed_by_host` candidates out of both sections (silent removal).

Purpose: The new-interest list is no longer append-only — a host can triage candidates and see what happened to each offer, all without ever broadcasting a rejection.
Output: Extended InterestedList + page loader + RTL test. Marked for forced-local visual-verify against 03-UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-marketplace-completeness-p1/03-CONTEXT.md
@.planning/phases/03-marketplace-completeness-p1/03-RESEARCH.md
@.planning/phases/03-marketplace-completeness-p1/03-PATTERNS.md
@.planning/phases/03-marketplace-completeness-p1/03-UI-SPEC.md
@docs/superpowers/DESIGN-SYSTEM.md

# Surfaces to extend (read before editing)
@apps/web/app/dates/[slug]/interested/InterestedList.tsx
@apps/web/app/dates/[slug]/interested/page.tsx
@apps/web/app/my-nights/NightCardActions.tsx
@apps/web/lib/after5/match.ts

<interfaces>
<!-- rejectCandidate(instance, candidate): Promise<null> — added in 03-02 (match.ts). -->
<!-- withdraw(instance): existing wrapper (match.ts ~128). -->
<!-- HostCandidate['status'] union (InterestedList line ~28) now includes passed_by_host (from 03-01 regen). -->
<!-- Optimistic-mutate-rollback house pattern: addToShortlist (~141-155); persistOrder (~117-133). -->
<!-- vaul confirm sheet pattern: NightCardActions.tsx ~166-195 (Drawer.Root controlled + overlay + bottom-sheet + grab handle + Title/Description + accent confirm + cancel). -->
<!-- "offer out" badge renders today at InterestedList ~188-191 (the frozen offer_active rank-1 row). -->
<!-- Outcome statuses already in union: offer_active / offer_passed / offer_expired / locked. -->
<!-- Neutral pill tokens: bg-shell-ink/5 text-shell-ink/55. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decline + withdraw confirm sheets (silent) + page outcome load</name>
  <files>apps/web/app/dates/[slug]/interested/InterestedList.tsx, apps/web/app/dates/[slug]/interested/page.tsx</files>
  <behavior>
    - Tapping decline on a new-interest row opens a vaul confirm ("pass on {name}?"); confirming calls rejectCandidate and optimistically removes the row.
    - Decline is SILENT — no candidate-facing copy anywhere; no notification.
    - On reject failure, the row is restored and an error toast shows.
    - Tapping withdraw on an offer_active row opens a confirm ("pull this offer back?"); confirming calls withdraw(instance).
  </behavior>
  <read_first>
    - apps/web/app/dates/[slug]/interested/InterestedList.tsx (addToShortlist ~141-155, persistOrder ~117-133, offer-out badge ~188-191, status union ~28, section memos ~106-115)
    - apps/web/app/my-nights/NightCardActions.tsx (vaul confirm sheet ~166-195)
    - apps/web/app/dates/[slug]/interested/page.tsx (loader)
    - 03-PATTERNS.md §InterestedList + §"vaul confirm sheet" + §"Optimistic mutate + rollback + toast"; 03-UI-SPEC §E12 (copy, tokens, framing)
  </read_first>
  <action>
    In `InterestedList.tsx`: add a quiet decline control on each new-interest row — a small lucide `X`/`UserMinus` icon button (`h-11 w-11 rounded-full text-shell-ink/40`, accent on focus, `aria-label`). Tapping opens a vaul confirm sheet (copy the NightCardActions confirm structure): title `pass on {name}?`, body `they drop off your list. they won't be told — no awkwardness.`, confirm `pass`, cancel `keep them`. On confirm: optimistic-mutate-with-rollback — `const prev = rows; setRows(optimistic-set-to-passed_by_host); try { await rejectCandidate(instanceId, c.candidate_id); } catch { setRows(prev); toast.error("couldn't pass on them. try again?"); }`. Then filter `passed_by_host` OUT of both section memos. Success toast `passed. off your list.`. NOTHING candidate-facing about rejection anywhere (D-04).

    Add a withdraw control under the `offer out` badge on the frozen `offer_active` rank-1 row — a text button (`font-body text-sm lowercase text-shell-ink/55`) opening a vaul confirm: title `pull this offer back?`, body `they lose the offer. you can send a new one.`, confirm `pull it`, cancel `leave it` → calls the existing `withdraw(instance)` wrapper. Success toast `offer pulled.`.

    In `page.tsx`: load whatever offer-outcome status the rows need (the `status` union already carries the outcome values) so the list can render outcome pills in Task 2. Semantic tokens only; ≥44px tap targets.
  </action>
  <verify>
    <automated>grep -q "rejectCandidate" apps/web/app/dates/[slug]/interested/InterestedList.tsx && grep -q "withdraw" apps/web/app/dates/[slug]/interested/InterestedList.tsx && pnpm -w typecheck</automated>
    <human-check>forced-local Playwright 420px: decline + withdraw confirm sheets render per 03-UI-SPEC; silent decline copy; no candidate-facing rejection text</human-check>
  </verify>
  <done>Decline (silent, optimistic, filtered out) + withdraw confirm sheets wired; copy matches UI-SPEC; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Outcome pills + RTL/axe test</name>
  <files>apps/web/app/dates/[slug]/interested/InterestedList.tsx, apps/web/app/dates/[slug]/interested/__tests__/InterestedList.test.tsx</files>
  <behavior>
    - offer_active → "offer out" pink pill; locked → "accepted" pill; offer_passed → "they passed" neutral pill; offer_expired → "expired" neutral pill.
    - passed_by_host rows do not appear in either section.
    - The extended list has no a11y violations.
  </behavior>
  <read_first>
    - apps/web/app/dates/[slug]/interested/InterestedList.tsx (status union ~28, existing "offer out" badge ~188-191, section memos ~106-115)
    - 03-UI-SPEC §E12 (pill labels + tokens); 03-PATTERNS.md §InterestedList (outcome pills)
  </read_first>
  <action>
    Render a small lowercase outcome pill per row off the existing `status` union: `offer_active` → existing `offer out` pink pill (keep); `locked`/accepted → `accepted` pill (soft success ground — `bg-shell-pink` with `shell.accent` text + check icon; do NOT introduce an un-tokenized hex); `offer_passed` → `they passed` neutral pill (`bg-shell-ink/5 text-shell-ink/55`); `offer_expired` → `expired` neutral pill. No harsh language. Keep `passed_by_host` filtered out of both sections.

    Add/extend `apps/web/app/dates/[slug]/interested/__tests__/InterestedList.test.tsx` (RTL + jest-axe): assert (a) decline confirm → rejectCandidate called and row removed (mock the wrapper); (b) a `passed_by_host` row never renders; (c) each outcome status renders its correct pill label; (d) withdraw confirm → withdraw called; (e) no a11y violations.
  </action>
  <verify>
    <automated>pnpm --filter web test -- InterestedList && pnpm -w typecheck</automated>
    <human-check>forced-local Playwright 420px: outcome pills render with correct lowercase labels + tokens per 03-UI-SPEC 6-pillar bars</human-check>
  </verify>
  <done>Outcome pills render correctly; passed_by_host filtered out; RTL+axe green; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host action → rejectCandidate / withdraw | Host triages candidates; server-side auth enforced in the RPC (03-02) |
| decline → candidate | Must remain silent (no notification) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-20 | Information Disclosure | rejection leaked to candidate via UI copy | mitigate | No candidate-facing rejection copy anywhere; decline is host-side only; server reject is silent (03-02); RTL asserts removal not notification (Task 2) |
| T-03-21 | Tampering | host rejects another host's candidate | mitigate | Authorization enforced server-side in reject_candidate (42501, 03-02); UI only calls the wrapper |
| T-03-22 | Tampering | withdrawing/declining the wrong row (optimistic drift) | mitigate | Optimistic-mutate-with-rollback restores on failure; toast surfaces the error (Task 1) |
| T-03-SC | Tampering | package installs | mitigate | Zero new packages (RESEARCH audit N/A) |
</threat_model>

<verification>
- `pnpm --filter web test -- InterestedList` + `pnpm -w typecheck` green.
- grep confirms rejectCandidate + withdraw wired in InterestedList.
- RTL asserts silent removal (no notification path) + correct outcome pills + passed_by_host filtered out.
- Forced-local visual-verify against 03-UI-SPEC 6-pillar bars.
</verification>

<success_criteria>
- Silent decline + withdraw + outcome pills live on the interested list.
- passed_by_host filtered from both sections.
- Optimistic + rollback + toast on every action; a11y clean.
</success_criteria>

<output>
Create `.planning/phases/03-marketplace-completeness-p1/03-07-SUMMARY.md` when done.
</output>
