---
phase: 02-loop-closure-host-controls-p0
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - supabase/migrations/20260604121000_e5_loop_completion.sql
  - supabase/tests/e5_loop_completion.sql
  - apps/web/app/api/cron/close-loop/route.ts
  - apps/web/app/api/cron/close-loop/route.test.ts
  - apps/web/vercel.json
autonomous: true
requirements: [REQ-E5]
must_haves:
  truths:
    - "A past-dated active lock transitions to completed (and its date_instance to completed)"
    - "A past-dated unmatched seeking night is swept to 'expired'"
    - "Either party of a lock can flag no_show, reaching the previously-unreachable enum value"
    - "A Vercel cron route invokes the sweep on a schedule, authed by CRON_SECRET"
  artifacts:
    - path: supabase/migrations/20260604121000_e5_loop_completion.sql
      provides: "sweep_loop_terminus() service-role RPC + flag_no_show() membership-auth RPC"
      contains: "sweep_loop_terminus"
    - path: apps/web/app/api/cron/close-loop/route.ts
      provides: "Cron route that invokes sweep_loop_terminus via admin client"
      contains: "sweep_loop_terminus"
    - path: supabase/tests/e5_loop_completion.sql
      provides: "psql assertions for completion + expired sweep + no_show"
  key_links:
    - from: "apps/web/app/api/cron/close-loop/route.ts"
      to: "sweep_loop_terminus"
      via: "admin.rpc('sweep_loop_terminus')"
      pattern: "sweep_loop_terminus"
    - from: "apps/web/vercel.json"
      to: "/api/cron/close-loop"
      via: "crons schedule entry"
      pattern: "close-loop"
---

<objective>
Build the E5 loop terminus (D-01/D-02/D-03): a service-role `sweep_loop_terminus()` RPC that (a) flips past-dated active locks to `completed` and their `date_instances` to `completed`, and (b) sweeps past-dated unmatched `seeking` nights to `expired`; plus a `flag_no_show()` DEFINER RPC that EITHER lock member can call to set the previously-unreachable `locks.status='no_show'`. Schedule the sweep via a new `/api/cron/close-loop` Vercel cron route.

E5 only PRODUCES the terminal states + no-show signal (D-03) — reliability aggregation is E17/Phase 6. Keep the `completed`/`no_show`/rating-window data shape clean for E17 to consume.

This plan is sequenced AFTER E9 cleanup (02-01) per the audit dependency — no new cron job is scheduled until the queue is safe. It depends on 02-02 for the `expired` enum value.

Purpose: The loop must always terminate (never live forever as `active`/`seeking`) and capture no-shows.
Output: One migration (2 RPCs), one psql test, one cron route (+test), a vercel.json cron entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loop-closure-host-controls-p0/02-CONTEXT.md
@.planning/phases/02-loop-closure-host-controls-p0/02-RESEARCH.md
@.planning/phases/02-loop-closure-host-controls-p0/02-PATTERNS.md

<read_first>
- supabase/migrations/20260527127200_p5_job_rpcs_backfill.sql — `close_rating_window` (PATTERNS cites lines 79-100): the service-role-only, idempotent, STALE-TOLERANT sweep exemplar. Copy its `revoke all ... from public, anon, authenticated` grant and its never-raise-on-missing posture (a raise re-poisons the loop).
- supabase/migrations/20260527126300_p5_make_offer.sql — DEFINER skeleton for `flag_no_show` (auth re-check, advisory lock, idempotency).
- supabase/migrations/20260525120700_p0_locks.sql — `lock_status` enum already has `completed` + `no_show`; `locks` has `creator_id`, `matched_user_id` for the membership predicate.
- supabase/migrations/20260525120300_p0_date_instances.sql — GENERATED `time_range`; `date_match_status` (now includes `expired` after 02-02). NOTE Pitfall 1: `date_match_status` has NO `no_show` — no_show is LOCK-level only; date_instances terminal state is `completed`.
- supabase/migrations/20260527126400_p5_accept_lock.sql — grace convention `upper(time_range) + interval '2 hours'` for the rating window; align E5's grace anchor so completion + rating-window close are coherent.
- supabase/tests/b_job_rpcs.sql + supabase/tests/_fixtures.sql — psql-assertion convention; `mk_user`/`mk_itinerary`/`mk_instance(itin, creator, starts_at)`. For PAST-dated data insert via the fixture (sets status='seeking', duration_min=150), NOT via post_night (its starts_at>now() guard rejects past data).
- apps/web/app/api/cron/offer-expiring/route.ts — auth block (Bearer CRON_SECRET or ?secret=), `createAdminClient()`, `force-dynamic`, `maxDuration`. Copy this shape for close-loop.
- apps/web/app/api/cron/process-jobs/route.test.ts — mirror for the close-loop route test.
- apps/web/vercel.json — the `offer-expiring` crons entry to mirror.
</read_first>

<interfaces>
From close_rating_window pattern (service-role sweep, never raises):
  create or replace function sweep_loop_terminus() returns int language plpgsql security definer set search_path=public
  -- revoke all on function sweep_loop_terminus() from public, anon, authenticated;

flag_no_show membership-auth predicate (DIFFERENT from creator-only E6/E7 — Pitfall 5):
  select creator_id, matched_user_id into cre, matched from locks where id=p_lock for update;
  if auth.uid() not in (cre, matched) then raise exception 'not_member' using errcode='42501'; end if;

dispatch_notification(p_user uuid, p_type notification_type, p_payload jsonb) — revoked from public; only perform inside DEFINER.
admin.rpc signature: const admin = createAdminClient(); await admin.rpc('sweep_loop_terminus');
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write E5 migration — sweep_loop_terminus + flag_no_show RPCs + psql test</name>
  <files>supabase/migrations/20260604121000_e5_loop_completion.sql, supabase/tests/e5_loop_completion.sql</files>
  <behavior>
    - A past-dated active lock + matched date_instance → after sweep, lock.status='completed' AND date_instances.status='completed'.
    - A past-dated seeking date_instance with no lock → after sweep, date_instances.status='expired' (NOT 'completed').
    - sweep is idempotent: running twice produces no error and no further state change.
    - flag_no_show by the matched_user (a member) → lock.status='no_show'; date_instances.status remains 'completed' (never set to no_show — would throw enum error).
    - flag_no_show by a non-member → raises (errcode 42501).
    - A future-dated active lock is NOT swept.
  </behavior>
  <action>
Write `sweep_loop_terminus()` as a service-role-only DEFINER RPC mirroring `close_rating_window`: idempotent, stale-tolerant, NEVER raises (a raise re-poisons the loop). Completion path: `update locks set status='completed' where status='active' and upper((select time_range from date_instances d where d.id=locks.date_instance_id)) + interval '3 hours' < now()` (grace anchor = upper(time_range)+grace per accept_lock; 3h is Claude's-discretion default per D-01 — note it in a comment), then set the corresponding `date_instances.status='completed'` where currently 'matched'. Expiry path: `update date_instances set status='expired' where status='seeking' and lower(time_range) + interval '3 hours' < now()` (D-02/D-10 — 'expired', NOT 'completed'). Return rows affected. `revoke all on function sweep_loop_terminus() from public, anon, authenticated;`.

Write `flag_no_show(p_actor uuid, p_lock uuid, p_idem_key uuid)` as a DEFINER RPC using the make_offer skeleton BUT with a MEMBERSHIP auth predicate (Pitfall 5 / D-01): re-check `p_actor = auth.uid()`, load the lock FOR UPDATE, then `if auth.uid() not in (creator_id, matched_user_id) then raise 'not_member' errcode 42501`. Set `locks.status='no_show'` ONLY — never touch `date_instances.status` to no_show (Pitfall 1: `date_match_status` has no `no_show`; it throws). Use idempotency helpers (`match_idem_lookup`/`match_idem_store`) and `pg_advisory_xact_lock(match_instance_lock_key(...))`. `revoke from public, anon; grant execute to authenticated` (auth enforced inside). No fenced code in the RPC body beyond what the analogs provide.

Write `supabase/tests/e5_loop_completion.sql` per the psql-assertion convention (`\i _fixtures.sql`, DO blocks, RAISE on failed assert) covering every behavior above. Insert past-dated instances via the fixture (not post_night).
  </action>
  <verify>
    <automated>psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -v ON_ERROR_STOP=1 -f supabase/tests/e5_loop_completion.sql</automated>
  </verify>
  <done>Migration defines both RPCs with correct grants; the psql test asserts completion, expired-sweep, idempotency, no_show-by-member, and reject-non-member, and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add the close-loop cron route + test + vercel.json entry</name>
  <files>apps/web/app/api/cron/close-loop/route.ts, apps/web/app/api/cron/close-loop/route.test.ts, apps/web/vercel.json</files>
  <action>
Create `/api/cron/close-loop/route.ts` copying the `offer-expiring/route.ts` auth block verbatim (Bearer CRON_SECRET or `?secret=`; 500 if CRON_SECRET unset; 401 if mismatch), then `createAdminClient()` and `await admin.rpc('sweep_loop_terminus')`. Export `dynamic = 'force-dynamic'` and a `maxDuration`. Support `?dry_run=true` to skip the rpc. Reuse existing secrets — NO new env vars. Add the cron entry to `apps/web/vercel.json` mirroring the `offer-expiring` block: `{ "path": "/api/cron/close-loop", "schedule": "*/15 * * * *" }` (15-min cadence is Claude's-discretion per D-01). Write `route.test.ts` mirroring `process-jobs/route.test.ts`: assert 401 on missing/wrong secret, 500 on unset CRON_SECRET, and that a valid call invokes the sweep rpc.
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/app/api/cron/close-loop/route.test.ts</automated>
  </verify>
  <done>Cron route authed by CRON_SECRET invokes `sweep_loop_terminus`; vercel.json has the close-loop entry; route test green.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply locally, regen types, typecheck, security advisor</name>
  <files>packages/types/src/database.ts</files>
  <action>
[BLOCKING] Apply the full migration chain to LOCAL only (prod apply OUT OF SCOPE/gated): `pnpm db:reset` → `pnpm db:types` → `pnpm -w typecheck`. Then run `mcp__supabase__get_advisors type=security` against local and assert no new high/critical findings (security_enforcement: block on high) — pay attention to the two new DEFINER functions' grants (`sweep_loop_terminus` service-role-only; `flag_no_show` authenticated). Record advisor result in the SUMMARY.
  </action>
  <verify>
    <automated>pnpm db:reset && pnpm db:types && pnpm -w typecheck && echo "THEN: mcp__supabase__get_advisors type=security — assert no new high/critical"</automated>
    <human-check>Security advisor shows no new high/critical after the E5 DEFINER RPCs.</human-check>
  </verify>
  <done>Migrations apply from scratch; types regen; typecheck green; advisor clean; recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Cron → /api/cron/close-loop | Untrusted internet can hit the cron URL; must be secret-gated |
| authenticated user → flag_no_show | Either lock member may flag, but no outsider |
| service role → sweep_loop_terminus | Batch mutation runs as service role only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Spoofing/DoS | /api/cron/close-loop | mitigate | `Bearer CRON_SECRET` or `?secret=` gate copied verbatim from offer-expiring; 401 on mismatch, 500 if unset |
| T-02-06 | Elevation of Privilege | flag_no_show DEFINER | mitigate | `p_actor = auth.uid()` re-check + membership predicate `auth.uid() in (creator_id, matched_user_id)`; raises 42501 for non-members; `set search_path=public` |
| T-02-07 | Elevation of Privilege | sweep_loop_terminus DEFINER | mitigate | `revoke all from public, anon, authenticated` — service-role/cron only; no caller-supplied input |
| T-02-08 | DoS | sweep RPC | mitigate | Idempotent + stale-tolerant + never-raise (mirrors close_rating_window) so a partial sweep cannot poison-loop |
| T-02-SC | Tampering | npm/deno installs | accept | No new packages installed; legitimacy gate N/A |
</threat_model>

<verification>
- `psql ... -f supabase/tests/e5_loop_completion.sql` green (completion, expired sweep, idempotency, no_show-by-member, reject-non-member).
- `pnpm vitest run apps/web/app/api/cron/close-loop/route.test.ts` green.
- `pnpm db:reset && pnpm db:types && pnpm -w typecheck` green.
- `mcp__supabase__get_advisors type=security` no new high/critical.
- date_instances is NEVER set to no_show (Pitfall 1 respected).
- PROD APPLY OUT OF SCOPE — local-green only; cron deploys with the app on a separate gated step.
</verification>

<success_criteria>
A past-dated lock reaches `completed` and a past-dated seeking night reaches `expired`; either lock member can reach `no_show`; the cron route invokes the sweep authed by CRON_SECRET; advisor clean. The loop terminates and never traps the user.
</success_criteria>

<output>
Create `.planning/phases/02-loop-closure-host-controls-p0/02-03-SUMMARY.md` when done.
</output>
