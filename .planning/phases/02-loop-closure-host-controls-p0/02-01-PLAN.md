---
phase: 02-loop-closure-host-controls-p0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/process-jobs/handlers.ts
  - supabase/functions/process-jobs/handlers_test.ts
  - supabase/functions/process-jobs/handlers_rpc_fail_closed_test.ts
autonomous: true
requirements: [REQ-E9]
must_haves:
  truths:
    - "No enqueueable job handler references a missing RPC"
    - "The process-jobs runner still fails closed on an unknown job type"
    - "The Deno handler test suite is green after the deletions"
  artifacts:
    - path: supabase/functions/process-jobs/handlers.ts
      provides: "HANDLERS dispatch table with the 6 dead handlers removed"
      contains: "HANDLERS"
    - path: supabase/functions/process-jobs/handlers_test.ts
      provides: "ALL_TYPES pruned to drop the 6 removed types"
      contains: "ALL_TYPES"
    - path: supabase/functions/process-jobs/handlers_rpc_fail_closed_test.ts
      provides: "4 dead-handler reject cases removed; chat_purge/analytics_relay kept"
  key_links:
    - from: "supabase/functions/process-jobs/handlers.ts"
      to: "supabase/functions/process-jobs/index.ts"
      via: "HANDLERS[job.type] lookup + if(!handler) throw fail-closed net"
      pattern: "HANDLERS\\["
---

<objective>
Remove the 6 dead job handlers (`reconfirm_timeout`, `stale_date_close`, `pending_expiry`, `process_deletion`/`deletion_process`, `day_of_reconfirm`, `safety_checkin`) from the `process-jobs` dispatch table and prune the two coupled Deno test files in lockstep, so the job queue cannot poison-loop on a handler that references a missing RPC.

This is the FIRST plan in Phase 2 (per the audit dependency note D-08/D-11): E9 cleanup MUST land before E5 schedules any new cron job. It is almost entirely deletion — no migration, no new RPC.

Purpose: A handler that reads as real but calls a missing RPC is a latent poison-loop. Removing the dead branches leaves only the runner's `if(!handler) throw` + `raise_admin_alert('job_missing_rpc')` fail-closed net, which already protects orphaned enqueues.
Output: Trimmed `handlers.ts` + two synchronized test files, Deno suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loop-closure-host-controls-p0/02-CONTEXT.md
@.planning/phases/02-loop-closure-host-controls-p0/02-RESEARCH.md
@.planning/phases/02-loop-closure-host-controls-p0/02-PATTERNS.md

<read_first>
- supabase/functions/process-jobs/handlers.ts — the HANDLERS dispatch table. Delete keys `stale_date_close`, `pending_expiry`, `day_of_reconfirm`, `safety_checkin`, `reconfirm_timeout`, `deletion_process` (research PATTERNS §E9 cites lines 73,75,76,77,78,83). Also delete the now-orphan `notifyLockParties` helper (only `day_of_reconfirm`/`safety_checkin` used it — PATTERNS cites lines 50-58).
- supabase/functions/process-jobs/handlers_test.ts — `ALL_TYPES` (lines 5-9) asserts every type has a handler. Prune the 6 removed types. KEEP `chat_purge`, `analytics_relay`, `notify`, and the working handlers (`offer_expiry`, `standby_roll`, `bulk_withdraw`, `rating_window`).
- supabase/functions/process-jobs/handlers_rpc_fail_closed_test.ts — delete the `assertRejects` cases for `deletion_process`, `stale_date_close`, `pending_expiry`, `reconfirm_timeout` (PATTERNS cites lines 87-99, 101-111, 113-123, 125-135). KEEP the `chat_purge` (137-147) and `analytics_relay` (149-159) cases.
- supabase/functions/process-jobs/index.ts — DO NOT edit. Confirm the `if(!handler) throw` + `raise_admin_alert('job_missing_rpc')` fail-closed net is intact after the deletion.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the 6 dead handlers + orphan helper from handlers.ts</name>
  <files>supabase/functions/process-jobs/handlers.ts</files>
  <action>
Per D-08/D-11: delete EXACTLY these 6 keys from the `HANDLERS` object — `stale_date_close`, `pending_expiry`, `day_of_reconfirm`, `safety_checkin`, `reconfirm_timeout`, `deletion_process`. Delete the now-orphan `notifyLockParties` helper (only the two removed safety handlers referenced it; grep to confirm zero remaining references before deleting). Do NOT touch `chat_purge` or `analytics_relay` (dead but owned by P6/P11 — D-11). Do NOT drop any `job_type` Postgres enum value (destructive in Postgres; orphan values are harmless — nothing enqueues them and the runner fails closed). Do NOT edit `index.ts` — the `if(!handler) throw` + `raise_admin_alert('job_missing_rpc')` fail-closed net stays the safety floor.
  </action>
  <verify>
    <automated>grep -n -v '^[[:space:]]*//' supabase/functions/process-jobs/handlers.ts | grep -E "stale_date_close|pending_expiry|day_of_reconfirm|safety_checkin|reconfirm_timeout|deletion_process|notifyLockParties" ; test $? -ne 0 && echo "PASS: dead handlers + orphan helper gone"</automated>
  </verify>
  <done>The 6 dead handler keys and the `notifyLockParties` helper are absent from `handlers.ts`; `chat_purge`/`analytics_relay` and all working handlers remain.</done>
</task>

<task type="auto">
  <name>Task 2: Prune the two coupled Deno test files in lockstep</name>
  <files>supabase/functions/process-jobs/handlers_test.ts, supabase/functions/process-jobs/handlers_rpc_fail_closed_test.ts</files>
  <action>
In `handlers_test.ts`: prune `ALL_TYPES` to drop the 6 removed types (`pending_expiry`, `stale_date_close`, `day_of_reconfirm`, `safety_checkin`, `reconfirm_timeout`, `deletion_process`). KEEP `chat_purge`, `analytics_relay`, `notify`, and the working types so the "every type has a handler" assertion still passes for the remaining set. In `handlers_rpc_fail_closed_test.ts`: delete the `assertRejects` cases for `deletion_process`, `stale_date_close`, `pending_expiry`, `reconfirm_timeout`. KEEP the `chat_purge` and `analytics_relay` reject cases (those handlers stay, still call missing RPCs, and the fail-closed test still asserts they reject). Run the Deno suite to confirm green.
  </action>
  <verify>
    <automated>deno test --allow-env supabase/functions/process-jobs/handlers_test.ts supabase/functions/process-jobs/handlers_rpc_fail_closed_test.ts</automated>
  </verify>
  <done>Both Deno test files updated; `deno test` is green; `ALL_TYPES` no longer lists the 6 removed types; chat_purge/analytics_relay reject cases retained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job producer → process-jobs runner | An enqueued `job_type` whose handler was removed must not silently no-op; it must fail closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | process-jobs dispatch table | mitigate | Remove dead branches that read as real; rely on runner's `if(!handler) throw` + `raise_admin_alert('job_missing_rpc')` fail-closed net (index.ts, untouched) so any orphan enqueue is alerted, not silently looped |
| T-02-02 | Tampering | handlers_test.ts / fail_closed_test.ts | mitigate | Lockstep test edits keep the suite asserting the fail-closed net for remaining missing-RPC handlers (chat_purge/analytics_relay) |
| T-02-SC | Tampering | npm/deno installs | accept | No new packages installed in this plan (deletion-only); legitimacy gate N/A |
</threat_model>

<verification>
- `deno test --allow-env supabase/functions/process-jobs/handlers_test.ts supabase/functions/process-jobs/handlers_rpc_fail_closed_test.ts` green.
- `grep` confirms the 6 dead handlers + `notifyLockParties` are gone from `handlers.ts`.
- `git diff supabase/functions/process-jobs/index.ts` is empty (runner untouched).
- No `job_type` enum value dropped (no migration in this plan).
</verification>

<success_criteria>
No enqueueable handler references a missing RPC; the queue cannot poison-loop; the fail-closed net is intact; `chat_purge`/`analytics_relay` untouched; Deno suite green. This plan is sequenced BEFORE any E5 cron scheduling (Plan 02-03).
</success_criteria>

<output>
Create `.planning/phases/02-loop-closure-host-controls-p0/02-01-SUMMARY.md` when done.
</output>
