---
phase: 02-loop-closure-host-controls-p0
plan: 06
type: execute
wave: 3
depends_on: ["02-04", "02-05"]
files_modified:
  - packages/api-client/src/feed.ts
  - apps/web/app/my-nights/page.tsx
  - apps/web/app/my-nights/NightCardActions.tsx
  - apps/web/app/my-nights/__tests__/NightCardActions.test.tsx
autonomous: false
requirements: [REQ-E6, REQ-E7]
must_haves:
  truths:
    - "A host sees a cancel affordance on a seeking night on /my-nights and can confirm a soft-cancel"
    - "A host sees an edit affordance to change time/venue/duration/ambient and can submit it"
    - "Success shows a toast; the night's state reflects the change"
  artifacts:
    - path: packages/api-client/src/feed.ts
      provides: "cancelNight + updateNight typed RPC wrappers (generate p_idem_key client-side)"
      contains: "cancelNight"
    - path: apps/web/app/my-nights/NightCardActions.tsx
      provides: "Cancel/edit affordances on the host's own seeking nights"
  key_links:
    - from: "apps/web/app/my-nights/NightCardActions.tsx"
      to: "cancelNight / updateNight"
      via: "api-client wrapper → rpc"
      pattern: "cancelNight|updateNight"
    - from: "packages/api-client/src/feed.ts"
      to: "cancel_night / update_night RPC"
      via: "client.rpc('cancel_night'|'update_night', { p_... })"
      pattern: "cancel_night|update_night"
---

<objective>
Add the minimal host UI (D-04/D-05 affordances) on `/my-nights` that calls the E6/E7 RPCs: a cancel action (soft-unpublish, with a confirm) and an edit action (time/venue/duration/ambient) on a host's own `seeking` nights, plus the typed `cancelNight`/`updateNight` api-client wrappers. Small surfaces following DESIGN-SYSTEM.md.

Depends on the Plan 02-04 RPCs (`cancel_night`/`update_night`) and the Plan 02-05 notif-map render meta. This is the only non-autonomous plan in the phase — it ends with a forced-local Playwright visual-verify checkpoint (agents are blind; compile-clean ≠ good UX, per the project's visual-verify rule).

Purpose: A host can correct or take down a night from the surface where they manage their nights.
Output: api-client wrappers, a NightCard actions component (+ unit test), wired onto /my-nights, visual-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loop-closure-host-controls-p0/02-CONTEXT.md
@.planning/phases/02-loop-closure-host-controls-p0/02-PATTERNS.md
@docs/superpowers/DESIGN-SYSTEM.md

<read_first>
- packages/api-client/src/feed.ts — `postNight` (PATTERNS cites lines 19-30): the exact wrapper shape `client.rpc(fn, { p_... }); if (error) throw error`. Copy it for `cancelNight`/`updateNight`. Generate `p_idem_key` (UUID) client-side per the idempotency-ledger contract.
- apps/web/app/my-nights/page.tsx — `NightCard` (PATTERNS cites lines 53-123): the host surface + its tokens (`cn()`, `shell.*`, `font-heading`/`font-body`, `rounded-3xl`, `shadow-fun`, ≥44px tap targets, `focus-visible:ring-shell-accent/40`, `motion-reduce:*`). Add the actions only on the host's OWN `seeking` nights.
- apps/web/app/dates/[slug]/interested/CancelWithReasonPicker.tsx — an existing cancel-confirm pattern to reuse for the cancel affordance.
- docs/superpowers/DESIGN-SYSTEM.md — use `vaul` (bottom-sheet confirm/edit), `sonner` (success toast), `framer-motion`; no AI-slop; stop-slop copy.
</read_first>

<interfaces>
cancelNight(client, { instance_id, idem_key }): client.rpc('cancel_night', { p_actor: uid, p_instance: instance_id, p_idem_key: idem_key })
updateNight(client, { instance_id, starts_at?, duration_min?, venue?, ambient_sound_id?, idem_key }): client.rpc('update_night', { p_actor: uid, p_instance, p_starts_at, p_duration_min, p_venue, p_ambient_sound_id, p_idem_key })
RPC error codes to surface (map via existing MatchError/messageForCode if present): 42501 not_creator, P0001 not_cancellable/invalid venue/ambient, P0002 no_instance.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cancelNight + updateNight api-client wrappers</name>
  <files>packages/api-client/src/feed.ts</files>
  <behavior>
    - cancelNight calls rpc('cancel_night', {p_actor, p_instance, p_idem_key}); throws on error.
    - updateNight calls rpc('update_night', {...}) passing only the provided fields (null/undefined = leave unchanged); throws on error.
    - Each generates a UUID p_idem_key when not supplied.
  </behavior>
  <action>
Add `cancelNight` and `updateNight` exported wrappers to `feed.ts` copying the `postNight` shape: `client.rpc(<fn>, { p_... }); if (error) throw error; return data`. Generate `p_idem_key` client-side (UUID — use the same generator postNight/match wrappers use) per the idempotency-ledger contract. `updateNight` maps optional args to `p_starts_at`/`p_duration_min`/`p_venue`/`p_ambient_sound_id`, passing null for omitted fields (RPC treats null as "leave unchanged"). Use the generated `Database` types from `@after5/types` for the rpc names/args. Add a focused unit test if the package has a test setup, else assert via typecheck.
  </action>
  <verify>
    <automated>pnpm -w typecheck && grep -Eq "cancel_night|update_night" packages/api-client/src/feed.ts</automated>
  </verify>
  <done>Both wrappers exist, typed against the regenerated Database types, generate p_idem_key, throw on error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NightCardActions component + wire onto /my-nights</name>
  <files>apps/web/app/my-nights/NightCardActions.tsx, apps/web/app/my-nights/__tests__/NightCardActions.test.tsx, apps/web/app/my-nights/page.tsx</files>
  <behavior>
    - On a host's own seeking night, a cancel button and an edit button render.
    - On a non-seeking (matched/completed/expired/cancelled) night, the cancel/edit affordances do NOT render.
    - Clicking cancel opens a confirm (vaul); confirming calls cancelNight and shows a success toast (sonner).
    - Clicking edit opens an edit sheet for starts_at/duration/venue/ambient; submitting calls updateNight and shows a toast.
    - Error from the RPC surfaces a readable error toast (mapped from the error code).
  </behavior>
  <action>
Create `NightCardActions.tsx` (client leaf, `'use client'`) reusing the `CancelWithReasonPicker` pattern for the cancel confirm and a `vaul` bottom-sheet for the edit form. Render the cancel/edit affordances ONLY when the night is the host's own and `status === 'seeking'`. Wire `cancelNight`/`updateNight` from the api-client; on success show a `sonner` toast and refresh the card state (router.refresh() or optimistic update); on error map the RPC error code (42501/P0001/P0002) to readable copy (stop-slop — specific, no filler) and toast it. Use only DESIGN-SYSTEM.md tokens (`cn()`, `shell.*`, `font-heading`/`font-body`, `rounded-3xl`, `shadow-fun`, ≥44px tap targets, `focus-visible:ring-shell-accent/40`, `motion-reduce:*`); no hardcoded hex. Mount `<NightCardActions />` inside `NightCard` in `page.tsx`. Write `NightCardActions.test.tsx` (Testing Library + jest-axe): assert the affordances render only on seeking nights, the confirm flow calls cancelNight, and no a11y violations.
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/app/my-nights/__tests__/NightCardActions.test.tsx</automated>
  </verify>
  <done>Cancel/edit affordances render only on the host's seeking nights, call the RPCs, toast on success/error, pass a11y; wired into /my-nights; unit test green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Cancel + edit host affordances on /my-nights (seeking nights only), calling the E6/E7 RPCs with success/error toasts, styled per DESIGN-SYSTEM.md.</what-built>
  <how-to-verify>
1. Run the app locally against the local Supabase stack (`pnpm dev`, ensure `pnpm db:reset` has applied Phase 2 migrations).
2. Sign in as a host with at least one `seeking` night (use the local QA login recipe).
3. Navigate to `/my-nights`. Confirm a seeking night shows a cancel and an edit affordance; a matched/completed night does NOT.
4. Tap edit → change the time and venue → submit. Confirm a success toast and the card reflects the new time.
5. Tap cancel → confirm. Confirm a success toast and the night is unpublished (removed from feed eligibility, row kept).
6. EXECUTION-TIME forced-local Playwright visual-verify: render `/my-nights` at 375px, screenshot the seeking NightCard with actions, and critique against the DESIGN-SYSTEM rubric (tokens only, no AI-slop, ≥44px tap targets, motion-reduce respected, contrast WCAG AA). Attach the screenshot to the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/functional issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → cancel_night/update_night RPC | The UI may only invoke the host's own actions; the RPC re-checks auth.uid()=creator (defense in depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Elevation of Privilege | NightCardActions | mitigate | UI gates affordances to the host's own seeking nights; authority is enforced server-side by the DEFINER RPCs' creator re-check (the UI gate is convenience, not the security boundary) |
| T-02-17 | Tampering | edit form input | mitigate | Server-side validators (venue live+active, ambient active, duration CHECK) reject invalid input; client surfaces the error toast |
| T-02-SC | Tampering | npm installs | accept | No new packages installed (vaul/sonner/framer-motion already in package.json per CLAUDE.md); legitimacy gate N/A |
</threat_model>

<verification>
- `pnpm vitest run apps/web/app/my-nights/__tests__/NightCardActions.test.tsx` green (affordance gating + a11y).
- `pnpm -w typecheck` green (api-client wrappers typed against regenerated Database).
- Human visual-verify checkpoint approved (forced-local Playwright screenshot + DESIGN-SYSTEM critique).
- Affordances render only on the host's own seeking nights.
</verification>

<success_criteria>
A host can cancel and edit a seeking night from /my-nights via the E6/E7 RPCs, with success/error toasts, styled to DESIGN-SYSTEM.md, and the surface passes a forced-local visual-verify.
</success_criteria>

<output>
Create `.planning/phases/02-loop-closure-host-controls-p0/02-06-SUMMARY.md` when done. Attach the visual-verify screenshot.
</output>
