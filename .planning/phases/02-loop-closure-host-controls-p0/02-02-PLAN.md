---
phase: 02-loop-closure-host-controls-p0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260604120000_e2_loop_closure_enums.sql
autonomous: true
requirements: [REQ-E5, REQ-E6, REQ-E7]
must_haves:
  truths:
    - "date_match_status has an 'expired' value (for the past-dated seeking sweep)"
    - "notification_type has 'night_cancelled' and 'night_changed' values"
    - "All migrations apply cleanly from scratch via pnpm db:reset"
    - "Generated types regenerate and the workspace typechecks"
  artifacts:
    - path: supabase/migrations/20260604120000_e2_loop_closure_enums.sql
      provides: "Additive enum values: date_match_status 'expired', notification_type 'night_cancelled'/'night_changed'"
      contains: "add value if not exists"
  key_links:
    - from: "supabase/migrations/20260604120000_e2_loop_closure_enums.sql"
      to: "packages/types/src/database.ts"
      via: "pnpm db:types regen surfaces the new enum members"
      pattern: "night_cancelled|expired"
---

<objective>
Add the three additive enum values that the E5/E6/E7 RPCs in Wave 2 consume: `date_match_status 'expired'` (D-10, the terminus for the past-dated unmatched seeking sweep), and `notification_type 'night_cancelled'` + `'night_changed'` (D-09, the cancel/material-change notifications to interested candidates). This is a standalone enum-only migration, sequenced AHEAD of any RPC that uses the values (Postgres requires `ALTER TYPE ... ADD VALUE` to be committed before later use).

Includes the phase's [BLOCKING] local schema-apply gate and the post-DDL security advisor — the first DDL in the phase passes through here so Wave 2 builds on a regenerated type surface.

Purpose: Unblock E5's `expired` sweep terminus and E6/E7's candidate notifications without a destructive enum change.
Output: One additive enum migration; local stack re-applied from scratch; types regenerated; advisor clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loop-closure-host-controls-p0/02-CONTEXT.md
@.planning/phases/02-loop-closure-host-controls-p0/02-RESEARCH.md
@.planning/phases/02-loop-closure-host-controls-p0/02-PATTERNS.md

<read_first>
- supabase/migrations/20260603120000_gated_inbox_notification_types.sql — the EXACT additive-enum convention to copy: `alter type notification_type add value if not exists '<value>';` in a standalone migration. Copy this shape.
- supabase/migrations/20260525120300_p0_date_instances.sql — defines `date_match_status` enum (none/seeking/matched/completed/cancelled). The new `expired` value is added here additively.
- 02-PATTERNS.md §"Additive enum migration" — confirms bare enum-adds go in their own migration file ahead of the RPCs that use the value (Postgres ADD VALUE + same-tx use constraint).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the additive enum migration</name>
  <files>supabase/migrations/20260604120000_e2_loop_closure_enums.sql</files>
  <action>
Create the migration with exactly three additive, idempotent statements (per D-09/D-10, copying the `gated_inbox_notification_types.sql` convention): `alter type date_match_status add value if not exists 'expired';`, `alter type notification_type add value if not exists 'night_cancelled';`, `alter type notification_type add value if not exists 'night_changed';`. Add a header comment documenting WHY each value exists (D-10 seeking-sweep terminus distinct from `completed`; D-09 cancel + material-change candidate notifications) and that this is GATED local-only (prod apply is owner-approved, separate). Do NOT drop or rename any enum value. This file must sort BEFORE the E5/E6/E7 RPC migrations (timestamp `20260604120000` precedes the Wave 2 migration timestamps).
  </action>
  <verify>
    <automated>grep -c "add value if not exists" supabase/migrations/20260604120000_e2_loop_closure_enums.sql | grep -qx 3 && echo "PASS: 3 additive enum statements"</automated>
  </verify>
  <done>Migration exists with the three `add value if not exists` statements for `expired`, `night_cancelled`, `night_changed`; no destructive enum operation present.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply locally, regenerate types, typecheck</name>
  <files>packages/types/src/database.ts</files>
  <action>
[BLOCKING — must complete before Wave 2 plans run.] Apply ALL migrations from scratch against the LOCAL stack (127.0.0.1 only — prod apply is OUT OF SCOPE/gated): run `pnpm db:reset`, then `pnpm db:types` to regenerate `packages/types/src/database.ts`, then `pnpm -w typecheck`. Confirm the three new enum members appear in the regenerated types. This is the phase's first DDL apply; if `db:reset` fails, the enum migration is malformed — fix before proceeding.
  </action>
  <verify>
    <automated>pnpm db:reset && pnpm db:types && grep -Eq "night_cancelled|night_changed" packages/types/src/database.ts && grep -q "'expired'" packages/types/src/database.ts && pnpm -w typecheck</automated>
  </verify>
  <done>`pnpm db:reset` applies cleanly from scratch; `database.ts` contains the three new enum members; `pnpm -w typecheck` passes.</done>
</task>

<task type="auto">
  <name>Task 3: Run the Supabase security advisor (post-DDL gate)</name>
  <files></files>
  <action>
Per CLAUDE.md ("run the Supabase security advisor after every DDL") and D-06: run `mcp__supabase__get_advisors` with `type=security` against the LOCAL project. Assert NO new high/critical findings vs the pre-phase baseline. An additive enum adds no RLS surface, so this should be clean; record the result in the SUMMARY. (security_enforcement: block on high — do not proceed past a new high finding.)
  </action>
  <verify>
    <automated>echo "MANUAL — run mcp__supabase__get_advisors type=security; assert no new high/critical findings; record in SUMMARY"</automated>
    <human-check>Security advisor output shows no new high/critical findings after the enum DDL.</human-check>
  </verify>
  <done>Security advisor run against local; no new high/critical findings; result recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live schema | DDL alters shared enum types consumed by RLS-gated tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | enum migration | mitigate | Additive `add value if not exists` only; no drop/rename; idempotent and non-destructive (matches the proven gated-types convention) |
| T-02-04 | Information Disclosure | new enum surface | accept | Enum values add no RLS surface and expose no data; security advisor run post-DDL confirms no new finding |
| T-02-SC | Tampering | npm/deno installs | accept | No new packages installed (SQL-only); legitimacy gate N/A |
</threat_model>

<verification>
- `pnpm db:reset` applies the full migration chain (including this enum file) cleanly from scratch.
- `pnpm db:types` regenerates `database.ts` with `expired`/`night_cancelled`/`night_changed`.
- `pnpm -w typecheck` green.
- `mcp__supabase__get_advisors type=security` → no new high/critical.
- PROD APPLY IS OUT OF SCOPE — this plan ends local-green only.
</verification>

<success_criteria>
The three additive enum values exist locally, types regenerate, the workspace typechecks, and the security advisor is clean. Wave 2 RPC plans can now reference `expired`/`night_cancelled`/`night_changed`.
</success_criteria>

<output>
Create `.planning/phases/02-loop-closure-host-controls-p0/02-02-SUMMARY.md` when done.
</output>
