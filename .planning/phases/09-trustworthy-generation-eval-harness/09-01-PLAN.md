---
phase: 09-trustworthy-generation-eval-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/generate-plan/prompt.ts
  - supabase/functions/generate-plan/prompt.test.ts
  - supabase/functions/generate-plan/scoring.ts
  - supabase/functions/generate-plan/scoring.test.ts
  - supabase/functions/generate-plan/places-filter.ts
autonomous: true
requirements: [PLAN-01]
must_haves:
  truths:
    - "A user generates a coherent multi-stop date for their city in one tap, and the LLM emits schema-valid copy via a forced tool call — no fence-strip + JSON.parse (Area 1: tool-use migration)"
    - "Consecutive stops in a generated date are actually close together, gated by real haversine distance against a named max-hop constant, not the drive_cluster string (Area 1: haversine hop-gate)"
    - "A stop with unknown coords is EXCLUDED from a hop, never silently passed (DATA-03 fail-loud convention, mirrors withinRadius)"
    - "drive_cluster stays as a display label but no longer gates adjacency (Area 1: back-compat)"
  artifacts:
    - path: "supabase/functions/generate-plan/prompt.ts"
      provides: "tool-use forced copy pass replacing parseLLMResponse"
      contains: "tool_choice"
    - path: "supabase/functions/generate-plan/scoring.ts"
      provides: "withinHop haversine adjacency gate + MAX_HOP_KM constant"
      contains: "MAX_HOP_KM"
    - path: "supabase/functions/generate-plan/places-filter.ts"
      provides: "exported haversineKm reusable by the hop-gate"
      contains: "export function haversineKm"
  key_links:
    - from: "supabase/functions/generate-plan/scoring.ts"
      to: "supabase/functions/generate-plan/places-filter.ts"
      via: "import { haversineKm }"
      pattern: "haversineKm"
    - from: "supabase/functions/generate-plan/prompt.ts"
      to: "mergeWriting / patchEmptyStops"
      via: "tool_use.input.itineraries → same LLMItineraryWriting field names"
      pattern: "template_id|why_it_works|what_to_do"
---

<objective>
Harden the existing generate-plan engine for PLAN-01: migrate the Sonnet copy pass to Anthropic tool-use (forced `tool_choice`), and replace the `drive_cluster` string adjacency gate with a real haversine consecutive-hop gate. The deterministic place-picking invariant ("the LLM never picks places") is preserved — the LLM still only writes copy over frozen `place_id`s.

Purpose: One-tap any-city generation that produces structurally coherent dates (close-together stops, schema-valid copy) that generalize beyond curated Kelowna.
Output: tool-use copy pass, haversine hop-gate, exported `haversineKm`, unit tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-CONTEXT.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-RESEARCH.md

<interfaces>
<!-- Extracted from the codebase. Use these directly — no exploration needed. -->

Current copy-pass call site (supabase/functions/generate-plan/prompt.ts ~131):
```typescript
const response = await client.messages.create({
  model, max_tokens: 4096, temperature: 0.7,
  system: [{ type: 'text', text: buildSystemPrompt(input.city), cache_control: { type: 'ephemeral' } }],
  messages: [{ role: 'user', content: userMessage }],
});
const textBlock = response.content.find((b) => b.type === 'text');
// then: parseLLMResponse(textBlock.text)  ← fence-strip + JSON.parse, DELETE
```

The merge target type (KEEP field names identical in the tool schema):
`LLMItineraryWriting` = { template_id, title, hook, why_it_works, stops: [{ place_id, what_to_do }] }
KEEP: mergeWriting, patchEmptyStops, buildFallbackWhatToDo, the cache_control ephemeral system prompt, the empty-what_to_do fallback.

haversine (supabase/functions/generate-plan/places-filter.ts ~118) — currently MODULE-PRIVATE, must export:
```typescript
function haversineKm(lat1, lng1, lat2, lng2): number  // export this
export function withinRadius(lat, lng, centroidLat, centroidLng, maxKm): boolean  // already exported, mirror its null→false convention
```

The gate to replace (supabase/functions/generate-plan/scoring.ts:102-112, used at :141 as `score -= 5`):
```typescript
function clusterCompatible(picked: Place[], candidate: Place): boolean { /* drive_cluster string set membership */ }
```
`buildItineraryFromTemplate` (scoring.ts:242) is the pick loop; `Place` has `lat: number|null`, `lng: number|null`, `drive_cluster: string`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the copy pass to Anthropic tool-use</name>
  <files>supabase/functions/generate-plan/prompt.ts, supabase/functions/generate-plan/prompt.test.ts</files>
  <behavior>
    - A messages.create with tool_choice {type:'tool', name:'emit_itineraries'} returns a content block of type 'tool_use' whose input.itineraries merges cleanly via mergeWriting (field names identical to LLMItineraryWriting).
    - When no tool_use block is present, the deterministic fallback path (empty array → fallback titles) still runs — no throw.
    - patchEmptyStops still fills empty what_to_do (tool-use guarantees shape, not non-emptiness).
  </behavior>
  <action>Per Area 1 (CONTEXT D — tool-use migration): replace `parseLLMResponse` text-extraction in `callLLMWritingPass` with a forced tool call against the in-tree `@anthropic-ai/sdk@0.40.1` (edge imports `npm:@anthropic-ai/sdk@^0.40.0`). Add `tools: [{ name: 'emit_itineraries', input_schema: {...} }]` and `tool_choice: { type: 'tool', name: 'emit_itineraries' }`. The `input_schema` field names MUST be byte-identical to `LLMItineraryWriting` (template_id/title/hook/why_it_works/stops[].place_id/what_to_do) to keep `mergeWriting`/`patchEmptyStops` working (Pitfall 3). Extract via `response.content.find((b) => b.type === 'tool_use')`; cast `toolUse.input` to `{ itineraries: LLMItineraryWriting[] }`. DELETE `parseLLMResponse` and the fence-strip logic. KEEP `mergeWriting`, `patchEmptyStops`, `buildFallbackWhatToDo`, the ephemeral `cache_control` system prompt, and the empty-`what_to_do` fallback (defense-in-depth). Do NOT enforce copy length in the schema — length stays in eval gates (A4). Verify the `tool_use` block extraction shape against the installed SDK TS types before coding (A1).</action>
  <verify>
    <automated>cd supabase/functions/generate-plan && deno test prompt.test.ts --allow-env --allow-read</automated>
  </verify>
  <done>tool_choice forces emit_itineraries; a mocked tool_use response merges cleanly; parseLLMResponse is gone; fallback path intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace drive_cluster adjacency with a haversine hop-gate</name>
  <files>supabase/functions/generate-plan/places-filter.ts, supabase/functions/generate-plan/scoring.ts, supabase/functions/generate-plan/scoring.test.ts</files>
  <behavior>
    - withinHop(prev, cand) returns true when consecutive stops are ≤ MAX_HOP_KM apart.
    - Returns false when > MAX_HOP_KM apart.
    - Returns false (EXCLUDE) when prev or cand has null lat/lng (fail-loud, mirrors withinRadius:114).
    - Returns true when prev is undefined (first stop).
    - A generated plan whose far stop exceeds MAX_HOP_KM is post-validated and the far stop is repaired (swapped for the nearest in-slot candidate) rather than shipped incoherent.
  </behavior>
  <action>Per Area 1 (CONTEXT D — haversine hop-gate): export `haversineKm` from `places-filter.ts` (currently module-private at ~line 118). In `scoring.ts`, add a named tunable constant `MAX_HOP_KM = 2.0` (Claude's discretion on exact value + whether to weight by stop type — CONTEXT grants this) and a `withinHop(prev, cand, maxKm = MAX_HOP_KM)` predicate that imports `haversineKm`, returns `true` for undefined prev, returns `false` when either coord is null (DATA-03 fail-loud), else returns `haversineKm(...) <= maxKm`. Replace the `clusterCompatible` adjacency penalty at scoring.ts:141 with the hop-gate. Per Open Question 1, use **post-validate + repair** (GENERATION.md recommendation): keep the soft score signal in the pick loop to preserve plan availability on thin pools, then post-validate the assembled itinerary — if any consecutive hop exceeds MAX_HOP_KM, repair by swapping the far stop for the nearest in-slot candidate; only surface "no coherent plan" if repair is impossible. `drive_cluster` stays for display (`estimateDriveMin` may keep using it but should agree with the gate); do NOT drop the column. Remove `clusterCompatible` as an adjacency gate.</action>
  <verify>
    <automated>cd supabase/functions/generate-plan && deno test scoring.test.ts places-filter.test.ts --allow-env --allow-read</automated>
  </verify>
  <done>withinHop passes ≤2km, rejects >2km, excludes null coords, allows first stop; assembled plans post-validate + repair far hops; clusterCompatible no longer gates adjacency; haversineKm exported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edge fn → Anthropic | LLM copy pass; untrusted model output crosses back into persist |
| place corpus → itinerary | venues with possibly-null coords feed the proximity gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | LLM copy pass reorders/invents place_ids | mitigate | tool-use schema emits copy only over frozen place_ids; mergeWriting matches by template_id/place_id; LLM never picks places |
| T-09-02 | Tampering | null-coord venue passes proximity gate silently | mitigate | withinHop returns false on null coords (DATA-03 fail-loud), mirrors withinRadius |
| T-09-03 | Denial of Service | tool-use call hangs/fails | accept | existing deterministic fallback (fallback titles) keeps generation alive; rate-limit already in index.ts |
</threat_model>

<verification>
- `deno test` green for prompt, scoring, places-filter.
- `parseLLMResponse` removed; `tool_choice` present in prompt.ts.
- `haversineKm` exported; `withinHop` + `MAX_HOP_KM` present in scoring.ts.
- The eval harness (Plan 09-03) re-runs against the hardened pick logic at the phase gate.
</verification>

<success_criteria>
A user generates a coherent multi-stop date for their city in one tap, with stops that are actually close together (haversine hop-gate, not a string label), and schema-valid copy from a forced tool call.
</success_criteria>

<output>
Create `.planning/phases/09-trustworthy-generation-eval-harness/09-01-SUMMARY.md` when done.
</output>
