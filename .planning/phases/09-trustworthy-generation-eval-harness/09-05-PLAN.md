---
phase: 09-trustworthy-generation-eval-harness
plan: 05
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - supabase/functions/generate-plan/improve.ts
  - supabase/functions/generate-plan/improve.test.ts
  - supabase/functions/generate-plan/index.ts
  - apps/web/app/create/CreateFlow.tsx
  - apps/web/app/create/ImproveControls.tsx
  - apps/web/app/create/__tests__/ImproveControls.test.tsx
autonomous: true
requirements: [PLAN-02]
must_haves:
  truths:
    - "A user can swap a single stop — deterministic re-pick of just that slot holding the others fixed, plus a cheap Haiku rewrite of only that stop's copy (Area 2: single-stop swap)"
    - "A user can apply natural-language tweaks ('cheaper', 'more romantic', 'later') — Haiku parses free text into scoring knobs (budget cap / vibe weight / time shift) and the pipeline re-runs with them (Area 2: NL tweaks)"
    - "After any swap/tweak the itinerary is re-validated for proximity (the new hop-gate) + budget + hours; if the change breaks coherence it is surfaced, not silently shipped (Area 2: coherence-preserving)"
    - "All writes go through the existing update_itinerary_stops RPC — the single owner-checked write path (Area 2: persist)"
    - "The improve UI (per-stop tweak affordance + NL tweak input) lives in /create, Barbiecore, mobile-first, lowercase/dry copy (Area 4: improve-loop UI home)"
  artifacts:
    - path: "supabase/functions/generate-plan/improve.ts"
      provides: "single-slot re-pick + NL-knob parse + re-validate, behind index.ts dispatch"
      contains: "update_itinerary_stops"
    - path: "apps/web/app/create/ImproveControls.tsx"
      provides: "per-stop tweak affordance + NL tweak input in the create flow"
      contains: "tweak"
  key_links:
    - from: "supabase/functions/generate-plan/improve.ts"
      to: "buildItineraryFromTemplate (single-slot re-pick) + withinHop (re-validate)"
      via: "hold other place_ids in usedAcrossBatch; re-run hop/budget/hours"
      pattern: "buildItineraryFromTemplate|withinHop"
    - from: "supabase/functions/generate-plan/improve.ts"
      to: "update_itinerary_stops RPC"
      via: "the only write path"
      pattern: "update_itinerary_stops"
    - from: "apps/web/app/create/ImproveControls.tsx"
      to: "generate-plan improve dispatch"
      via: "browserAfter5Client edge call"
      pattern: "generate-plan|improve"
---

<objective>
PLAN-02: the customize/improve loop. Swap a single stop (deterministic re-pick of that slot + a cheap Haiku copy rewrite of just that stop) and apply natural-language tweaks (Haiku parses "cheaper"/"more romantic"/"later" into scoring knobs; the pipeline re-runs). After any change, re-validate proximity (the Plan 09-01 hop-gate) + budget + hours and surface incoherence rather than shipping it. The improve UI lands in `/create`.

Purpose: Fast, cheap, coherent customization — the LLM classifies the wish, code grants it; structure stays deterministic.
Output: improve.ts (re-pick + NL-knob parse + re-validate), an index.ts dispatch, ImproveControls UI, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-CONTEXT.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-RESEARCH.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-01-SUMMARY.md
@docs/superpowers/DESIGN-SYSTEM.md

<interfaces>
<!-- Extracted from the codebase. Reuse; do not invent new write paths. -->

Reusable re-pick machinery (supabase/functions/generate-plan/scoring.ts):
```typescript
export function buildItineraryFromTemplate(
  template, candidates, inputs, startTime?,
  usedAcrossBatch: Set<string> = new Set(),  // hold OTHER stops' place_ids here to exclude them for a single-slot re-pick
  opts: { skipHoursFilter?; taste? } = {},
): Itinerary | null
export function isOpenAt(p, slotStart): boolean
```
The new hop-gate from Plan 09-01: `withinHop(prev, cand, maxKm = MAX_HOP_KM)` + `MAX_HOP_KM` (scoring.ts) — use for re-validation after a swap/tweak.

Write boundary (m3 migration 20260602140000_m3_update_itinerary_stops): `update_itinerary_stops` RPC — re-derives total_cost_pp/total_duration_min, clamps ≤12 stops, validates each stop (place_name/start_time/non-negative cost+duration), checks ownership (user_id = auth.uid(), 42501 if not owner). DO NOT invent another write path.

Edge dispatch (supabase/functions/generate-plan/index.ts): `serve` handler, `InputSchema` (zod). Add an action/mode discriminator so the same function serves both generate and improve, OR a sibling route — Claude's discretion; keep zod validation + the existing rate-limit + auth extraction.

NL-knob mapping (Pattern 4, Claude's discretion on exact knobs):
- "cheaper" → budget_per_person ↓ (or greedy-swap most expensive stop)
- "later"/"evening" → time_of_day / start_at shift → re-run hours filter (effectiveStartAt derives from time_of_day)
- "more romantic"/"adventurous" → phrase → vibe[] tags + intent → re-score
The rule (GENERATION.md): the LLM classifies the wish via Haiku tool-use → {vibe,intent,budget_delta,time_shift}; code re-runs the pipeline with those knobs. NEVER let the LLM free-form rewrite structure.

UI home: apps/web/app/create/CreateFlow.tsx (the generate funnel reached from /create/generate). Models: claude-haiku-4-5 for the rewrite + the NL-knob parse.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Single-slot re-pick + NL-knob parse + re-validate (edge)</name>
  <files>supabase/functions/generate-plan/improve.ts, supabase/functions/generate-plan/improve.test.ts, supabase/functions/generate-plan/index.ts</files>
  <behavior>
    - Swapping stop i re-picks only slot i (other stops' place_ids held in usedAcrossBatch so they're excluded), holds the rest fixed, and the result differs from the original stop i.
    - A Haiku copy rewrite refreshes only stop i's what_to_do (and nudges why_it_works so it doesn't name a removed venue).
    - An NL tweak ("cheaper"/"more romantic"/"later") is parsed by Haiku tool-use into a knob delta {budget_delta, vibe, intent, time_shift}; the pipeline re-runs with those knobs.
    - After any swap/tweak: hop-gate (withinHop) + budget sum + hours are re-validated; a change that breaks coherence is SURFACED (returned as an error/warning), not persisted.
    - Persist goes through update_itinerary_stops; a non-owner is rejected (42501).
    - NL-tweak free text is length-capped before Haiku (prompt-injection mitigation); Haiku output is constrained to the knob schema and never executed as instructions.
  </behavior>
  <action>Per Area 2 (CONTEXT D): create `improve.ts`. (a) Single-slot swap: drive `buildItineraryFromTemplate` for slot i with the OTHER stops' place_ids in `usedAcrossBatch` to exclude them, re-validate the new stop against neighbors i-1/i+1 via `withinHop` (Plan 09-01), then one Haiku tool-use call rewrites the swapped stop's `what_to_do` + nudges `why_it_works` (Pitfall 4: stale prose must not name a removed venue). (b) NL tweak: one Haiku tool-use call parses length-capped free text → `{budget_delta, vibe, intent, time_shift}` knobs (Pattern 4 mapping; exact knobs Claude's discretion), then re-run the pipeline with those knobs. (c) After EITHER: re-run hop-gate + budget sum + hours; if the change breaks coherence, surface it ("this swap puts you 8km from the next stop") rather than persisting (CONTEXT requires this). Persist via `update_itinerary_stops` ONLY (re-derives totals + checks ownership — Anti-Pattern: no new write path). Wire an action/mode discriminator into the `index.ts` serve handler (keep zod validation, rate-limit, auth extraction). NEVER let the LLM pick/reorder places or free-form rewrite structure.</action>
  <verify>
    <automated>cd supabase/functions/generate-plan && deno test improve.test.ts --allow-env --allow-read</automated>
  </verify>
  <done>single-slot re-pick holds others + excludes used + re-validates; NL-knob parse maps phrases→knobs; incoherent changes surface not persist; writes go through update_itinerary_stops with the ownership check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Improve-loop UI in /create (per-stop tweak + NL input)</name>
  <files>apps/web/app/create/ImproveControls.tsx, apps/web/app/create/CreateFlow.tsx, apps/web/app/create/__tests__/ImproveControls.test.tsx</files>
  <behavior>
    - Each stop renders a "tweak" affordance that triggers a single-stop swap via the improve dispatch and updates the itinerary in place.
    - An NL tweak input accepts free text and applies the parsed change.
    - A surfaced incoherence (hop/budget/hours break) renders a sonner toast / inline message, not a silent swap.
  </behavior>
  <action>Per Area 4 (CONTEXT D — improve-loop UI lives in /create): build `ImproveControls.tsx` — a per-stop "tweak" affordance + an NL tweak text input — and mount it in `CreateFlow.tsx` (the generate funnel). Call the improve dispatch via `browserAfter5Client`. Render surfaced incoherence with sonner (not a silent ship). Barbiecore + mobile-first per docs/superpowers/DESIGN-SYSTEM.md: `font-heading`/`font-body`/`shell.*` tokens, `vibePalette`, tap targets ≥44px, design at 375px / `max-w-[420px]`, `vaul` for any bottom sheet, ARIA on icon buttons. Copy is lowercase/dry/stop-slop (CONTEXT specifics: all new copy lowercase/dry/Barbiecore). No hardcoded hex; use `cn()`.</action>
  <verify>
    <automated>cd /Users/lucas/Projects/After5 && pnpm --filter web test -- ImproveControls && pnpm --filter web typecheck</automated>
    <human-check>At 420px in the create flow: each stop has a discoverable lowercase "tweak" affordance; the NL tweak input applies a change; an incoherent swap shows a toast, not a silent swap; tokens are Barbiecore (no raw hex), tap targets ≥44px. Screenshot @420px and critique against DESIGN-SYSTEM.md.</human-check>
  </verify>
  <done>per-stop tweak + NL input render and call the improve dispatch; incoherence surfaces via toast; component test + typecheck green; visual-verify @420px deferred to the phase gate (Plan 09-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → improve edge fn | untrusted NL free text + stop-index input |
| improve fn → Anthropic (Haiku) | NL-tweak text could attempt prompt injection |
| improve fn → update_itinerary_stops | owner-scoped write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Tampering | prompt injection via NL-tweak free text | mitigate | Haiku output constrained to a knob tool schema (never executed as instructions); free text length-capped (zod) before the call; code grants only known knobs |
| T-09-12 | Elevation of Privilege | improve edits another user's itinerary | mitigate | update_itinerary_stops re-checks auth.uid() + ownership (42501 if not owner); improve dispatch validates input with zod |
| T-09-13 | Tampering | incoherent date silently shipped after a swap | mitigate | mandatory re-validate (hop-gate + budget + hours); surface, do not persist on break |
</threat_model>

<verification>
- `deno test improve.test.ts` green; `pnpm --filter web test -- ImproveControls` + typecheck green.
- Re-validation rejects/ surfaces incoherent swaps; writes go through update_itinerary_stops.
- Visual-verify @420px executed at the phase gate (Plan 09-06).
</verification>

<success_criteria>
A user can swap a single stop and apply natural-language tweaks ("cheaper", "more romantic", "later") and the itinerary stays coherent and persisted.
</success_criteria>

<output>
Create `.planning/phases/09-trustworthy-generation-eval-harness/09-05-SUMMARY.md` when done.
</output>
