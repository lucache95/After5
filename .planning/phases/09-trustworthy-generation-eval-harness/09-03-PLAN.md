---
phase: 09-trustworthy-generation-eval-harness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/date-quality/src/gates.ts
  - packages/date-quality/src/runEval.ts
  - packages/date-quality/src/__tests__/gates.test.ts
  - packages/date-quality/src/__tests__/runEval.test.ts
  - packages/date-quality/fixtures/dategen/coldcity-v0/coldcity-thin-01.json
  - packages/date-quality/fixtures/dategen/coldcity-v0/coldcity-thin-02.json
  - packages/date-quality/fixtures/dategen/coldcity-v0/coldcity-usable-01.json
autonomous: true
requirements: [EVAL-01]
must_haves:
  truths:
    - "A scheduleMonotonic critical gate catches time-travel and overlap (start_time[i] + duration + drive ≤ start_time[i+1]) — the gate the package lacked (Area 3: deterministic hard checks)"
    - "A cold on-the-fly city is in the golden set, fixture-seeded with deliberately thin (null coord/hours) stops so it runs with NO live key (Area 3: golden set incl. cold city)"
    - "unverified_rate is a FIRST-CLASS scored signal per city with its own regression threshold — a thin cold city FAILS the suite, it does NOT read green by skipping null-data gates (THE biggest risk; Area 3)"
    - "unverified_rate uses the SAME logic as production computeUnverifiedRate so the eval metric and the production metric are identical (Area 3)"
  artifacts:
    - path: "packages/date-quality/src/gates.ts"
      provides: "scheduleMonotonic critical gate, registered in GATES"
      contains: "scheduleMonotonic"
    - path: "packages/date-quality/src/runEval.ts"
      provides: "unverified_rate on FixtureResult/EvalReport + UNVERIFIED_RATE_THRESHOLD regression check + per-city grouping"
      contains: "UNVERIFIED_RATE_THRESHOLD"
    - path: "packages/date-quality/fixtures/dategen/coldcity-v0/coldcity-thin-01.json"
      provides: "a deliberately-thin cold-city fixture (null coords/hours)"
      contains: "null"
  key_links:
    - from: "packages/date-quality/src/runEval.ts"
      to: "computeUnverifiedRate logic"
      via: "identical null-field fraction (lat/lng/opens/closes)"
      pattern: "lat == null|lng == null|opens|closes"
    - from: "packages/date-quality/src/runEval.ts"
      to: "Regression"
      via: "new 'unverified_rate' regression kind on threshold breach"
      pattern: "unverified_rate"
---

<objective>
EVAL-01 (core): EXTEND `@after5/date-quality` — do NOT rebuild. Add the two deterministic checks the package lacks and close THE biggest risk: the cold-city eval reading green vacuously because gates skip null-data stops. Add a cold-city fixture set and make `unverified_rate` a first-class scored signal with its own regression threshold so a thin cold city FAILS the suite.

Purpose: The harness is the product's actual test. It must catch the cold-city silent collapse (the Phase-8 risk), not certify a quality bar new markets don't hit.
Output: scheduleMonotonic gate, cold-city fixtures, unverified_rate plumbing + threshold, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-CONTEXT.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-RESEARCH.md

<interfaces>
<!-- Extracted from packages/date-quality. EXTEND these; do not duplicate. -->

Existing gates already present (DO NOT re-add): travelPacing (haversine, critical, SKIPS null coords), openAtArrival (critical, SKIPS null hours), budgetRealism, timeOfDayOrder, + 15 more. parseTime helper exists in gates.ts:626.
GATES registry: `export const GATES: readonly Gate[]` at gates.ts:848. `Gate = (fixture, date) => GateResult`. Severity caps: critical→40, major→55, minor→70.

Production metric to mirror (supabase/functions/generate-plan/providers/unverified-rate.ts):
```typescript
export function computeUnverifiedRate(candidates: Place[]): number {
  // fraction with p.lat == null || p.lng == null || !p.opens || !p.closes; empty → 0
}
```
The eval reads fixture.stops (FixtureStop has lat/lng/opens/closes). Compute the eval's unverified_rate with the IDENTICAL null-field rule.

runEval.ts shapes to extend:
- FixtureResult (runEval.ts:30) — add `unverified_rate: number`.
- EvalReport (runEval.ts:51) — add per-city grouping (fixtures already carry a city via fixture.inputs / fixture.id prefix).
- Regression (runEval.ts:65) `kind` union — add `'unverified_rate'`.
- Thresholds: MEAN_DROP_THRESHOLD=3, FIXTURE_DROP_THRESHOLD=10 (runEval.ts:90). Add `UNVERIFIED_RATE_THRESHOLD`.
- compareToBaseline (runEval.ts:340) emits Regression[] — add the unverified_rate check there.

Fixture shape (packages/date-quality/fixtures/dategen/kelowna-v0/*.json): { id, inputs{occasion,vibe,budget_per_person,duration_min,...}, stops:[{ place_id, place_name, place_type, start_time, duration_min, estimated_cost_pp, lat, lng, opens, closes, vibe_tags, pairing_tags, quality_score, facts }] }. Cold-city stops set lat/lng and/or opens/closes to null.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the scheduleMonotonic critical gate</name>
  <files>packages/date-quality/src/gates.ts, packages/date-quality/src/__tests__/gates.test.ts</files>
  <behavior>
    - PASS when each stop's start_time strictly increases AND start_time[i] + duration_min[i] + drive(i→i+1) ≤ start_time[i+1].
    - FAIL (critical) on a time-travel case (start_time decreases).
    - FAIL (critical) on an overlap case (stop i+1 starts before stop i finishes + travel).
    - Skips gracefully when a start_time is unparseable (consistent with the package's existing skip-on-missing convention) — but does NOT skip when times are present and wrong.
  </behavior>
  <action>Per Area 3 (CONTEXT — schedule monotonic, currently MISSING): add an exported `scheduleMonotonic` gate `(fixture, date) => GateResult`, severity `critical`. Reuse the existing `parseTime` helper (gates.ts:626) and the same haversine→drive estimate `travelPacing` uses (AVG_KMH) for the per-hop drive term. Register it in the `GATES` array (gates.ts:848) and export it (mirror the other gate exports surfaced in src/index.ts). Add it to the index.ts export list. Do NOT duplicate proximity/hours/budget — those gates exist.</action>
  <verify>
    <automated>cd /Users/lucas/Projects/After5 && pnpm --filter @after5/date-quality test -- gates</automated>
  </verify>
  <done>scheduleMonotonic catches time-travel + overlap, is critical, is in GATES + index exports; existing gate tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cold-city fixtures + unverified_rate as a scored signal</name>
  <files>packages/date-quality/fixtures/dategen/coldcity-v0/coldcity-thin-01.json, packages/date-quality/fixtures/dategen/coldcity-v0/coldcity-thin-02.json, packages/date-quality/fixtures/dategen/coldcity-v0/coldcity-usable-01.json, packages/date-quality/src/runEval.ts, packages/date-quality/src/__tests__/runEval.test.ts</files>
  <behavior>
    - A deliberately-thin cold-city fixture (≥half its stops null-coord and/or null-hours) produces a HIGH unverified_rate and is FLAGGED as a regression by UNVERIFIED_RATE_THRESHOLD — it does NOT pass with 0 failed gates.
    - A "usable" cold-city fixture (mostly full data) clears the threshold.
    - unverified_rate on a fixture equals the production computeUnverifiedRate value for the same stops.
    - The report carries per-city unverified_rate.
  </behavior>
  <action>Per Area 3 (CONTEXT — golden set incl. cold city; THE biggest risk per RESEARCH Pitfall 1): create `fixtures/dategen/coldcity-v0/` with 2 deliberately-thin fixtures (model on Phase-8 Foursquare data: some stops with `lat/lng: null`, some with `opens/closes: null` — Open Question 2, faithful proxy not strawman) and 1 "usable" cold-city fixture (mostly full data) so the threshold has a passing case. Add `unverified_rate: number` to `FixtureResult` (computed per fixture using the IDENTICAL rule as production `computeUnverifiedRate`: fraction of stops with null lat/lng OR null opens/closes — keep eval and prod metrics identical). Add per-city grouping to `EvalReport`. Add `UNVERIFIED_RATE_THRESHOLD` (set from what a genuinely-usable cold city should clear — Open Question 2 recommendation, Claude's discretion) and a `'unverified_rate'` Regression kind emitted in `compareToBaseline` when a fixture exceeds it. This is the explicit must-have: a thin cold city must FAIL the suite, not pass because travelPacing/openAtArrival skipped null-data stops.</action>
  <verify>
    <automated>cd /Users/lucas/Projects/After5 && pnpm --filter @after5/date-quality test -- runEval</automated>
  </verify>
  <done>thin cold-city fixtures flag an unverified_rate regression; the usable cold city clears it; eval unverified_rate == production computeUnverifiedRate; report groups per city.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture data → gate verdict | thin/null-field data must not silently pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Repudiation | cold-city eval reads green while quality is poor (vacuous pass) | mitigate | unverified_rate is a first-class scored signal with UNVERIFIED_RATE_THRESHOLD; thin city FAILS not skips (the lead must-have + test) |
| T-09-07 | Tampering | eval metric diverges from production metric over time | mitigate | eval unverified_rate uses the identical null-field rule as computeUnverifiedRate; asserted by test |
</threat_model>

<verification>
- `pnpm --filter @after5/date-quality test` green (gates + runEval).
- Thin cold-city fixtures produce a flagged unverified_rate regression; usable cold city passes.
- scheduleMonotonic in GATES + index exports.
- Baseline regeneration deferred to Plan 09-04 (after the judge param + CI land, so one baseline covers all new checks/fixtures).
</verification>

<success_criteria>
The golden set includes a cold on-the-fly city; deterministic hard checks include schedule monotonicity; unverified_rate is surfaced per city and a thin city cannot read green.
</success_criteria>

<output>
Create `.planning/phases/09-trustworthy-generation-eval-harness/09-03-SUMMARY.md` when done.
</output>
