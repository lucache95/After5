---
phase: 09-trustworthy-generation-eval-harness
plan: 04
type: execute
wave: 2
depends_on: ["09-03"]
files_modified:
  - packages/date-quality/src/judge.ts
  - packages/date-quality/src/__tests__/judge.test.ts
  - packages/date-quality/scripts/eval-dategen.ts
  - packages/date-quality/baselines/dategen/baseline-v0.json
  - .github/workflows/eval.yml
autonomous: true
requirements: [EVAL-01]
must_haves:
  truths:
    - "The Opus-4.8 LLM-judge judges each fixture against its own locale — JUDGE_CITY is a per-fixture param, not hard-coded Kelowna (Area 3: cold city judged against its own city)"
    - "The live noHallucinatedVenue check resolves every emitted place_id against a pinned places snapshot in --live mode (Area 3: no hallucinated venues)"
    - "Deterministic checks HARD-FAIL CI on regression; the Opus judge is advisory warn-threshold only and never blocks a merge (Area 3: CI gating)"
    - "CI runs the dry deterministic eval with NO API key on PRs touching generate-plan/ or date-quality/ (Area 3)"
    - "The committed baseline is regenerated to cover the new gates + cold-city fixtures so the regression diff is meaningful"
  artifacts:
    - path: ".github/workflows/eval.yml"
      provides: "net-new CI gate: dry eval hard-fail on PR + advisory live judge step"
      contains: "date-quality"
    - path: "packages/date-quality/scripts/eval-dategen.ts"
      provides: "live noHallucinatedVenue resolution against a pinned places snapshot"
      contains: "noHallucinatedVenue"
    - path: "packages/date-quality/baselines/dategen/baseline-v0.json"
      provides: "regenerated baseline covering new checks + cold-city fixtures"
  key_links:
    - from: ".github/workflows/eval.yml"
      to: "packages/date-quality eval (dry)"
      via: "pnpm --filter @after5/date-quality eval, exit 1 on regression, no key"
      pattern: "eval"
    - from: "packages/date-quality/src/judge.ts"
      to: "per-fixture locale"
      via: "JUDGE_CITY as a param"
      pattern: "city"
---

<objective>
EVAL-01 (judge + CI): make the Opus judge locale-aware (per-fixture city), add the live `noHallucinatedVenue` resolution, regenerate the baseline to cover the new checks/fixtures, and wire the net-new CI gate (`.github/workflows/` does not exist). Deterministic checks hard-fail; the judge is advisory.

Purpose: Close out EVAL-01 — the harness gates in CI exactly as CONTEXT locks (hard deterministic, advisory judge), and the cold-city judge reasons about the right place.
Output: JUDGE_CITY param, live no-hallucination check, regenerated baseline, CI workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-CONTEXT.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-RESEARCH.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-03-SUMMARY.md

<interfaces>
<!-- Extracted from packages/date-quality. Depends on 09-03's gates/fixtures/unverified_rate being in place. -->

judge.ts: `export const JUDGE_CITY` is hard-coded 'Kelowna, BC' and baked into the SYSTEM_PROMPT / buildJudgeUserMessage. 6-dim rubric (desirability/arc/vibe_coherence/city_context_fit/specificity_taste/hook) already maps the CONTEXT rubric. Injected `InvokeLLM` seam (`{system,user}=>Promise<string>`) — judge.ts NEVER imports the SDK.

scripts/eval-dategen.ts: the CLI. dry default; `--update-baseline`; `--live`. Exits 1 on regression. This is the CI entry. Loads fixtures via loadFixtures(dir), runs runEval, compares to baseline, writes eval-results/.

Anti-pattern (Pitfall 2): NEVER import supabase/functions/generate-plan/prompt.ts (npm:@anthropic-ai/sdk Deno specifier) into the Node package — it won't resolve under vitest/vite-node. The live judge adapter (Opus) lives in the script/CI layer, in Node, against the node @anthropic-ai/sdk, wired through the InvokeLLM seam.

noHallucinatedVenue (Area 3, live-mode only per RESEARCH): dry mode can't hallucinate (writer only writes copy over frozen place_ids). The teeth are in --live: assert every emitted place_id resolves in a pinned JSON places snapshot per city fixture dir (Open Question 3 recommendation: pin a JSON snapshot, keeps eval offline + reproducible).

No `.github/workflows/` directory exists — create it. Root scripts: `pnpm --filter @after5/date-quality eval` (dry, no key, exit 1), `eval:update`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-fixture JUDGE_CITY + live noHallucinatedVenue</name>
  <files>packages/date-quality/src/judge.ts, packages/date-quality/src/__tests__/judge.test.ts, packages/date-quality/scripts/eval-dategen.ts</files>
  <behavior>
    - The judge prompt names the fixture's own city (Kelowna for kelowna-v0, the cold city for coldcity-v0), not a hard-coded Kelowna.
    - In --live mode, a place_id absent from the pinned places snapshot fails noHallucinatedVenue (critical); a resolvable place_id passes.
    - In dry mode, noHallucinatedVenue is a no-op (writer can't hallucinate over frozen ids).
  </behavior>
  <action>Per Area 3 (CONTEXT): make `JUDGE_CITY` a per-fixture parameter — thread the fixture's city (from fixture.inputs / fixture id prefix) into `buildJudgeUserMessage` and the SYSTEM_PROMPT locale anchor so the cold-city fixture judges against its own locale (RESEARCH: judge.ts JUDGE_CITY currently hard-coded). Keep the injected `InvokeLLM` seam — do NOT import the SDK in the package. Add the live `noHallucinatedVenue` resolution in `scripts/eval-dategen.ts` (the live runner, NOT the pure gate set): in `--live` mode, load a pinned `places.snapshot.json` per city fixture dir and assert every emitted place_id resolves; flag as critical when it doesn't (Pitfall 2: this is anti-fabrication and belongs in the live runner; Open Question 3: pin a JSON snapshot for offline reproducibility).</action>
  <verify>
    <automated>cd /Users/lucas/Projects/After5 && pnpm --filter @after5/date-quality test -- judge</automated>
  </verify>
  <done>JUDGE_CITY is per-fixture; live noHallucinatedVenue resolves against a pinned snapshot; dry mode unaffected; package still imports no SDK.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate baseline + wire the CI gate</name>
  <files>packages/date-quality/baselines/dategen/baseline-v0.json, .github/workflows/eval.yml</files>
  <action>Per Area 3 (CONTEXT — CI gating): regenerate the committed baseline via `pnpm --filter @after5/date-quality eval:update` so the regression diff covers the new scheduleMonotonic gate, the cold-city fixtures, and unverified_rate (RESEARCH: baseline must be regenerated after new checks/fixtures land). Commit it. Create `.github/workflows/eval.yml` (the directory is net-new): on PRs touching `supabase/functions/generate-plan/**` or `packages/date-quality/**`, run the DRY deterministic eval (`pnpm --filter @after5/date-quality eval`) — NO API key, hard-fails (exit 1) on regression (Pitfall 5). Add a SEPARATE, advisory, label-gated-or-scheduled job for the `--live` Opus judge that uses `ANTHROPIC_API_KEY` as a CI secret and does NOT block the merge (warn-threshold only — CONTEXT locks: deterministic hard-fail, judge advisory). Set up Node 22 + pnpm 9.12 to match the repo.</action>
  <verify>
    <automated>cd /Users/lucas/Projects/After5 && pnpm --filter @after5/date-quality eval && test -f .github/workflows/eval.yml</automated>
  </verify>
  <done>Baseline regenerated + committed; dry eval passes against the new baseline; eval.yml runs the dry hard-fail gate keyless on PRs + an advisory live judge job that never blocks merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → Anthropic | live judge step needs a key; hard gate must not |
| generation output → places snapshot | live no-hallucination resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Information Disclosure | ANTHROPIC_API_KEY leak in CI | mitigate | dry hard-gate needs no key; live judge uses a CI secret, advisory step only |
| T-09-09 | Tampering | hallucinated venue persisted/certified | mitigate | live noHallucinatedVenue resolves every place_id against a pinned snapshot (critical) |
| T-09-10 | Denial of Service | flaky Opus judge blocks merges | mitigate | judge is advisory/label-gated, warn-threshold; deterministic gates are the only hard fail |
</threat_model>

<verification>
- `pnpm --filter @after5/date-quality eval` passes against the regenerated baseline.
- judge.test green: per-fixture city; live no-hallucination resolves against snapshot.
- `.github/workflows/eval.yml` exists: dry hard-gate (keyless) + advisory live judge.
</verification>

<success_criteria>
The eval harness scores generated dates with deterministic hard checks plus an Opus-4.8 judge rubric over a golden set that includes a cold city, surfaces unverified_rate per city, and gates in CI (deterministic hard-fail, judge advisory).
</success_criteria>

<output>
Create `.planning/phases/09-trustworthy-generation-eval-harness/09-04-SUMMARY.md` when done.
</output>
