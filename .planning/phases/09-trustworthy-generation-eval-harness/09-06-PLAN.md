---
phase: 09-trustworthy-generation-eval-harness
plan: 06
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03", "09-04", "09-05"]
files_modified:
  - .planning/phases/09-trustworthy-generation-eval-harness/09-06-SUMMARY.md
autonomous: false
requirements: [PLAN-01, PLAN-02, EVAL-01, SOUND-01]
must_haves:
  truths:
    - "The full local suite is green: pnpm test + pnpm typecheck + the date-quality eval (dry) passes with no regressions against the regenerated baseline + the generate-plan deno tests"
    - "The improve-loop UI is visually verified @420px against DESIGN-SYSTEM.md (Barbiecore, lowercase/dry, tap targets ≥44px)"
    - "The cold-city eval cannot read green: the thin cold-city fixture flags an unverified_rate regression (the lead risk is provably closed)"
    - "Prod-bound changes (the SOUND-01 ambient seed migration) are applied to prod ONLY behind this gate, after local-green, batched, advisor-clean"
    - "The ElevenLabs audio generation + bucket upload (service_role JWT) runs as an isolated gated manual step, separate from the row seed"
  artifacts:
    - path: ".planning/phases/09-trustworthy-generation-eval-harness/09-06-SUMMARY.md"
      provides: "phase-gate evidence: suite/eval/visual results + prod-apply log"
  key_links:
    - from: "local-green gate"
      to: "prod ufufmcpnysvwtutpbian"
      via: "gated batched migration apply after advisor check"
      pattern: "ambient_loops_seed"
---

<objective>
Phase gate (mirrors the v1.0 pattern): prove Phase 9 green locally — full test suite + the eval harness passing (the harness IS much of the validation) + visual-verify @420px — then apply the only prod-bound change (the SOUND-01 ambient seed migration) behind the gate, with the ElevenLabs audio upload isolated as a manual service_role step.

Purpose: One controlled gate where everything is proven and prod is touched once, advisor-clean.
Output: a SUMMARY with suite/eval/visual evidence + the prod-apply log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-01-SUMMARY.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-02-SUMMARY.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-03-SUMMARY.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-04-SUMMARY.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-05-SUMMARY.md
@docs/superpowers/SOUND-GENERATION.md
@docs/superpowers/DESIGN-SYSTEM.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local-green full suite + eval harness</name>
  <files>.planning/phases/09-trustworthy-generation-eval-harness/09-06-SUMMARY.md</files>
  <action>Run the full local gate and record results in the SUMMARY: `pnpm typecheck`, `pnpm test` (root vitest), `pnpm --filter @after5/date-quality eval` (dry — must PASS with no regressions against the regenerated baseline), and the generate-plan deno tests (`deno test` across prompt/scoring/places-filter/improve/persist). Explicitly confirm the lead-risk closure: the thin cold-city fixture flags an unverified_rate regression (it must NOT read green). The eval harness IS much of the validation — note this in the SUMMARY (set nyquist_compliant per VALIDATION.md). Apply the SOUND-01 seed migration on a fresh `supabase db reset` and run the Supabase security advisor — record it clean. If anything fails, STOP and fix before the gate proceeds.</action>
  <verify>
    <automated>cd /Users/lucas/Projects/After5 && pnpm typecheck && pnpm test && pnpm --filter @after5/date-quality eval && supabase db reset</automated>
  </verify>
  <done>typecheck + root test + dry eval (no regressions) + deno tests green; cold-city unverified_rate regression confirmed; migration applies on fresh reset; advisor clean — all recorded in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The improve-loop UI (per-stop tweak affordance + NL tweak input) in /create, the hardened one-tap generation, and the vibe-matched ambient auto-pick.</what-built>
  <how-to-verify>
    1. Run the app locally; sign in; go to /create → generate a date.
    2. At 420px: confirm each stop has a discoverable lowercase "tweak" affordance; tap it → a single stop swaps and the itinerary updates in place.
    3. Enter an NL tweak ("cheaper" / "more romantic" / "later") → the date re-runs and stays coherent (close stops, budget, hours).
    4. Force an incoherent swap → confirm a toast surfaces it (no silent ship).
    5. Confirm the generated date has an ambient sound whose vibe matches its cover.
    6. Screenshot @420px and critique against docs/superpowers/DESIGN-SYSTEM.md (Barbiecore tokens, lowercase/dry copy, tap targets ≥44px, no raw hex).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The SOUND-01 ambient seed migration (rows only) is local-green. The audio objects + the prod migration apply are the only prod-bound, manual-credential steps.</what-built>
  <how-to-verify>
    1. Generate the 6-10 ElevenLabs loops per docs/superpowers/SOUND-GENERATION.md (loop:true, 15s, mono m4a) and upload them to the `ambient-sounds` bucket with a service_role JWT (v1.0 recipe — no authenticated write policy).
    2. Apply the SOUND-01 seed migration to prod (ufufmcpnysvwtutpbian) as a batched, gated apply after local-green; run the Supabase security advisor and confirm clean.
    3. Spot-check: a freshly generated prod date receives a vibe-matched ambient_sound_id whose object resolves.
    Record the audio paths, the prod migration apply, and the advisor result in the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "applied" with the prod migration + advisor result, or "skip-audio" to ship the auto-pick against the existing 10 loops and add new loops later (RESEARCH fallback).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → prod ufufmcpnysvwtutpbian | the single gated migration apply |
| operator → ambient-sounds bucket | service_role audio upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-14 | Tampering | ungated/unreviewed prod migration | mitigate | local-green first; batched gated apply; advisor run + recorded (secure-by-default / gated-prod-apply) |
| T-09-15 | Information Disclosure | service_role JWT misuse in audio upload | mitigate | isolated manual checkpoint step; one-time content upload, not runtime; key never committed |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | none this phase — RESEARCH Package Legitimacy Audit: no new installs; all deps in lockfile |
</threat_model>

<verification>
- Full suite + dry eval green; cold-city unverified_rate regression confirmed.
- Visual-verify @420px approved against DESIGN-SYSTEM.md.
- SOUND-01 migration applied to prod behind the gate, advisor clean; audio uploaded (or skip-audio fallback recorded).
</verification>

<success_criteria>
Phase 9 is proven green locally (suite + eval harness + visual), the four success criteria (one-tap coherent generate, swap+NL improve loop, vibe-matched sound, CI-gated eval incl. cold city) hold, and the only prod-bound change is applied once behind this gate, advisor-clean.
</success_criteria>

<output>
Create `.planning/phases/09-trustworthy-generation-eval-harness/09-06-SUMMARY.md` when done.
</output>
