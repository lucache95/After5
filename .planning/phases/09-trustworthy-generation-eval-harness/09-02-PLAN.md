---
phase: 09-trustworthy-generation-eval-harness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260606160000_sound01_ambient_loops_seed.sql
  - supabase/functions/generate-plan/persist.ts
  - supabase/functions/generate-plan/persist.test.ts
  - docs/superpowers/SOUND-GENERATION.md
autonomous: true
requirements: [SOUND-01]
must_haves:
  truths:
    - "A generated date auto-receives the best vibe_tags-overlapping active ambient sound as its ambient_sound_id on persist, reusing the feed's exact && lateral (Area 4: vibe-matched sound)"
    - "Sound ↔ cover cohere because both derive from the night's vibe_tags"
    - "The expanded ambient library covers the itinerary vibe taxonomy with 6-10 new loops, seeded idempotently (upsert-on-name) (Area 4)"
    - "New ambient rows are inserted via a migration; audio object upload to the ambient-sounds bucket is isolated behind a gated manual step (service_role JWT)"
  artifacts:
    - path: "supabase/migrations/20260606160000_sound01_ambient_loops_seed.sql"
      provides: "idempotent upsert of 6-10 new ambient_sounds rows with vibe_tags"
      contains: "on conflict"
    - path: "supabase/functions/generate-plan/persist.ts"
      provides: "vibe-overlap ambient auto-pick set on date_instances.ambient_sound_id at persist"
      contains: "vibe_tags"
  key_links:
    - from: "supabase/functions/generate-plan/persist.ts"
      to: "ambient_sounds (vibe_tags && lateral)"
      via: "the browse_feed_for_viewer overlap pick lifted verbatim"
      pattern: "vibe_tags"
---

<objective>
SOUND-01: expand the ambient loop library and auto-assign each generated date a vibe-matched ambient sound. Reuses the m4 ambient infrastructure wholesale — the `vibe_tags &&` lateral from `browse_feed_for_viewer`, the `ambient-sounds` bucket, and the v1.0 ElevenLabs recipe. No new pick logic; lift the lateral.

Purpose: Every generated date gets a coherent soundtrack; sound and cover cohere via shared vibe tags.
Output: a seed migration for new loops, the auto-pick on persist, a SOUND-GENERATION runbook for the (gated, manual) audio generation + upload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-CONTEXT.md
@.planning/phases/09-trustworthy-generation-eval-harness/09-RESEARCH.md

<interfaces>
<!-- The proven feed lateral to lift verbatim (m4 browse_feed_ambient migration, lines 40-51): -->
```sql
select s.id from ambient_sounds s
where s.is_active = true and s.vibe_tags && <itinerary.vibe_tags>
order by s.sort_order desc, s.id
limit 1
```
ambient_sounds: (id, name UNIQUE [ambient_sounds_name_key], vibe_tags text[], sort_order, is_active, duration_sec CHECK 5-120, storage path <vibe>/<slug>.m4a in public ambient-sounds bucket).
date_instances.ambient_sound_id column EXISTS (m4 date_instances_ambient migration).
The 10 base vibes already seeded: cozy/nightlife/romantic/adventurous/art/late-night/chill/foodie/outdoorsy/classy.
Bucket write requires service_role JWT (no authenticated write policy — m4 ambient_sounds.sql:22).
persist.ts (supabase/functions/generate-plan/persist.ts) inserts itineraries (it.title, it.vibe_tags). The auto-pick runs at persist, writing date_instances.ambient_sound_id.
Latest migration timestamp on disk: 20260606150100 — new migration must sort AFTER it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed migration for the expanded ambient library</name>
  <files>supabase/migrations/20260606160000_sound01_ambient_loops_seed.sql, docs/superpowers/SOUND-GENERATION.md</files>
  <action>Per Area 4 (CONTEXT D — more tracks): add a migration that upserts 6-10 new `ambient_sounds` rows that fill gaps in the itinerary `vibe_tags` taxonomy (do NOT duplicate the existing 10 base vibes). Upsert on the `ambient_sounds_name_key` unique index (`on conflict (name) do update`) so re-running is idempotent. Set `vibe_tags`, `sort_order`, `is_active`, `duration_sec` within the 5-120 check, and the storage `path` as `<vibe>/<slug>.m4a`. Mirror the m4 `ambient_sounds_real_paths` recipe. The migration only inserts ROWS — it does NOT upload audio. Write `docs/superpowers/SOUND-GENERATION.md`: the v1.0 ElevenLabs Sound Effects recipe (loop:true, 15s, mono m4a/AAC) + the upload step (service_role JWT to the ambient-sounds bucket), marked as a gated manual content task to run at the phase gate (Plan 09-05). Number of new sounds + vibe coverage is Claude's discretion (CONTEXT). Run the Supabase security advisor after the DDL (no new table/policy expected — verify advisor clean).</action>
  <verify>
    <automated>cd /Users/lucas/Projects/After5 && supabase db reset && supabase db diff --schema public 2>&1 | tail -5</automated>
  </verify>
  <done>Migration applies on a fresh local reset; 6-10 new ambient_sounds rows exist with vibe_tags; re-running the seed is idempotent (upsert-on-name); advisor clean; SOUND-GENERATION.md documents the gated audio step.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-pick the vibe-matched ambient sound on persist</name>
  <files>supabase/functions/generate-plan/persist.ts, supabase/functions/generate-plan/persist.test.ts</files>
  <behavior>
    - On persist of a generated date, the best vibe_tags-overlapping active ambient sound (sort_order desc, id) is written to date_instances.ambient_sound_id.
    - An itinerary whose vibe_tags overlap no active sound gets a null/no-change ambient_sound_id (graceful — no crash).
    - The pick uses the same overlap semantics as browse_feed_for_viewer (lift the lateral, do not re-implement).
  </behavior>
  <action>Per Area 4 (CONTEXT D — auto vibe-match): in `persist.ts`, after the itinerary insert, run the `vibe_tags &&` overlap pick (lift the `browse_feed_for_viewer` lateral verbatim — do NOT invent new pick logic, Anti-Pattern) against the itinerary's `vibe_tags`, and set the result on `date_instances.ambient_sound_id`. Sound↔cover cohere because both derive from the night's vibe. Handle the no-overlap case gracefully (leave ambient_sound_id null). Keep it offline-testable: the test mocks the supabase client's ambient query and asserts the chosen id.</action>
  <verify>
    <automated>cd supabase/functions/generate-plan && deno test persist.test.ts --allow-env --allow-read</automated>
  </verify>
  <done>persist sets ambient_sound_id from the vibe-overlap pick; no-overlap leaves it null; the lateral is lifted, not re-implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content generation → ambient-sounds bucket | one-time audio upload requires RLS-bypass (service_role) |
| persist (service role) → date_instances | auto-sets ambient_sound_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | public write to ambient-sounds bucket | mitigate | no authenticated write policy; service_role-only upload (m4 migration); upload isolated to the gated manual step in SOUND-GENERATION.md |
| T-09-05 | Tampering | seed migration applied to prod ungated | mitigate | migration is row-only + idempotent; prod apply is batched behind the phase gate (Plan 09-05) per gated-prod-apply |
</threat_model>

<verification>
- `supabase db reset` applies the seed migration; advisor clean.
- persist.test.ts green: ambient_sound_id auto-set from vibe overlap.
- Audio object upload deferred to the gated phase-gate step (Plan 09-05).
</verification>

<success_criteria>
A generated date auto-receives an ambient sound that fits its cover (sound ↔ cover cohere via shared vibe tags), drawn from an expanded track library.
</success_criteria>

<output>
Create `.planning/phases/09-trustworthy-generation-eval-harness/09-02-SUMMARY.md` when done.
</output>
