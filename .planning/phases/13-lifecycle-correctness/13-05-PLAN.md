---
phase: 13-lifecycle-correctness
plan: 05
type: execute
wave: 2
depends_on: [13-02, 13-03, 13-04]
files_modified:
  - supabase/tests/_all_5b.sh
autonomous: false
requirements: [LIFE-01, LIFE-02, LIFE-03, LIFE-04]
user_setup: []

must_haves:
  truths:
    - "All Phase-13 migrations apply cleanly to the local stack (local-green) before any prod apply"
    - "The Supabase security advisor reports no NEW ERROR-level findings after the Phase-13 DDL"
    - "The full Phase-13 test surface (pgTAP + Vitest + Deno handler) passes locally"
    - "Gated prod apply (migrations + edge-fn deploys) is presented to the developer as an explicit human-gated step, never run autonomously"
  artifacts:
    - path: "supabase/tests/_all_5b.sh"
      provides: "the full local suite that must pass at the phase gate"
      contains: "b_complete"
  key_links:
    - from: "local migration apply"
      to: "security advisor"
      via: "advisor run after DDL"
      pattern: "advisor"
---

<objective>
Phase-13 gate (LIFE-01..04). Apply all Phase-13 migrations to the LOCAL Supabase stack (the [BLOCKING] step — build/type checks pass WITHOUT applying migrations, so skipping this produces a false-positive verification state), run the full local test surface (pgTAP + Vitest + Deno handler tests) to prove every requirement green, run the Supabase security advisor and review for new ERROR-level findings, then present the GATED prod-apply + edge-fn-deploy steps as an explicit human-gated handoff. Prod apply is NEVER run autonomously (CLAUDE.md gated prod-apply).

Purpose: De-risk the false-green from migration-less type checks, satisfy secure-by-default verification, and keep prod apply human-gated.
Output: A local-green run record, an advisor review, and a prod-apply/deploy handoff checklist (including the LIFE-04 backfill decision from 13-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-lifecycle-correctness/13-RESEARCH.md
@.planning/phases/13-lifecycle-correctness/13-01-SUMMARY.md
@.planning/phases/13-lifecycle-correctness/13-02-SUMMARY.md
@.planning/phases/13-lifecycle-correctness/13-03-SUMMARY.md
@.planning/phases/13-lifecycle-correctness/13-04-SUMMARY.md

<interfaces>
Phase-13 migrations to apply (local):
- 20260613140000_life03_standby_notify.sql        (LIFE-03: standby_bumped enum + match_autowithdraw_user_conflicts dispatch)
- 20260613141000_life02_file_report.sql           (LIFE-02: file_report DEFINER RPC)
- 20260613142000_life01_cancel_closes_chat.sql    (LIFE-01: revoke_chat_thread + match_cancel_lock edit)
Phase-13 NON-migration change: supabase/functions/process-jobs/handlers.ts (LIFE-04 handler — needs prod redeploy)
Phase-13 NEW edge fns (need prod deploy): file-report, match-flag-no-show

Local apply: `supabase db reset` (clean apply of all migrations incl. seed) OR `supabase migration up` (apply pending only) against the local stack (port 54322).
Tests: `pnpm db:test` (pgTAP loop over supabase/tests/*.sql) · `pnpm vitest run` · `deno test supabase/functions/process-jobs/handlers_test.ts` · `supabase/tests/_all_5b.sh` (full: db reset → pgTAP → Deno → Vitest → E2E).
Advisor: mcp__supabase__get_advisors (type='security') or `supabase` advisor CLI.
Prod ref: ufufmcpnysvwtutpbian (apply is GATED — human only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply all Phase-13 migrations to the local stack + run the full local suite</name>
  <files>supabase/tests/_all_5b.sh</files>
  <read_first>
    - 13-VALIDATION.md (the Per-Task Verification Map + Sampling Rate — the commands per requirement)
    - supabase/tests/_all_5b.sh (what the full run does: db reset → pgTAP → Deno → Vitest → E2E)
    - package.json scripts (db:test, vitest) — confirm the exact invocations
    - The three Phase-13 migration files (confirm they are present and ordered after the latest existing migration 20260613130000)
  </read_first>
  <action>[BLOCKING — must run before verification is trusted; build/type checks pass without applying migrations and would otherwise give a false green.] Apply the three Phase-13 migrations to the LOCAL stack: run `supabase db reset` (clean re-apply of all migrations, the safest for catching ordering/idempotency issues) — or `supabase migration up` if the stack is already current and only the three new files are pending. Then run the full local test surface: `pnpm db:test` (all pgTAP, including the new life01_cancel_closes_chat.sql (thread closure both branches), life03_standby_notify.sql (standby-bump), and life02_file_report.sql), `pnpm vitest run` (StandbyList, LockDetail, thread-view), and `deno test supabase/functions/process-jobs/handlers_test.ts` (LIFE-04 kind-branch). If `_all_5b.sh` / the db:test loop needs the new test files registered, add `supabase/tests/life01_cancel_closes_chat.sql`, `supabase/tests/life03_standby_notify.sql`, and `supabase/tests/life02_file_report.sql` to its pgTAP loop. Capture pass/fail per requirement into the SUMMARY. All four LIFE requirements must be green locally before proceeding.</action>
  <verify>
    <automated>pnpm db:test && pnpm vitest run && deno test --allow-none supabase/functions/process-jobs/handlers_test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The three Phase-13 migrations apply with no error on the local stack (db reset / migration up exits 0)
    - `pnpm db:test` passes, including life01_cancel_closes_chat.sql (closure both branches), life03_standby_notify.sql (standby_bumped), and life02_file_report.sql (party insert + 42501 deny + RLS deny)
    - `pnpm vitest run` passes (StandbyList badge, LockDetail producers, thread-view cancelled case)
    - `deno test supabase/functions/process-jobs/handlers_test.ts` passes (all standby_roll kinds)
    - life01_cancel_closes_chat.sql, life03_standby_notify.sql, and life02_file_report.sql are all included in the pgTAP loop (_all_5b.sh / db:test)
  </acceptance_criteria>
  <done>All Phase-13 migrations are local-green and the full local suite passes for LIFE-01..04.</done>
</task>

<task type="auto">
  <name>Task 2: Run the Supabase security advisor after the Phase-13 DDL</name>
  <files>supabase/tests/_all_5b.sh</files>
  <read_first>
    - 13-RESEARCH.md "Security Domain" + the known threat patterns table (what to look for: over-broad RLS, RLS-less tables, USING(true))
    - CLAUDE.md: run the Supabase security advisor after every DDL; secure-by-default RLS
    - The three Phase-13 migrations (file_report grants/RLS, revoke_chat_thread grants, the recreated DEFINER RPCs)
  </read_first>
  <action>Against the LOCAL stack (post-apply), run the Supabase security advisor: `mcp__supabase__get_advisors` with type='security' (or the `supabase` advisor CLI). Review the findings and DIFF against the known/accepted baseline (per ACCT-01's pattern in STATE.md: authed SECURITY DEFINER RPCs surface as WARN and are an accepted pattern; an intentional RLS-no-policy table is INFO). Confirm: NO new ERROR-level finding; the new DEFINER RPCs (file_report, revoke_chat_thread, recreated match_cancel_lock / match_autowithdraw_user_conflicts) appear at most as the accepted authed-DEFINER WARN; reports gains no new permissive policy; no table was left RLS-less. Record the advisor delta in the SUMMARY. If any NEW ERROR appears, STOP and fix the offending migration before the gate passes (do not proceed to prod handoff).</action>
  <verify>
    <automated>echo "Advisor run is MCP/CLI-driven — record findings delta in SUMMARY; gate fails on any NEW ERROR-level finding"</automated>
  </verify>
  <acceptance_criteria>
    - The security advisor was run against the local stack after applying the Phase-13 DDL
    - No NEW ERROR-level finding vs the accepted baseline; the new DEFINER RPCs surface only as the accepted authed-DEFINER WARN
    - reports has no new permissive (select/update/delete) policy; no table left RLS-less
    - The advisor delta is recorded in the SUMMARY
  </acceptance_criteria>
  <done>Security advisor is clean (no new ERROR) for the Phase-13 DDL.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Gated prod-apply + edge-fn deploy handoff (HUMAN ONLY — never autonomous)</name>
  <what-built>All Phase-13 changes are local-green and advisor-clean: three migrations (LIFE-01/02/03), the LIFE-04 handler fix, two new edge fns (file-report, match-flag-no-show). None of this is live on prod yet. Prod apply + edge-fn deploy is a batched, human-gated step (CLAUDE.md), and there may be a one-time data backfill from the 13-01 prod verification.</what-built>
  <how-to-verify>
The developer performs the gated prod rollout against `ufufmcpnysvwtutpbian` (do NOT run these autonomously):
1. Review each migration's live SQL one more time (local-vs-prod drift watch), then apply the three migrations to prod (`supabase db push` or the batched apply per the project's gated procedure).
2. Re-run the security advisor against PROD after apply; confirm no new ERROR.
3. Deploy the edge functions: `supabase functions deploy process-jobs` (LIFE-04 handler — currently live at v9 with the broken handler), `supabase functions deploy file-report`, `supabase functions deploy match-flag-no-show`. Confirm process-jobs bumps past v9 and is ACTIVE.
4. BACKFILL DECISION (from 13-01-SUMMARY): if the prod verification found non-trivial wrong-state rows — (a) cancelled/no_show locks whose chat_threads are still not 'closed', and/or (b) historically no-op'd standby_roll conflict jobs — run the guarded one-time data sweep: for (a) `update chat_threads set state='closed', revoked_at=coalesce(revoked_at,now()) where lock_id in (select id from locks where status in ('cancelled','no_show')) and state<>'closed' and not legal_hold;` (mirrors revoke_chat_thread); for (b) decide with the developer whether to re-enqueue cascade jobs for affected locks or accept forward-only. If 13-01 recorded "forward-only, no backfill", skip this and document it.
5. Smoke-verify on prod: cancel a disposable test lock → its thread reads "this date was cancelled" / not messageable; file a test report → a reports row appears; (read-only) confirm a fresh conflicting accept now closes/withdraws conflicts.
  </how-to-verify>
  <resume-signal>Type "shipped" with: migrations applied + prod advisor clean + the three functions deployed (process-jobs version) + the backfill action taken (swept N rows / forward-only) + smoke-verify result. Or "holding" to defer prod apply.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → prod apply | Migrations + edge fns cross from local-green to live prod only via a human-gated step |
| advisor review | Security advisor gates the DDL before prod |
| prod data backfill | A one-time sweep mutates prod rows — human-gated, mirrors the DEFINER helper logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-15 | Tampering | unreviewed prod DDL / drift | mitigate | Local-green + advisor-clean required before the gated human apply; live SQL reviewed for drift (CLAUDE.md) |
| T-13-16 | Elevation of Privilege | new DEFINER RPC ships with a security regression | mitigate | Advisor run after DDL diffed against the accepted baseline; gate fails on any NEW ERROR |
| T-13-17 | Tampering | autonomous prod mutation | mitigate | Prod apply, edge-fn deploy, and the data backfill are checkpoint:human-action — never run autonomously (workflow.auto_advance ignored) |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (zero installs across all plans); slopcheck N/A |
</threat_model>

<verification>
- Local apply of all three migrations clean; full local suite (pgTAP + Vitest + Deno) green for LIFE-01..04
- Security advisor: no new ERROR vs baseline
- Prod apply + edge-fn deploy + backfill = explicit human-gated handoff (never autonomous)
</verification>

<success_criteria>
- All Phase-13 requirements proven green on a stack where the migrations are actually applied (no false-green)
- Advisor clean
- Developer has a complete, ordered prod-rollout + backfill checklist
</success_criteria>

<output>
Create `.planning/phases/13-lifecycle-correctness/13-05-SUMMARY.md` when done. Record the local-green run, the advisor delta, and the prod-rollout/backfill handoff state.
</output>
