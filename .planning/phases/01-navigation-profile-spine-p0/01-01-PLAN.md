---
phase: 01-navigation-profile-spine-p0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/BottomTabShell.tsx
  - apps/web/components/UserMenu.tsx
  - apps/web/components/__tests__/BottomTabShell.test.tsx
  - apps/web/components/__tests__/UserMenu.test.tsx
autonomous: true
requirements: [REQ-E2]
must_haves:
  truths:
    - "The bottom-nav 'dates' tab navigates to /matches (the user's matched/locked dates), not /my-nights"
    - "The bottom-nav 'profile' tab navigates to /account (the real hub), not /home"
    - "The UserMenu 'your profile' item navigates to /account, not /home"
    - "Active-tab state still lights correctly: /matches/* lights dates, /account/* lights profile"
  artifacts:
    - path: "apps/web/components/BottomTabShell.tsx"
      provides: "Tier-1 bottom nav with corrected dates + profile tab hrefs"
      contains: "href: '/matches'"
    - path: "apps/web/components/UserMenu.tsx"
      provides: "User menu with profile link repointed to /account"
      contains: "href: '/account'"
    - path: "apps/web/components/__tests__/BottomTabShell.test.tsx"
      provides: "Unit test asserting tab href map + active-state"
  key_links:
    - from: "apps/web/components/BottomTabShell.tsx"
      to: "/matches and /account"
      via: "TABS href map"
      pattern: "href: '/(matches|account)'"
---

<objective>
Fix bottom-nav semantics (REQ-E2 / D-04, D-05): repoint the "dates" tab from `/my-nights` → `/matches` and the "profile" tab from `/home` → `/account`, and repoint `UserMenu`'s "your profile" link from `/home` → `/account`. This is the smallest slice and a hard dependency for E3 (the repointed profile tab needs `/account` as its real destination) — it lands first.

This plan ALSO carries the **Wave-0 reconcile/clean-tree precondition** for the entire phase (the worktree/mobile-UX-fleet landmine): before any edit, confirm `main` is the edit target and no pending merge is mid-flight on the phase's shared files.

Purpose: Make the "profile" tab and "dates" tab reach real, correct destinations so the user is never sent to a marketing teaser (`/home`) or the wrong list (`/my-nights` = nights you POSTED, not dates you're going ON).
Output: Two corrected `href` values in `BottomTabShell.tsx`, one in `UserMenu.tsx`, and a unit test locking the tab map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-navigation-profile-spine-p0/01-CONTEXT.md
@.planning/phases/01-navigation-profile-spine-p0/01-PATTERNS.md
@.planning/phases/01-navigation-profile-spine-p0/01-UI-SPEC.md

<interfaces>
<!-- Current state verified against main 2026-06-03. Edit ONLY the main checkout. -->
BottomTabShell.tsx TABS array (lines 21-24):
  - line 22: { key: 'dates',   label: 'dates',   href: '/my-nights', icon: CalendarHeart }   ← change href to '/matches'
  - line 24: { key: 'profile', label: 'profile', href: '/home',      icon: UserRound }        ← change href to '/account'
  - isActive() (line 27-29) uses pathname.startsWith(`${href}/`) — no change needed; /matches/* lights dates, /account/* lights profile automatically.

UserMenu.tsx MENU_ITEMS (line 28-32):
  - line 29: { href: '/home', label: 'your profile' }   ← change href to '/account'
  - keep '/my-nights' ('your nights'), '/matches', '/messages' as-is (D-04 surfaces posted nights from the menu/hub, not the dates tab).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 reconcile / clean-tree precondition (HARD GATE for the whole phase)</name>
  <read_first>
    - .planning/phases/01-navigation-profile-spine-p0/01-RESEARCH.md (Pitfall 5 + Landmine flag)
    - .planning/phases/01-navigation-profile-spine-p0/01-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>
    Confirm the edit target is the MAIN checkout, not a worktree copy, before any file change in this phase. Run `git rev-parse --abbrev-ref HEAD` (must be `main`) and `git worktree list` (the 9+ `.claude/worktrees/*` entries are locked parallel-agent trees — NEVER edit files under those paths). Run `git status --short` on the four phase-shared files (`apps/web/components/BottomTabShell.tsx`, `apps/web/components/UserMenu.tsx`, `apps/web/app/account/page.tsx`, `apps/web/app/messages/[threadId]/page.tsx`) to confirm no mobile-UX-fleet merge is mid-flight on them (clean = safe to proceed). If any of those files are mid-merge / show conflict markers, STOP and report — do not edit under a conflicting merge. This gate protects every plan in the phase (E1/E2/E3/E4 all touch shared nav/profile files).
  </action>
  <verify>
    <automated>test "$(git rev-parse --abbrev-ref HEAD)" = "main" && ! git status --short apps/web/components/BottomTabShell.tsx apps/web/components/UserMenu.tsx apps/web/app/account/page.tsx "apps/web/app/messages/[threadId]/page.tsx" "apps/web/app/offers/[offerId]/page.tsx" "apps/web/app/matches/[lockId]/page.tsx" "apps/web/app/dates/[slug]/interested/page.tsx" | grep -q '^UU' && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - Current branch is `main` (or the explicitly intended base), not a `.claude/worktrees/` checkout.
    - No phase-shared file shows an in-progress merge conflict (`^UU` / conflict markers absent).
    - No edits in this phase target any path containing `.claude/worktrees/`.
  </acceptance_criteria>
  <done>Branch and clean-tree confirmed; safe to edit the main checkout for all of Phase 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repoint the two bottom-nav tab targets + UserMenu profile link</name>
  <files>apps/web/components/BottomTabShell.tsx, apps/web/components/UserMenu.tsx</files>
  <read_first>
    - apps/web/components/BottomTabShell.tsx (the file being modified — TABS array + isActive)
    - apps/web/components/UserMenu.tsx (the file being modified — MENU_ITEMS)
    - .planning/phases/01-navigation-profile-spine-p0/01-PATTERNS.md (§BottomTabShell + §UserMenu — exact 2-line + 1-line edits)
  </read_first>
  <behavior>
    - The 'dates' tab object's href === '/matches' (was '/my-nights') per D-04.
    - The 'profile' tab object's href === '/account' (was '/home') per D-05.
    - UserMenu MENU_ITEMS[0] ('your profile') href === '/account' (was '/home') per D-05.
    - isActive logic is UNCHANGED — /matches/[lockId] still lights the dates tab, /account/* still lights profile (startsWith match).
  </behavior>
  <action>
    In `BottomTabShell.tsx` TABS array: change the `dates` tab `href` from `/my-nights` to `/matches` (D-04 — "dates you're going ON" = matched/locked, not nights you posted) and the `profile` tab `href` from `/home` to `/account` (D-05). Do NOT change labels, icons, keys, `badge`, or `isActive()` — the `pathname.startsWith(`${href}/`)` logic already lights `/matches/*` as dates and `/account/*` as profile. Keep the active label ink-colored (pink-on-cream fails AA per the file's own comment). In `UserMenu.tsx` MENU_ITEMS: change `MENU_ITEMS[0]` ('your profile') `href` from `/home` to `/account` (D-05); leave `/my-nights` ('your nights'), `/matches`, `/messages` unchanged (D-04 surfaces posted nights from the menu, not the dates tab).
  </action>
  <verify>
    <automated>grep -q "href: '/matches'" apps/web/components/BottomTabShell.tsx && grep -q "href: '/account'" apps/web/components/BottomTabShell.tsx && grep -q "href: '/account'" apps/web/components/UserMenu.tsx && ! grep -qE "href: '/home'" apps/web/components/BottomTabShell.tsx apps/web/components/UserMenu.tsx && echo EDITS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `BottomTabShell.tsx` 'dates' tab line has `href: '/matches'`.
    - `BottomTabShell.tsx` 'profile' tab line has `href: '/account'`.
    - `UserMenu.tsx` "your profile" item has `href: '/account'`.
    - Neither file contains `href: '/home'` any longer.
    - `pnpm -w typecheck` (or `pnpm --filter web exec tsc --noEmit`) is clean.
  </acceptance_criteria>
  <done>Both tabs and the user-menu profile link reach their correct destinations; active-state behavior unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock the tab map with unit tests (REQ-E2)</name>
  <files>apps/web/components/__tests__/BottomTabShell.test.tsx, apps/web/components/__tests__/UserMenu.test.tsx</files>
  <read_first>
    - apps/web/components/__tests__/UserMenu.test.tsx (existing test to extend — establishes the RTL + mock pattern for these components)
    - apps/web/components/BottomTabShell.tsx (the rendered href attributes under test)
    - .planning/phases/01-navigation-profile-spine-p0/01-VALIDATION.md (REQ-E2 row + Wave 0 Gaps)
  </read_first>
  <behavior>
    - Test: rendered BottomTabShell exposes a 'dates' tab link whose href === '/matches'.
    - Test: rendered BottomTabShell exposes a 'profile' tab link whose href === '/account'.
    - Test: BottomTabShell renders no link with href === '/home'.
    - Test: UserMenu "your profile" item links to '/account' (extend the existing UserMenu.test.tsx).
    - Test (active-state regression): with pathname '/matches/abc', the dates tab carries aria-current="page"; with '/account', the profile tab does.
  </behavior>
  <action>
    Create `BottomTabShell.test.tsx` (Wave-0 gap — UserMenu.test.tsx exists, this one does not). Follow the existing `UserMenu.test.tsx` RTL + `next/navigation` mock pattern (mock `usePathname` to drive `isActive`). Assert the rendered tab `href` map (dates→`/matches`, profile→`/account`, no `/home`) and the active-state regression (mocked pathname `/matches/abc` lights dates via `aria-current="page"`, `/account` lights profile). Extend the existing `UserMenu.test.tsx` to assert the "your profile" item now links to `/account`. Use the existing component/jsdom test setup; keep runtime under ~30s.
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/components/__tests__/BottomTabShell.test.tsx apps/web/components/__tests__/UserMenu.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `BottomTabShell.test.tsx` exists and asserts dates→`/matches`, profile→`/account`, and absence of `/home`.
    - Active-state regression assertion passes (`/matches/*`→dates, `/account`→profile).
    - `UserMenu.test.tsx` asserts "your profile" → `/account`.
    - `pnpm vitest run apps/web/components/__tests__/BottomTabShell.test.tsx apps/web/components/__tests__/UserMenu.test.tsx` is green.
  </acceptance_criteria>
  <done>REQ-E2 is locked by passing unit tests; tab semantics cannot silently regress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→client nav | Tab/menu clicks change route; no data crosses a trust boundary in this plan (pure client navigation targets). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | nav href targets | accept | All targets (`/matches`, `/account`) are existing auth-gated tab roots (`getUser()`→`redirect('/login?next=...')`); a wrong/forged href cannot reach data — RLS + page gate is the backstop. No new exposure. |
| T-01-02 | Information Disclosure | profile tab destination | mitigate | Repointing to `/account` (already RLS-gated, `auth.uid()`-scoped) does not widen any read; `/account` already redirects unauthenticated users to login. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (zero new deps — confirmed by RESEARCH Package Legitimacy Audit "Not applicable"). |
</threat_model>

<verification>
- `pnpm vitest run apps/web/components/__tests__/BottomTabShell.test.tsx apps/web/components/__tests__/UserMenu.test.tsx` green.
- `pnpm -w typecheck` clean.
- Grep gate: no `href: '/home'` remains in either nav file.
- (Wave-merge) Playwright `route-smoke`: clicking the profile tab lands on `/account`, dates tab lands on `/matches`.
</verification>

<success_criteria>
- The "dates" tab reaches `/matches`; the "profile" tab reaches `/account`; `UserMenu` "your profile" reaches `/account`.
- Active-tab highlighting still correct for deep sub-routes.
- Unit tests lock the tab map; typecheck clean.
- Wave-0 reconcile gate passed (main checkout confirmed, no mid-flight merge on shared files).
</success_criteria>

<output>
Create `.planning/phases/01-navigation-profile-spine-p0/01-01-SUMMARY.md` when done.
</output>
