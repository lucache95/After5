---
phase: 05-progressive-reveal-p2
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - supabase/migrations/20260606120100_e16_dispatch_identity_revealed.sql
  - tailwind.config.ts
  - apps/web/app/matches/[lockId]/RevealModal.tsx
  - apps/web/app/matches/[lockId]/LockDetail.tsx
  - apps/web/e2e/05-reveal-ceremony.spec.ts
  - apps/web/e2e/5b-happy-path.spec.ts
autonomous: true
requirements: [REQ-E16]
must_haves:
  truths:
    - "Crossing the post-lock threshold dispatches an identity_revealed notification to BOTH parties at match_accept_offer AND match_resolve_reciprocal"
    - "identity_revealed respects matches_enabled consent (sibling of new_match)"
    - "Crossing the threshold renders an animated un-blur ceremony in RevealModal with a sonner toast, gated on justLocked"
    - "With prefers-reduced-motion the clear photo shows immediately with an opacity cross-fade and the toast still fires"
  artifacts:
    - path: "supabase/migrations/20260606120100_e16_dispatch_identity_revealed.sql"
      provides: "identity_revealed dispatched at both lock RPCs + added to the matches_enabled consent branch"
      contains: "identity_revealed"
    - path: "apps/web/app/matches/[lockId]/RevealModal.tsx"
      provides: "framer-motion unblur dissolve gated on a ceremony/justLocked prop + reduced-motion fallback"
    - path: "apps/web/app/matches/[lockId]/LockDetail.tsx"
      provides: "auto-open RevealModal in ceremony mode + fire sonner reveal toast on justLocked"
    - path: "apps/web/e2e/05-reveal-ceremony.spec.ts"
      provides: "ceremony visual + reduced-motion + toast assertions"
  key_links:
    - from: "match_accept_offer / match_resolve_reciprocal"
      to: "dispatch_notification"
      via: "identity_revealed dispatch to both parties alongside new_match"
      pattern: "dispatch_notification\\(.*identity_revealed"
    - from: "apps/web/app/matches/[lockId]/LockDetail.tsx"
      to: "RevealModal"
      via: "auto-open in ceremony mode on justLocked"
      pattern: "justLocked"
---

<objective>
Rung 3 of the reveal ladder — the threshold (post-lock) — plus the REQ-E16 reveal moment, top to bottom: the full reveal + ceremony fire the moment both sides lock. Wires the `identity_revealed` dispatch into BOTH lock DEFINER RPCs (mirroring the existing `new_match` dispatch and respecting `matches_enabled` consent), and enhances the existing `RevealModal` into an animated un-blur ceremony (framer-motion dissolve + one Barbiecore flourish + sonner toast), gated on `justLocked` with a reduced-motion fallback.

The clear photo is ALREADY signed server-side in `matches/[lockId]/page.tsx` via `signClearUrls` gated by `match_reveal_allowed_pair` — this plan does NOT change clear-photo signing; it adds the dispatch + the animation. Implements D-02, D-04, and REQ-E16.

Purpose: deliver the emotional payoff of the product — the earned face reveal — without re-opening any privacy gate.
Output: identity_revealed dispatched at both lock sites (local-applied + advisor-clean); ceremony in RevealModal; ceremony + dispatch e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-progressive-reveal-p2/05-CONTEXT.md
@.planning/phases/05-progressive-reveal-p2/05-RESEARCH.md
@.planning/phases/05-progressive-reveal-p2/05-PATTERNS.md
@.planning/phases/05-progressive-reveal-p2/05-UI-SPEC.md

<interfaces>
<!-- Extracted from codebase. -->

The two lock dispatch sites (supabase/migrations/20260527127800_p5_match_cohort_allowlist.sql):
- match_accept_offer (CREATE OR REPLACE at line 252, `SET search_path TO 'public'` at 256) — new_match dispatch at lines 357-358:
```sql
perform dispatch_notification(cand, 'new_match', jsonb_build_object('instance', inst, 'lock_id', lid));
perform dispatch_notification(cre,  'new_match', jsonb_build_object('instance', inst, 'lock_id', lid));
```
- match_resolve_reciprocal (CREATE OR REPLACE at line 438, `SET search_path TO 'public'` at 442) — new_match dispatch at lines 538-540:
```sql
perform dispatch_notification(cand, 'new_match', jsonb_build_object('instance', p_chosen_instance, 'lock_id', lid, 'via', 'reciprocal'));
perform dispatch_notification(cre,  'new_match', jsonb_build_object('instance', p_chosen_instance, 'lock_id', lid, 'via', 'reciprocal'));
```
These re-CREATE with NO signature change, so grants survive — keep the existing `SET search_path TO 'public'` on each.

The consent gate (supabase/migrations/20260525123600_p2_dispatch_notification.sql, line 52):
```sql
elsif p_type = 'new_match' and not v_prefs.matches_enabled then
  v_allowed := false;
```
identity_revealed must JOIN this branch → `p_type in ('new_match','identity_revealed')` (resolved decision: respect matches_enabled, sibling of new_match).

The reduced-motion + framer-motion analog (apps/web/app/matches/[lockId]/MatchConfirmation.tsx, lines 8/14/41):
```typescript
import { motion, useReducedMotion } from 'framer-motion';
const reduce = useReducedMotion();
{!reduce && ( /* decorative motion only; the announcement survives reduced-motion */ )}
```

RevealModal today (apps/web/app/matches/[lockId]/RevealModal.tsx): a vaul Drawer.Root on bg-shell-base rendering <ProfileCard> with signed clear `photos: string[]` + `prompts`. No entrance animation currently.

LockDetail today: `<MatchConfirmation name={name} show={justLocked} />` (line 68); RevealModal mounts (line 85) opened by the quiet "see their profile" button (lines 78-85); `import { toast } from 'sonner'` (line 6), existing usage `toast('that date is called off.')` (line 55).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Dispatch identity_revealed at both lock RPCs + consent branch + LOCAL apply + advisor</name>
  <files>supabase/migrations/20260606120100_e16_dispatch_identity_revealed.sql</files>
  <read_first>
    - supabase/migrations/20260527127800_p5_match_cohort_allowlist.sql (match_accept_offer at line 252; match_resolve_reciprocal at line 438; new_match dispatch at 357-358 and 538-540; the full RPC bodies to re-CREATE verbatim plus the added dispatch)
    - supabase/migrations/20260525123600_p2_dispatch_notification.sql (the consent gate, line 52)
    - .planning/phases/05-progressive-reveal-p2/05-PATTERNS.md (the e16 migration assignment + Pitfall 3: both sites)
    - .planning/STATE.md (gated-prod-apply rule; E8 dispatch precedent at line 90; prod ref ufufmcpnysvwtutpbian — do NOT touch prod)
  </read_first>
  <action>
    Create supabase/migrations/20260606120100_e16_dispatch_identity_revealed.sql. It does three re-CREATEs (no signature changes, so grants survive — keep each function's existing `SET search_path`):
    1. match_accept_offer: re-CREATE the full function body verbatim, ADDING after the two new_match dispatches (lines 357-358) two identity_revealed dispatches to BOTH parties, deep-linked to /matches/[lockId]: `perform dispatch_notification(cand, 'identity_revealed', jsonb_build_object('lock_id', lid, 'instance', inst));` and the same for `cre`.
    2. match_resolve_reciprocal: re-CREATE the full function body verbatim, ADDING after the two new_match dispatches (lines 538-540) the same two identity_revealed dispatches to both parties (carry `'via','reciprocal'` in the payload to match the sibling new_match style).
    3. dispatch_notification: re-CREATE so the consent branch at line 52 becomes `p_type in ('new_match','identity_revealed') and not v_prefs.matches_enabled` — identity_revealed now respects matches_enabled consent (resolved decision: sibling of new_match). Re-CREATE the full function verbatim with only this branch changed; keep its existing search_path + grants.
    Dispatch ONLY to the two resolved lock participants (cand/cre) already bound inside each DEFINER body — never an extra user (Spoofing mitigation).
    Apply to the LOCAL Supabase stack and regenerate TypeScript types from the LOCAL db. Build/type checks alone are a false positive.
    Run the Supabase security advisor after the DDL; confirm only the established accepted DEFINER-executable warning pattern remains (no new findings). Do NOT apply to prod — gated to the 05-04 checkpoint.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260606120100_e16_dispatch_identity_revealed.sql | grep -c "identity_revealed"</automated>
  </verify>
  <acceptance_criteria>
    - identity_revealed is dispatched at BOTH match_accept_offer and match_resolve_reciprocal, to both cand and cre (>= 4 dispatch_notification(...identity_revealed...) calls total; `grep -c "dispatch_notification(.*identity_revealed" <file>` >= 4).
    - dispatch_notification's matches_enabled branch includes identity_revealed (`grep -c "in ('new_match','identity_revealed')" <file>` >= 1, or equivalent ordering).
    - Local apply succeeds; security advisor shows no NEW findings vs. the established accepted DEFINER pattern.
    - No `USING(true)` introduced; each re-CREATEd function keeps its SET search_path.
  </acceptance_criteria>
  <done>identity_revealed dispatched at both lock sites to both parties, gated by matches_enabled consent, applied LOCAL, advisor clean, prod untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Promote sage token + ceremony unblur dissolve in RevealModal</name>
  <files>tailwind.config.ts, apps/web/app/matches/[lockId]/RevealModal.tsx</files>
  <read_first>
    - apps/web/app/matches/[lockId]/RevealModal.tsx (the vaul sheet + ProfileCard render; no entrance animation today)
    - apps/web/app/matches/[lockId]/MatchConfirmation.tsx (the canonical useReducedMotion + framer-motion pattern, lines 8/14/41)
    - tailwind.config.ts (shell.* token block — add sage per UI-SPEC sage-promotion note)
    - .planning/phases/05-progressive-reveal-p2/05-UI-SPEC.md (The Ceremony interaction contract; Color §sage promotion; reduced-motion fallback)
    - .planning/phases/05-progressive-reveal-p2/05-PATTERNS.md (RevealModal assignment)
  </read_first>
  <action>
    If the ceremony uses a success/confirmation tick, promote `sage` #5CDBA0 to tailwind.config.ts (as `shell.sage` or top-level `sage`) — never hardcode the hex in a component (CLAUDE.md: no hardcoded hex). If no tick is used, skip the token and fall back to shell.accent; do not add an unused token.
    Add a `ceremony` (or `justLocked`) boolean prop to RevealModal. When true and `!useReducedMotion()`: mount the clear photo at the rung-2 held state and animate CSS `filter` blur(12px)→blur(0px) over ~900ms with `ease:[0.22,1,0.36,1]`, paired with `scale` 1.02→1.0 and `opacity` 0.85→1 (framer-motion). Add ONE hot-pink `shell.accent` flourish element (a single sparkle or soft radial glow behind the polaroid, deterministic tiny rotation) fading in ~600ms — one element, NOT a particle burst. Total choreography ≤ ~1.4s, front-loaded on the unblur.
    Reduced-motion: when `useReducedMotion()` is true, render the clear photo immediately at full clarity with a single ≤200ms opacity cross-fade, no blur/scale/glow motion, the flourish static. The source asset is the now-signed CLEAR photo already passed in as `photos` (do NOT sign anything here).
    When `ceremony` is false (return visit), open static — no entrance animation (Pitfall 5).
    Handle the three states per UI-SPEC: loading (signing in flight → hold the light-blur, no spinner), no-photo (ProfileCard Polaroid gradient fallback), error (keep light-blur + quiet "couldn't load the photo. pull to retry. the plan's still locked in." line).
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - RevealModal accepts a ceremony/justLocked prop and gates the unblur animation on it (`grep -E "ceremony|justLocked" apps/web/app/matches/[lockId]/RevealModal.tsx` matches).
    - RevealModal imports useReducedMotion (`grep -c "useReducedMotion" apps/web/app/matches/[lockId]/RevealModal.tsx` >= 1).
    - No hardcoded hex in RevealModal (`grep -E "#[0-9A-Fa-f]{6}" apps/web/app/matches/[lockId]/RevealModal.tsx` returns nothing); if sage is used it exists in tailwind.config.ts.
    - `pnpm --filter web exec tsc --noEmit` passes.
  </acceptance_criteria>
  <done>RevealModal animates an unblur dissolve gated on ceremony/justLocked with a reduced-motion fallback and one pink flourish; sage promoted to config if used.</done>
</task>

<task type="auto">
  <name>Task 3: Fire ceremony on justLocked + reveal toast + ceremony/dispatch e2e</name>
  <files>apps/web/app/matches/[lockId]/LockDetail.tsx, apps/web/e2e/05-reveal-ceremony.spec.ts, apps/web/e2e/5b-happy-path.spec.ts</files>
  <read_first>
    - apps/web/app/matches/[lockId]/LockDetail.tsx (MatchConfirmation mount on justLocked line 68; RevealModal mount line 85; toast import line 6 + usage line 55; "see their profile" button lines 78-85)
    - apps/web/app/matches/[lockId]/RevealModal.tsx (the ceremony prop from Task 2)
    - apps/web/e2e/5b-happy-path.spec.ts (the lock/accept happy path to extend for the dispatch assertion)
    - apps/web/e2e/05-reveal-feed.spec.ts (privacy-invariant helper shape to mirror — do NOT edit it)
    - .planning/phases/05-progressive-reveal-p2/05-UI-SPEC.md (Copywriting Contract — toast/headline/sub-line/CTA strings)
    - .planning/phases/05-progressive-reveal-p2/05-PATTERNS.md (LockDetail assignment)
  </read_first>
  <action>
    In LockDetail.tsx: when `justLocked` is true, auto-open RevealModal in ceremony mode (pass the ceremony prop) and fire a sonner toast with the reveal copy `the face behind the night. say hi.` (lowercase, no em-dash — mirror the existing `toast(...)` usage at line 55). Keep the on-demand "see their profile" button opening RevealModal QUIET (ceremony=false, no animation) for return visits (justLocked=false). Use the UI-SPEC copy verbatim: reveal headline `you cooked.`, sub-line `here's {first_name}.`, primary CTA `slide in` (opens chat with the now-revealed match). Introduce no new em-dashes.
    Create apps/web/e2e/05-reveal-ceremony.spec.ts: reach a just-locked match, assert the ceremony renders (clear photo settles in, the unblur runs), the sonner toast fires with the reveal copy, and it settles into the Tier-3 ProfileCard. Add a reduced-motion case (emulate `prefers-reduced-motion: reduce`): assert no blur animation, immediate clear photo + opacity cross-fade, toast still fires.
    Extend apps/web/e2e/5b-happy-path.spec.ts (and its reciprocal path if present): after the lock, assert an identity_revealed notification row is dispatched to BOTH parties (mirror the existing notification-row assertions in that spec). This covers Pitfall 3 (both lock sites).
  </action>
  <verify>
    <automated>pnpm --filter web exec playwright test e2e/05-reveal-ceremony.spec.ts e2e/5b-happy-path.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - LockDetail auto-opens RevealModal in ceremony mode on justLocked (`grep -E "ceremony|justLocked" apps/web/app/matches/[lockId]/LockDetail.tsx` matches) and fires a sonner toast with `the face behind the night. say hi.`.
    - No new em-dash (`grep -c "—" apps/web/app/matches/[lockId]/LockDetail.tsx` does not increase).
    - 05-reveal-ceremony.spec.ts passes including the reduced-motion case.
    - 5b-happy-path.spec.ts asserts identity_revealed dispatched to both parties post-lock and passes.
  </acceptance_criteria>
  <done>The ceremony fires once on justLocked with the reveal toast; return visits stay quiet; e2e covers ceremony, reduced-motion, and both-party dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → lock DEFINER RPC | the atomic lock transition; dispatch fires inside the DEFINER body to resolved participants only |
| RevealModal → clear photo | clear photo already signed upstream gated by match_reveal_allowed_pair; this plan only animates it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Spoofing | identity_revealed dispatched to wrong/extra user | mitigate | dispatch only to cand/cre already resolved inside the DEFINER body; payload is server-constructed jsonb |
| T-05-08 | Tampering | re-CREATE of lock RPCs / dispatch_notification drops grants or search_path | mitigate | no signature change (grants survive); re-CREATE verbatim keeping each function's SET search_path; run security advisor after DDL |
| T-05-09 | Information Disclosure | ceremony re-signs or exposes the clear path improperly | accept | RevealModal only animates the already-gated signed clear url passed from matches/[lockId]/page.tsx; this plan signs nothing new |
| T-05-10 | Denial of Service | identity_revealed wired at only one lock site (reciprocal misses it) | mitigate | dispatch added at BOTH match_accept_offer AND match_resolve_reciprocal; 5b reciprocal path asserts the row |
</threat_model>

<verification>
- `grep` gates on the migration (both sites, consent branch, no USING(true)).
- `pnpm --filter web exec playwright test e2e/05-reveal-ceremony.spec.ts e2e/5b-happy-path.spec.ts` green.
- `pnpm --filter web exec tsc --noEmit` green.
- Local migration applied + types regenerated; advisor clean.
- Visual-verify @420px (forced-local) deferred to the phase gate (05-04): ceremony unblur + flourish + toast + reduced-motion.
</verification>

<success_criteria>
- identity_revealed dispatched to both parties at both lock RPCs, respecting matches_enabled, applied local + advisor clean, prod untouched.
- The ceremony renders an animated unblur + one flourish + sonner toast on justLocked, with a reduced-motion fallback; return visits stay quiet.
</success_criteria>

<output>
Create `.planning/phases/05-progressive-reveal-p2/05-03-SUMMARY.md` when done.
</output>
