---
phase: 05-progressive-reveal-p2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/after5/photos.ts
  - apps/web/lib/after5/photos.test.ts
  - packages/api-client/src/feed.ts
  - packages/api-client/src/feed.test.ts
  - supabase/migrations/20260606120000_e15_browse_feed_host_hint.sql
  - apps/web/app/feed/page.tsx
  - apps/web/app/feed/NightCard.tsx
  - apps/web/app/feed/NightDetailSheet.tsx
  - apps/web/e2e/05-reveal-feed.spec.ts
autonomous: true
requirements: [REQ-E15]
must_haves:
  truths:
    - "On the feed card a searcher sees a heavily-blurred host avatar plus the host first name and age, and the face is unreadable (D-01)"
    - "On the night detail sheet the same blurred-avatar+name+age hint shows and the prior 'host stays anonymous' copy is softened"
    - "No clear host photo signed URL is ever requested on the feed or detail surface pre-lock"
  artifacts:
    - path: "apps/web/lib/after5/photos.ts"
      provides: "signBlurredUrls() — signs blurred storage paths with no reveal gate"
      contains: "export async function signBlurredUrls"
    - path: "packages/api-client/src/feed.ts"
      provides: "FeedNight extended with host_blurred_photo_url, host_first_name, host_age"
      contains: "host_blurred_photo_url"
    - path: "supabase/migrations/20260606120000_e15_browse_feed_host_hint.sql"
      provides: "browse_feed_for_viewer widened with 3 host-hint columns + re-applied grants"
      contains: "host_blurred_photo_url"
    - path: "apps/web/app/feed/NightCard.tsx"
      provides: "rung-1 blurred avatar + {first_name}, {age} label, CSS blur(8px)"
    - path: "apps/web/e2e/05-reveal-feed.spec.ts"
      provides: "rung-1 visual + privacy-invariant network assertion for feed/detail"
  key_links:
    - from: "apps/web/app/feed/page.tsx"
      to: "signBlurredUrls"
      via: "server-side signing of host_blurred_photo_url paths after browseFeed"
      pattern: "signBlurredUrls"
    - from: "apps/web/app/feed/page.tsx"
      to: "browse_feed_for_viewer"
      via: "browseFeed RPC returning the 3 host-hint columns"
      pattern: "browseFeed"
---

<objective>
Rung 1 of the reveal ladder (pre-match), top to bottom: the searcher senses a real person on the feed (blurred face + first name + age) but cannot judge the face. Delivers `signBlurredUrls()`, the widened feed RPC, the `FeedNight` host-hint contract, server-side signing in the feed loader, and the blurred-avatar render on both feed surfaces. Also lands the shared Wave-0 test scaffolds (`photos.test.ts`, the rung-1 e2e spec carrying the privacy-invariant network assertion).

This is the foundation slice: `signBlurredUrls()` and the `FeedNight` shape are consumed by the rung-2 plan (05-02). Implements D-01 and the rung-1 half of REQ-E15.

Purpose: make "swipe on the date, not the face" real at the entry point of the loop without leaking the clear face.
Output: blurred host hint on feed card + detail; widened RPC (local-applied + advisor-clean); signing util + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-progressive-reveal-p2/05-CONTEXT.md
@.planning/phases/05-progressive-reveal-p2/05-RESEARCH.md
@.planning/phases/05-progressive-reveal-p2/05-PATTERNS.md
@.planning/phases/05-progressive-reveal-p2/05-UI-SPEC.md

<interfaces>
<!-- Extracted from codebase. Executor uses these directly — no exploration needed. -->

From apps/web/lib/after5/photos.ts (the pattern signBlurredUrls mirrors, line 127; BUCKET='profile-photos' at line 20):
```typescript
export async function signClearUrls(client: After5Client, paths: string[], ttl = 600): Promise<string[]> {
  if (paths.length === 0) return [];
  const { data, error } = await client.storage.from(BUCKET).createSignedUrls(paths, ttl);
  if (error) throw new Error(error.message);
  return (data ?? []).map((d) => d.signedUrl).filter((u): u is string => !!u);
}
```

From packages/api-client/src/feed.ts (FeedNight today, lines 4-16 — extend with exactly 3 fields, mirror the `// E10 (REQ-E10 / D-03):` annotation style on line 12):
```typescript
export interface FeedNight {
  date_instance_id: string; city_id: string; time_window_start: string;
  /* ... existing fields ... */
  fit: boolean;
  // ADD (E15 / REQ-E15 / D-01): limited host hint — blind contract preserved minus these 3.
  // host_blurred_photo_url: string | null;
  // host_first_name: string | null;
  // host_age: number | null;
}
```

From supabase/migrations/20260605120500_e10_browse_feed_filters.sql (the IMMEDIATELY PRIOR widen of this exact fn — copy its full structure):
- line 42: `drop function if exists browse_feed_for_viewer(uuid, geography, timestamptz, uuid, int);`
- line 43: `create or replace function browse_feed_for_viewer(...)`
- line 49-54: `returns table (... fit boolean)` — add the 3 host-hint cols AFTER `fit boolean`
- the creator join is ALREADY in scope: `join profiles cr on cr.id = di.creator_id` (cr.first_name, cr.age, cr.blurred_photo_url available)
- grant tail (lines after `$fn$;`) — MUST be copied verbatim because adding columns changes the signature:
```sql
revoke execute on function browse_feed_for_viewer(uuid, geography, timestamptz, uuid, int) from public;
revoke execute on function browse_feed_for_viewer(uuid, geography, timestamptz, uuid, int) from anon;
grant  execute on function browse_feed_for_viewer(uuid, geography, timestamptz, uuid, int) to authenticated;
```
- keep `language sql security definer set search_path = public, extensions` unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 scaffolds + signBlurredUrls + FeedNight contract</name>
  <files>apps/web/lib/after5/photos.ts, apps/web/lib/after5/photos.test.ts, packages/api-client/src/feed.ts, packages/api-client/src/feed.test.ts, apps/web/e2e/05-reveal-feed.spec.ts</files>
  <read_first>
    - apps/web/lib/after5/photos.ts (the file being modified; signClearUrls at line 127 is the analog)
    - packages/api-client/src/feed.ts (FeedNight interface lines 4-16; getNightDetail defensive mapper lines 230-301 for str()/num() pattern)
    - packages/api-client/src/feed.test.ts (existing vitest shape to extend)
    - apps/web/e2e/5b-happy-path.spec.ts (Playwright shape to mirror for the new spec)
    - .planning/phases/05-progressive-reveal-p2/05-PATTERNS.md (photos.ts + feed.ts assignments)
  </read_first>
  <behavior>
    - signBlurredUrls([]) returns [] (empty-array short-circuit, no storage call).
    - signBlurredUrls(client, paths) calls client.storage.from('profile-photos').createSignedUrls(paths, 600) and returns the signedUrl strings, filtering nulls. No reveal gate (blurred reads are authorized for any authenticated viewer).
    - FeedNight type carries host_blurred_photo_url: string | null, host_first_name: string | null, host_age: number | null.
    - feed.test.ts asserts a browseFeed-mapped row exposes the 3 new fields.
  </behavior>
  <action>
    Add `signBlurredUrls(client: After5Client, paths: string[], ttl = 600): Promise<string[]>` to apps/web/lib/after5/photos.ts, byte-for-byte mirroring signClearUrls (same BUCKET, same createSignedUrls, ttl=600), with a JSDoc line stating blurred reads need NO reveal gate (authorized by storage policy profile_photos_blurred_read_v2). Named export only.
    Create apps/web/lib/after5/photos.test.ts (vitest) covering signBlurredUrls: empty-array short-circuit (no client call), and a mocked-client happy path returning signed urls. This is the Wave-0 test that did not exist.
    Extend the FeedNight interface in packages/api-client/src/feed.ts with exactly the 3 fields above and nothing more (no creator_id, no itinerary_id) — tag them `// E15 (REQ-E15 / D-01): limited host hint`. Do NOT change the browseFeed cast beyond carrying the new fields; the RPC returns the relative blurred PATH and signing happens app-side in feed/page.tsx (Task 3).
    Extend packages/api-client/src/feed.test.ts to assert the 3 new fields are present on a mapped row.
    Create the Wave-0 e2e scaffold apps/web/e2e/05-reveal-feed.spec.ts mirroring 5b-happy-path.spec.ts auth/setup; include the privacy-invariant network assertion helper (capture all `storage/v1/object/sign` requests on the feed + detail pages and assert every signed path ends in `_blurred.jpg`, never a bare `<uid>/<id>.jpg`). The visual rung-1 assertions land in Task 4; this task lands the spec skeleton + the network helper so later tasks fill behavior.
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/lib/after5/photos.test.ts packages/api-client/src/feed.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function signBlurredUrls" apps/web/lib/after5/photos.ts` returns 1.
    - `grep -c "host_blurred_photo_url" packages/api-client/src/feed.ts` >= 1.
    - `pnpm vitest run apps/web/lib/after5/photos.test.ts packages/api-client/src/feed.test.ts` passes; photos.test.ts contains an empty-array short-circuit assertion.
    - apps/web/e2e/05-reveal-feed.spec.ts exists and contains a `storage/v1/object/sign` capture assertion checking for `_blurred.jpg`.
  </acceptance_criteria>
  <done>signBlurredUrls exported + unit-tested; FeedNight carries the 3 host-hint fields; the rung-1 e2e spec scaffold with the privacy-invariant network helper exists.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Widen browse_feed_for_viewer + LOCAL apply + advisor</name>
  <files>supabase/migrations/20260606120000_e15_browse_feed_host_hint.sql</files>
  <read_first>
    - supabase/migrations/20260605120500_e10_browse_feed_filters.sql (the IMMEDIATELY PRIOR widen of this exact fn — copy its full structure: drop → create or replace → grant tail)
    - .planning/phases/05-progressive-reveal-p2/05-PATTERNS.md (the e15 migration assignment + Pitfall 2)
    - .planning/STATE.md (gated-prod-apply rule, lines 46 + 106; prod ref ufufmcpnysvwtutpbian — do NOT touch prod here)
  </read_first>
  <action>
    Create supabase/migrations/20260606120000_e15_browse_feed_host_hint.sql. Copy the full structure of the e10 migration: `drop function if exists browse_feed_for_viewer(uuid, geography, timestamptz, uuid, int);` then `create or replace function browse_feed_for_viewer(...) returns table (...)` keeping all existing columns and ADDING 3 columns after `fit boolean`: `host_blurred_photo_url text, host_first_name text, host_age int`. In the SELECT, project `cr.blurred_photo_url, cr.first_name, cr.age` from the already-in-scope `join profiles cr on cr.id = di.creator_id`. Project ONLY these 3 — never cr.id, cr.email, cr.clear_photo_url, cr.instagram. Keep `language sql security definer set search_path = public, extensions` unchanged. Copy the revoke-public/revoke-anon/grant-authenticated tail VERBATIM (the signature change resets privileges). Annotate the migration header that this widens the e10 RPC with the E15 host hint.
    Apply to the LOCAL Supabase stack (`supabase migration up`, or `supabase db reset` if the local ledger requires it) and regenerate TypeScript types from the LOCAL db (the project's typegen command). Build/type checks alone are a false positive — types must come from the live local schema.
    Run the Supabase security advisor after the DDL and confirm only the established accepted DEFINER-executable warning pattern remains (no new findings, no anon-executable on browse_feed_for_viewer).
    Do NOT apply to prod. Prod apply is gated to the human checkpoint in 05-04.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260606120000_e15_browse_feed_host_hint.sql | grep -c "host_blurred_photo_url"</automated>
  </verify>
  <acceptance_criteria>
    - The migration file contains the 3 host-hint columns in both RETURNS TABLE and the SELECT, and does NOT reference cr.id / cr.email / cr.clear_photo_url / cr.instagram (`grep -E "cr\.(id|email|clear_photo_url|instagram)" <file>` returns nothing).
    - The verbatim grant tail (revoke from public, revoke from anon, grant to authenticated) is present.
    - Local apply succeeds and local typegen reflects the 3 new return columns on the browse_feed_for_viewer row type.
    - Supabase security advisor after DDL shows no NEW findings vs. the established accepted DEFINER pattern; browse_feed_for_viewer is not anon-executable.
  </acceptance_criteria>
  <done>Feed RPC widened with exactly 3 host-hint columns, applied LOCAL, types regenerated from the live local schema, advisor clean. Prod NOT touched.</done>
</task>

<task type="auto">
  <name>Task 3: Sign host blurred url in the feed loader</name>
  <files>apps/web/app/feed/page.tsx</files>
  <read_first>
    - apps/web/app/feed/page.tsx (the SSR loader; existing createClient → getUser → browseFeed → SwipeDeck flow, ~lines 9-23; .catch(() => []) resilience at ~line 21)
    - apps/web/lib/after5/photos.ts (signBlurredUrls from Task 1)
    - .planning/phases/05-progressive-reveal-p2/05-PATTERNS.md (feed/page.tsx assignment)
  </read_first>
  <action>
    In apps/web/app/feed/page.tsx, after `browseFeed(...)`, collect the non-null `host_blurred_photo_url` relative paths across the returned FeedNight[], call `signBlurredUrls(supabase, paths)` with the RLS'd viewer session client already in scope, and map the signed URLs back onto each FeedNight's `host_blurred_photo_url` (path → signed url). Pass the enriched feed into SwipeDeck unchanged otherwise. Wrap the signing in the existing resilience pattern: a signing failure degrades to a null avatar (no host hint), never crashes the feed — mirror the `.catch(() => [])` style. Keep `export const dynamic = 'force-dynamic'`.
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "signBlurredUrls" apps/web/app/feed/page.tsx` returns 1.
    - `grep -c "signClearUrls" apps/web/app/feed/page.tsx` returns 0 (the clear path is NEVER signed on the feed — privacy invariant).
    - `pnpm --filter web exec tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The feed loader signs the blurred host paths server-side and passes signed urls into SwipeDeck; clear signing is never reachable from the feed.</done>
</task>

<task type="auto">
  <name>Task 4: Render rung-1 blurred avatar + name/age on card + detail + soften copy</name>
  <files>apps/web/app/feed/NightCard.tsx, apps/web/app/feed/NightDetailSheet.tsx, apps/web/e2e/05-reveal-feed.spec.ts</files>
  <read_first>
    - apps/web/app/feed/NightCard.tsx (meta-row + sticker render lines 91-178; next/image lines 3, 76-84; fit pill lines 106-110 — the small on-scrim lowercase label model)
    - apps/web/app/feed/NightDetailSheet.tsx (host-anonymous copy at line 137 and lines 289-291; cover pattern lines 106-107, 143)
    - apps/web/app/matches/[lockId]/MatchConfirmation.tsx (NOT for motion here — but confirms cn() usage convention)
    - .planning/phases/05-progressive-reveal-p2/05-UI-SPEC.md (Reveal-Ladder Visual Contract; Copywriting Contract; Identity-led not photo-led)
    - .planning/phases/05-progressive-reveal-p2/05-PATTERNS.md (NightCard + NightDetailSheet assignments)
  </read_first>
  <action>
    NightCard: render the signed `host_blurred_photo_url` as a 48px (`h-12 w-12`) circular thumbnail in the card footer/meta row (secondary to the full-bleed cover — the experience leads). Apply heavy CSS `filter: blur(8px)` via `cn()` (never concat class strings). Beside it render the name+age label `{host_first_name}, {host_age}` as Label type (14px Fredoka 500 lowercase, e.g. `maya, 27`) — no "host" word, no tagline. If `host_blurred_photo_url` is null, render the existing avatar fallback (no broken image). Use `next/image` with `draggable={false}` per the existing pattern.
    NightDetailSheet: render the same 48px blurred avatar + `{first_name}, {age}` hint. Soften the two anonymity copy lines (line 137 "the host stays anonymous until you match." and lines 289-291 "who's hosting stays a secret...") to reflect that name+age now show pre-match — lowercase, dry, stop-slop, NO em-dash. Keep the existing cover pattern.
    Fill the rung-1 visual assertions in apps/web/e2e/05-reveal-feed.spec.ts: assert the feed card shows a blurred avatar + a `{name}, {age}` label, and run the privacy-invariant network assertion (no clear signed url) against both the feed and the opened detail sheet.
  </action>
  <verify>
    <automated>pnpm --filter web exec playwright test e2e/05-reveal-feed.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - NightCard renders a blurred avatar with a blur class (`grep -E "blur\(8px\)|blur-" apps/web/app/feed/NightCard.tsx` matches) and a name+age label.
    - NightDetailSheet no longer contains the literal "stays anonymous" / "stays a secret" copy (`grep -i "stays anonymous\|stays a secret" apps/web/app/feed/NightDetailSheet.tsx` returns nothing); no new em-dash (`grep -c "—" apps/web/app/feed/NightDetailSheet.tsx` does not increase).
    - 05-reveal-feed.spec.ts passes including the privacy-invariant assertion (every signed photo path on feed + detail ends in `_blurred.jpg`).
  </acceptance_criteria>
  <done>Rung 1 renders heavy-blur host avatar + name/age on card + detail, anonymity copy softened, privacy-invariant e2e green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → SSR feed loader | untrusted viewer session requests the feed; RLS'd client binds auth.uid() |
| SSR loader → storage signing | server mints signed URLs for the viewer; storage RLS authorizes the read |
| client → browse_feed_for_viewer (DEFINER RPC) | RPC is the blind-safe projection boundary; must not over-expose creator identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | clear host photo on feed/detail | mitigate | feed/page.tsx + NightCard + NightDetailSheet call ONLY signBlurredUrls, never signClearUrls; privacy-invariant Playwright network assertion (every signed path ends in `_blurred.jpg`) |
| T-05-02 | Information Disclosure | browse_feed_for_viewer over-projects creator identity | mitigate | project EXACTLY 3 hint columns (blurred_photo_url, first_name, age); grep gate forbids cr.id/email/clear_photo_url/instagram in the migration |
| T-05-03 | Elevation of Privilege | anon re-enabled when feed RPC re-CREATEd | mitigate | drop+recreate changes signature → re-apply verbatim revoke-public/revoke-anon/grant-authenticated tail; run security advisor after DDL |
| T-05-04 | Information Disclosure | CSS blur removed in devtools to see clear face | accept | structurally mitigated — CSS blurs an already-downscaled-to-64px blurred asset; removing CSS reveals only the blurred artifact, not the face |
</threat_model>

<verification>
- `pnpm vitest run apps/web/lib/after5 packages/api-client` green.
- `pnpm --filter web exec playwright test e2e/05-reveal-feed.spec.ts` green (incl. privacy-invariant network assertion).
- Local migration applied + types regenerated from local schema; security advisor clean.
- Visual-verify @420px (forced-local) deferred to the phase gate (05-04): rung-1 card + detail.
</verification>

<success_criteria>
- Feed card + detail show a heavy-blur host avatar + first name + age; the face is unreadable.
- No clear host photo signed URL is requested on feed or detail pre-lock.
- browse_feed_for_viewer returns exactly the 3 host-hint columns, applied local, advisor clean, prod untouched.
</success_criteria>

<output>
Create `.planning/phases/05-progressive-reveal-p2/05-01-SUMMARY.md` when done.
</output>
