---
phase: 05-progressive-reveal-p2
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/web/app/offers/[offerId]/page.tsx
  - apps/web/app/offers/[offerId]/OfferDetail.tsx
  - apps/web/e2e/05-reveal-offer.spec.ts
autonomous: true
requirements: [REQ-E15]
must_haves:
  truths:
    - "On the searcher's offer-received surface the host avatar is softened (one step less blur than the feed) but still not identifiable (D-03)"
    - "The offer surface leads with the night/plan, not the host photo (experience-led)"
    - "No clear host photo signed URL is requested on the offer surface pre-lock"
  artifacts:
    - path: "apps/web/app/offers/[offerId]/page.tsx"
      provides: "offer loader selects blurred_photo_url (not clear) + signs via signBlurredUrls"
      contains: "signBlurredUrls"
    - path: "apps/web/app/offers/[offerId]/OfferDetail.tsx"
      provides: "host avatar rendered at rung-2 CSS blur(3px), experience-led layout preserved"
    - path: "apps/web/e2e/05-reveal-offer.spec.ts"
      provides: "rung-2 visual (less blur than rung 1) + privacy-invariant network assertion for the offer surface"
  key_links:
    - from: "apps/web/app/offers/[offerId]/page.tsx"
      to: "signBlurredUrls"
      via: "server-side signing of the host blurred path"
      pattern: "signBlurredUrls"
---

<objective>
Rung 2 of the reveal ladder (the offer stage), top to bottom on the SEARCHER's offer-received surface (`apps/web/app/offers/[offerId]/`). Today this surface selects and renders the host's CLEAR photo path pre-lock — a privacy-invariant violation and a latent bug (it renders a raw path that does not load). This plan switches it to the blurred photo, signs it via `signBlurredUrls`, and applies a softer CSS `blur(3px)` so the face begins to resolve as a reward for getting matched, without giving away the clear photo. The night/plan stays the lead; the softening face is secondary.

Implements D-03 and the rung-2 half of REQ-E15. Resolves the planning open question: the rung-2 surface is the candidate's offer-received view at `offers/[offerId]`, NOT `InterestedList` (the host triage screen, which is not a reveal-ladder rung and is left untouched).

Purpose: reward the match with a one-step face resolution while preserving the privacy invariant and the experience-led contract.
Output: offer-received surface renders a rung-2 softened host avatar; rung-2 e2e with privacy-invariant assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-progressive-reveal-p2/05-CONTEXT.md
@.planning/phases/05-progressive-reveal-p2/05-RESEARCH.md
@.planning/phases/05-progressive-reveal-p2/05-PATTERNS.md
@.planning/phases/05-progressive-reveal-p2/05-UI-SPEC.md

<interfaces>
<!-- Consumed from 05-01. -->

signBlurredUrls (apps/web/lib/after5/photos.ts — shipped in 05-01):
```typescript
export async function signBlurredUrls(client: After5Client, paths: string[], ttl = 600): Promise<string[]>;
```

The offer-received surface as it stands TODAY (the CORRECTION to fix — see PATTERNS.md):
- offers/[offerId]/page.tsx:43 — `host:profiles!offers_creator_id_fkey ( first_name, age, city, clear_photo_url )`
- offers/[offerId]/page.tsx:115 — `photo_url: host.clear_photo_url ?? null`
- offers/[offerId]/OfferDetail.tsx:90-103 — the experience-led host header (Polaroid avatar secondary to the plan):
```tsx
<Polaroid src={host.photo_url ?? '/places/place-walk.jpg'} alt={host.first_name} size="sm" tone="dating" />
<p className="font-body text-lg font-semibold lowercase text-shell-ink">{name}{host.age ? `, ${host.age}` : ''}</p>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch the offer loader from clear to signed blurred host photo</name>
  <files>apps/web/app/offers/[offerId]/page.tsx</files>
  <read_first>
    - apps/web/app/offers/[offerId]/page.tsx (the offer embed select line 43; photo_url mapping line 115)
    - apps/web/lib/after5/photos.ts (signBlurredUrls from 05-01)
    - .planning/phases/05-progressive-reveal-p2/05-PATTERNS.md (offers/[offerId] assignment + the CORRECTION note)
    - .planning/phases/05-progressive-reveal-p2/05-RESEARCH.md (Pitfall 1: clear photo leak at the offer stage)
  </read_first>
  <action>
    In apps/web/app/offers/[offerId]/page.tsx, change the host profile embed (line ~43) to select `blurred_photo_url` instead of `clear_photo_url`. Sign the host's blurred path via `signBlurredUrls(supabase, [host.blurred_photo_url])` using the RLS'd viewer session client already in scope, and map the signed URL onto `photo_url` (line ~115). Never select or sign clear_photo_url on this surface. If the host has no blurred path or signing fails, degrade `photo_url` to null (OfferDetail falls back to the existing placeholder) — never crash the offer page.
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "clear_photo_url" apps/web/app/offers/[offerId]/page.tsx` returns 0.
    - `grep -c "signBlurredUrls" apps/web/app/offers/[offerId]/page.tsx` returns 1; `grep -c "signClearUrls" apps/web/app/offers/[offerId]/page.tsx` returns 0.
    - `pnpm --filter web exec tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The offer loader reads + signs the blurred host photo and never touches the clear path.</done>
</task>

<task type="auto">
  <name>Task 2: Render rung-2 softened blur + rung-2 e2e</name>
  <files>apps/web/app/offers/[offerId]/OfferDetail.tsx, apps/web/e2e/05-reveal-offer.spec.ts</files>
  <read_first>
    - apps/web/app/offers/[offerId]/OfferDetail.tsx (host header Polaroid lines 90-103; the experience-led layout to preserve)
    - apps/web/e2e/05-reveal-feed.spec.ts (the 05-01 spec — mirror its privacy-invariant network helper; do NOT edit it)
    - apps/web/e2e/5b-happy-path.spec.ts (auth/setup shape to reach an offer-received surface)
    - .planning/phases/05-progressive-reveal-p2/05-UI-SPEC.md (rung-2 row of the Reveal-Ladder Visual Contract; Identity-led not photo-led)
  </read_first>
  <action>
    In OfferDetail.tsx, apply rung-2 CSS `filter: blur(3px)` to the host avatar only (one step softer than rung-1's blur(8px)), via `cn()` (never concat class strings). Keep the exact experience-led layout — the night/plan via PlanTimeline leads, the avatar stays the small secondary Polaroid. Name+age label copy is unchanged from rung 1 (`{first_name}, {age}` lowercase). The blur is applied to the signed BLURRED url from Task 1, never a clear url.
    Create apps/web/e2e/05-reveal-offer.spec.ts mirroring the 05-reveal-feed.spec.ts privacy-invariant network helper: reach the searcher's offer-received surface, assert the host avatar is present and visibly LESS blurred than the feed rung (assert the blur(3px) class/style), assert the surface is experience-led (the plan/PlanTimeline is rendered above/around the avatar), and run the privacy-invariant assertion (every signed photo path on the offer surface ends in `_blurred.jpg`, never a bare clear path).
  </action>
  <verify>
    <automated>pnpm --filter web exec playwright test e2e/05-reveal-offer.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - OfferDetail.tsx applies a blur(3px)-equivalent class/style to the host avatar (`grep -E "blur\(3px\)|blur-" apps/web/app/offers/[offerId]/OfferDetail.tsx` matches).
    - 05-reveal-offer.spec.ts passes including the privacy-invariant assertion and the experience-led check (plan present).
    - No new em-dash introduced (`grep -c "—" apps/web/app/offers/[offerId]/OfferDetail.tsx` does not increase).
  </acceptance_criteria>
  <done>Rung 2 renders a softened (blur(3px)) host avatar on the experience-led offer surface; rung-2 e2e with privacy-invariant green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → SSR offer loader | candidate session requests an offer they are party to; RLS'd client binds auth.uid() |
| SSR loader → storage signing | server mints a signed blurred URL for the candidate viewer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | clear host photo leaked at the offer stage | mitigate | offer page selects + signs ONLY blurred_photo_url; grep gate forbids clear_photo_url/signClearUrls on the surface; privacy-invariant Playwright network assertion |
| T-05-06 | Information Disclosure | "lighter blur" tempts fetching the clear photo + CSS-blurring it (removable in devtools) | mitigate | rung 2 reuses the SAME blurred asset as rung 1 with CSS blur(3px); the clear path is never signed pre-lock |
</threat_model>

<verification>
- `pnpm --filter web exec playwright test e2e/05-reveal-offer.spec.ts` green (incl. privacy-invariant + experience-led checks).
- `pnpm --filter web exec tsc --noEmit` green.
- Visual-verify @420px (forced-local) deferred to the phase gate (05-04): rung-2 offer surface, blur visibly softer than rung 1.
</verification>

<success_criteria>
- The offer-received surface shows a softened host avatar (blur(3px)) that is less blurred than the feed but not identifiable.
- The offer surface is experience-led (plan leads, avatar secondary).
- No clear host photo signed URL is requested on the offer surface pre-lock.
</success_criteria>

<output>
Create `.planning/phases/05-progressive-reveal-p2/05-02-SUMMARY.md` when done.
</output>
