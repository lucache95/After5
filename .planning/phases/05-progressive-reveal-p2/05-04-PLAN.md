---
phase: 05-progressive-reveal-p2
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified: []
autonomous: false
requirements: [REQ-E15, REQ-E16]
must_haves:
  truths:
    - "All three reveal rungs render correctly @420px against the UI-SPEC visual contract"
    - "The two new e15/e16 migrations are applied to prod ONLY after explicit human approval"
    - "The prod security advisor shows no new findings after the gated apply"
  artifacts:
    - path: ".planning/phases/05-progressive-reveal-p2/05-04-SUMMARY.md"
      provides: "phase-gate record: visual-verify results + gated prod-apply outcome"
  key_links:
    - from: "local-green migrations"
      to: "prod ufufmcpnysvwtutpbian"
      via: "human-approved batched apply"
      pattern: "apply_migration"
---

<objective>
Phase gate for Progressive Reveal: (1) forced-local visual-verify @420px of all three reveal tiers + the ceremony + reduced-motion against the UI-SPEC, and (2) the GATED prod-apply of the two new SECURITY DEFINER migrations. Per the project's standing rule (STATE.md + CLAUDE.md), the autonomous plans (05-01..05-03) ship local-green + advisor-clean; prod apply is NEVER auto-pushed — it is gated to an explicit human checkpoint here.

This plan is `autonomous: false`. The visual-verify task is automatable up to the screenshots; the prod-apply task is a blocking human checkpoint that must NOT run during autonomous execution.

Purpose: confirm the reveal experience lands visually and apply the schema changes to prod under human control.
Output: visual-verify pass record + gated prod-apply outcome in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-progressive-reveal-p2/05-UI-SPEC.md
@.planning/phases/05-progressive-reveal-p2/05-01-PLAN.md
@.planning/phases/05-progressive-reveal-p2/05-02-PLAN.md
@.planning/phases/05-progressive-reveal-p2/05-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Forced-local visual-verify @420px of all three reveal tiers</name>
  <files>apps/web/e2e/05-reveal-feed.spec.ts, apps/web/e2e/05-reveal-offer.spec.ts, apps/web/e2e/05-reveal-ceremony.spec.ts</files>
  <read_first>
    - .planning/phases/05-progressive-reveal-p2/05-UI-SPEC.md (Visual-Verify Checklist @420px — the rubric to critique against)
    - .planning/STATE.md (the reusable forced-local visual-verify recipe + local QA browser-login recipe referenced in memory)
    - apps/web/e2e/05-reveal-feed.spec.ts, 05-reveal-offer.spec.ts, 05-reveal-ceremony.spec.ts (the specs to run @420px)
  </read_first>
  <action>
    Run the full reveal-ladder e2e suite against the forced-local stack at the 420px viewport and capture screenshots of each tier: rung-1 feed card + NightDetailSheet, rung-2 offer-received surface, rung-3 ceremony (unblur landed + flourish + toast), and the reduced-motion ceremony state. Critique each screenshot against the UI-SPEC Visual-Verify Checklist: cover photo leads (not the host), 48px secondary blurred avatar, 14px lowercase {name, age} label, rung-2 visibly less blurred than rung-1, ceremony un-blur lands (~900ms) with ONE pink flourish (not a burst), sonner toast fires, settles into the Tier-3 ProfileCard, contrast passes WCAG AA, "slide in" CTA ≥44px. Record PASS/findings per tier in the SUMMARY. Any clear-photo leak found here is a blocking failure — route back to the owning plan.
  </action>
  <verify>
    <human-check>Screenshots of all three rungs + reduced-motion captured and critiqued against the UI-SPEC checklist; each tier marked PASS or with explicit findings.</human-check>
  </verify>
  <acceptance_criteria>
    - All three reveal-ladder specs run green @420px against forced-local.
    - Screenshots exist for rung-1 (card + detail), rung-2 (offer), rung-3 (ceremony + reduced-motion).
    - Each tier is critiqued against the UI-SPEC Visual-Verify Checklist with a PASS/findings note in the SUMMARY; no clear-photo leak observed on any pre-lock surface.
  </acceptance_criteria>
  <done>All three reveal tiers + reduced-motion visually verified @420px against the UI-SPEC; results recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [GATED PROD-APPLY] Apply e15 + e16 migrations to prod under human approval</name>
  <what-built>
    Two new SECURITY DEFINER migrations, local-green + security-advisor-clean:
    - 20260606120000_e15_browse_feed_host_hint.sql (widens browse_feed_for_viewer +3 host-hint columns; signature change → drop+recreate with re-applied revoke-anon/grant-authenticated tail)
    - 20260606120100_e16_dispatch_identity_revealed.sql (re-CREATEs match_accept_offer, match_resolve_reciprocal, dispatch_notification to dispatch identity_revealed to both parties under matches_enabled consent)
  </what-built>
  <action>This is a blocking human checkpoint, NOT autonomous work: apply the two local-green e15 + e16 migrations to prod under explicit human approval per the steps in how-to-verify. Do not execute during autonomous execution.</action>
  <how-to-verify>
    GATED PROD-APPLY — do NOT run during autonomous execution. With explicit human approval:
    1. Review both migration files one more time against the live prod schema before applying (watch for local-vs-prod ledger drift; prod uses MCP-assigned versions — local files remain source of truth).
    2. Apply both migrations to prod ufufmcpnysvwtutpbian in dependency order (e15 then e16) via the established MCP apply_migration path.
    3. Verify on prod: browse_feed_for_viewer returns the 3 host-hint columns and is NOT anon-executable; identity_revealed dispatches at both lock RPCs; dispatch_notification's matches_enabled branch includes identity_revealed.
    4. Run the prod Supabase security advisor; confirm NO new findings vs. the established accepted DEFINER-executable pattern (the same accepted warning shared by all match_* RPCs).
    5. Record the prod apply outcome (MCP-assigned versions, advisor result) in the SUMMARY and update STATE.md's gated-prod-apply log.
  </how-to-verify>
  <resume-signal>Type "applied" with the prod advisor result, or describe issues to route back.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local-green schema → prod | the human-gated boundary; changes cross to prod only on explicit approval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Elevation of Privilege | prod feed RPC re-CREATE re-opens anon | mitigate | post-apply prod check: browse_feed_for_viewer not anon-executable; prod advisor run after apply |
| T-05-12 | Tampering | autonomous run applies schema to prod unsupervised | mitigate | prod apply is a blocking-human checkpoint; plan autonomous:false; never executed without approval |
| T-05-13 | Information Disclosure | clear-photo leak ships unverified on a reveal surface | mitigate | visual-verify @420px gate + the per-plan privacy-invariant network assertions must be green before this gate passes |
</threat_model>

<verification>
- All three reveal-ladder e2e specs green @420px forced-local; screenshots critiqued against the UI-SPEC checklist.
- Prod apply executed ONLY after human approval; prod advisor clean; prod feed RPC not anon-executable.
- STATE.md gated-prod-apply log updated with the e15/e16 outcome.
</verification>

<success_criteria>
- All three reveal tiers + reduced-motion verified @420px against the UI-SPEC.
- The two migrations applied to prod under human control with a clean prod advisor; no anon re-open on the feed RPC.
</success_criteria>

<output>
Create `.planning/phases/05-progressive-reveal-p2/05-04-SUMMARY.md` when done.
</output>
