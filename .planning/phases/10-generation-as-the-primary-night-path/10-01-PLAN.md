---
phase: 10-generation-as-the-primary-night-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/create/CreateChooser.tsx
  - apps/web/app/create/page.tsx
  - apps/web/components/BottomTabShell.tsx
  - apps/web/components/UserMenu.tsx
  - apps/web/app/create/__tests__/CreateChooser.test.tsx
autonomous: true
requirements: [FLOW-01]
must_haves:
  truths:
    - "From the create entry, generating a date is the primary path (Area 1): the global + tab and create CTA open the generate funnel directly, and the chooser presents generate as the dominant action."
    - "The manual-from-scratch door is demoted to a quiet secondary 'or build from scratch' link, NOT a co-equal door (Area 1, Area 3)."
    - "The demoted manual path still works end-to-end — no trap, no dead link (Area 3): clicking it still creates a blank itinerary and opens /plans/[id]/edit."
    - "/places stays a post-match venue-detail surface with no 'create from this venue' CTA resurrected (Area 1)."
  artifacts:
    - path: "apps/web/app/create/CreateChooser.tsx"
      provides: "Demoted-manual chooser: generate primary, manual as quiet secondary link"
      contains: "router.push('/create/generate')"
    - path: "apps/web/components/BottomTabShell.tsx"
      provides: "Center + tab routes to the generate funnel"
      contains: "/create/generate"
  key_links:
    - from: "apps/web/components/BottomTabShell.tsx"
      to: "/create/generate"
      via: "Link href"
      pattern: "/create/generate"
    - from: "apps/web/app/create/CreateChooser.tsx"
      to: "createBlankItinerary"
      via: "demoted secondary link still functional"
      pattern: "createBlankItinerary"
---

<objective>
Make generation the primary, obvious night-creation path and demote the manual-from-scratch door to a quiet escape hatch, without breaking it (Area 1 + Area 3).

Purpose: FLOW-01 success criterion 1 (generation is the primary path) and criterion 2 (no competing/dead creation funnel). The generate funnel and one-path publish already exist (09-05) — this re-prioritizes the entry surfaces only.
Output: Updated chooser (generate dominant, manual demoted-but-working), global + tab and create CTAs pointing at the generate funnel, and a guard test that /places has no creation CTA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-generation-as-the-primary-night-path/10-CONTEXT.md

@apps/web/app/create/CreateChooser.tsx
@apps/web/app/create/page.tsx
@apps/web/components/BottomTabShell.tsx
@apps/web/components/UserMenu.tsx

<interfaces>
<!-- The demoted-manual door must keep this working path. Extracted from CreateChooser.tsx. -->
From @after5/api-client:
  createBlankItinerary(client): Promise<string>   // returns new itinerary id; route to /plans/${id}/edit

From apps/web/lib/after5/client.ts:
  browserAfter5Client()   // client-side Supabase client for the blank-itinerary call

Routing facts:
- Door 1 (generate funnel) lives at /create/generate (apps/web/app/create/generate/page.tsx).
- The authed /create page renders <CreateChooser/>; anon renders <CreateFlow/> (the funnel) directly — leave the anon branch untouched.
- /places already has NO "create from this venue" CTA (v1.0 E21). The generic site-wide "/create" links on /places (page.tsx:55,118) are nav chrome, not venue-creation CTAs — do not treat them as resurrected creation CTAs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Demote the manual door in CreateChooser, keep it working</name>
  <files>apps/web/app/create/CreateChooser.tsx, apps/web/app/create/__tests__/CreateChooser.test.tsx</files>
  <behavior>
    - Generate ("build it for me") renders as the single dominant primary action (pink shell-accent card) and routes to /create/generate.
    - The manual-from-scratch path renders as a quiet secondary text link (NOT a co-equal bordered card) with copy like "or build from scratch" — Barbiecore, lowercase, dry, no em-dashes.
    - Clicking the demoted manual link still calls createBlankItinerary then routes to /plans/${id}/edit (no trap, no dead link).
    - On createBlankItinerary failure, a sonner error toast shows and the link re-enables (no stuck spinner).
  </behavior>
  <action>Restructure CreateChooser per D-Area-1/Area-3: keep the generate door as the one dominant pink card routing to /create/generate; convert Door 2 from a co-equal bordered card into a quiet secondary link beneath it ("or build from scratch" — lowercase, dry, no em-dash). Preserve the existing startFromScratch() logic verbatim (createBlankItinerary → router.push(`/plans/${id}/edit`) with the loading toast + error toast + re-enable) so the demoted path still works. Keep the ≥44px tap target on the link via padding. Add CreateChooser.test.tsx asserting: (a) the generate action is present and points to /create/generate, (b) the manual affordance renders as a secondary link not a primary card, (c) clicking the manual link invokes createBlankItinerary (mock @after5/api-client) and navigates to /plans/<id>/edit.</action>
  <verify>
    <automated>cd apps/web && pnpm vitest run app/create/__tests__/CreateChooser.test.tsx 2>&1 | grep -v '^#' | tail -20</automated>
  </verify>
  <done>Chooser shows generate as the dominant action and manual as a demoted-but-functional secondary link; test green.</done>
</task>

<task type="auto">
  <name>Task 2: Route global create entry points to the generate funnel + verify no /places creation CTA</name>
  <files>apps/web/components/BottomTabShell.tsx, apps/web/components/UserMenu.tsx, apps/web/app/create/page.tsx</files>
  <action>Per Area 1, point the global create CTA / + tab directly at the generate funnel. In BottomTabShell.tsx change the center "+" Link href from "/create" to "/create/generate" (keep aria-current isActive matching both /create and /create/generate so the tab still highlights on the chooser). In UserMenu.tsx change the "plan a date" wedge link href from "/create" to "/create/generate". Leave apps/web/app/create/page.tsx behavior intact (authed → chooser, anon → funnel) — the chooser remains reachable for hosts who navigate to /create, but the primary + entry now lands directly on the funnel. Do NOT touch the marketing/landing "/create" links (LandingHero, page.tsx hero, dates/types/neighborhoods/places nav chrome) — those are top-of-funnel and correctly drop anon onto the funnel via the anon branch.</action>
  <verify>
    <automated>cd /Users/lucas/Projects/After5 && grep -c "/create/generate" apps/web/components/BottomTabShell.tsx apps/web/components/UserMenu.tsx && grep -rin "create from this venue\|build a date here\|plan from this place" apps/web/app/places/ | grep -v '^#' | wc -l | xargs -I{} test {} -eq 0 && echo "NO_PLACES_CREATION_CTA_OK"</automated>
  </verify>
  <done>Center + tab and UserMenu wedge route to /create/generate; /places has zero venue-creation CTAs; grep gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→/plans/[id]/edit | Demoted manual door creates an itinerary via createBlankItinerary (existing owner-scoped RPC) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | demoted manual door | accept | No new DDL/RPC; createBlankItinerary is an existing owner-scoped path reused verbatim — no new attack surface, UI re-prioritization only. |
| T-10-02 | Denial of Service | repeated blank-itinerary clicks | mitigate | Existing `starting` guard + disabled state prevents double-submit; preserved verbatim. |
</threat_model>

<verification>
- `pnpm vitest run app/create/__tests__/CreateChooser.test.tsx` green.
- grep gates: /create/generate present in both nav files; zero venue-creation CTAs under /places.
- Manual smoke (folded into 10-03 visual-verify): chooser shows generate dominant + manual as a quiet link that still opens /plans/[id]/edit.
</verification>

<success_criteria>
- Generate is the primary create path from the + tab, the create CTA, and the chooser.
- Manual door demoted to a quiet secondary link that still works (no trap/dead link).
- No /places creation CTA resurrected.
</success_criteria>

<output>
Create `.planning/phases/10-generation-as-the-primary-night-path/10-01-SUMMARY.md` when done.
</output>
