---
phase: 10-generation-as-the-primary-night-path
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - apps/web/e2e/10-create-primary.spec.ts
  - apps/web/e2e/10-visual-capture.spec.ts
autonomous: false
requirements: [FLOW-01]
must_haves:
  truths:
    - "The full primary-path loop is proven green: pick city → write primary_city_id + enqueue → generate (Kelowna or cold-start) → improve → publish to feed (FLOW-01 criteria 1+2+3)."
    - "Visual-verify @420px confirms the create entry, the city picker, and the improve-placement read Barbiecore (lowercase/dry, ≥44px taps) and that the manual door reads as demoted."
    - "Local suite + typecheck are green before any prod-bound work; this phase introduces NO prod-bound migration (no DDL), so there is nothing to bundle into the Phase-8/9 key-gated cutover beyond noting it."
  artifacts:
    - path: "apps/web/e2e/10-create-primary.spec.ts"
      provides: "End-to-end primary-path assertion (entry → city → generate → improve → publish)"
    - path: "apps/web/e2e/10-visual-capture.spec.ts"
      provides: "@420px screenshots of the entry, city picker, and improve placement"
  key_links:
    - from: "10-visual-capture.spec.ts"
      to: "create entry + city picker + improve"
      via: "CAPTURE_VISUAL @420px"
      pattern: "420"
---

<objective>
Phase gate: prove the primary-path loop end-to-end and visual-verify the UI-bearing surfaces @420px, mirroring the v1.0 phase-gate posture. Confirm there is no prod-bound DDL this phase, so nothing needs bundling into the Phase-8/9 key-gated cutover.

Purpose: Ship-gate for FLOW-01 — generation is the primary path, it publishes to the feed, the manual door is demoted-but-working, and the city/improve/publish chain holds.
Output: An e2e primary-path spec, a @420px visual-capture spec, a local-green run (suite + typecheck), and a blocking human visual-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-generation-as-the-primary-night-path/10-CONTEXT.md

@apps/web/app/create/CreateChooser.tsx
@apps/web/app/create/CreateFlow.tsx
@apps/web/app/api/profile/city/route.ts

<interfaces>
<!-- Reuse the established forced-local visual-verify recipe + authed-session helper. -->
- Authed Playwright session: apps/web/e2e/_helpers/seed.ts (PKCE via /login + Mailpit; curated Kelowna city id via cities.select('id').eq('slug','kelowna')).
- Existing visual-capture pattern: apps/web/e2e/06-visual-capture.spec.ts (CAPTURE_VISUAL screenshot recipe @420px viewport).
- Builds/tests against curated Kelowna — NO live Foursquare key. A cold curated city exercises the 08-04 "warming up" fallthrough.
- Publish path: PublishToFeedButton → /nights/new?itinerary=<id> → post_night → feed (unchanged, one path).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end primary-path spec + local-green run</name>
  <files>apps/web/e2e/10-create-primary.spec.ts</files>
  <action>Write a Playwright spec (authed session via _helpers/seed.ts, Kelowna) that walks the primary path: open the + entry → it lands on the generate funnel (/create/generate) → pick the curated Kelowna chip → assert POST /api/profile/city fired and profiles.primary_city_id is set for the user (read back via the admin/seed client) → generate a date → exercise one ImproveControls action (swap or NL tweak) and assert the itinerary stays coherent + persisted → click "publish to the feed" and assert it routes to /nights/new?itinerary=<id> (the existing post path). Also assert the demoted manual link still opens /plans/[id]/edit (no trap). Use curated Kelowna so no live Foursquare key is needed. Then run the local gate: targeted vitest suites from 10-01/10-02 + this e2e spec + web typecheck.</action>
  <verify>
    <automated>cd apps/web && pnpm vitest run app/create app/api/profile/city 2>&1 | grep -v '^#' | tail -15 && pnpm exec tsc --noEmit 2>&1 | grep -v '^#' | tail -5 && pnpm exec playwright test e2e/10-create-primary.spec.ts 2>&1 | grep -v '^#' | tail -15</automated>
  </verify>
  <done>Primary-path e2e green; create + city vitest suites green; web typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: @420px visual-capture of the UI-bearing surfaces</name>
  <files>apps/web/e2e/10-visual-capture.spec.ts</files>
  <action>Write a CAPTURE_VISUAL spec (authed, @420px viewport, mirroring 06-visual-capture.spec.ts) that screenshots: (1) the create entry/chooser (generate dominant, manual demoted to a quiet link), (2) the city picker step in the funnel with the curated chips + a prefilled saved city, and (3) the generate result with ImproveControls placed before the publish CTA. Save screenshots to the established artifacts dir so they can be critiqued against the Barbiecore rubric (lowercase/dry copy, no em-dashes, ≥44px taps, three-tier color).</action>
  <verify>
    <automated>cd apps/web && CAPTURE_VISUAL=1 pnpm exec playwright test e2e/10-visual-capture.spec.ts 2>&1 | grep -v '^#' | tail -10 && ls -1 e2e/**/10-*.png 2>/dev/null | head</automated>
  </verify>
  <done>Three @420px screenshots captured for entry, city picker, and improve placement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual-verify the create entry, city picker, and improve placement @420px</name>
  <action>Human visual-verify of the captured @420px screenshots and the demoted-but-working manual door before the phase ships. Paired with the automated capture in Task 2.</action>
  <what-built>The primary-path night-creation loop (entry → city → generate → improve → publish), the demoted-but-working manual door, and @420px screenshots of the create entry, city picker, and improve placement.</what-built>
  <how-to-verify>
    1. Review the three screenshots from apps/web/e2e (10-visual-capture): does the create entry sell "generate a date" as the default, with the manual door clearly demoted to a quiet link? Is the city picker clear with curated chips + a prefilled saved city? Is ImproveControls placed in the result before "publish to the feed"?
    2. Confirm Barbiecore: lowercase/dry copy, no em-dashes, ≥44px taps, on-token color/type.
    3. Confirm no trap: the demoted "or build from scratch" link still opens the canvas, and nothing reads as a dead/competing funnel.
    4. Confirm there is NO prod-bound migration this phase (no DDL added) — nothing to bundle into the Phase-8/9 key-gated cutover.
  </how-to-verify>
  <verify>
    <human-check>Three @420px screenshots reviewed; entry/city/improve pass the Barbiecore rubric; demoted manual door confirmed working (no trap).</human-check>
  </verify>
  <done>Visual rubric approved; manual door confirmed non-trapping; no prod-bound migration to bundle.</done>
  <resume-signal>Type "approved" or describe what to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds tests only; no new runtime trust boundary. The city-set boundary is modeled in 10-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering | phase-gate (no DDL) | accept | This phase introduces no migration/RPC/edge change; nothing is prod-bound, so there is no gated prod-apply and nothing to bundle into the Phase-8/9 cutover. Verified by absence of new files under supabase/migrations. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this phase; if any appear, block on a legitimacy checkpoint before install. |
</threat_model>

<verification>
- Local-green: create + city vitest suites, web typecheck, and both e2e specs pass against curated Kelowna (no live Foursquare key).
- Visual-verify @420px approved at the blocking checkpoint.
- Confirmed: zero new files under supabase/migrations/ this phase → no prod-bound cutover bundle.
</verification>

<success_criteria>
- FLOW-01 fully proven: generation is the primary path, publishes to the feed, improve is in-flow, manual door demoted-but-working, no traps.
- UI-bearing surfaces pass the @420px Barbiecore visual rubric.
</success_criteria>

<output>
Create `.planning/phases/10-generation-as-the-primary-night-path/10-03-SUMMARY.md` when done.
</output>
