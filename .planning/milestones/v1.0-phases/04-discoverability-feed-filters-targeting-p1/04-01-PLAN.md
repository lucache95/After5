---
phase: 04-discoverability-feed-filters-targeting-p1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260605120400_e10_feed_filters_column.sql
  - supabase/migrations/20260605120500_e10_browse_feed_filters.sql
  - supabase/migrations/20260605120600_e10_reach_preview.sql
  - supabase/migrations/20260605120700_e10_feed_indexes.sql
  - supabase/tests/e10_browse_feed_filters.sql
  - supabase/tests/e10_reach_preview.sql
  - supabase/tests/e10_feed_filters_rls.sql
  - supabase/tests/s5_browse_feed_blind.sql
  - packages/types/database.ts
autonomous: true
requirements: [REQ-E10]

must_haves:
  truths:
    - "A searcher's hard filters (host gender / max price / max distance) HIDE non-matching nights (WHERE)"
    - "Soft filters (vibe / who-pays / time-of-day) only RE-SORT (a soft-mismatched night still appears)"
    - "fit is a pure targeting signal: a night whose target_genders+target_age_range genuinely include the viewer yields fit=true EVEN when the viewer has zero soft filters set (D-03/SC-1); soft filters never gate the pill"
    - "An open night (everyone-literal or empty array) yields fit=true for a matching-gender viewer and is counted by reach_preview"
    - "browse_feed_for_viewer returns a per-card fit boolean and keeps the existing 13 blind columns (no identity leak)"
    - "reach_preview returns an aggregate count only; anon EXECUTE revoked, authenticated granted"
    - "profiles.feed_filters is self-write only (another user cannot write mine) via profiles_owner_all"
    - "Keyset cursor still paginates without skip or dupe under the new ORDER BY"
  artifacts:
    - path: "supabase/migrations/20260605120500_e10_browse_feed_filters.sql"
      provides: "browse_feed_for_viewer extended with hard WHERE + soft ORDER BY + targeting-only fit"
      contains: "fit boolean"
    - path: "supabase/migrations/20260605120600_e10_reach_preview.sql"
      provides: "reach_preview DEFINER count RPC + grant trio"
      contains: "create or replace function reach_preview"
    - path: "supabase/tests/e10_browse_feed_filters.sql"
      provides: "hard-hide / soft-resort / fit-targeting / everyone-norm / keyset no-dup assertions"
  key_links:
    - from: "browse_feed_for_viewer"
      to: "profiles.feed_filters"
      via: "me CTE reads coalesce(pr.feed_filters,'{}') by auth.uid()"
      pattern: "feed_filters"
    - from: "browse_feed_for_viewer fit/boost AND reach_preview"
      to: "date_instances.target_genders"
      via: "everyone-literal and empty-array open normalization before me.gender = any(...)"
      pattern: "everyone"
---

<objective>
Build the DB foundation for REQ-E10: profiles.feed_filters jsonb, extended browse_feed_for_viewer (hard filters in WHERE, soft score in ORDER BY, targeting-only fit in SELECT), new lean reach_preview count RPC, supporting indexes, and the SQL test suite locking the 4 ROADMAP success criteria. Then apply locally, regen types, run the security advisor.

Purpose: Everything downstream reads these contracts. Regenerated types must reflect the live local schema before TypeScript wiring (build/typecheck passes against stale types while the real schema is wrong = false-positive verification).
Output: 4 migrations, 3 new + 1 extended SQL tests, regenerated packages/types/database.ts, green local apply, clean advisor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-discoverability-feed-filters-targeting-p1/04-RESEARCH.md
@.planning/phases/04-discoverability-feed-filters-targeting-p1/04-PATTERNS.md

<interfaces>
<!-- Live contracts. Full templates are in 04-PATTERNS.md + 04-RESEARCH.md Code Examples. Do NOT re-explore. -->
browse_feed_for_viewer (20260602120400_m4_browse_feed_ambient.sql) is the function to EXTEND. Keep the 5-param signature (p_viewer, p_point, p_after_starts, p_after_id, p_limit). 13 blind cols; date_trunc('hour', di.starts_at) is the time-blinding (KEEP). me CTE reads gender/gender_preferences/age/age_pref/distance_pref_km/pt. ALL existing WHERE gates STAY verbatim (mutual-pref baseline + keyset cursor (di.starts_at,di.id) > (p_after_starts,p_after_id) + ORDER BY di.starts_at asc, di.id asc + limit greatest(1,least(coalesce(p_limit,20),50))).

date_instances targeting (live/prod, 20260605120000): target_genders text[] default '{}', target_age_range int4range, search_radius_km numeric. Open nights store the LITERAL everyone-array (STATE.md:46), NOT empty array.

itineraries.total_cost_pp = per-person price. profiles_owner_all RLS (capture_full_schema.sql:46-54) already covers a new profiles column (NO new policy). cities.centroid geography(Point,4326); profiles.primary_city_id -> cities.

Test harness (_fixtures.sql + e11_targeting.sql): mk_user/mk_itinerary/mk_instance (city 'kelowna'); set local role authenticated + set_config('request.jwt.claims', json_build_object('sub',uid,'role','authenticated')::text, true); positional RPC; ROLLBACK. profiles_private(user_id,birthdate) drives age; set dating_enabled=true, verification='verified', primary_city_id.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 SQL test scaffolds (Nyquist) — author the suite first</name>
  <read_first>
    - supabase/tests/e11_targeting.sql (jwt-claims + positional RPC + anon-privilege assertion + ROLLBACK)
    - supabase/tests/p1_preferences.sql (self-write + EXCEPTION harness, for the RLS test)
    - supabase/tests/s5_browse_feed_blind.sql:21-32 (no-identity-leak assertion to EXTEND, not rewrite)
    - supabase/tests/_fixtures.sql (mk_user/mk_itinerary/mk_instance/city 'kelowna')
    - 04-PATTERNS.md "Test files (NEW)"; 04-RESEARCH.md "Validation Architecture" Test Map
  </read_first>
  <action>
    Three new tests (each \i supabase/tests/_fixtures.sql, RAISE EXCEPTION in DO blocks, ROLLBACK) + extend one. RED until Task 2.

    e10_browse_feed_filters.sql: (a) hard host_genders HIDES man-hosted, keeps woman-hosted; (b) hard max_price=50 hides total_cost_pp=80, keeps 40; (c) hard max_distance_km=10 hides far-city, keeps same-city; (d) soft vibes only re-sorts (mismatched still appears; matched ranks above); (e) fit is targeting-ONLY: a night whose target_genders + target_age_range genuinely include the viewer yields fit=true EVEN WHEN the viewer has ZERO soft filters set (feed_filters='{}') — this is the D-03/SC-1 regression to lock; a non-targeting night yields fit=false; (f) everyone-normalization guard: open night (target_genders = everyone-array) -> matching-gender viewer fit=true; (g) keyset no-dup/no-skip across two pages via (p_after_starts,p_after_id). Set filters via update profiles set feed_filters='...'::jsonb where id=viewer.

    e10_reach_preview.sql: (a) counts matching verified+dating_enabled profiles (excluding auth.uid()); (b) everyone-array and empty-array both count everyone (>= specific-gender count); (c) age+radius narrowing reduces count monotonically; (d) has_function_privilege('anon','reach_preview(text[], int4range, uuid, numeric)','execute') false, authenticated true.

    e10_feed_filters_rls.sql (model p1_preferences.sql): user A cannot update userB's feed_filters (0 rows); self-write succeeds.

    EXTEND s5_browse_feed_blind.sql: assert fit column EXISTS in the return AND no creator_id/itinerary_id/venue_id was introduced (reuse its information_schema / pg_get_function_result pattern).
  </action>
  <verify>
    <automated>for f in e10_browse_feed_filters e10_reach_preview e10_feed_filters_rls; do test -f "supabase/tests/$f.sql" || exit 1; done; grep -q "fit" supabase/tests/s5_browse_feed_blind.sql</automated>
  </verify>
  <acceptance_criteria>Three new e10_* test files exist; s5_browse_feed_blind.sql references fit. The fit case (e) asserts fit=true for a gender+age matched night with feed_filters='{}'. Assertions are RAISE EXCEPTION + ROLLBACK. RED against current schema.</acceptance_criteria>
  <done>e10_* SQL suite encodes every Test-Map behavior; RED until migrations land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 4 migrations — feed_filters column, browse_feed extension, reach_preview, indexes</name>
  <read_first>
    - supabase/migrations/20260602120400_m4_browse_feed_ambient.sql (FULL live body to preserve and layer onto)
    - supabase/migrations/20260605120000_e11_targeting_cols.sql (additive-column pattern)
    - supabase/migrations/20260605120200_e11_post_night_targeting.sql:152-154 (grant trio)
    - supabase/migrations/20260527120000_s4_date_instances_feed_columns.sql (index conventions)
    - 04-PATTERNS.md per-migration templates + LANDMINE list; 04-RESEARCH.md "Code Examples" + Pitfalls 1/2/3/5; spec sections 1/3/5/6
  </read_first>
  <behavior>
    Drives the Task-1 suite RED->GREEN: hard filters HIDE, soft only RE-SORT, fit is a TARGETING-only boolean (gender + age inclusion, independent of the viewer's soft filters), open targeting (everyone-array or empty-array) normalizes to "no restriction" in BOTH RPCs, blind 13-col + fit preserved, keyset stable, anon revoked.
  </behavior>
  <action>
    Write 4 migrations with next-in-series timestamps; bodies follow the 04-PATTERNS.md templates verbatim.

    20260605120400_e10_feed_filters_column.sql: add column if not exists feed_filters jsonb not null default '{}'::jsonb + a not-valid check (jsonb_typeof='object'). NO new RLS (comment: profiles_owner_all covers it).

    20260605120500_e10_browse_feed_filters.sql: drop+recreate, EXACT 5-param signature, 13 blind cols + fit boolean. me CTE adds coalesce(pr.feed_filters,'{}'::jsonb) as ff; an f CTE unpacks ff (max_price, max_distance_km, host_genders, vibes, who_pays, time_buckets, host_age_range). HARD WHERE each gated "f.<key> is null or ...": host gender vs cr.gender, max_price vs it.total_cost_pp, max_distance_km via st_dwithin(cc.centroid, me.pt, f.max_distance_km*1000); existing least(distance_pref_km) gate STAYS. date_fits_viewer normalizes via case when di.target_genders='{}' or di.target_genders=array['everyone'] then true else me.gender=any(di.target_genders) end AND (di.target_age_range is null or me.age <@ di.target_age_range). vibe_pts/pay_pts/time_pts = 0 when the viewer soft filter absent else 1 on match. Define IMMUTABLE time_bucket_of(timestamptz) (this_weekend/weeknights/daytime). fit = date_fits_viewer ONLY — fit is a pure TARGETING signal (the night's target genuinely includes this viewer); it MUST NOT be gated by the soft score, or a brand-new searcher with feed_filters='{}' (vibe_pts+pay_pts+time_pts=0) would never see the "looks for someone like you" pill on a perfectly-targeted night (D-03/SC-1 regression). The soft score is used in ORDER BY ONLY. ORDER BY: order by (date_fits_viewer::int * 4 + vibe_pts + pay_pts + time_pts) desc, di.starts_at asc, di.id asc — so a genuine targeting match boosts to the top AND soft prefs refine ranking, WITHOUT gating fit. CURSOR DECISION (comment inline, Open Question 1): keyset stays on (starts_at,id), the composite soft+targeting score is the LEADING ORDER BY key, "best-effort within fetched window v1"; cursor predicate (di.starts_at,di.id) > (p_after_starts,p_after_id) UNCHANGED. Re-emit grant trio.

    20260605120600_e10_reach_preview.sql: reach_preview(p_target_genders text[] default '{}', p_target_age_range int4range default null, p_city uuid default null, p_radius_km numeric default null) returns integer, security definer set search_path=public,extensions; count(*)::int over profiles (dating_enabled, verified, id<>auth.uid()), everyone/empty normalization, optional age <@ and radius st_dwithin; aggregate count ONLY; grant trio.

    20260605120700_e10_feed_indexes.sql: create index if not exists profiles_reach_idx on profiles (dating_enabled, verification); add itineraries(total_cost_pp)/profiles(gender) btrees only if EXPLAIN/advisor flags (comment the call).

    No em-dashes in SQL comments.
  </action>
  <verify>
    <automated>grep -q "fit boolean" supabase/migrations/20260605120500_e10_browse_feed_filters.sql && grep -q "revoke execute on function reach_preview" supabase/migrations/20260605120600_e10_reach_preview.sql && grep -q "everyone" supabase/migrations/20260605120500_e10_browse_feed_filters.sql && grep -Eq "date_fits_viewer[^*]*as fit|as fit" supabase/migrations/20260605120500_e10_browse_feed_filters.sql && for m in 20260605120400_e10_feed_filters_column 20260605120500_e10_browse_feed_filters 20260605120600_e10_reach_preview 20260605120700_e10_feed_indexes; do psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -1 -v ON_ERROR_STOP=1 -f - <<SQL || exit 1
BEGIN;
\i supabase/migrations/$m.sql
ROLLBACK;
SQL
done</automated>
  </verify>
  <acceptance_criteria>The 4 migrations exist with next-in-series timestamps; browse_feed projects fit = date_fits_viewer (targeting-only, NOT gated by vibe_pts/pay_pts/time_pts); soft score appears only in ORDER BY; reach_preview revokes anon. Each migration parses + applies cleanly inside a rolled-back transaction (the psql syntax/apply dry check passes) — Task 3 remains the authoritative full apply + typegen + advisor gate.</acceptance_criteria>
  <done>4 migrations written, fit is targeting-only, each parses and applies in a dry-run transaction; ready for the Task 3 blocking apply.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: [BLOCKING] Local apply + type regen + security advisor (pre-verification)</name>
  <read_first>
    - CLAUDE.md (gated prod-apply: local-green first, NOT prod push; run advisor after every DDL)
    - .planning/STATE.md:46 (prod-apply ledger + MCP version-drift note; prod-apply is OUT OF SCOPE here)
    - supabase/config.toml (local ports 54321 API / 54322 DB)
    - package.json:21 (db:test runner)
  </read_first>
  <action>
    AFTER the 4 migrations are written, BEFORE phase verification, apply LOCALLY only:
    1. Apply the new migrations to the local stack: `supabase migration up` (or `supabase db reset` if the local history needs a clean replay). Confirm the 4 e10_* migrations applied with no error.
    2. Regenerate TypeScript types from the LIVE local schema into the canonical types file: `supabase gen types typescript --local > packages/types/database.ts` (use the project's existing gen path; confirm the path matches how prior migrations regenerated types). The new `feed_filters` column and the `reach_preview` / extended `browse_feed_for_viewer` function signatures MUST appear in the regenerated output.
    3. Run the Task-1 SQL suite against local: `pnpm db:test` (or the targeted psql calls for e10_*). All e10_* assertions + the extended s5_browse_feed_blind.sql must be GREEN.
    4. Run the Supabase security advisor against the local changes (per CLAUDE.md). Resolve any NEW finding. The DEFINER-executable warning on reach_preview is the app's established accepted pattern (shared by all match_* RPCs, STATE.md advisor note) and is NOT a new finding; document it if it surfaces.

    Prod-apply is OUT OF PLAN SCOPE: it is a separate gated/batched step the user runs after phase verification (consistent with Phases 1 to 3). Do NOT push to prod ufufmcpnysvwtutpbian.
  </action>
  <verify>
    <automated>pnpm db:test 2>&1 | tail -5; grep -q "feed_filters" packages/types/database.ts && grep -q "reach_preview" packages/types/database.ts</automated>
  </verify>
  <acceptance_criteria>
    The 4 migrations apply cleanly to local; packages/types/database.ts contains feed_filters + reach_preview; the full e10_* SQL suite + extended blind test pass GREEN (including the fit=true-with-empty-filters targeting case); the security advisor shows no NEW finding (the DEFINER-executable note on reach_preview is the established accepted pattern). No prod migration applied.
  </acceptance_criteria>
  <done>Local schema + regenerated types + green SQL suite + clean advisor. Downstream TypeScript wiring (Plan 04-02) can now compile against real types. Prod-apply deferred to the gated step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| searcher client -> browse_feed_for_viewer RPC | untrusted viewer; RPC defaults p_viewer := auth.uid(); must not leak host identity/location/targeting internals |
| host client -> reach_preview RPC | DEFINER reads all profiles' gender/age/city a searcher cannot normally see |
| any client -> profiles.feed_filters write (PostgREST) | self-owned jsonb write; must be self-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | reach_preview / re-emitted browse_feed signature anon-executable | mitigate | grant trio (revoke public + revoke anon + grant authenticated) on every signature; security advisor after DDL (Task 3). Asserted by e10_reach_preview.sql anon-revoke test. |
| T-04-02 | Information Disclosure | new fit projection in browse_feed_for_viewer leaks identity | mitigate | SELECT stays the 13 blind cols + computed fit (no identity); extended s5_browse_feed_blind.sql asserts no creator_id/itinerary_id/venue_id; date_trunc('hour') time-blinding kept. |
| T-04-03 | Information Disclosure | reach_preview DEFINER reads all profiles | accept | returns ONLY count(*)::int, never row identity; same accepted DEFINER pattern as match_* RPCs (STATE.md advisor note). Aggregate count enables no individual enumeration. |
| T-04-04 | Tampering / EoP | cross-user write of another user's feed_filters | mitigate | profiles_owner_all WITH CHECK(id=auth.uid()); NO new weakening policy; asserted by e10_feed_filters_rls.sql. |
| T-04-05 | Tampering / DoS | malformed jsonb crashes the feed RPC; arbitrary keys drive query logic | mitigate | feed_filters_is_object CHECK; only the whitelisted keys (max_price, max_distance_km, host_genders, vibes, who_pays, time_buckets, host_age_range) are unpacked in the f CTE; absent/unknown keys = no filter (guarded casts). |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | phase installs ZERO packages (RESEARCH Package Legitimacy Audit: N/A); no install task exists. |
</threat_model>

<verification>
- `pnpm db:test` green (all e10_* + extended s5_browse_feed_blind.sql, including fit=true with empty feed_filters).
- Security advisor: no NEW finding after the 4 DDL migrations (DEFINER-executable on reach_preview is the accepted match_* pattern).
- packages/types/database.ts regenerated from live local schema (contains feed_filters + reach_preview).
- No prod migration applied (gated step deferred).
</verification>

<success_criteria>
- profiles.feed_filters jsonb exists (self-read/write via profiles_owner_all, no new policy).
- browse_feed_for_viewer returns 13 blind cols + fit; hard filters HIDE, soft filters only RE-SORT; fit is targeting-only (not soft-gated); open targeting normalized; keyset stable.
- reach_preview returns an aggregate count, everyone-normalized, anon-revoked.
- Full e10_* SQL suite GREEN locally; advisor clean; types regenerated.
</success_criteria>

<output>
Create `.planning/phases/04-discoverability-feed-filters-targeting-p1/04-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
