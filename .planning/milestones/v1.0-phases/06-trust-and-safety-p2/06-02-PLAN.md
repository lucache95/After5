---
phase: 06-trust-and-safety-p2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/messages/[threadId]/page.tsx
  - apps/web/e2e/e18-chat-nav-edges.spec.ts
  - supabase/tests/e18_chat_rls_denies_nonparty.sql
autonomous: true
requirements: [REQ-E18]

must_haves:
  truths:
    - "From a locked conversation the user can reach the counterpart's profile and the night via two controls in the chat-header right slot (Chat→Profile, Chat→Night)"
    - "The two chat-header controls render ONLY when the thread is lock-promoted (lock_id != null) — pre-lock renders no control, no identity leak (reveal-gated)"
    - "Night→Profile and Night→Chat already navigate from LockDetail and are confirmed unchanged (the other two of the four edges)"
    - "chat_threads_party_read already exists and DENIES a non-party SELECT (verified, not re-created)"
  artifacts:
    - path: "apps/web/app/messages/[threadId]/page.tsx"
      provides: "lock_id selected on the chat_threads loader + two reveal-gated icon controls in DeepRouteHeader.right"
      contains: "lock_id"
    - path: "apps/web/e2e/e18-chat-nav-edges.spec.ts"
      provides: "E2E covering all 4 edges + reveal-gating + aria-labels"
    - path: "supabase/tests/e18_chat_rls_denies_nonparty.sql"
      provides: "SQL assertion: chat_threads_party_read denies a non-party (0 rows)"
  key_links:
    - from: "apps/web/app/messages/[threadId]/page.tsx (loader)"
      to: "chat_threads.lock_id"
      via: "added to the existing select"
      pattern: "lock_id"
    - from: "apps/web/app/messages/[threadId]/page.tsx (DeepRouteHeader right slot)"
      to: "/matches/${lockId}"
      via: "Chat→Profile + Chat→Night Links gated on lock_id"
      pattern: "/matches/\\$\\{"
---

<objective>
Deliver E18: wire the two missing chat↔profile↔night nav edges (Chat→Profile, Chat→Night) into the existing DeepRouteHeader right slot of the conversation page, reveal-gated on the thread's lock_id; verify the already-existing Night→Profile / Night→Chat edges and the already-existing chat RLS read policy.

Purpose: a conversation connects back to who you're talking to and the plan you're talking about — closing the trust nav loop. Post-lock identity is already revealed (Phase 5), so the header may safely link to the revealed profile and the night.
Output: one loader change (select lock_id) + two reveal-gated header controls, a chat-RLS deny-non-party SQL assertion, and an E2E proving all four edges and the aria-labels.

NOTE (RESEARCH correction, LOCKED): chat_threads_party_read ALREADY EXISTS (Phase 7, 20260601100100). This plan does NOT create a chat RLS policy — it VERIFIES it and adds a deny-non-party test. The only chat-loader change is adding lock_id to the existing select.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trust-and-safety-p2/06-RESEARCH.md
@.planning/phases/06-trust-and-safety-p2/06-PATTERNS.md
@.planning/phases/06-trust-and-safety-p2/06-UI-SPEC.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

From apps/web/components/DeepRouteHeader.tsx:
- export interface DeepRouteHeaderProps { backHref: string; backLabel: ...; title?: string; right?: React.ReactNode; className?: ... }
- The `right` slot already renders as `{right && <div className="ml-auto shrink-0">{right}</div>}` — NO component change needed; author the slot content in the messages page.
- Back-Link chrome to copy for each control: `inline-flex h-11 w-11 shrink-0 items-center justify-center rounded-full ... focus-visible:ring-4 focus-visible:ring-shell-accent/40` (44px tap target + focus ring).

From apps/web/app/messages/[threadId]/page.tsx:
- The loader selects `id, state, both_ready, revoked_at` + the offer FK at lines 39-49; lock_id is NOT yet selected.
- DeepRouteHeader is imported (line 12) and rendered at line 86 with title={counterpart name}.

From apps/web/app/matches/[lockId]/LockDetail.tsx (NO change — confirm only):
- Night→Profile: the `see their profile` button (~line 98-104, opens RevealModal) — SATISFIES the contract.
- Night→Chat: `<Link href={`/messages/${threadId}`}>message {name}</Link>` (~line 115-126) — the one already-working edge.

From the DB (NO change — verify only):
- chat_threads_party_read policy + chat_thread_party(p_thread, p_uid) helper at 20260601100100_p7_chat_rls_party_read.sql:10-23. Re-creating the policy without `drop policy if exists` throws duplicate_object — do NOT re-create.
- chat_threads.lock_id already exists on the table (20260525124500_p2_chat_core.sql).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select lock_id + render the two reveal-gated chat-header controls</name>
  <files>apps/web/app/messages/[threadId]/page.tsx</files>
  <read_first>
    - apps/web/app/messages/[threadId]/page.tsx:38-58 (the select + the thread result cast type), :86-90 (DeepRouteHeader render)
    - apps/web/components/DeepRouteHeader.tsx:23-24,49 (the right slot), :37-43 (back-Link icon-button chrome to copy)
    - 06-RESEARCH.md §Pattern 5 + §Code Examples (E18 loader change)
    - 06-PATTERNS.md "messages/[threadId]/page.tsx" + "DeepRouteHeader" entries
    - 06-UI-SPEC.md §E18 (labels `their profile` / `the night`, both → /matches/[lockId], quiet ink, mandatory aria-label, reveal-gated)
  </read_first>
  <action>
    Add `lock_id` to the chat_threads select in messages/[threadId]/page.tsx (line ~41) and add `lock_id: string | null` to the thread result cast type (lines 51-58). Author the DeepRouteHeader `right` slot content: when `thread.lock_id != null`, render two icon-only Links, each a 44px tap target with the back-Link chrome (h-11 w-11 rounded-full + focus-visible:ring-4 ring-shell-accent/40) — Chat→Profile (lucide-react `UserRound`, aria-label `their profile`, href `/matches/${thread.lock_id}`) and Chat→Night (lucide-react icon e.g. `CalendarHeart` or `MapPin`, aria-label `the night`, href `/matches/${thread.lock_id}`). Both quiet ink by default per UI-SPEC (pink reserved; default both quiet). When lock_id is null (pre-lock thread) render NO control (reveal-gated — do not leak identity pre-lock). Wrap the two controls in a flex container with gap-2 and pass as the `right` prop. Use cn() for conditional classes; aria-label is mandatory on each icon-only control (UI-SPEC checker FLAG). Icons from lucide-react (already a dep) — no new imports beyond the icon.
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit && grep -q "lock_id" apps/web/app/messages/\[threadId\]/page.tsx && grep -q "their profile" apps/web/app/messages/\[threadId\]/page.tsx && grep -q "the night" apps/web/app/messages/\[threadId\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: loader selects lock_id; thread type includes lock_id; right slot renders two Links gated on lock_id != null
    - behavior: both controls href `/matches/${lock_id}`; each has an aria-label (`their profile`, `the night`); 44px tap targets + focus ring; quiet ink (no red)
    - security: controls render only when lock_id present (reveal-gated; no pre-lock identity leak — T-06-05)
    - test-command: `tsc --noEmit` clean + grep gate finds lock_id and both aria-labels
  </acceptance_criteria>
  <done>The locked conversation header links to the revealed profile and the night; pre-lock threads show no control.</done>
</task>

<task type="auto">
  <name>Task 2 (Wave 0): E2E for the 4 nav edges + chat-RLS deny-non-party SQL assertion</name>
  <files>apps/web/e2e/e18-chat-nav-edges.spec.ts, supabase/tests/e18_chat_rls_denies_nonparty.sql</files>
  <read_first>
    - apps/web/e2e/route-03-visual.spec.ts (existing forced-local E2E harness shape for an authed session)
    - apps/web/app/matches/[lockId]/LockDetail.tsx (the two existing edges to assert unchanged)
    - apps/web/app/messages/[threadId]/page.tsx (Task 1 output)
    - supabase/migrations/20260601100100_p7_chat_rls_party_read.sql:10-23 (the policy + helper being verified)
    - 06-RESEARCH.md §Validation Architecture → REQ-E18 rows; §Pitfall 5 (do not re-create the policy)
  </read_first>
  <action>
    (a) Create apps/web/e2e/e18-chat-nav-edges.spec.ts (Playwright, forced-local authed session, mirror the existing visual-spec harness): assert on a LOCKED conversation the two header controls render with aria-labels `their profile` and `the night`, both pointing at `/matches/<lockId>`; assert a PRE-LOCK thread (or a thread with null lock_id) renders NEITHER control; assert from LockDetail the existing Night→Profile (`see their profile`) and Night→Chat (`message {name}`) still navigate. (b) Create supabase/tests/e18_chat_rls_denies_nonparty.sql: a SQL assertion script that, under a non-party auth.uid() (set_config request.jwt.claims / set role to a non-party), SELECTs a chat_threads row by id and asserts 0 rows returned; then under a party uid asserts 1 row. This VERIFIES chat_threads_party_read denies non-parties — it does NOT create or drop the policy. RAISE EXCEPTION on assertion failure. Both files are run in plan 06-05 (E2E in the phase-gate visual/forced-local pass; SQL after local migration apply); this task authors them.
  </action>
  <verify>
    <automated>test -f apps/web/e2e/e18-chat-nav-edges.spec.ts && test -f supabase/tests/e18_chat_rls_denies_nonparty.sql && grep -q "their profile" apps/web/e2e/e18-chat-nav-edges.spec.ts && grep -qi "chat_threads" supabase/tests/e18_chat_rls_denies_nonparty.sql && ! grep -qi "create policy" supabase/tests/e18_chat_rls_denies_nonparty.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - source: e18-chat-nav-edges.spec.ts asserts all 4 edges + reveal-gating + aria-labels; SQL script asserts deny-non-party then allow-party
    - behavior: SQL script does NOT contain `create policy` (verify-only per RESEARCH Pitfall 5)
    - test-command: grep gate prints OK (specs executed in 06-05)
  </acceptance_criteria>
  <done>The 4-edge E2E and the chat-RLS deny-non-party SQL assertion are authored, ready for the 06-05 run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → chat_threads | a user requests a thread by id; RLS must scope to parties only |
| pre-lock thread → identity | the header must not expose a profile/night link before the reveal threshold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information disclosure | pre-lock identity leak via chat nav | mitigate | Chat→Profile/Night render gated on lock_id != null; pre-lock thread renders no control (reveal-gated) |
| T-06-06 | Information disclosure | chat thread read by a non-party | mitigate | chat_threads_party_read party-scoped policy (verify-only); deny-non-party SQL assertion added — no USING(true), no new policy created |
| T-06-07 | Tampering | accidental duplicate/loosened RLS on chat | accept | this plan creates NO RLS DDL; verify-only path (RESEARCH Pitfall 5) means no advisor delta for E18 |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs this plan (RESEARCH §Package Legitimacy Audit — zero installs) |
</threat_model>

<verification>
- `pnpm --filter web exec tsc --noEmit` clean
- loader selects lock_id; both header controls present with aria-labels (grep gate)
- e18-chat-nav-edges.spec.ts + e18_chat_rls_denies_nonparty.sql authored (executed in 06-05)
- No chat RLS DDL created (verify-only)
</verification>

<success_criteria>
The four chat↔profile↔night edges are satisfied: Chat→Profile + Chat→Night (new, reveal-gated on lock_id), Night→Profile + Night→Chat (existing, confirmed). chat_threads_party_read is verified to deny non-parties. REQ-E18 complete (E2E + SQL execution deferred to 06-05).
</success_criteria>

<output>
Create `.planning/phases/06-trust-and-safety-p2/06-02-SUMMARY.md` when done
</output>
