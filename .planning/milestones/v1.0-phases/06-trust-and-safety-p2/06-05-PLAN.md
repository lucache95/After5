---
phase: 06-trust-and-safety-p2
plan: 05
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - packages/types/src/database.types.ts
autonomous: false
requirements: [REQ-E17, REQ-E18, REQ-E19]

must_haves:
  truths:
    - "All four phase-6 migrations apply cleanly to the LOCAL Supabase stack and types regenerate from the LOCAL db"
    - "The Supabase security advisor shows only the established accepted DEFINER-warning pattern after the new DDL (no new HIGH findings, no USING(true))"
    - "All phase-6 SQL assertion scripts and the e18 Playwright spec pass against the local stack (the 4 safety-critical assertions included)"
    - "Visual-verify @420px confirms the reliability pill + reconfirm/check-in cards + chat-header edges are on-brand, warm-not-alarmist, no red"
    - "PROD migration apply is a SEPARATE human-gated step, NOT run during autonomous execution (gated-prod-apply)"
  artifacts:
    - path: "packages/types/src/database.types.ts"
      provides: "types regenerated from the LOCAL db after migrations apply"
  key_links:
    - from: "local Supabase stack"
      to: "packages/types/src/database.types.ts"
      via: "supabase gen types after local apply"
      pattern: "reliability_score|recompute_reliability"
---

<objective>
Phase-6 gate: apply all four phase-6 migrations to the LOCAL Supabase stack, regenerate types from LOCAL, run the Supabase security advisor, execute every phase-6 SQL/E2E assertion, visual-verify the new surfaces @420px, then present a human checkpoint for the GATED prod-apply (which is NOT executed autonomously).

Purpose: enforce the project's gated-prod-apply + secure-by-default rules. Build/types alone are a false positive — this plan is where local-green is actually proven and where prod application is deliberately deferred to a human.
Output: local-applied migrations + regenerated types + advisor-clean confirmation + green SQL/E2E + visual-verify notes, and a blocking checkpoint listing the four migrations for human prod-apply.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trust-and-safety-p2/06-RESEARCH.md
@.planning/phases/06-trust-and-safety-p2/06-UI-SPEC.md
@.planning/phases/06-trust-and-safety-p2/06-VALIDATION.md

<migrations_to_apply>
1. supabase/migrations/20260605120000_e17_recompute_reliability.sql (06-01) — net-new recompute_reliability + CREATE OR REPLACE close_rating_window
2. supabase/migrations/20260605120100_e19_safety_dispatch_rpcs.sql (06-03) — net-new dispatch_date_reconfirm + dispatch_safety_checkin
3. supabase/migrations/20260605120200_e19_lock_rpc_producers.sql (06-04) — CREATE OR REPLACE both lock RPCs
(No E18 migration — chat RLS is verify-only.)

GRANT note: all CREATE OR REPLACE functions (close_rating_window, both lock RPCs) keep their existing grants — no regrant needed. recompute_reliability + the two dispatch RPCs are net-new revoke-all-internal.
</migrations_to_apply>

<sql_and_e2e_to_run>
- supabase/tests/e17_recompute_reliability.sql (06-01)
- supabase/tests/e18_chat_rls_denies_nonparty.sql (06-02)
- supabase/tests/e19_safety_handlers.sql (06-03) — the 4 safety-critical assertions
- supabase/tests/e19_producers.sql (06-04)
- apps/web/e2e/e18-chat-nav-edges.spec.ts (06-02) — forced-local
</sql_and_e2e_to_run>
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING]: Local migration apply + type regen + advisor + run all phase-6 SQL/E2E</name>
  <files>packages/types/src/database.types.ts</files>
  <read_first>
    - .planning/STATE.md (gated-prod-apply rule + secure-by-default DB rules; prod ref ufufmcpnysvwtutpbian — do NOT touch)
    - the three migration files + the four SQL test scripts + the e18 spec listed in <context>
    - 06-RESEARCH.md §Validation Architecture → Sampling Rate (per-wave merge + phase gate)
  </read_first>
  <action>
    Against the LOCAL Supabase stack only (prod ref ufufmcpnysvwtutpbian MUST NOT be touched): run `supabase db reset` (or the project's local apply path) to apply the three phase-6 migrations in order. Regenerate types from the LOCAL db (the project's `supabase gen types` path) into packages/types/src/database.types.ts and confirm reliability_score / the new RPCs surface. Run the Supabase security advisor (mcp__supabase__get_advisors lint on the local project, or `supabase db lint`) and confirm: no new HIGH findings; the only DEFINER-executable warnings are the established accepted app pattern (every match_* RPC shares it); no USING(true); the three net-new/replaced functions pin search_path=public. Then execute every phase-6 assertion against the local stack: pipe each of the four supabase/tests/e1*.sql scripts through psql to the local db (a RAISE in any script = non-zero exit = fail) and run the forced-local Playwright spec `apps/web/e2e/e18-chat-nav-edges.spec.ts`. Also run the full unit suite (`pnpm -w test`) + `pnpm --filter web exec tsc --noEmit`. Record advisor output + each script's pass/fail in the SUMMARY. If anything fails, STOP and report — do NOT proceed to the prod checkpoint.
  </action>
  <verify>
    <automated>pnpm -w test && pnpm --filter web exec tsc --noEmit && grep -q "recompute_reliability\|reliability_score" packages/types/src/database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: three migrations applied to LOCAL; types regenerated; prod ref untouched
    - security: advisor shows only the established accepted DEFINER pattern, no new HIGH, no USING(true), search_path pinned on the new functions
    - behavior: all four SQL assertion scripts pass (incl. the 4 safety-critical assertions: no-ack-no-autocancel, chat RLS deny non-party, recompute math incl. no_show + new-until-3, handler poison-loop idempotency); e18 spec green; unit suite + tsc green
    - test-command: `pnpm -w test` + `tsc --noEmit` green and types contain the new reliability symbols
  </acceptance_criteria>
  <done>Local-green is proven: migrations apply, advisor clean, every phase-6 assertion + E2E passes. Prod still untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual-verify @420px — reliability pill + reconfirm/check-in cards + chat-header edges</name>
  <files>apps/web/components/ProfileCard.tsx, apps/web/app/matches/[lockId]/LockDetail.tsx, apps/web/app/messages/[threadId]/page.tsx</files>
  <action>Render the phase-6 surfaces forced-local @420px and present screenshots for human critique against 06-UI-SPEC.md (see how-to-verify). This is a verification-only checkpoint — no code is written; Claude drives the forced-local render + screenshots, the human judges brand/tone.</action>
  <what-built>
    The reliability pill on the revealed ProfileCard (`new here` blush new-member + `{score}% · reliable` sage-ticked established), the soft `still on?` reconfirm card + blush `no reply on the day-of check yet.` warning + `all good?` safety check-in card on LockDetail, and the two reveal-gated chat-header nav controls (`their profile`, `the night`). Forced-local render @420px per the project's visual-verify recipe.
  </what-built>
  <how-to-verify>
    1. Render the app forced-local @420px (the project's forced-local visual-verify harness) and screenshot: (a) a revealed ProfileCard for a NEW member → confirm `new here` blush pill, no number, encouraging not punitive; (b) a revealed ProfileCard for an established member → confirm `{score}% · reliable` with a tiny sage tick, neutral Tier-3 surface, no red, no hero-sized number; (c) LockDetail morning-of with a live reconfirm → confirm the `still on?` card (neutral fill, `yep, still on` / `gotta bail`, no red); (d) the `no reply` soft-warning line (blush/amber wash, no CTA, no red); (e) LockDetail post-window with a live check-in → `all good?` card + quiet `something's wrong` text-link; (f) a LOCKED conversation header → two icon controls with visible focus rings + correct aria-labels (`their profile`, `the night`), and a pre-lock thread shows NEITHER.
    2. Critique each against 06-UI-SPEC.md: lowercase/dry copy, no em-dashes, no red/destructive, warm-not-alarmist, 44px tap targets, blush/sage used only where specified.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the visual issues to fix.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: GATED PROD-APPLY — human applies the three migrations to prod (NOT autonomous)</name>
  <files>supabase/migrations/20260605120000_e17_recompute_reliability.sql, supabase/migrations/20260605120100_e19_safety_dispatch_rpcs.sql, supabase/migrations/20260605120200_e19_lock_rpc_producers.sql</files>
  <action>Hand the human the exact gated-prod-apply set (see how-to-verify). Claude does NOT apply to prod autonomously — the human runs MCP apply_migration in dependency order, redeploys process-jobs, and runs the prod advisor. Claude resumes only on the resume-signal.</action>
  <what-built>
    Three phase-6 migrations are local-green + advisor-clean. They are NOT applied to prod by this plan. This checkpoint hands the human the exact apply set.
  </what-built>
  <how-to-verify>
    Apply to prod ref ufufmcpnysvwtutpbian, in this dependency order, via the established MCP `apply_migration` gated-prod-apply path (NOT auto-pushed):
    1. 20260605120000_e17_recompute_reliability.sql (recompute_reliability + close_rating_window hook)
    2. 20260605120100_e19_safety_dispatch_rpcs.sql (dispatch_date_reconfirm + dispatch_safety_checkin)
    3. 20260605120200_e19_lock_rpc_producers.sql (CREATE OR REPLACE match_accept_offer + match_resolve_reciprocal — grants survive)
    Then on prod: deploy the updated `process-jobs` edge function (handlers.ts gained day_of_reconfirm + safety_checkin); run the security advisor on prod and confirm no new findings; spot-check that match_accept_offer's prod body now contains `day_of_reconfirm` (Pitfall 1 warning sign). Reliability backfill is OPTIONAL/forward-only (NULL renders as new — acceptable; only run a one-shot recompute loop if product wants existing reliable users badged now). Note the MCP-assigned version drift vs local filenames (pre-existing reconciliation pattern; local files remain source of truth).
  </how-to-verify>
  <resume-signal>Type "prod-applied" once the three migrations + the process-jobs redeploy are live and the prod advisor is clean, or "skip" to defer prod-apply.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local stack → prod (ufufmcpnysvwtutpbian) | gated-prod-apply: prod changes only via the human checkpoint, never autonomous |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Tampering | unreviewed DDL reaching prod | mitigate | prod-apply is a blocking human checkpoint (autonomous:false); local advisor must be clean first |
| T-06-17 | Elevation | a new DEFINER function shipping with an unpinned search_path or USING(true) | mitigate | advisor run on local (Task 1) AND prod (Task 3) before sign-off; all new functions verified search_path-pinned + revoke-all |
| T-06-18 | DoS | producer silently overwritten (superseded-migration trap) on prod | mitigate | Task 3 spot-checks prod match_accept_offer body contains day_of_reconfirm (Pitfall 1 warning sign) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs this plan (RESEARCH §Package Legitimacy Audit — zero installs) |
</threat_model>

<verification>
- `pnpm -w test` + `pnpm --filter web exec tsc --noEmit` green; types contain the new reliability symbols
- local advisor clean (only the established accepted DEFINER pattern); no USING(true)
- all four supabase/tests/e1*.sql pass + e18 Playwright spec green
- visual-verify @420px approved (human checkpoint)
- prod-apply performed by a human (or explicitly skipped) — NOT autonomous
</verification>

<success_criteria>
The three phase-6 migrations are local-applied + advisor-clean, types regenerated, all SQL/E2E assertions green, the new surfaces visual-verified @420px on-brand, and prod application is gated to a human checkpoint. Phase 6 (REQ-E17 + E18 + E19) verified local-green and ready for gated prod-apply.
</success_criteria>

<output>
Create `.planning/phases/06-trust-and-safety-p2/06-05-SUMMARY.md` when done
</output>
