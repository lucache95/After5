---
phase: 06-trust-and-safety-p2
plan: 04
type: execute
wave: 2
depends_on: ["06-03"]
files_modified:
  - supabase/migrations/20260605120200_e19_lock_rpc_producers.sql
  - supabase/tests/e19_producers.sql
autonomous: true
requirements: [REQ-E19]

must_haves:
  truths:
    - "match_accept_offer enqueues both day_of_reconfirm (morning-of, city tz) and safety_checkin (post-window) jobs beside its existing rating_window enqueue"
    - "match_resolve_reciprocal enqueues both jobs too — the reciprocal path is wired (uses p_chosen_instance, not inst)"
    - "Both lock RPCs keep their inherited grant to authenticated (CREATE OR REPLACE, never DROP) — PUBLIC C2 RPCs stay callable"
    - "Dedup keys mirror rating:||lid → reconfirm:||lid and checkin:||lid"
  artifacts:
    - path: "supabase/migrations/20260605120200_e19_lock_rpc_producers.sql"
      provides: "CREATE OR REPLACE of both live lock RPCs (full body + 2 new enqueues each)"
      contains: "enqueue_job('day_of_reconfirm'"
    - path: "supabase/tests/e19_producers.sql"
      provides: "SQL assertion: both lock RPCs enqueue both jobs with correct dedup keys"
  key_links:
    - from: "supabase/migrations/20260605120200_e19_lock_rpc_producers.sql (match_accept_offer)"
      to: "enqueue_job day_of_reconfirm + safety_checkin"
      via: "added beside the rating_window enqueue site"
      pattern: "enqueue_job\\('(day_of_reconfirm|safety_checkin)'"
    - from: "supabase/migrations/20260605120200_e19_lock_rpc_producers.sql (match_resolve_reciprocal)"
      to: "enqueue_job day_of_reconfirm + safety_checkin"
      via: "added beside the reciprocal rating_window enqueue site"
      pattern: "p_chosen_instance"
---

<objective>
Deliver the E19 producer half: re-create the two LIVE lock RPCs (match_accept_offer + match_resolve_reciprocal) via CREATE OR REPLACE, adding a day_of_reconfirm and a safety_checkin enqueue beside each function's existing rating_window enqueue, so accepting a date schedules both safety jobs atomically with the lock.

Purpose: closes REQ-E19 — accepting a date enqueues the two jobs whose handlers (06-03) dispatch the reconfirm/check-in notifications.
Output: one gated migration re-creating both lock RPCs in a band > 127800, plus the producers SQL assertion.

LOCKED CORRECTIONS (authoritative): (1) the LIVE definitions are in 20260527127800_p5_match_cohort_allowlist.sql (match_accept_offer @252, enqueue site @353-354; match_resolve_reciprocal @438, enqueue site @525-526). The older _p5_accept_lock.sql / _p5_b_complete.sql are SUPERSEDED — editing them is a runtime no-op. (2) The reciprocal path has NO `inst` local — the instance is `p_chosen_instance`. (3) Use CREATE OR REPLACE (NOT DROP+CREATE) — these PUBLIC C2 RPCs rely on an inherited `grant to authenticated`; DROP would strip it.

depends_on 06-03: the enqueued jobs route to day_of_reconfirm/safety_checkin handlers + dispatch RPCs created there; producers ship after consumers exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trust-and-safety-p2/06-RESEARCH.md
@.planning/phases/06-trust-and-safety-p2/06-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

LIVE source (the ONLY correct targets) — supabase/migrations/20260527127800_p5_match_cohort_allowlist.sql:
- match_accept_offer(p_actor uuid, p_offer uuid, p_idem_key uuid) RETURNS uuid — function @252.
  Locals available at the enqueue site: cre, cand, inst, rng (tstzrange), lid, lock_end.
  Existing rating_window enqueue @353-354:
    lock_end := upper(rng) + interval '2 hours';
    perform enqueue_job('rating_window', lock_end, jsonb_build_object('lock_id', lid, 'instance', inst), 'rating:'||lid::text);
- match_resolve_reciprocal(p_actor uuid, p_pair_id uuid, p_chosen_instance uuid, p_idem_key uuid) RETURNS uuid — function @438.
  Locals available: cre, cand, oid, lid, rng (tstzrange). NO `inst` local — the instance is p_chosen_instance.
  Existing rating_window enqueue @524-526:
    perform enqueue_job('rating_window', upper(rng) + interval '2 hours', jsonb_build_object('lock_id', lid, 'instance', p_chosen_instance), 'rating:'||lid::text);
- Both are declared SECURITY DEFINER SET search_path TO 'public'; the file has NO explicit grant line for these two — they rely on the inherited `grant to authenticated`. CREATE OR REPLACE preserves it. DROP would strip it.

City-tz resolution pattern — supabase/migrations/20260525123600_p2_dispatch_notification.sql:67-72:
- select c.timezone from profiles pr join cities c on c.id = pr.primary_city_id where pr.id = <user>;
- RESEARCH A5: for the DATE, resolve the city from the date_instance (the lock's instance), NOT primary_city_id. Confirm the date_instance→city FK before authoring run_after; degrade permissive (UTC offset) if tz unknown.

enqueue_job signature (20260525123100_p2_jobs_rpcs.sql):
- enqueue_job(p_type job_type, p_run_after timestamptz, p_payload jsonb, p_dedup_key text)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CREATE OR REPLACE both lock RPCs adding the two safety enqueues each (migration)</name>
  <files>supabase/migrations/20260605120200_e19_lock_rpc_producers.sql</files>
  <read_first>
    - supabase/migrations/20260527127800_p5_match_cohort_allowlist.sql:252-... (full match_accept_offer body), :353-354 (rating_window enqueue), :438-... (full match_resolve_reciprocal body), :524-526 (reciprocal rating_window enqueue)
    - supabase/migrations/20260525123600_p2_dispatch_notification.sql:67-72 (city-tz join pattern)
    - 06-RESEARCH.md §Pattern 3 + §Pitfall 1 + §Pitfall 2 + §Anti-Patterns (CREATE OR REPLACE, both paths, p_chosen_instance, tz anchor)
    - 06-PATTERNS.md "2026060x_e19_lock_rpc_producers.sql" entry
  </read_first>
  <action>
    Create supabase/migrations/20260605120200_e19_lock_rpc_producers.sql that CREATE OR REPLACEs BOTH functions, copying their FULL current body from 20260527127800 (preserving signatures, SECURITY DEFINER SET search_path, all existing logic and the rating_window enqueue), and adding beside each rating_window enqueue:
    - day_of_reconfirm: run_after = the morning-of the date in the date city's tz. Resolve the tz by joining the lock's date_instance to its city (cities.timezone), reusing the dispatch_notification join pattern but keyed off the date_instance (RESEARCH A5 — confirm the date_instance→city FK; if the FK/tz is unresolved, degrade permissive to a UTC-offset fallback e.g. `lower(rng) - interval '6 hours'`). Compute `date_trunc('day', lower(rng) at time zone v_tz) at time zone v_tz + interval '9 hours'`. Payload `jsonb_build_object('lock_id', lid)`. Dedup key `'reconfirm:'||lid::text`.
    - safety_checkin: run_after = `upper(rng) + interval '2 hours'` (post-window). Payload `jsonb_build_object('lock_id', lid)`. Dedup key `'checkin:'||lid::text`.
    In match_accept_offer use the available locals (cre/cand/inst/rng/lid/lock_end). In match_resolve_reciprocal there is NO `inst` — use p_chosen_instance where an instance is needed and the reciprocal locals (cre/cand/oid/lid/rng). Use CREATE OR REPLACE for both (grants survive — do NOT DROP, do NOT add a revoke/grant line for these two PUBLIC C2 RPCs). Do NOT apply to prod — local apply gated to 06-05.
  </action>
  <verify>
    <automated>grep -c "enqueue_job('day_of_reconfirm'" supabase/migrations/20260605120200_e19_lock_rpc_producers.sql | grep -qx 2 && grep -c "enqueue_job('safety_checkin'" supabase/migrations/20260605120200_e19_lock_rpc_producers.sql | grep -qx 2 && grep -q "p_chosen_instance" supabase/migrations/20260605120200_e19_lock_rpc_producers.sql && grep -q "create or replace function match_accept_offer" supabase/migrations/20260605120200_e19_lock_rpc_producers.sql && grep -q "create or replace function match_resolve_reciprocal" supabase/migrations/20260605120200_e19_lock_rpc_producers.sql && ! grep -qiE "drop function (if exists )?match_(accept_offer|resolve_reciprocal)" supabase/migrations/20260605120200_e19_lock_rpc_producers.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - source: both functions CREATE OR REPLACE'd with full body; each gains a day_of_reconfirm + safety_checkin enqueue (2 of each total across the two functions)
    - source: reciprocal path uses p_chosen_instance; no DROP of either RPC (grants preserved)
    - behavior: dedup keys reconfirm:||lid and checkin:||lid; morning-of tz anchor with permissive degrade
    - security: SECURITY DEFINER SET search_path preserved on both; no USING(true); no grant/revoke change to the two PUBLIC RPCs
    - test-command: grep gate prints OK (local apply + advisor gated to 06-05)
  </acceptance_criteria>
  <done>Both lock paths schedule the reconfirm + check-in jobs atomically with the lock; grants intact; reciprocal path correct.</done>
</task>

<task type="auto">
  <name>Task 2 (Wave 0 SQL scaffold): e19 producers SQL assertion — both paths enqueue both jobs</name>
  <files>supabase/tests/e19_producers.sql</files>
  <read_first>
    - supabase/migrations/20260605120200_e19_lock_rpc_producers.sql (Task 1 output)
    - supabase/migrations/20260525123100_p2_jobs_rpcs.sql (jobs table shape for assertions)
    - 06-RESEARCH.md §Validation Architecture → REQ-E19 producers row; §Pitfall 2 (both paths)
  </read_first>
  <action>
    Create supabase/tests/e19_producers.sql as a SQL assertion script. Seed the fixtures and drive each lock RPC to a successful lock (or, if a full RPC drive is impractical in a SQL script, assert at the enqueue contract level): after match_accept_offer produces a lock `lid`, assert jobs rows exist with dedup keys `reconfirm:`||lid and `checkin:`||lid AND a `rating:`||lid (confirming the new enqueues sit beside the existing one); repeat for a reciprocal lock via match_resolve_reciprocal, asserting the same three dedup keys exist for that lid (proving Pitfall 2 — the reciprocal path is wired). RAISE EXCEPTION on any missing job. Executed in 06-05 after local migration apply; this task authors it.
  </action>
  <verify>
    <automated>test -f supabase/tests/e19_producers.sql && grep -q "reconfirm:" supabase/tests/e19_producers.sql && grep -q "checkin:" supabase/tests/e19_producers.sql && grep -q "match_resolve_reciprocal" supabase/tests/e19_producers.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - source: script asserts both reconfirm:||lid and checkin:||lid jobs for BOTH the accept path and the reciprocal path
    - behavior: assertions RAISE on a missing job; reciprocal path explicitly covered (Pitfall 2)
    - test-command: file exists referencing both dedup prefixes + match_resolve_reciprocal (grep gate prints OK)
  </acceptance_criteria>
  <done>The producer-coverage SQL contract (both paths, both jobs) is authored, ready for 06-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → match_accept_offer / match_resolve_reciprocal | PUBLIC C2 RPCs; re-check auth.uid() internally (unchanged); now also enqueue safety jobs atomically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Elevation | DROP+CREATE stripping the authenticated grant (RPCs become uncallable / regrant drift) | mitigate | CREATE OR REPLACE only (grants survive); grep gate forbids DROP of either RPC |
| T-06-13 | Tampering | producer wired into a superseded migration (no-op at runtime) | mitigate | targets the LIVE 127800 lineage in a band > 127800; producers SQL assertion proves jobs actually enqueue post-apply |
| T-06-14 | Elevation | search_path hijack on the re-created RPCs | mitigate | SECURITY DEFINER SET search_path preserved verbatim from 127800; advisor run in 06-05 |
| T-06-15 | DoS | reciprocal path missing the jobs (Pitfall 2) | mitigate | both paths wired (p_chosen_instance); e19_producers.sql asserts reciprocal coverage |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs this plan (RESEARCH §Package Legitimacy Audit — zero installs) |
</threat_model>

<verification>
- migration grep gate prints OK (2× day_of_reconfirm, 2× safety_checkin, p_chosen_instance, CREATE OR REPLACE both, no DROP)
- supabase/tests/e19_producers.sql authored (run in 06-05)
- Local apply + advisor + prod apply gated to 06-05
</verification>

<success_criteria>
Both lock RPCs (accept + reciprocal) enqueue day_of_reconfirm + safety_checkin beside their rating_window enqueue, via CREATE OR REPLACE with grants intact, reciprocal path using p_chosen_instance. REQ-E19 producer half complete (local apply + advisor + the e19_producers.sql run in 06-05).
</success_criteria>

<output>
Create `.planning/phases/06-trust-and-safety-p2/06-04-SUMMARY.md` when done
</output>
