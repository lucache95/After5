---
phase: 06-trust-and-safety-p2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260605120000_e17_recompute_reliability.sql
  - packages/business/src/reliability.test.ts
  - packages/business/src/eligibility.ts
  - apps/web/components/ProfileCard.tsx
  - apps/web/components/__tests__/ProfileCard.test.tsx
  - apps/web/app/matches/[lockId]/RevealModal.tsx
  - apps/web/app/matches/[lockId]/page.tsx
  - supabase/tests/e17_recompute_reliability.sql
autonomous: true
requirements: [REQ-E17]

must_haves:
  truths:
    - "A reliability_score (0-100 integer percent) is computed from match_ratings for a ratee and written to profiles.reliability_score (per D-01)"
    - "A profile with <3 rated dates keeps reliability_score NULL and renders the 'new here' new-member treatment, not a number (per D-01)"
    - "no_show locks (locks.status='no_show') count as missed dates in the score and toward the >=3 threshold, read from locks NOT match_ratings (per D-02)"
    - "close_rating_window recomputes reliability for BOTH lock parties after stamping rating_closed_at, idempotently"
    - "The revealed ProfileCard renders a reliability pill: 'new here' (blush) when isNew, '{score}% · reliable' (sage tick) when established"
  artifacts:
    - path: "supabase/migrations/20260605120000_e17_recompute_reliability.sql"
      provides: "recompute_reliability(uuid) DEFINER + close_rating_window CREATE OR REPLACE calling it for both parties"
      contains: "recompute_reliability"
    - path: "packages/business/src/reliability.test.ts"
      provides: "Unit coverage of the reliability formula + >=3 threshold + badgeFor mapping"
    - path: "apps/web/components/ProfileCard.tsx"
      provides: "Reliability pill in the header meta row driven by badgeFor()"
      contains: "badgeFor"
    - path: "supabase/tests/e17_recompute_reliability.sql"
      provides: "SQL assertion: no_show feed + idempotent double-call no-op"
  key_links:
    - from: "supabase/migrations/20260605120000_e17_recompute_reliability.sql (close_rating_window)"
      to: "recompute_reliability"
      via: "perform recompute_reliability for l.creator_id and l.matched_user_id"
      pattern: "perform recompute_reliability"
    - from: "apps/web/components/ProfileCard.tsx"
      to: "@after5/business badgeFor"
      via: "import + badgeFor({ verification, reliability_score })"
      pattern: "badgeFor"
    - from: "apps/web/app/matches/[lockId]/RevealModal.tsx"
      to: "apps/web/components/ProfileCard.tsx"
      via: "verification + reliability_score props pass-through"
      pattern: "reliability_score"
---

<objective>
Deliver E17 reliability: a dedicated DEFINER aggregation RPC computes reliability_score from match_ratings (+ no_show locks) when the rating window closes, and the revealed ProfileCard surfaces it as a warm, non-punitive pill via the existing badgeFor() derivation and public_profile_card view.

Purpose: closes the trust half of the loop — a rated user earns a visible reliability signal; a new user is encouraged ("new here"), not punished. Honors D-01 (weighted %, new-until-3) and D-02 (no_show feeds the score, no enforcement).
Output: one gated migration (recompute_reliability + close_rating_window hook), the reliability pill on ProfileCard, the prop wiring from RevealModal + lock loader, and the unit/SQL tests proving the formula and no_show feed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trust-and-safety-p2/06-RESEARCH.md
@.planning/phases/06-trust-and-safety-p2/06-PATTERNS.md
@.planning/phases/06-trust-and-safety-p2/06-UI-SPEC.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From packages/business/src/eligibility.ts:
- export type VerificationState = 'unverified' | 'pending' | 'verified' | 'failed' | 'appeal';
- export const MIN_RATINGS_FOR_ESTABLISHED = 3;
- export interface BadgeInput { verification: VerificationState; reliability_score: number | null; }
- export function badgeFor(p: BadgeInput): { verified: boolean; isNew: boolean }
  (verified = verification === 'verified'; isNew = verified && reliability_score == null)

From apps/web/components/ProfileCard.tsx:
- export interface ProfileCardProps { ... } (header block at line ~70; vibe-chip pill at lines 81-86 is the pill chrome to mirror)
- export function ProfileCard({ ... }: ProfileCardProps)

From apps/web/app/matches/[lockId]/page.tsx:
- The lock loader selects creator_id/matched_user_id/date_instance_id and resolves `counterpart` (line ~63), passing it into <LockDetail .../> with title={counterpart.first_name}. RevealModal is rendered with the counterpart profile. reliability_score + verification must be selected for the counterpart so RevealModal can pass them to ProfileCard.

From the DB (no change needed to these):
- profiles.reliability_score numeric(4,2) — nullable; NULL = "new"
- public_profile_card view already projects reliability_score + badge_is_new
- close_rating_window(p_lock uuid) at 20260527127200_p5_job_rpcs_backfill.sql:79-100 — the CREATE OR REPLACE target
- match_ratings columns: showed_up, on_time, cancelled_with_notice, unsafe_or_disrespectful, lock_id, ratee_id (indexed match_ratings_ratee_idx)
- locks: status (lock_status enum incl. 'no_show'), creator_id, matched_user_id
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Reliability formula unit spec + extract a pure scoring function</name>
  <files>packages/business/src/reliability.test.ts, packages/business/src/reliability.ts, packages/business/src/index.ts</files>
  <read_first>
    - packages/business/src/eligibility.ts (badgeFor, MIN_RATINGS_FOR_ESTABLISHED, VerificationState)
    - 06-RESEARCH.md §Architecture Pattern 1 (recommended weights) + §Pitfall 4 (rated∩no_show overlap)
    - 06-PATTERNS.md "reliability.test.ts" entry (Vitest mock pattern)
  </read_first>
  <behavior>
    - Test: a ratee with all-showed_up, on_time, no unsafe ratings (>=3 dates) scores high (near 100)
    - Test: a ratee with 3 no_show locks (no match_ratings rows) scores 0 and is NOT new (>=3 dates counted)
    - Test: a ratee with <3 total rated+no_show dates returns null (new-member)
    - Test: an unsafe_or_disrespectful=true date applies a hard penalty to that date's contribution (floored at 0)
    - Test: badgeFor({verification:'verified', reliability_score:null}).isNew === true
    - Test: badgeFor({verification:'verified', reliability_score:94}).isNew === false
  </behavior>
  <action>
    Create packages/business/src/reliability.test.ts. Extract the scoring math into a pure exported function `computeReliability(input)` in a new packages/business/src/reliability.ts that the SQL formula mirrors 1:1: input is an array of per-date outcomes ({ showed_up, on_time, cancelled_with_notice, unsafe_or_disrespectful } | { no_show: true }) plus the MIN_RATINGS_FOR_ESTABLISHED threshold; returns number | null. Lock the EXACT weights here (these become the SQL weights in Task 2): per-date contribution = showed_up component weighted ~0.6 (showed_up true → full), on_time +~0.2, cancelled_with_notice +~0.2, unsafe_or_disrespectful applies a fixed −40 to that date floored at 0; each no_show date contributes 0. Average across (rated + no_show) dates, clamp 0-100, round to integer. Return null when (rated + no_show) < 3. Export computeReliability from packages/business/src/index.ts. The rated∩no_show overlap rule (RESEARCH Pitfall 4 / Open Q3): no_show is authoritative — a lock that is no_show counts once as a missed date and is NOT also credited a showed_up rating. Encode this in computeReliability's input contract (callers pass no_show locks separately from rated locks; a lock appears in exactly one bucket).
  </action>
  <verify>
    <automated>pnpm vitest run packages/business/src/reliability.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: packages/business/src/reliability.ts exports computeReliability; re-exported from index.ts
    - behavior: all 6 behavior cases above pass
    - test-command: `pnpm vitest run packages/business/src/reliability.test.ts` is green
  </acceptance_criteria>
  <done>computeReliability is a pure tested function whose weights the SQL formula will copy verbatim; badgeFor mapping covered.</done>
</task>

<task type="auto">
  <name>Task 2: recompute_reliability DEFINER RPC + close_rating_window hook (migration)</name>
  <files>supabase/migrations/20260605120000_e17_recompute_reliability.sql</files>
  <read_first>
    - supabase/migrations/20260527127200_p5_job_rpcs_backfill.sql:79-100 (close_rating_window analog: null-guard, found-check, idempotent no-op, revoke-all)
    - supabase/migrations/20260604121000_e5_loop_completion.sql:155-157 (flag_no_show writes locks.status only — no match_ratings row)
    - supabase/migrations/20260525120800_p0_match_ratings.sql:1-15 (match_ratings columns + ratee index)
    - supabase/migrations/20260525120100_p0_profiles_dating.sql:35 (reliability_score numeric(4,2) nullable)
    - packages/business/src/reliability.ts (the locked weights from Task 1 — copy them exactly)
    - 06-RESEARCH.md §Pattern 1 + §Pattern 2 + §Migrations List row (a)
    - 06-PATTERNS.md "2026060x_e17_recompute_reliability.sql" entry
  </read_first>
  <action>
    Create supabase/migrations/20260605120000_e17_recompute_reliability.sql with TWO functions. (1) recompute_reliability(p_ratee uuid) returns void, language plpgsql, security definer, set search_path=public: null-guard returns early; count distinct rated locks from match_ratings where ratee_id=p_ratee; count no_show locks where status='no_show' and (creator_id=p_ratee or matched_user_id=p_ratee), EXCLUDING any lock_id that already has a match_ratings row for this ratee (no_show authoritative, no double-count — RESEARCH Pitfall 4); if (rated + no_show) < 3 then update profiles set reliability_score = null and return; otherwise compute the weighted percentage using the EXACT weights from packages/business/src/reliability.ts (showed_up ~0.6, on_time/cancelled_with_notice ~0.2 each, unsafe_or_disrespectful −40 fixed penalty floored at 0, each no_show date = 0, averaged over rated+no_show, clamped greatest(0,least(100,...)), rounded to integer); update profiles set reliability_score accordingly. Add a SQL comment documenting the rated∩no_show overlap rule. (2) CREATE OR REPLACE close_rating_window(p_lock uuid) — copy the full current body from the analog (preserving its rating_closed_at stamping, analytics_events insert, null/found/idempotent guards and grants), then AFTER the rating_closed_at update add `perform recompute_reliability(l.creator_id); perform recompute_reliability(l.matched_user_id);` (select creator_id/matched_user_id into locals from the lock row). Trailing grants: `revoke all on function recompute_reliability(uuid) from public, anon, authenticated;`. Do NOT add a revoke/grant change for close_rating_window beyond what the analog already declares (CREATE OR REPLACE preserves its grants). Do NOT apply to prod — local apply is gated to plan 06-05.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260605120000_e17_recompute_reliability.sql | grep -c "perform recompute_reliability" | grep -qx 2 && grep -q "set search_path=public" supabase/migrations/20260605120000_e17_recompute_reliability.sql && grep -q "revoke all on function recompute_reliability" supabase/migrations/20260605120000_e17_recompute_reliability.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - source: recompute_reliability and close_rating_window both present; close_rating_window calls recompute_reliability for both parties
    - source: no_show counted from locks.status, NOT match_ratings; overlap rule documented in a comment
    - security: recompute_reliability pins search_path=public + revoke-all from public/anon/authenticated; no USING(true); no DROP of close_rating_window
    - test-command: the grep gate above prints OK (local apply + advisor run deferred to 06-05)
  </acceptance_criteria>
  <done>The migration computes reliability with the Task-1 weights and hooks both parties into the window-close, never raising on a stale lock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reliability pill on ProfileCard + prop wiring (RevealModal + lock loader)</name>
  <files>apps/web/components/ProfileCard.tsx, apps/web/components/__tests__/ProfileCard.test.tsx, apps/web/app/matches/[lockId]/RevealModal.tsx, apps/web/app/matches/[lockId]/page.tsx</files>
  <read_first>
    - apps/web/components/ProfileCard.tsx (header block ~70-75; vibe-chip pill chrome 81-86)
    - apps/web/components/__tests__/ProfileCard.test.tsx (existing mock pattern to extend)
    - apps/web/app/matches/[lockId]/RevealModal.tsx:90-97 (the <ProfileCard .../> call to extend)
    - apps/web/app/matches/[lockId]/page.tsx (counterpart select ~34, RevealModal render — add reliability_score + verification to the counterpart select)
    - packages/business/src/eligibility.ts:14-17 (badgeFor)
    - 06-UI-SPEC.md §E17 (blush new-member pill, sage tick established, 0-100 integer, aria-label, no red, no hero number)
    - 06-PATTERNS.md "ProfileCard.tsx" + "RevealModal.tsx" entries
  </read_first>
  <behavior>
    - Test: ProfileCard with verification='verified' + reliability_score=null renders text "new here", NO percentage number
    - Test: ProfileCard with verification='verified' + reliability_score=94 renders "94% · reliable" and an aria-label spelling out the score
    - Test: the pill carries an aria-label in both states (new-member + established)
  </behavior>
  <action>
    Add `verification: VerificationState` and `reliability_score: number | null` to ProfileCardProps. In the ProfileCard header meta row (under the name heading), render a single rounded-full pill driven by badgeFor({verification, reliability_score}) imported from @after5/business: isNew → `new here` on a blush wash (`bg-[#FFB3D1]/25 text-profile-ink/80`, text-[13px] semibold, no number) with optional subtext `no rated dates yet` at text-[13px] text-profile-ink/50; established → `{score}% · reliable` on `bg-profile-ink/[0.06]` with a tiny sage tick (lucide-react `Check`, the only non-neutral color on this Tier-3 surface). Pill carries an explicit aria-label per UI-SPEC (`new member, no rated dates yet` / `reliability: {score} percent, established`). Mirror the vibe-chip pill chrome (lines 81-86). Use cn() for conditional classes; no hardcoded class concatenation. In RevealModal.tsx pass `verification={...}` and `reliability_score={...}` through to ProfileCard. In matches/[lockId]/page.tsx add `reliability_score` and `verification` to the counterpart profile select so RevealModal receives them (source from the counterpart's profiles row / public_profile_card). Extend apps/web/components/__tests__/ProfileCard.test.tsx with the three behavior cases.
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/components/__tests__/ProfileCard.test.tsx && pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - source: ProfileCardProps has verification + reliability_score; pill rendered via badgeFor; RevealModal + lock loader pass them through
    - behavior: the three ProfileCard test cases pass; aria-label present in both states; no hero-sized score number
    - color: new-member uses blush wash, established uses neutral + tiny sage tick; no red introduced (UI-SPEC §Color)
    - test-command: `pnpm vitest run apps/web/components/__tests__/ProfileCard.test.tsx` green + `tsc --noEmit` clean
  </acceptance_criteria>
  <done>The revealed profile shows a warm reliability pill; visual-verify @420px is deferred to the 06-05 phase gate.</done>
</task>

<task type="auto">
  <name>Task 4 (Wave 0 SQL scaffold): e17 SQL assertion script — no_show feed + idempotent recompute</name>
  <files>supabase/tests/e17_recompute_reliability.sql</files>
  <read_first>
    - supabase/migrations/20260605120000_e17_recompute_reliability.sql (Task 2 output)
    - supabase/migrations/20260525120800_p0_match_ratings.sql (match_ratings shape for seeding)
    - 06-RESEARCH.md §Validation Architecture → Test Map rows for REQ-E17
  </read_first>
  <action>
    Create supabase/tests/e17_recompute_reliability.sql as a SQL assertion script (the project's local-apply verification posture; no pgTAP harness in-tree). Seed two profiles + locks: (a) a ratee with 3 no_show locks and zero match_ratings rows → call recompute_reliability → assert reliability_score = 0 (NOT null, since >=3 dates counted from locks). (b) a ratee with 3 positive match_ratings (showed_up=true) → call recompute_reliability → assert a high score and reliability_score IS NOT NULL. (c) a ratee with <3 total dates → assert reliability_score IS NULL (new-member). (d) idempotency: call close_rating_window twice for the same lock → assert no error and a stable reliability_score (double-call no-op). Use RAISE EXCEPTION on any failed assertion so a non-zero exit signals failure when piped to psql. This script is EXECUTED in plan 06-05 against the local stack after migrations apply; this task only authors it.
  </action>
  <verify>
    <automated>test -f supabase/tests/e17_recompute_reliability.sql && grep -q "recompute_reliability" supabase/tests/e17_recompute_reliability.sql && grep -q "no_show" supabase/tests/e17_recompute_reliability.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - source: script seeds no_show locks + rated locks + a <3 case and asserts each expected reliability_score
    - behavior: assertions RAISE on mismatch (executed in 06-05); no_show-as-missed + new-until-3 both covered
    - test-command: file exists with recompute_reliability + no_show references (grep gate prints OK)
  </acceptance_criteria>
  <done>The SQL contract for the no_show feed + idempotent recompute is authored, ready for the 06-05 local-apply run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job consumer (service-role) → recompute_reliability | close_rating_window runs as the rating_window handler (service-role); recompute writes profiles.reliability_score |
| client → ProfileCard | reliability_score arrives via the RLS-scoped lock loader; no client write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | reliability_score direct UPDATE | mitigate | reliability_score written ONLY by recompute_reliability (revoke-all DEFINER); no client/anon/authenticated grant — no write path |
| T-06-02 | Elevation | recompute_reliability search_path hijack | mitigate | `set search_path=public` pinned (mirrors every existing RPC); advisor run in 06-05 |
| T-06-03 | Information disclosure | raw ratings leak via badge | mitigate | only the aggregate (reliability_score) is written/surfaced; recompute reads match_ratings server-side and writes a single number — raw rating rows never leave the DB |
| T-06-04 | Tampering | rating stuffing inflating score | accept | match_ratings_rater_insert policy (already exists) requires rater participation; no new write path added this plan |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs this plan (verified: RESEARCH §Package Legitimacy Audit — zero installs) |
</threat_model>

<verification>
- `pnpm vitest run packages/business/src/reliability.test.ts` green (formula + threshold)
- `pnpm vitest run apps/web/components/__tests__/ProfileCard.test.tsx` green (pill states + aria-label)
- `pnpm --filter web exec tsc --noEmit` clean
- migration grep gate prints OK; recompute_reliability pins search_path + revoke-all
- supabase/tests/e17_recompute_reliability.sql authored (run in 06-05)
- Local migration apply + advisor + prod apply are all gated to plan 06-05 (NOT this plan)
</verification>

<success_criteria>
reliability_score is computed from match_ratings + no_show locks with the locked weights, recomputed for both parties on window-close, NULL until >=3 dates; the revealed ProfileCard renders a warm reliability pill (blush "new here" / sage-ticked "{score}% · reliable") with an aria-label and no red. REQ-E17 backend + badge UI complete (local migration apply + visual-verify deferred to 06-05).
</success_criteria>

<output>
Create `.planning/phases/06-trust-and-safety-p2/06-01-SUMMARY.md` when done
</output>
