---
phase: 01-navigation-profile-spine-p0
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/web/app/account/page.tsx
  - apps/web/components/SelfViewSheet.tsx
  - apps/web/app/account/__tests__/page.test.tsx
autonomous: true
requirements: [REQ-E3]
must_haves:
  truths:
    - "The /account hub shows the user's identity (photo, name, age, city, verification)"
    - "The hub shows a dating-profile summary (bio, prompt preview, vibe chips), with an authored empty-state when bare"
    - "The hub offers an 'as others see it' self-view that opens ProfileCard (reused, not rebuilt) with the user's own photos + fields"
    - "The hub links to edit profile (/account/profile), preferences (/account/preferences), and notifications (/account/notifications)"
    - "The hub renders NO marketing/onboarding teaser content, while keeping its existing working links (saved-plans wedge, sign-out, edit-profile, post-a-night, your-nights)"
  artifacts:
    - path: "apps/web/app/account/page.tsx"
      provides: "Enhanced profile hub: identity header + dating summary + self-view entry + secondary links"
      contains: "SelfViewSheet"
    - path: "apps/web/components/SelfViewSheet.tsx"
      provides: "Thin vaul wrapper rendering ProfileCard in preview ('as others see it')"
      contains: "ProfileCard"
  key_links:
    - from: "apps/web/app/account/page.tsx"
      to: "/account/preferences and /account/notifications"
      via: "secondary link rows"
      pattern: "/account/(preferences|notifications)"
    - from: "apps/web/components/SelfViewSheet.tsx"
      to: "ProfileCard"
      via: "vaul Drawer wrapping the reused card"
      pattern: "ProfileCard"
    - from: "apps/web/app/account/page.tsx"
      to: "signed owner photos"
      via: "listMyPhotos + signClearUrls"
      pattern: "listMyPhotos"
---

<objective>
Make the profile tab land on a real identity hub (REQ-E3 / D-01, D-02, D-03): enhance the existing `/account` server component in place to lead with IDENTITY — add an identity header (photo/name/age/city/verification), a dating-profile summary (bio/prompts/vibe), an "as others see it" self-view that reuses `ProfileCard`, and clear secondary links to edit/preferences/notifications. Strip the `/home` marketing-teaser association (F1) WITHOUT over-stripping the hub's existing working links.

This is an independently-shippable vertical slice: after it lands (and the E2 tab repoint), a real user tapping "profile" reaches their dating home — identity-forward, with their own card preview and a path to settings — not a marketing page and not a settings dump.

**De-scoped (do NOT build here):** computed profile stats (nights hosted/matches/response rate/reviews) are E17/Phase 6 (need reliability aggregation that doesn't exist yet); the `/account`→`/plan/i/` dead link is Phase 7 (D-06). Reuse `ProfileCard` — do not rebuild a profile preview.

Purpose: ISSUE #15 — the profile tab finally has a real, identity-forward destination aggregating who the user is + where to manage it.
Output: enhanced `account/page.tsx`, a new `SelfViewSheet.tsx` (thin vaul wrapper over `ProfileCard`), and a component test asserting identity fields + self-view trigger + links + no-teaser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-navigation-profile-spine-p0/01-CONTEXT.md
@.planning/phases/01-navigation-profile-spine-p0/01-RESEARCH.md
@.planning/phases/01-navigation-profile-spine-p0/01-PATTERNS.md
@.planning/phases/01-navigation-profile-spine-p0/01-UI-SPEC.md
@docs/superpowers/DESIGN-SYSTEM.md

<interfaces>
<!-- Enhance in place (D-01). ADDITIVE — do not over-strip (Pitfall 4). All verified by RESEARCH/PATTERNS. -->
account/page.tsx existing skeleton to KEEP: force-dynamic; getUser then redirect('/login?next=/account'); masthead header; NotificationToast userId + BottomTabShell userId at bottom; saved-plans wedge; sign-out; edit-profile link; post-a-night CTA; your-nights row.

Widen the profiles read (current select is only first_name) — mirror account/profile/page.tsx:23-30:
  profiles.select('first_name, age, city, neighborhood, verification, clear_photo_url, vibe_tags, prompt_answers, pronouns, height_cm, occupation').eq('id', user.id).maybeSingle()
  profiles_private.select('bio, instagram_handle').eq('user_id', user.id).maybeSingle()   // bio lives here; instagram NOT passed to self-view

Owner photo load (copy verbatim from account/profile/page.tsx:10,32-52):
  listMyPhotos(supabase, user.id) then signClearUrls(supabase, rows.map(r => r.clear_path)); legacy clear_photo_url fallback when no gallery rows.

Prompt-label join (copy from matches/[lockId]/page.tsx:88-99): prompt_answers to {label, answer}[] for ProfileCard.prompts.

ProfileCard contract (components/ProfileCard.tsx:14-26 — reuse, D-03):
  { name, age, place, pronouns?, occupation?, height_cm?, photos[] (signed clear, primary first), vibe_tags[], prompts[{label,answer}] }   // OMIT instagram_handle (A1 — self-preview, no PII contact card)

SelfViewSheet (NEW — analog matches/[lockId]/RevealModal.tsx, a thin vaul Drawer over ProfileCard):
  'use client'; Drawer.Root open onOpenChange; Drawer.Title sr-only "as others see it"; renders ProfileCard; drag-to-dismiss + Escape.

Secondary link rows (reuse the existing hub icon-well + ArrowRight row visual, account/page.tsx:92-108):
  edit profile to /account/profile ; preferences to /account/preferences ; notifications to /account/notifications ; your nights to /my-nights (keep, D-04).

Reserved-accent rule (UI-SPEC §Color): shell.accent only on the after5 wordmark, post-a-night CTA, active nav, icon wells, focus rings, inline links. Self-view stays Tier-3 neutral (profile.base/profile.ink) — do not re-skin ProfileCard.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SelfViewSheet — a thin vaul wrapper reusing ProfileCard ('as others see it')</name>
  <files>apps/web/components/SelfViewSheet.tsx</files>
  <read_first>
    - apps/web/app/matches/[lockId]/RevealModal.tsx (the EXACT analog — a thin vaul Drawer already wrapping ProfileCard)
    - apps/web/components/ProfileCard.tsx (the reuse target — prop contract lines 14-26)
    - .planning/phases/01-navigation-profile-spine-p0/01-PATTERNS.md (§Self-view sheet wrapper — copy-ready excerpt)
    - .planning/phases/01-navigation-profile-spine-p0/01-UI-SPEC.md (§Self-View — sheet title, dismiss, Tier-3 neutral, omit instagram)
  </read_first>
  <action>
    Create `apps/web/components/SelfViewSheet.tsx` (`'use client'`) modeled on `RevealModal.tsx`: a `vaul` `Drawer.Root` (controlled `open`/`onOpenChange`) with a bottom-sheet `Drawer.Content` (`rounded-t-3xl bg-shell-base`, max-h ~92dvh, drag handle, `Drawer.Title` sr-only `as others see it`), rendering the REUSED `ProfileCard` (D-03 — do not rebuild) fed the owner's own signed clear photos + `{name, age, place, pronouns, occupation, height_cm, vibe_tags, prompts}`. Do NOT pass `instagram_handle` (A1 — self-preview is not a contact card). Dismissible via drag + Escape + overlay click. Tap targets at least 44px; the self-view content stays Tier-3 neutral (do not re-skin ProfileCard to Barbiecore). Keep the wrapper thin — presentation/event-handling only, no data fetching. The trigger (a `preview my profile` row with an `Eye` icon) lives in the hub page (Task 2) and toggles this sheet's `open` state.
  </action>
  <verify>
    <automated>grep -q "ProfileCard" apps/web/components/SelfViewSheet.tsx && grep -q "use client" apps/web/components/SelfViewSheet.tsx && grep -q "vaul" apps/web/components/SelfViewSheet.tsx && ! grep -q "instagram" apps/web/components/SelfViewSheet.tsx && echo SHEET_OK</automated>
  </verify>
  <acceptance_criteria>
    - `SelfViewSheet.tsx` exists, is `'use client'`, wraps `vaul` `Drawer`, and renders the reused `ProfileCard` (no rebuilt preview).
    - It does NOT reference `instagram_handle` (A1 — no PII in self-preview).
    - Sheet is dismissible (drag + Escape + overlay) with an sr-only `Drawer.Title`.
    - `pnpm -w typecheck` clean.
  </acceptance_criteria>
  <done>A reusable "as others see it" sheet exists, built on the existing ProfileCard + RevealModal pattern.</done>
  <ui_verify>Standing visual-verify rule: open the self-view sheet at 420px → screenshot → critique vs 01-UI-SPEC.md §Self-View (Tier-3 neutral, drag handle, title) before marking done.</ui_verify>
</task>

<task type="auto">
  <name>Task 2: Enhance /account hub in place — identity + dating summary + self-view entry + secondary links (+ component test)</name>
  <files>apps/web/app/account/page.tsx, apps/web/app/account/__tests__/page.test.tsx</files>
  <read_first>
    - apps/web/app/account/page.tsx (the file being enhanced — existing skeleton, link-row visual 92-108, masthead 76-80, bottom-mount 216-218)
    - apps/web/app/account/profile/page.tsx (the widened profiles select + owner photo-load block to copy, lines 10,23-52)
    - apps/web/app/matches/[lockId]/page.tsx (the prompt-label join to copy, lines 88-99)
    - apps/web/components/ProfileCard.tsx (prop contract for the self-view feed)
    - apps/web/components/SelfViewSheet.tsx (created Task 1 — the sheet this page triggers)
    - apps/web/components/Polaroid.tsx (identity photo render, tone="dating")
    - .planning/phases/01-navigation-profile-spine-p0/01-PATTERNS.md (§account/page.tsx — keep-skeleton list, widened read, photo block, link rows; Pitfall 4 do-not-over-strip)
    - .planning/phases/01-navigation-profile-spine-p0/01-UI-SPEC.md (§Surface 1 — section order, copy contract, empty-state, reuse map)
  </read_first>
  <action>
    Enhance `apps/web/app/account/page.tsx` ADDITIVELY (D-01; Pitfall 4 — do not delete the saved-plans wedge, sign-out, edit-profile link, post-a-night CTA, or your-nights row; keep `force-dynamic`, the `getUser` then `redirect('/login?next=/account')` gate, masthead, and the bottom `NotificationToast` + `BottomTabShell`). Widen the `profiles` select to the identity + dating-summary columns and add the `profiles_private` read for `bio` (mirror `account/profile/page.tsx`). Copy the owner photo-load block verbatim (`listMyPhotos` + `signClearUrls` with the legacy `clear_photo_url` fallback) and the prompt-label join (`prompt_answers` to `{label,answer}[]`). Re-order so IDENTITY leads, per UI-SPEC §Surface 1 section order: (1) masthead, (2) NEW identity block — `Polaroid tone="dating"` + `font-heading {first_name}, {age}` + meta `{city} · {pronouns?}` + a verification chip (verified pill / unverified pill linking to verify), (3) NEW dating-profile summary — bio (clamped ~3 lines), 1–2 prompt cards, vibe sticker chips, with the authored empty-state (`your profile's a little bare` / `add a bio and a couple prompts so people get the vibe.` / CTA `edit profile`) when bare, (4) NEW `preview my profile` row (Eye icon) that opens `SelfViewSheet` fed the owner's signed photos + fields (omit instagram), (5) NEW secondary link rows reusing the existing icon-well + `ArrowRight` row visual: `edit profile` to `/account/profile`, `preferences` to `/account/preferences`, `notifications` to `/account/notifications`, (6) keep the `your nights` row (D-04), (7) keep post-a-night CTA, (8) keep saved-plans wedge, (9) keep `your account` sign-out. Do NOT add computed stats (E17/Phase 6 deferred) and do NOT touch the `/plan/i/` dead link (Phase 7, D-06). Strip any `/home` teaser/marketing/onboarding-teaser association if present (F1) — but note `/account` is already mostly clean; the diff should ADD far more than it removes. Use only Barbiecore semantic tokens; reserved-accent rule per UI-SPEC §Color; container `max-w-[420px] px-5 pb-28` (tab root, nav clearance). The self-view trigger requires a small `'use client'` boundary — keep it a thin client child (the page stays a server component; pass server-fetched signed URLs + fields down as props). Then create `apps/web/app/account/__tests__/page.test.tsx` (component, mocked Supabase) asserting: identity fields render (name/age/city + a verification chip), a self-view trigger is present, the three secondary links (`/account/profile`, `/account/preferences`, `/account/notifications`) render, and NO teaser/marketing copy strings appear.
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/app/account/__tests__/page.test.tsx && grep -q "SelfViewSheet" apps/web/app/account/page.tsx && grep -q "/account/preferences" apps/web/app/account/page.tsx && grep -q "/account/notifications" apps/web/app/account/page.tsx && grep -q "listMyPhotos" apps/web/app/account/page.tsx && echo HUB_OK</automated>
  </verify>
  <acceptance_criteria>
    - `/account/page.tsx` renders an identity block (name/age/city/verification via Polaroid + chip), a dating-profile summary with authored empty-state, a `preview my profile` row opening `SelfViewSheet`, and link rows to `/account/profile`, `/account/preferences`, `/account/notifications`.
    - Existing working elements remain: saved-plans wedge, sign-out, post-a-night CTA, your-nights row, masthead, `BottomTabShell`, `NotificationToast`, auth gate, `force-dynamic`.
    - No computed stats; no `/plan/i/` change; no marketing/teaser copy.
    - Self-view feed omits `instagram_handle`; photos come from `listMyPhotos`/`signClearUrls`.
    - Component test asserts identity fields + self-view trigger + 3 links + absence of teaser strings; `pnpm vitest run apps/web/app/account/__tests__/page.test.tsx` green.
    - `pnpm -w typecheck` clean.
  </acceptance_criteria>
  <done>The profile tab lands on an identity-forward hub with a self-view and settings paths; existing functionality preserved; no teaser, no premature stats.</done>
  <ui_verify>Standing visual-verify rule: render `/account` at 420px → screenshot → critique vs 01-UI-SPEC.md §Surface 1 (identity-forward, reserved accent, not a settings dump) before marking done.</ui_verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated to /account | Existing `getUser` then redirect gate (kept); no change to the boundary. |
| server to client self-view props | Server-fetched signed photo URLs + profile fields pass to the thin client `SelfViewSheet`; only the viewer's OWN data (no cross-user read). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | self-view rendering the user's own profile | mitigate | Self-view is the viewer's OWN data (owner read passes RLS); no cross-user read. Omit `instagram_handle` (A1) so no contact PII leaks into the preview surface. |
| T-04-02 | Information Disclosure | widened `profiles`/`profiles_private` read | mitigate | All reads stay `.eq('id'/'user_id', user.id)` under the existing RLS + `getUser` gate; `profiles_private` is the user's own row. No new columns exposed cross-user. |
| T-04-03 | Tampering | over-stripping working hub links | accept (process control) | Pitfall 4: the enhance is additive; the acceptance test asserts existing links survive, catching an over-eager strip. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH Package Legitimacy Audit "Not applicable"); `vaul`/`lucide-react`/`next/image` already in production. |
</threat_model>

<verification>
- `pnpm vitest run apps/web/app/account/__tests__/page.test.tsx` green.
- Grep gates: hub references `SelfViewSheet`, `/account/preferences`, `/account/notifications`, `listMyPhotos`; self-view omits `instagram`.
- Existing-link regression: test asserts saved-plans/sign-out/edit-profile/your-nights still present.
- `pnpm -w typecheck` clean.
- (Wave-merge) Playwright: profile tab → `/account` shows the user's name/age + opens the self-view sheet.
- Standing manual visual-verify of `/account` vs 01-UI-SPEC.md §Surface 1.
</verification>

<success_criteria>
- The profile hub shows identity + dating-profile summary + an "as others see it" self-view (reused ProfileCard) + edit/preferences/notifications links.
- No marketing/onboarding teaser; no computed stats (deferred); existing working links preserved.
- Self-view omits contact PII; reads stay RLS-scoped to the viewer.
- Component test + typecheck green.
</success_criteria>

<output>
Create `.planning/phases/01-navigation-profile-spine-p0/01-04-SUMMARY.md` when done.
</output>
