---
phase: 03-marketplace-completeness-p1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/PlanTimeline.tsx
  - apps/web/app/feed/NightDetailSheet.tsx
  - apps/web/components/__tests__/PlanTimeline.test.tsx
autonomous: true
requirements: [REQ-E13]
must_haves:
  truths:
    - "a shared PlanTimeline component renders the numbered-stop timeline (thumb + dashed connector + name + meta + desc + $pp + map link)"
    - "NightDetailSheet imports PlanTimeline instead of its private StopRow copy and renders identically (no visual regression)"
    - "PlanTimeline accepts the existing stops/accent/vibeTags contract and normalizes thin/rich stop shapes"
  artifacts:
    - path: "apps/web/components/PlanTimeline.tsx"
      provides: "Shared blind-safe stop timeline (extracted StopRow + StopTime)"
      min_lines: 40
      exports: ["PlanTimeline"]
    - path: "apps/web/components/__tests__/PlanTimeline.test.tsx"
      provides: "RTL test: renders stops, handles empty"
  key_links:
    - from: "apps/web/app/feed/NightDetailSheet.tsx"
      to: "apps/web/components/PlanTimeline.tsx"
      via: "import PlanTimeline"
      pattern: "PlanTimeline"
---

<objective>
Extract the private `StopRow` + `StopTime` timeline from `apps/web/app/feed/NightDetailSheet.tsx` into a shared `apps/web/components/PlanTimeline.tsx`, and have NightDetailSheet consume it. This is the interface-first step: the two E13 consumers (OfferDetail, LockDetail in 03-05) render the matched plan via this one component, so it must exist and be proven first.

Purpose: One canonical stop timeline so feed, offer, and match screens render identical plans — no fork, no drift.
Output: New shared PlanTimeline, refactored NightDetailSheet (behavior-preserving), RTL test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-marketplace-completeness-p1/03-CONTEXT.md
@.planning/phases/03-marketplace-completeness-p1/03-RESEARCH.md
@.planning/phases/03-marketplace-completeness-p1/03-PATTERNS.md
@.planning/phases/03-marketplace-completeness-p1/03-UI-SPEC.md

# Extraction source (read before editing)
@apps/web/app/feed/NightDetailSheet.tsx
@packages/api-client/src/feed.ts

<interfaces>
<!-- StopRow lives at NightDetailSheet.tsx ~lines 365-449; StopTime helper ~46-58; the <ol> loop body ~275-288. -->
<!-- Props contract to preserve: stops: NightDetailStop[], accent: string (from vibePalette(vibeTags).accent), vibeTags. -->
<!-- normalizeNightDetailStops (packages/api-client/src/feed.ts ~208-228) handles place_name/name + place_type/type drift. -->
<!-- StopRow markup: numbered photo thumb + dashed connector + name + (neighborhood · type · time) + one-line desc w/ "more" + $pp + map link. Semantic shell.* tokens; tabular-nums on $pp/time. -->
<!-- Do NOT use components/itinerary/StopCard.tsx — it links /places/[slug] (identity-bearing); wrong for offer/match. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract PlanTimeline + rewire NightDetailSheet + RTL test</name>
  <files>apps/web/components/PlanTimeline.tsx, apps/web/app/feed/NightDetailSheet.tsx, apps/web/components/__tests__/PlanTimeline.test.tsx</files>
  <behavior>
    - PlanTimeline renders an <ol> of numbered stop rows from a stops array.
    - PlanTimeline with an empty/zero-length stops array renders nothing (or the caller's degrade path), never crashes.
    - NightDetailSheet renders the same timeline output it did before the extraction.
  </behavior>
  <read_first>
    - apps/web/app/feed/NightDetailSheet.tsx (StopRow ~365-449, StopTime ~46-58, ol loop ~275-288)
    - packages/api-client/src/feed.ts (NightDetailStop type, normalizeNightDetailStops ~208-228)
    - 03-PATTERNS.md §PlanTimeline (EXTRACT) + §"Stop normalization + StopRow render"
  </read_first>
  <action>
    Create `apps/web/components/PlanTimeline.tsx`: move `StopRow` + `StopTime` verbatim (semantics-preserving), export a `PlanTimeline` component that renders the `<ol>` of `<StopRow>` (the loop body currently inline in NightDetailSheet ~275-288). Preserve the prop contract exactly: `stops: NightDetailStop[]`, `accent: string`, `vibeTags`. Keep the markup and shell.* tokens identical (numbered thumb, dashed connector, name, `neighborhood · type · time`, desc-with-"more", `$pp`, map link, tabular-nums). Do NOT add scrubbing logic (post-match identity is revealed; the StopRow shape is already blind-safe and renders fine either way — D-07 means no reveal-ordering change). Normalize incoming stops with `normalizeNightDetailStops`.

    Rewire `NightDetailSheet.tsx` to import and render `<PlanTimeline>` instead of its private `StopRow`/`StopTime`; delete the now-duplicated private definitions. This must be behavior-preserving — the feed sheet renders identically.

    Add `apps/web/components/__tests__/PlanTimeline.test.tsx` (RTL): assert a multi-stop array renders the expected number of numbered rows with names/cost, and an empty array renders no rows (no crash).
  </action>
  <verify>
    <automated>grep -q "export function PlanTimeline\|export const PlanTimeline" apps/web/components/PlanTimeline.tsx && grep -q "PlanTimeline" apps/web/app/feed/NightDetailSheet.tsx && pnpm --filter web test -- PlanTimeline && pnpm -w typecheck</automated>
    <human-check>forced-local Playwright: feed NightDetailSheet timeline renders identically to pre-extraction (no visual regression)</human-check>
  </verify>
  <done>PlanTimeline exported and consumed by NightDetailSheet (private copy deleted); RTL test green; typecheck green; feed timeline visually unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stop JSON → render | Stops come from itinerary JSON (rich/thin shape drift) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | malformed stop JSON shape | mitigate | normalizeNightDetailStops handles place_name/name + place_type/type drift; empty array renders no rows (Task 1) |
| T-03-13 | Information Disclosure | wrong timeline component leaks venue slugs | mitigate | Use NightDetailSheet's StopRow shape, NOT StopCard.tsx (which links /places/[slug]) — explicit in action |
| T-03-SC | Tampering | package installs | mitigate | Zero new packages (RESEARCH audit N/A) |
</threat_model>

<verification>
- `pnpm --filter web test -- PlanTimeline` + `pnpm -w typecheck` green.
- grep confirms NightDetailSheet imports PlanTimeline and the private StopRow copy is gone.
- Forced-local visual-verify: feed sheet timeline unchanged.
</verification>

<success_criteria>
- Shared PlanTimeline exists, exported, consumed by NightDetailSheet behavior-preservingly.
- Ready for OfferDetail + LockDetail consumption (03-05).
</success_criteria>

<output>
Create `.planning/phases/03-marketplace-completeness-p1/03-04-SUMMARY.md` when done.
</output>
