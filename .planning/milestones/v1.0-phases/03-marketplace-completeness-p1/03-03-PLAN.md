---
phase: 03-marketplace-completeness-p1
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/web/app/nights/new/PostNightForm.tsx
  - apps/web/app/nights/new/page.tsx
  - apps/web/app/plans/[id]/edit/CoverUploader.tsx
  - apps/web/app/plans/[id]/edit/ItineraryEditor.tsx
  - apps/web/app/create/PublishToFeedButton.tsx
  - apps/web/lib/after5/photos.ts
  - apps/web/app/nights/new/__tests__/PostNightForm.test.tsx
autonomous: true
requirements: [REQ-E11]
must_haves:
  truths:
    - "the post/customize flow exposes who-pays, vibe tags, the why, per-night radius, exact schedule, and target gender/age"
    - "targeting fields default to inclusive (everyone / unbounded), pre-filled and overridable, never reading as exclusion"
    - "a host can upload a real cover image (storage-backed, not just re-pick a stop photo)"
    - "the Door-2 canvas has a publish CTA that carries the itinerary id to /nights/new"
    - "/nights/new accepts ?itinerary= and pre-selects that plan"
    - "PublishToFeedButton no longer carries a hardcoded date — it converges into the real form (F#4)"
  artifacts:
    - path: "apps/web/app/nights/new/PostNightForm.tsx"
      provides: "Creator-control fieldsets (cover/who's-this-for/why/targeting)"
      min_lines: 30
    - path: "apps/web/app/plans/[id]/edit/CoverUploader.tsx"
      provides: "Storage-backed cover uploader"
      min_lines: 20
    - path: "apps/web/app/plans/[id]/edit/ItineraryEditor.tsx"
      provides: "Sticky publish CTA carrying itineraryId"
  key_links:
    - from: "apps/web/app/plans/[id]/edit/ItineraryEditor.tsx"
      to: "/nights/new?itinerary="
      via: "router.push publish CTA"
      pattern: "nights/new\\?itinerary"
    - from: "apps/web/app/plans/[id]/edit/CoverUploader.tsx"
      to: "supabase storage + update_itinerary_stops"
      via: "client.storage.upload then updateItineraryStops"
      pattern: "storage.from"
    - from: "apps/web/app/nights/new/PostNightForm.tsx"
      to: "postNight RPC targeting params"
      via: "postNight wrapper call"
      pattern: "target"
---

<objective>
Build the E11 creator controls on the live post/customize surfaces: extend `PostNightForm` with the who-pays / vibe / why / radius / target-gender / target-age fieldsets (per D-01/D-10, pre-filled and inclusively framed), add a real storage-backed cover uploader, add a publish CTA on the Door-2 canvas that carries the forked itinerary id to `/nights/new`, and collapse the hardcoded-date `PublishToFeedButton` into the real form (F#4).

Purpose: A host can fully configure a night before posting it; "who's this for?" never reads as exclusion; the Door-2 dead-end gets a publish affordance.
Output: Extended PostNightForm + page param-read, new CoverUploader, ItineraryEditor publish bar, converged PublishToFeedButton, RTL test. Marked for forced-local visual-verify against 03-UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-marketplace-completeness-p1/03-CONTEXT.md
@.planning/phases/03-marketplace-completeness-p1/03-RESEARCH.md
@.planning/phases/03-marketplace-completeness-p1/03-PATTERNS.md
@.planning/phases/03-marketplace-completeness-p1/03-UI-SPEC.md
@docs/superpowers/DESIGN-SYSTEM.md

# Surfaces to extend (read before editing)
@apps/web/app/nights/new/PostNightForm.tsx
@apps/web/app/nights/new/page.tsx
@apps/web/app/plans/[id]/edit/ItineraryEditor.tsx
@apps/web/app/plans/[id]/edit/CoverPicker.tsx
@apps/web/app/create/PublishToFeedButton.tsx
@apps/web/lib/after5/photos.ts
@apps/web/app/my-nights/NightCardActions.tsx

<interfaces>
<!-- PostNightForm: existing fieldset/legend pattern (legend line ~229); ambient radiogroup (lines ~283-348) is the sticker-chip / roving-tabindex template; submit CTA (lines ~361-379) stays "post it". -->
<!-- postNight wrapper now accepts p_target_genders/p_target_age_range/p_search_radius_km (from 03-01); updateItineraryStops accepts p_pay_setting/p_vibe_tags/p_cover_image_url. -->
<!-- photos.ts addPhoto (lines ~45-66): crypto.randomUUID() path '<uid>/<id>.jpg'; client.storage.from(BUCKET).upload(path, blob, {upsert:true, contentType:'image/jpeg'}). -->
<!-- NightDetailSheet sticky footer classes: border-t border-shell-ink/10 bg-shell-base/95 backdrop-blur. -->
<!-- stickerRotation from apps/web/lib/sticker.ts; semantic shell.* tokens only (no hex / gray-*). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover uploader (storage-backed) + ItineraryEditor publish CTA + PublishToFeedButton convergence</name>
  <files>apps/web/app/plans/[id]/edit/CoverUploader.tsx, apps/web/app/plans/[id]/edit/ItineraryEditor.tsx, apps/web/app/create/PublishToFeedButton.tsx, apps/web/lib/after5/photos.ts</files>
  <behavior>
    - CoverUploader: selecting a file uploads to the cover bucket, then persists the URL via updateItineraryStops(p_cover_image_url) and shows the cover as a tappable thumbnail.
    - CoverUploader: upload failure shows the dry copy "couldn't upload that. try a different photo?" and does not persist a URL.
    - ItineraryEditor: the publish CTA navigates to /nights/new?itinerary=<itineraryId>.
    - PublishToFeedButton: no longer constructs a hardcoded date; routes through the same publish path (F#4 convergence).
  </behavior>
  <read_first>
    - apps/web/lib/after5/photos.ts (addPhoto storage pattern)
    - apps/web/app/plans/[id]/edit/CoverPicker.tsx (today only re-picks stop photos)
    - apps/web/app/create/PublishToFeedButton.tsx (hardcoded date to collapse)
    - 03-PATTERNS.md §CoverUploader + §"Cover / photo storage upload"; 03-UI-SPEC §E11 (cover states, copy)
  </read_first>
  <action>
    Create `apps/web/app/plans/[id]/edit/CoverUploader.tsx`: a client component reusing the `photos.ts` storage pattern — `crypto.randomUUID()` → `<uid>/<id>.jpg` → `client.storage.from(<cover bucket>).upload(...)` → resolve public/signed URL → persist via `updateItineraryStops({ ..., cover_image_url: url })`. Bucket choice is Claude's discretion (D-01): EITHER add a new `itinerary-covers` bucket (then add a bucket migration mirroring profile-photos folder-scoped RLS keyed on auth.uid() — LOCAL-only/gated; if so add it to files_modified in the SUMMARY) OR reuse `profile-photos`. Render the chosen cover as a tappable thumbnail (tap re-opens picker); empty state "no cover yet. add a photo that sells the night."; states `uploading…` + failure copy per UI-SPEC. Tap targets ≥44px; semantic tokens only.

    In `ItineraryEditor.tsx`: add a sticky bottom publish bar (mirror NightDetailSheet footer classes) with label `publish this night` that does `router.push('/nights/new?itinerary=' + itineraryId)`. Mount the CoverUploader alongside the existing CoverPicker affordance.

    In `PublishToFeedButton.tsx`: remove the hardcoded date construction; route through the same `/nights/new?itinerary=` path so there is ONE publish path with full creator controls (F#4). Do NOT touch `apps/web/app/create/CreateFlow.tsx` (open-city parallel surface — D-03).
  </action>
  <verify>
    <automated>grep -q "storage.from" apps/web/app/plans/[id]/edit/CoverUploader.tsx && grep -Eq "nights/new\?itinerary" apps/web/app/plans/[id]/edit/ItineraryEditor.tsx && pnpm -w typecheck</automated>
    <human-check>forced-local Playwright 420px: cover uploader thumbnail + sticky publish bar render per 03-UI-SPEC; copy is lowercase/dry</human-check>
  </verify>
  <done>CoverUploader uploads + persists + shows thumbnail with all states; publish CTA carries itineraryId; PublishToFeedButton converged; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PostNightForm creator-control fieldsets + page ?itinerary= param + RTL test</name>
  <files>apps/web/app/nights/new/PostNightForm.tsx, apps/web/app/nights/new/page.tsx, apps/web/app/nights/new/__tests__/PostNightForm.test.tsx</files>
  <behavior>
    - PostNightForm renders who-pays chips (i pay / they pay / split), target-gender chips (default "everyone" highlighted), target age min/max inputs, radius input, and a "the why" textarea.
    - Targeting fields default to inclusive (everyone selected, age unbounded) and are overridable.
    - Submitting posts via postNight with the new targeting params + via updateItineraryStops with pay/vibe/why.
    - /nights/new?itinerary=<id> pre-selects that plan in the form.
  </behavior>
  <read_first>
    - apps/web/app/nights/new/PostNightForm.tsx (fieldset/legend ~229; ambient roving-tabindex radiogroup ~283-348; submit CTA ~361-379)
    - apps/web/app/nights/new/page.tsx (param-read + plan list)
    - apps/web/lib/sticker.ts (stickerRotation)
    - 03-UI-SPEC §E11 (section order, copy, framing); 03-PATTERNS.md §PostNightForm
  </read_first>
  <action>
    Extend `PostNightForm.tsx` grouping new fields into `<fieldset>`s using the existing `<legend>` pattern, in the UI-SPEC section order: (2) the cover (mount uploader from Task 1), (3) "who's this for?" — who-pays sticker-chip single-select radiogroup (`i pay`/`they pay`/`split`), target-gender multi-select sticker chips (`women`/`men`/`nonbinary`/`everyone`, default `everyone` highlighted, helper "open to everyone unless you narrow it."), target age min/max number inputs (styled like the duration field, default unbounded placeholders 18/100), radius input ("within ~N km", default city default); (4) "the why?" textarea (helper "one line on why this night's worth it."). Reuse the ambient radiogroup's roving-tabindex/aria-checked pattern for the chip groups. Pre-fill targeting from profile defaults where available, overridable. On submit, pass `p_target_genders`/`p_target_age_range`/`p_search_radius_km` to `postNight` and pass `p_pay_setting`/`p_vibe_tags`/`p_why_note` to `updateItineraryStops`. Keep the submit CTA `post it`. NO fenced code in prose; semantic tokens only; ≥44px tap targets; tabular-nums on numeric metadata.

    In `apps/web/app/nights/new/page.tsx`: read `?itinerary=` and pre-select that plan in the form's plan radiogroup.

    Add/extend `apps/web/app/nights/new/__tests__/PostNightForm.test.tsx` (RTL + jest-axe): assert the new fields render, default to inclusive values, are overridable, and that submit calls postNight with the targeting params (mock the wrapper). Assert no a11y violations on the extended form.
  </action>
  <verify>
    <automated>pnpm --filter web test -- PostNightForm && pnpm -w typecheck</automated>
    <human-check>forced-local Playwright 420px: grouped fieldsets, sticker chips, inclusive defaults, lowercase dry copy per 03-UI-SPEC 6-pillar bars</human-check>
  </verify>
  <done>PostNightForm exposes all E11 creator+targeting fields with inclusive defaults; ?itinerary= pre-selects; RTL+axe green; submit passes new params; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase Storage (cover upload) | Untrusted blob + path from client |
| browser → postNight/updateItineraryStops RPC | Creator input crosses into DEFINER funcs (validated server-side in 03-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | cover-upload path traversal / cross-user write | mitigate | Folder-scoped storage path `<auth.uid()>/<uuid>.jpg`; bucket RLS keyed on auth.uid() (mirror profile-photos) — Task 1 |
| T-03-10 | Information Disclosure | targeting fields read as exclusion / leak preference intent | accept | Targeting is the host's own per-date config; rendered inclusively; feed-side application is Phase 4 — no candidate data exposed here |
| T-03-11 | Input Validation | malformed targeting payload | mitigate | Server-side validation lives in post_night (03-01); client provides defaulted, typed values |
| T-03-SC | Tampering | package installs | mitigate | Zero new packages (RESEARCH audit N/A) |
</threat_model>

<verification>
- `pnpm --filter web test -- PostNightForm` + `pnpm -w typecheck` green.
- grep confirms publish CTA targets `/nights/new?itinerary=` and CoverUploader uses `storage.from`.
- Forced-local Playwright visual-verify of the creator form + Door-2 publish bar against 03-UI-SPEC 6-pillar bars (orchestrator forced-local pass).
- No edit to CreateFlow.tsx (open-city parallel surface).
</verification>

<success_criteria>
- All E11 creator + targeting fields present, inclusive-by-default, overridable, pre-filled.
- Real cover upload works with all states.
- Door-2 publish CTA carries itineraryId; PublishToFeedButton converged (F#4).
</success_criteria>

<output>
Create `.planning/phases/03-marketplace-completeness-p1/03-03-SUMMARY.md` when done. If a new cover bucket migration was added, list it (LOCAL-only/gated) and confirm the local apply chain stayed green.
</output>
