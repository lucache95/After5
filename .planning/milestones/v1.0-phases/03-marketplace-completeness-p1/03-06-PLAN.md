---
phase: 03-marketplace-completeness-p1
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/api/offers/notify-offered/__tests__/route.test.ts
  - apps/web/lib/after5/match.ts
  - apps/web/app/api/offers/notify-offered/route.ts
autonomous: false
requirements: [REQ-E14]
must_haves:
  truths:
    - "offer creation guarantees an in-app offer_received notification (transactional, inside the RPC) that deep-links to /offers/[offerId]"
    - "the server-runtime email path (/api/offers/notify-offered, runtime=nodejs → Resend) is ownership-gated and best-effort (never blocks the offer)"
    - "the inbox/notification row for a received offer is reachable and routes to /offers/[offerId]"
    - "a unit test covers the notify-offered route ownership gate + best-effort skip"
    - "RESEND_API_KEY presence in the Vercel server runtime is verified at execution time (checkpoint)"
  artifacts:
    - path: "apps/web/app/api/offers/notify-offered/__tests__/route.test.ts"
      provides: "Unit test: ownership gate + best-effort skip for the offer-email route"
      contains: "notify-offered"
  key_links:
    - from: "notifications offer_received row"
      to: "/offers/[offerId]"
      via: "inbox deep-link payload"
      pattern: "offers/"
    - from: "apps/web/lib/after5/match.ts makeOffer"
      to: "/api/offers/notify-offered"
      via: "best-effort notifyOfferReceived"
      pattern: "notify-offered"
---

<objective>
Audit and harden — not rebuild — the offer-delivery chain (D-13). The in-app `offer_received` notification is the reliability guarantee (dispatched transactionally inside `match_make_offer`); the server-runtime email (`/api/offers/notify-offered`, runtime=nodejs → Resend) and web push are best-effort enhancements. Verify each link, guarantee the `/offers/[offerId]` deep-link, add the missing route unit test, and confirm `RESEND_API_KEY` is set in the Vercel server runtime at execution time.

Purpose: Every sent offer reaches the candidate via a guaranteed in-app notification (and email/push when configured) — the candidate always reaches `/offers/[id]`.
Output: A new notify-offered route unit test, any deep-link gap fix, and a human checkpoint confirming the Vercel RESEND env.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-marketplace-completeness-p1/03-CONTEXT.md
@.planning/phases/03-marketplace-completeness-p1/03-RESEARCH.md
@.planning/phases/03-marketplace-completeness-p1/03-PATTERNS.md
@.planning/phases/03-marketplace-completeness-p1/03-UI-SPEC.md

# Chain to audit (read in full — this is an audit, not a rebuild)
@apps/web/lib/after5/match.ts
@apps/web/app/api/offers/notify-offered/route.ts
@apps/web/app/api/cron/push-web/route.ts
@apps/web/app/api/cron/push-web/route.test.ts
@apps/web/lib/email/send-offer-received.ts
@apps/web/lib/email/resend.ts
@apps/web/lib/push/send.ts

<interfaces>
<!-- makeOffer (match.ts ~99-125) fires notifyOfferReceived best-effort on kind='offer'. -->
<!-- /api/offers/notify-offered: runtime='nodejs', ownership-gated (creator_id=user.id), best-effort 200, calls sendOfferReceivedEmail. -->
<!-- In-app offer_received dispatched INSIDE match_make_offer (line ~130) — transactional guarantee. -->
<!-- push-web cron scans undelivered web_push rows; inert without VAPID (A2). -->
<!-- A1: RESEND_API_KEY assumed set in Vercel server runtime; A2: VAPID may be absent → push inert. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chain audit + deep-link guarantee + notify-offered route unit test</name>
  <files>apps/web/app/api/offers/notify-offered/__tests__/route.test.ts, apps/web/app/api/offers/notify-offered/route.ts, apps/web/lib/after5/match.ts</files>
  <read_first>
    - apps/web/lib/after5/match.ts (makeOffer → notifyOfferReceived best-effort ~99-125)
    - apps/web/app/api/offers/notify-offered/route.ts (whole file — runtime, ownership gate, best-effort)
    - apps/web/app/api/cron/push-web/route.test.ts (route-test convention to mirror)
    - 03-PATTERNS.md §"E14 — Audit, Not Build"; 03-UI-SPEC §E14 (inbox copy "someone sent you an offer →")
  </read_first>
  <action>
    Audit the four links and FIX only real gaps (do NOT add a parallel email sender — the server path exists, per anti-pattern in RESEARCH):
    1. Confirm the in-app `offer_received` notification row deep-links to `/offers/[offerId]` (payload carries the offer/instance id and the inbox row routes there). If the inbox row for `offer_received`/`new_offer` does NOT route to `/offers/[offerId]`, fix the deep-link mapping — this is the reliability floor (D-08). If it already routes correctly, record that in the SUMMARY (no change).
    2. Confirm `/api/offers/notify-offered` stays `runtime='nodejs'`, ownership-gates (caller's `creator_id = user.id`), and is best-effort (returns 200 / never throws into the offer flow). Preserve the ownership check (do not weaken it).
    3. Confirm `makeOffer` fires the email best-effort and a failure there never blocks the offer (in-app is the guarantee).

    Add `apps/web/app/api/offers/notify-offered/__tests__/route.test.ts` (mirror `push-web/route.test.ts` convention): assert (a) a non-owner caller is rejected (ownership gate); (b) when Resend/email throws or RESEND key is absent, the route still returns success/best-effort and does not block; (c) the owner path attempts the send. Mock Resend + the supabase client.

    Keep UI minimal (D-08): if the inbox row copy for a received offer is wrong/missing, align it to `someone sent you an offer →` (lowercase, dry) per 03-UI-SPEC; otherwise leave it.
  </action>
  <verify>
    <automated>pnpm --filter web test -- notify-offered && pnpm -w typecheck</automated>
  </verify>
  <done>Chain audited; deep-link to /offers/[offerId] guaranteed; ownership gate + best-effort preserved; route unit test green; typecheck green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify RESEND_API_KEY in the Vercel server runtime + the offer deep-link</name>
  <action>Human-only verification of live Vercel env state (RESEND/VAPID) and the inbox→/offers/[offerId] deep-link. No code change in this task.</action>
  <what-built>Audited the offer-delivery chain: in-app notification guarantee (transactional), server-runtime email (notify-offered, runtime=nodejs), web push (inert without VAPID), and the /offers/[offerId] deep-link. Added a notify-offered route unit test.</what-built>
  <how-to-verify>
    1. Confirm `RESEND_API_KEY` is present in the Vercel SERVER runtime (Project → Settings → Environment Variables, Production). If present, email sends on prod; if blank, the in-app notification still guarantees delivery (non-blocking, A1).
    2. (Optional) Confirm VAPID keys (`VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`) state — if absent, push stays inert (A2), which is acceptable this phase.
    3. Confirm the inbox notification for a received offer routes to `/offers/[offerId]` in a forced-local walk (the reliability floor).
  </how-to-verify>
  <verify>
    <automated>MISSING — manual-only: live Vercel env + deep-link walk (in-app guarantee covers email failure, A1)</automated>
  </verify>
  <resume-signal>Type "approved" (note whether RESEND/VAPID are set), or describe any deep-link gap to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → /api/offers/notify-offered | A non-creator could try to trigger an offer email to another user |
| notification payload → inbox deep-link | Wrong routing could trap the candidate away from /offers/[id] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Information Disclosure | email/push leaks offer existence to wrong user | mitigate | notify-offered verifies caller is offer creator before sending; unit test asserts non-owner rejected (Task 1) |
| T-03-18 | Denial of Service | email failure blocks the offer | mitigate | Best-effort 200; in-app dispatch is transactional inside the RPC (the guarantee); unit test asserts non-blocking (Task 1) |
| T-03-19 | Spoofing | missing RESEND/VAPID silently drops delivery | accept | In-app notification is the floor regardless; RESEND presence verified at the checkpoint (A1); VAPID-absent push inert is acceptable (A2) |
| T-03-SC | Tampering | package installs | mitigate | Zero new packages (RESEARCH audit N/A) |
</threat_model>

<verification>
- `pnpm --filter web test -- notify-offered` + `pnpm -w typecheck` green.
- Human checkpoint confirms RESEND_API_KEY in Vercel server runtime + the /offers/[offerId] deep-link.
- No parallel email sender added; ownership gate preserved.
</verification>

<success_criteria>
- In-app offer_received guaranteed + deep-links to /offers/[offerId].
- Server-runtime email best-effort + ownership-gated, covered by a unit test.
- RESEND env verified at execution.
</success_criteria>

<output>
Create `.planning/phases/03-marketplace-completeness-p1/03-06-SUMMARY.md` when done. Record the RESEND/VAPID env findings from the checkpoint.
</output>
