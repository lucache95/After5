---
phase: 07-enhancements-and-polish-p3
plan: 05
type: execute
wave: 3
depends_on: ["07-04"]
files_modified:
  - apps/web/app/feed/NightDetailSheet.tsx
  - apps/web/app/feed/__tests__/NightDetailSheet.test.tsx
autonomous: true
requirements: [REQ-E20, REQ-E25]
must_haves:
  truths:
    - "the detail sheet renders the real static RouteMap (>=1 stop with coords) in place of the placeholder route viz, falling back to the existing 'short hop apart' placeholder at 0 coords (E20)"
    - "a silent shimmer skeleton matching the new card shape shows while get_night_detail pends (detail===null && open), reduced-motion friendly via motion-reduce:animate-none (E25, D-02)"
    - "the feed sheet stays blind: PlanTimeline is rendered with linkSlugs unset (default false), no host identity, no /places link"
  artifacts:
    - path: "apps/web/app/feed/NightDetailSheet.tsx"
      provides: "RouteMap swap + in-sheet skeleton; blind contract preserved"
      contains: "RouteMap"
  key_links:
    - from: "apps/web/app/feed/NightDetailSheet.tsx"
      to: "apps/web/components/itinerary/RouteMap.tsx"
      via: "<RouteMap stops={stops} /> replacing the placeholder route viz"
      pattern: "RouteMap"
---

<objective>
Wire the real map and the loading skeleton into the feed detail sheet: replace the placeholder route viz with RouteMap (E20), and show a shimmer skeleton matching the new card while get_night_detail pends (E25). Keep the sheet blind.

Purpose: E20's payoff surface and E25's detail skeleton both live in NightDetailSheet. The sheet is a blind pre-swipe surface, so it must NOT set linkSlugs.
Output: NightDetailSheet modifications + the skeleton-while-pending test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-enhancements-and-polish-p3/07-PATTERNS.md
@.planning/phases/07-enhancements-and-polish-p3/07-UI-SPEC.md
@.planning/phases/07-enhancements-and-polish-p3/07-04-SUMMARY.md

<interfaces>
RouteMap (apps/web/components/itinerary/RouteMap.tsx, from Plan 04): takes NightDetailStop[], returns null at 0 coords.
NightDetailSheet (apps/web/app/feed/NightDetailSheet.tsx): detail loads in useEffect :83-97; `const stops = detail?.stops ?? []` :103; placeholder route viz :264-287 (after the timeline, before the host hint, eyebrow `the route`); PlanTimeline usage in the sheet (renders blind — pass NO linkSlugs).
Skeleton analog: apps/web/app/feed/loading.tsx — canonical shimmer atom :24 `animate-pulse rounded-full bg-shell-ink/10 motion-reduce:animate-none`.
UI-SPEC §"E20 — real map in the detail sheet" + §"E25 — detail-sheet loading skeleton" (silent shimmer, no spinner, matches new card shape; no caption).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: swap the placeholder route viz for RouteMap (E20)</name>
  <files>apps/web/app/feed/NightDetailSheet.tsx</files>
  <read_first>
    apps/web/app/feed/NightDetailSheet.tsx:103, :264-287 (placeholder route viz + the stops var).
    apps/web/components/itinerary/RouteMap.tsx (Plan 04 — null at 0 coords).
    07-UI-SPEC.md §"E20 — real map in the detail sheet".
  </read_first>
  <behavior>
    - When >=1 stop has lat/lng, the sheet renders `<RouteMap stops={stops} />` (pink static map, click-to-expand lightbox) in the `the route` section.
    - When 0 stops have coords, RouteMap returns null and the existing "short hop apart" placeholder tone shows (no broken tile).
    - The map block keeps the `the route` eyebrow and its position (after timeline, before host hint).
  </behavior>
  <action>
    In NightDetailSheet.tsx, replace the placeholder route viz at :264-287 with `<RouteMap stops={stops} />`, keeping the `the route` section eyebrow and position. Because RouteMap returns null at 0 coords, keep (or retain) the existing "short hop apart" placeholder so the section still reads when no coords are present. Do NOT pass linkSlugs to PlanTimeline anywhere in this sheet — the feed sheet stays blind (Pitfall 4). Implements REQ-E20 (detail-sheet map).
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/app/feed/__tests__/NightDetailSheet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: RouteMap replaces the :264-287 placeholder; `the route` eyebrow retained; no linkSlugs set in the sheet.
    - behavior: coords present → map; 0 coords → placeholder.
    - test-command: NightDetailSheet.test.tsx passes.
  </acceptance_criteria>
  <done>The detail sheet shows a real pink static map when coords exist and degrades to the placeholder otherwise, staying blind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: in-sheet loading skeleton while get_night_detail pends (E25)</name>
  <files>apps/web/app/feed/NightDetailSheet.tsx</files>
  <read_first>
    apps/web/app/feed/NightDetailSheet.tsx:83-103 (detail load + the null state).
    apps/web/app/feed/loading.tsx (FULL — the shimmer atom + shape to mirror).
    07-UI-SPEC.md §"E25 — detail-sheet loading skeleton" + 07-PATTERNS.md §"Reduced-motion shimmer skeleton".
  </read_first>
  <behavior>
    - While `detail === null && open`, the sheet renders a silent shimmer skeleton (no spinner, no caption) matching the new card shape: hero block + chip row + hook line + timeline-row placeholders.
    - Shimmer uses `animate-pulse ... bg-shell-ink/10 motion-reduce:animate-none` on bg-shell-base/white (reduced-motion friendly).
    - When detail resolves, the real content replaces the skeleton.
  </behavior>
  <action>
    Add a skeleton render branch in NightDetailSheet for `detail === null && open`, mirroring feed/loading.tsx EXACTLY (the :24 shimmer atom). Skeleton shape: hero block + chip row + hook line + several timeline-row placeholders — matching the NEW card/sheet shape. No spinner, no caption text. Keep the existing blind-summary fallback semantics where appropriate, but the pending state shows the shimmer. Implements REQ-E25 (D-02 skeleton half).
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/app/feed/__tests__/NightDetailSheet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: a skeleton branch keyed on `detail === null && open`; uses animate-pulse + motion-reduce:animate-none; no spinner.
    - behavior: skeleton while pending; real detail after resolve.
    - test-command: NightDetailSheet.test.tsx passes (skeleton-while-pending case).
  </acceptance_criteria>
  <done>A reduced-motion shimmer holds the new card's shape while the detail RPC resolves.</done>
</task>

<task type="auto">
  <name>Task 3: NightDetailSheet.test.tsx</name>
  <files>apps/web/app/feed/__tests__/NightDetailSheet.test.tsx</files>
  <read_first>
    Existing apps/web/app/feed/__tests__/ tests for the sheet render + how getNightDetail is mocked.
    07-VALIDATION.md (NightDetailSheet Wave-0 row).
  </read_first>
  <action>
    Create apps/web/app/feed/__tests__/NightDetailSheet.test.tsx asserting: the skeleton renders while detail is null and the sheet is open, then real content replaces it on resolve; and that the sheet never renders a /places link (blind-contract guard — linkSlugs not set). Author RED-first where practical.
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/app/feed/__tests__/NightDetailSheet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: covers skeleton-while-pending + no-/places-link blind guard.
    - test-command: passes.
  </acceptance_criteria>
  <done>Skeleton + blind contract locked by the test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feed NightDetailSheet (blind pre-swipe) | must never render host identity or a /places slug link |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17 | Information Disclosure | slug link or host identity leaks onto the blind feed sheet | mitigate | PlanTimeline rendered with linkSlugs unset (default false); RouteMap shows only coords/route, no host data; NightDetailSheet.test.tsx asserts no /places link |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (07-RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- NightDetailSheet.test.tsx green (map swap, skeleton-while-pending, no /places link).
- `pnpm typecheck` green.
</verification>

<success_criteria>
The feed detail sheet renders a real static map (or graceful placeholder) and a reduced-motion skeleton while loading, without breaking the blind contract.
</success_criteria>

<output>
Create `.planning/phases/07-enhancements-and-polish-p3/07-05-SUMMARY.md` when done.
</output>
