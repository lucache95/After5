---
phase: 07-enhancements-and-polish-p3
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/web/components/itinerary/RouteMap.tsx
  - apps/web/components/PlanTimeline.tsx
  - apps/web/components/itinerary/__tests__/RouteMap.test.tsx
  - apps/web/components/__tests__/PlanTimeline.test.tsx
autonomous: true
requirements: [REQ-E20, REQ-E21]
must_haves:
  truths:
    - "RouteMap renders a static Barbiecore-pink Mapbox PNG from NightDetailStop coords (>=1 stop with lat/lng) and returns null/sentinel at 0 coords (E20, Claude's Discretion static map)"
    - "PlanTimeline's per-stop map link uses a coord deep-link when lat/lng present, name-search fallback otherwise (E20)"
    - "PlanTimeline links a stop name to /places/[slug] ONLY when linkSlugs===true AND place_slug present — default OFF preserves the blind contract on feed/offer surfaces (D-01)"
  artifacts:
    - path: "apps/web/components/itinerary/RouteMap.tsx"
      provides: "pink/NightDetailStop static-map variant of ItineraryMap"
      min_lines: 40
    - path: "apps/web/components/PlanTimeline.tsx"
      provides: "coord href + opt-in linkSlugs prop (default false)"
      contains: "linkSlugs"
  key_links:
    - from: "apps/web/components/PlanTimeline.tsx"
      to: "/places/[slug]"
      via: "Link rendered only when linkSlugs && stop.place_slug"
      pattern: "linkSlugs"
    - from: "apps/web/components/itinerary/RouteMap.tsx"
      to: "Mapbox Static Images API"
      via: "buildStaticMapUrl reused from ItineraryMap"
      pattern: "api.mapbox.com"
---

<objective>
Build the shared timeline + map building blocks: a pink/NightDetailStop static-map variant (RouteMap) reusing ItineraryMap's URL builder/polyline/lightbox, and two surgical PlanTimeline edits — coord deep-links (E20) and the opt-in linkSlugs prop (E21) that keeps the blind contract intact.

Purpose: PlanTimeline is shared by the blind feed sheet, offer timelines, AND the revealed LockDetail. The slug link MUST be a per-call opt-in default-off prop. This plan is the contract every consuming surface (Plans 05, 06) builds against.
Output: RouteMap component + PlanTimeline modifications + their unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-enhancements-and-polish-p3/07-RESEARCH.md
@.planning/phases/07-enhancements-and-polish-p3/07-PATTERNS.md
@.planning/phases/07-enhancements-and-polish-p3/07-UI-SPEC.md
@.planning/phases/07-enhancements-and-polish-p3/07-01-SUMMARY.md

<interfaces>
NightDetailStop (packages/api-client/src/feed.ts) now carries lat/lng (already) + place_slug (added in Plan 01).
ItineraryMap (apps/web/components/itinerary/ItineraryMap.tsx): encodePolyline/encodeSigned :21-43 (REUSE verbatim), buildStaticMapUrl :45-63 (REUSE), MapLightbox :124-187 (REUSE), ACCENT='C2552B' :18 (change to 'E0218A'), reads Stop with s.lat/s.lng, TOKEN=process.env.NEXT_PUBLIC_MAPBOX_TOKEN, `<Image ... unoptimized />` :98-105.
PlanTimeline (apps/web/components/PlanTimeline.tsx): current name-search href :46; signature :134-156; StopRow name <p> :78; doc comment :9-17 warns NOT to default-link /places/[slug].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RouteMap.tsx — pink/NightDetailStop static-map variant</name>
  <files>apps/web/components/itinerary/RouteMap.tsx</files>
  <read_first>
    apps/web/components/itinerary/ItineraryMap.tsx (FULL — helpers :21-43, URL builder :45-63, Image :98-105, lightbox :124-187, ACCENT :18).
    07-PATTERNS.md "apps/web/components/itinerary/RouteMap.tsx (NEW component)" assignment.
    07-UI-SPEC.md §"E20 — real map in the detail sheet" + §Color "Map color note".
    07-RESEARCH.md Pitfall 5 (bare hex).
  </read_first>
  <behavior>
    - Given >=1 NightDetailStop with numeric lat AND lng, RouteMap builds a Mapbox Static Images API URL (light-v11 base, numbered pins + encoded-polyline route, ACCENT bare hex E0218A) and renders `<Image unoptimized>`, click-to-expand into the reused lightbox.
    - Given 0 stops with coords, RouteMap returns null (the sheet keeps its own placeholder).
    - The map is reduced-motion friendly (no GL canvas, static PNG).
  </behavior>
  <action>
    Create apps/web/components/itinerary/RouteMap.tsx by copying ItineraryMap and changing exactly three things: (1) ACCENT 'C2552B' → 'E0218A' (bare hex, NO leading '#' — Mapbox static overlays reject it; Pitfall 5); base style stays mapbox/light-v11. (2) accept `NightDetailStop[]` instead of the planner Stop — the coord filter is structurally identical (`s.lat`/`s.lng` numeric). (3) re-skin chrome to shell tokens (rounded-3xl, bg-shell-base/white, ring-shell-accent) and do NOT reuse ItineraryMap's FallbackList — return null at 0 coords (UI-SPEC: render only when >=1 coord, else NightDetailSheet keeps its placeholder). REUSE verbatim encodePolyline/encodeSigned, buildStaticMapUrl, and the lightbox (Don't-Hand-Roll). TOKEN = process.env.NEXT_PUBLIC_MAPBOX_TOKEN; `<Image unoptimized>` to avoid re-proxying. Implements REQ-E20 (the map render, Claude's-Discretion static variant).
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/components/itinerary/__tests__/RouteMap.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: ACCENT='E0218A' bare hex; takes NightDetailStop[]; returns null at 0 coords; reuses the ItineraryMap URL builder + lightbox; uses shell tokens.
    - behavior: URL built from >=1 coord; null at 0 coords.
    - test-command: RouteMap.test.tsx passes.
  </acceptance_criteria>
  <done>RouteMap renders a pink static map from coords and degrades to null with no broken tile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlanTimeline — coord href (E20) + opt-in linkSlugs (E21)</name>
  <files>apps/web/components/PlanTimeline.tsx</files>
  <read_first>
    apps/web/components/PlanTimeline.tsx (FULL — href :46, signature :134-156, StopRow name <p> :78, doc comment :9-17).
    07-PATTERNS.md "apps/web/components/PlanTimeline.tsx (MODIFY)" assignment + Pitfall 4 (slug leak).
    07-UI-SPEC.md §"E20 — per-stop coord deep-links" + §"E21 — venues into the loop".
  </read_first>
  <behavior>
    - The per-stop map link href is `https://www.google.com/maps/search/?api=1&query={lat},{lng}` when stop.lat AND stop.lng are non-null; else the existing name-search fallback. Label/icon/target unchanged.
    - With linkSlugs===true AND stop.place_slug present, the stop NAME renders as `<Link href="/places/{place_slug}">`; otherwise the existing plain `<p>` (graceful degrade).
    - With linkSlugs===false (default), the name is NEVER a /places link — blind contract.
  </behavior>
  <action>
    Edit PlanTimeline.tsx. E20: change the directions href (:46) to prefer coords (`${stop.lat},${stop.lng}`) when both present, else keep the name-search fallback; leave the MapPin icon + `map` label + target="_blank" rel="noopener noreferrer" unchanged. E21: add `linkSlugs = false` to the props signature (`stops: NightDetailStop[]; accent: string; vibeTags: string[] | null; linkSlugs?: boolean;`) and thread it to StopRow; in StopRow render the name as `<Link href={`/places/${stop.place_slug}`}>` ONLY when `linkSlugs === true && stop.place_slug`, else the existing plain `<p>` (D-01 graceful degrade). Honor the existing doc comment (:9-17) that the link is opt-in, never default. Implements REQ-E20 (coord link) + REQ-E21 (opt-in slug link).
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/components/__tests__/PlanTimeline.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: href uses coords when present, name fallback otherwise; linkSlugs default false; name links /places/[slug] only when linkSlugs && place_slug.
    - behavior: default-off renders no /places link (blind contract); on + slug present renders the link; on + slug absent renders plain text.
    - test-command: PlanTimeline.test.tsx passes including the linkSlugs-off and missing-slug cases.
  </acceptance_criteria>
  <done>Coord deep-links work; the slug link is opt-in default-off; the blind contract holds.</done>
</task>

<task type="auto">
  <name>Task 3: RouteMap.test.tsx + PlanTimeline.test.tsx</name>
  <files>apps/web/components/itinerary/__tests__/RouteMap.test.tsx, apps/web/components/__tests__/PlanTimeline.test.tsx</files>
  <read_first>
    Existing apps/web/components/__tests__/*.test.tsx for the RTL render style + how NightDetailStop fixtures are built.
    07-VALIDATION.md (RouteMap + PlanTimeline Wave-0 rows).
  </read_first>
  <action>
    Create apps/web/components/itinerary/__tests__/RouteMap.test.tsx (URL builds from >=1 coord; returns null/renders nothing at 0 coords) and apps/web/components/__tests__/PlanTimeline.test.tsx (coord href vs name fallback; linkSlugs=true+slug → /places link present; linkSlugs=true+no slug → plain text; linkSlugs=false → NO /places link — the blind-contract guard). Author RED-first where practical.
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/components/itinerary/__tests__/RouteMap.test.tsx apps/web/components/__tests__/PlanTimeline.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: both files cover the behaviors above incl. the linkSlugs-off blind-contract guard.
    - test-command: both pass.
  </acceptance_criteria>
  <done>The map URL build, coord href, and blind-contract opt-in are locked by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PlanTimeline render (shared blind + revealed) | the slug link must never appear on a blind surface; coords come from the DEFINER RPC, not a client places query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Information Disclosure | venue identity leaks onto the blind feed/offer via PlanTimeline | mitigate | `linkSlugs` defaults false; only LockDetail (Plan 06) sets true; PlanTimeline.test.tsx asserts default-off renders no /places link |
| T-07-13 | Information Disclosure | host de-anonymization via client-side coords | mitigate | RouteMap consumes coords already scrubbed-and-served by get_night_detail (Plan 01); no client `places` query introduced |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (mapbox-gl/next-image already present; 07-RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- RouteMap.test.tsx + PlanTimeline.test.tsx green.
- `pnpm typecheck` green (NightDetailStop.place_slug from Plan 01 consumed).
- Default PlanTimeline render contains no /places link.
</verification>

<success_criteria>
A reusable pink static-map component + a coord-aware, blind-safe opt-in PlanTimeline that downstream surfaces consume without re-implementing map or link logic.
</success_criteria>

<output>
Create `.planning/phases/07-enhancements-and-polish-p3/07-04-SUMMARY.md` when done.
</output>
