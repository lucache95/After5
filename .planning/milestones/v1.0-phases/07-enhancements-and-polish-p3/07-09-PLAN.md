---
phase: 07-enhancements-and-polish-p3
plan: 09
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05", "07-06", "07-07", "07-08"]
files_modified:
  - apps/web/e2e/route-07-visual.spec.ts
autonomous: false
requirements: [REQ-E20, REQ-E21, REQ-E22, REQ-E23, REQ-E24, REQ-E25]
must_haves:
  truths:
    - "all 3 Phase-7 migrations apply cleanly on a fresh local stack (supabase db reset), types regen from LOCAL, and the Supabase security advisor reports no NEW findings after the DDL (CLAUDE.md gated-prod-apply)"
    - "the full automated suite is green: pnpm test + pnpm typecheck + all 3 new .sql contract tests (incl. e23_feed_contract.sql regression) against the local stack"
    - "visual-verify @420px passes for the 6 changed surfaces vs 07-UI-SPEC/DESIGN-SYSTEM; then PROD apply happens in a SEPARATE human-checkpoint (gated, not auto-pushed; prod ufufmcpnysvwtutpbian)"
  artifacts:
    - path: "apps/web/e2e/route-07-visual.spec.ts"
      provides: "forced-local @420px visual spec for the 6 surfaces"
  key_links:
    - from: "local stack"
      to: "prod ufufmcpnysvwtutpbian"
      via: "GATED human-checkpoint apply (separate task, autonomous:false)"
      pattern: "gated-prod-apply"
---

<objective>
Phase gate: apply all 3 migrations locally, regen types, run the security advisor, run the full automated + SQL suite, visual-verify @420px the 6 surfaces, then a SEPARATE human checkpoint for the GATED prod-apply of the 3 migrations to prod ufufmcpnysvwtutpbian.

Purpose: enforce gated-prod-apply (local-green + advisor in-run; prod apply behind a human gate) and the blind/host-hint/keyset regression lock before this last milestone phase ships.
Output: visual spec + a green local gate + a prod-apply checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-enhancements-and-polish-p3/07-VALIDATION.md
@.planning/phases/07-enhancements-and-polish-p3/07-UI-SPEC.md
@.planning/phases/07-enhancements-and-polish-p3/07-01-SUMMARY.md
@.planning/phases/07-enhancements-and-polish-p3/07-02-SUMMARY.md
@.planning/phases/07-enhancements-and-polish-p3/07-03-SUMMARY.md

<interfaces>
Migrations to apply (in order): 20260606140000_e20_get_night_detail_coords.sql, 20260606140100_e23_browse_feed_city_and_tune.sql, 20260606140200_e24_withdraw_interest.sql.
SQL tests: supabase/tests/e20_night_detail_coords.sql, e23_feed_contract.sql (the regression lock), e24_withdraw_interest.sql.
Local DB URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres.
Prior visual harness recipe: apps/web/e2e/route-03-visual.spec.ts / route-03-e11-visual.spec.ts (forced-local @420px). Prod ref: ufufmcpnysvwtutpbian.
6 surfaces: detail-sheet map (E20), per-stop coord link (E20), LockDetail venue link (E21), NightCard city label (E23), standby card (E24), detail skeleton + my-nights archive tab (E25).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] local apply + typegen + security advisor + full + SQL suite</name>
  <files>apps/web/e2e/route-07-visual.spec.ts</files>
  <read_first>
    07-VALIDATION.md (the full Per-Task Verification Map + Sampling Rate phase-gate row).
    CLAUDE.md §Security/§Schema (gated-prod-apply, advisor after DDL).
    apps/web/e2e/route-03-visual.spec.ts (forced-local visual recipe to mirror).
  </read_first>
  <action>
    Run `supabase db reset` to apply ALL migrations including the 3 new Phase-7 ones on a fresh local stack (confirm ordering: the 3 sort after 20260606130200). Regenerate types FROM LOCAL (the project's typegen command) and rebuild @after5/api-client. Run the Supabase security advisor against the local DDL and confirm NO NEW findings beyond the app's established accepted DEFINER-executable pattern (the new RPCs pin search_path, no USING(true), revoke anon/public). Run the full automated suite: `pnpm test && pnpm typecheck`, plus all 3 SQL contract tests via psql (e20, e23 regression, e24). Author apps/web/e2e/route-07-visual.spec.ts (forced-local @420px) covering the 6 surfaces, mirroring the route-03 visual recipe. This task is [BLOCKING]: if the advisor reports a new finding or any test is red, STOP and fix before the prod checkpoint. Covers REQ-E20..E25 (the local verification gate).
  </action>
  <verify>
    <automated>supabase db reset && pnpm test && pnpm typecheck && for t in e20_night_detail_coords e23_feed_contract e24_withdraw_interest; do psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -f supabase/tests/$t.sql || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - source: 3 new migrations apply on a fresh reset; types regen from local; route-07-visual.spec.ts exists.
    - behavior: full suite + all 3 SQL tests green; security advisor reports no NEW findings.
    - test-command: the chained reset + test + typecheck + 3 SQL tests exit 0.
  </acceptance_criteria>
  <done>Local stack green with all 3 migrations applied, advisor clean, full + SQL suite passing, visual spec authored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: visual-verify @420px the 6 surfaces</name>
  <files>apps/web/e2e/route-07-visual.spec.ts</files>
  <action>Render the 6 changed surfaces forced-local @420px and critique against 07-UI-SPEC.md + DESIGN-SYSTEM.md before the prod gate.</action>
  <what-built>The 6 Phase-7 surfaces are implemented and green locally: detail-sheet static map + per-stop coord link (E20), LockDetail venue link with graceful degrade (E21), tuned feed order + city label + finer distance (E22/E23), candidate standby card + withdraw (E24), detail-sheet skeleton + /my-nights archive tab (E25).</what-built>
  <how-to-verify>
    1. Run the forced-local visual harness at 420px: `pnpm --filter web exec playwright test e2e/route-07-visual.spec.ts` (or the project's forced-local recipe).
    2. Open the detail sheet for a night with catalog stops — confirm a pink static map renders, click-to-expand lightbox works, and the per-stop `map` link deep-links to coords. For a coords-less night confirm the "short hop apart" placeholder (no broken tile).
    3. On a matched night (LockDetail) confirm stop names link to /places/[slug]; a no-slug stop is plain text; the /places page has NO "build a date here"/"build a date with this idea" CTA.
    4. On the feed confirm the lowercase city label on NightCard.
    5. On /inbox (as a candidate with a pending interest) confirm the standby card: position line + soft sub-line + the neutral `pull my interest` control -> vaul confirm -> toast.
    6. Confirm the detail-sheet shimmer skeleton while loading and the /my-nights upcoming/archive toggle (empty archive shows the funny empty state).
    Critique each against 07-UI-SPEC.md + DESIGN-SYSTEM.md (Barbiecore tokens, lowercase/dry copy, no em-dashes, >=44px tap targets, pink map pins not terracotta).
  </how-to-verify>
  <verify><human-check>Visual-verify @420px approved for all 6 surfaces against 07-UI-SPEC + DESIGN-SYSTEM.</human-check></verify>
  <done>All 6 surfaces approved at 420px or issues logged + fixed.</done>
  <resume-signal>Type "approved" or describe the visual/UX issues to fix.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: GATED prod-apply of the 3 migrations</name>
  <files>supabase/migrations/20260606140000_e20_get_night_detail_coords.sql, supabase/migrations/20260606140100_e23_browse_feed_city_and_tune.sql, supabase/migrations/20260606140200_e24_withdraw_interest.sql</files>
  <action>Apply the 3 Phase-7 migrations to prod ufufmcpnysvwtutpbian IN ORDER behind this human gate (never auto-pushed), then run the security advisor + spot-check on prod.</action>
  <what-built>GATED PROD-APPLY: the 3 Phase-7 migrations (get_night_detail re-CREATE; browse_feed_for_viewer DROP+CREATE; withdraw_interest RPC) are local-green + advisor-clean, ready for prod ufufmcpnysvwtutpbian. This is a SEPARATE, non-auto step (CLAUDE.md gated-prod-apply — never auto-pushed).</what-built>
  <how-to-verify>
    1. Review the 3 migration files one more time against prod reality (watch local-vs-prod drift; prod ledger uses MCP-assigned versions — local files are source of truth).
    2. Apply the 3 migrations to prod ufufmcpnysvwtutpbian IN ORDER (get_night_detail, then browse_feed_for_viewer DROP+CREATE, then withdraw_interest) via the gated apply path (Supabase MCP apply_migration or the reviewed CLI flow).
    3. Run the Supabase security advisor against PROD after the DDL; confirm no NEW findings (the DEFINER-executable warnings are the established accepted pattern).
    4. Spot-check on prod: browse_feed_for_viewer returns city_name + the 3 host-hint cols and anon is non-executable; get_night_detail returns per-stop coords; withdraw_interest exists and is authenticated-only.
    5. Confirm NEXT_PUBLIC_MAPBOX_TOKEN is present in the Vercel prod env (E20 map render); if absent, the map degrades to the placeholder (no crash) but should be set before shipping E20.
  </how-to-verify>
  <verify><human-check>3 migrations applied to prod in order; prod advisor clean; spot-checks pass.</human-check></verify>
  <done>Migrations applied to prod via the gate; advisor clean; spot-checks confirm city_name + host-hint cols + anon-non-exec + coords + withdraw_interest.</done>
  <resume-signal>Type "prod applied + advisor clean" or report a blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local stack → prod ufufmcpnysvwtutpbian | DDL crosses into the live DB only behind a human gate (gated-prod-apply) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-20 | Tampering | prod migration applied unreviewed / out of order | mitigate | Separate autonomous:false human checkpoint; apply in dependency order; review files vs prod reality first |
| T-07-21 | Information Disclosure | feed RPC re-CREATE re-grants anon or drops host-hint cols on PROD | mitigate | e23_feed_contract.sql run locally pre-apply + advisor + prod spot-check (anon non-exec, host-hint cols present) |
| T-07-22 | Elevation of Privilege | new DEFINER RPC advisor finding on prod | mitigate | security advisor run after PROD DDL; new RPCs pin search_path, no USING(true), revoke anon/public |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (07-RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `supabase db reset` applies all 3 migrations in order; types regen from local.
- `pnpm test && pnpm typecheck` + all 3 SQL contract tests green locally.
- Security advisor: no NEW findings (local, then prod post-apply).
- Visual-verify @420px approved for the 6 surfaces.
- Prod apply done via the gated human checkpoint; prod spot-check + advisor clean.
</verification>

<success_criteria>
Phase 7 is locally green with all 3 migrations applied + advisor clean + full/SQL suite passing + visual-verify @420px approved, and the migrations are applied to prod only behind the gated human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/07-enhancements-and-polish-p3/07-09-SUMMARY.md` when done.
</output>
